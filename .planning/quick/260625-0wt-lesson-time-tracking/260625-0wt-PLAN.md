# Quick Task 260625-0wt: Трекинг времени уроков

## Цель

Записывать входы в систему, управлять уроком на странице журнала (начать/закончить + таймер), блокировать таблицу учеников до старта, автоостанавливать урок при разлогине.

## must_haves

- truths: login audit для всех ролей; кнопки начать/закончить урок; таймер; блокировка таблицы; авто-end при logout
- artifacts: TeachingSession model, API routes, LessonTimerBar, e2e
- key_links: IdleSessionGuard → end active session; StudentList → LessonTimerBar

## Tasks

### Task 1: Schema + API + audit

**files:** prisma/schema.prisma, migration, domain-events/types.ts, validations, API routes, login-actions
**action:** TeachingSession model; USER_LOGIN/LOGOUT/LESSON_* events; CRUD API
**verify:** prisma migrate; API returns session
**done:** DB + API готовы

### Task 2: Journal UI

**files:** LessonTimerBar, StudentList, JournalStudentsTable, use-teaching-session, journal-store
**action:** Start/end buttons, timer, block table until active
**verify:** manual / e2e
**done:** UI работает

### Task 3: Logout cleanup + tests

**files:** sign-out helper, IdleSessionGuard, AppShell, e2e/journal.spec.ts
**action:** end session before signOut; update e2e
**verify:** vitest + e2e pattern
**done:** авто-stop при logout
