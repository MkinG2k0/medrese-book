# Quick Plan: добавь переименование групп

## Task 1: Server action updateGroup

**files:** `src/features/groups/actions/group-actions.ts`

**action:**
- Добавить `updateGroupSchema` (name: string min 1)
- Добавить `updateGroup(groupId, input)` с `requireRoles(['MANAGER', 'SUPER_ADMIN'])`
- `prisma.group.update`, revalidate `/groups`, `/groups/{id}`, `/my-group`, `/journal`

**verify:** TypeScript компилируется, action экспортируется

**done:** Менеджер может обновить название группы через server action

## Task 2: UI переименования в списке групп

**files:**
- `src/features/groups/ui/RenameGroupForm.tsx` (новый)
- `src/features/groups/ui/GroupsList.tsx`

**action:**
- Форма с полем «Название» и кнопкой «Сохранить», по паттерну `CreateGroupForm`
- В таблице групп — колонка «Действия» с кнопкой «Переименовать»
- Modal «Переименовать группу» с `RenameGroupForm`

**verify:** На `/groups` у каждой строки есть кнопка, модал сохраняет новое имя

**done:** Менеджер переименовывает группу из списка

## Task 3: E2E-тест

**files:** `e2e/navigation.spec.ts`

**action:**
- Тест: менеджер переименовывает группу, новое имя видно в таблице
- Уникальное имя через `Date.now()`

**verify:** `pnpm test:e2e e2e/navigation.spec.ts` проходит тест переименования

**done:** Регрессия переименования покрыта
