# Quick Task 260606-v2w: Рефакторинг LessonPage

## Task 1: lib + model

**files:** `src/features/journal/lib/*`, `src/features/journal/model/use-lesson-page.ts`

**action:** Вынести чистые функции (состояния шагов, часы, сессия) и хук `useLessonPage` с бизнес-логикой.

**verify:** `pnpm exec tsc --noEmit`

**done:** Хук инкапсулирует session loading, grades, save.

## Task 2: UI components

**files:** `src/features/journal/ui/lesson/*`, `src/features/journal/ui/LessonPage.tsx`

**action:** Разбить UI на Header, StepsSection, SaveBar; LessonPage — тонкий оркестратор.

**verify:** `pnpm exec tsc --noEmit`

**done:** LessonPage ~60 строк, поведение без изменений.
