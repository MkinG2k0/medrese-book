---
phase: 260718-33u-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/uploads/program/level1/
  - prisma/lib/program-import-utils.ts
  - prisma/lib/level1-import-utils.ts
  - prisma/lib/seed-program.ts
autonomous: true
requirements:
  - QUICK-level1-pdf-seed
user_setup: []

must_haves:
  truths:
    - "В репозитории лежат 32 PDF уровня 1 по пути public/uploads/program/level1/step-{1..33}/lesson.pdf (без step-26)"
    - "seedProgram create/upsert пишет Step.pdfUrl для уровня 1 и пустой content { blocks: [] }"
    - "Шаг 26: pdfUrl=null, content={ blocks: [] }"
    - "Уровни 2–5 по-прежнему получают content из buildContent(metadata); pdfUrl у них null"
    - "level1-teacher-notes.json не загружается в loadAllLevel1Steps"
  artifacts:
    - path: public/uploads/program/level1/step-1/lesson.pdf
      provides: "Стабильный URL /uploads/program/level1/step-1/lesson.pdf"
    - path: prisma/lib/program-import-utils.ts
      provides: "StepDef.pdfUrl + пустой content для level1; resolveStepContent уважает step.content"
    - path: prisma/lib/seed-program.ts
      provides: "create/upsert пишет pdfUrl: step.pdfUrl ?? null"
  key_links:
    - from: external Книга учителя/pdf
      to: public/uploads/program/level1/step-{N}/lesson.pdf
      via: "копирование с нормализацией имени → lesson.pdf"
    - from: loadAllLevel1Steps
      to: seedProgram create/upsert
      via: "StepDef.pdfUrl + resolveStepContent → Step.pdfUrl + Step.content"
---

<objective>
Для сидера первого уровня заменить rich-text содержание блоков на прикреплённые PDF из «Книга учителя»: ассеты в `public/uploads/program/level1/`, seed пишет `pdfUrl` и пустой `content`.

Purpose: После `pnpm db:seed:program` / `:force` шаги уровня 1 открываются с PDF как содержанием (UI уже готов в 260718-2pl); шаг 26 без PDF; уровни 2–5 без изменений.

Output: 32 PDF в uploads, `StepDef.pdfUrl`, `loadAllLevel1Steps` + `seed-program` пишут pdfUrl/пустой content.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@prisma/lib/seed-program.ts
@prisma/lib/program-import-utils.ts
@prisma/lib/level1-import-utils.ts
@prisma/schema.prisma
@prisma/data/level1-page1.json
@package.json
</context>

## Scope

Только seed + ассеты уровня 1. **Не трогать** UI формы шага / журнал / портал (уже умеют pdfUrl). **Не трогать** уровни 2–5. **Не удалять** `level1-teacher-notes.json` (если появится) и старые image-ассеты — только отключить загрузку notes (сейчас уже не подключена). Schema/`pdfUrl` уже есть — миграции не делать.

## Source Coverage Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL | Сидер уровня 1: PDF вместо блоков контента | COVERED — Task 1–2 |
| REQ | QUICK-level1-pdf-seed | COVERED — весь план |
| RESEARCH | (нет research phase) | N/A |
| CONTEXT | PDF из Книга учителя/pdf; step-N/lesson.pdf; empty content; step 26 null; levels 2–5 untouched | COVERED — Task 1–2 |
| Deferred | Удаление level1-teacher-notes.json; удаление старых PNG image assets | excluded (опционально, не приоритет) |
| Out of scope | UI StepForm/LessonContentView; уровни 2–5 content; Prisma migration | excluded |

<tasks>

<task type="auto">
  <name>Task 1: Скопировать PDF уровня 1 в public/uploads</name>
  <files>public/uploads/program/level1/</files>
  <action>
    1. Источник (Windows, вне репо): `d:\Data\Таблица шагов\1й ур. .48ч\1 уровень 1.12.25г\Книга учителя\pdf`. Файлы вида `Nй шаг..pdf` (N = 1…33, **без 26**). Ожидается ровно 32 файла.
    2. Для каждого PDF извлечь номер шага regex `^(\d+)й\s*шаг` (Unicode). Назначение: `public/uploads/program/level1/step-{order}/lesson.pdf`. Создать каталоги при необходимости. Перезаписать если уже есть.
    3. Стабильный публичный URL: `/uploads/program/level1/step-{order}/lesson.pdf`. Не коммитить исходные кириллические имена — только `lesson.pdf`.
    4. Убедиться, что каталога `step-26` с PDF нет (шага 26 в источнике нет). Не трогать UI и seed в этом таске.
    5. Предпочтительный способ: одноразовый `pnpm exec tsx -e "..."` или PowerShell `Copy-Item` с циклом; не добавлять npm-зависимостей и не оставлять постоянный copy-скрипт в package.json без нужды.
  </action>
  <verify>
    <automated>pnpm exec tsx -e "const fs=require('fs');const path=require('path');const root='public/uploads/program/level1';const orders=[...Array(33)].map((_,i)=>i+1);const present=orders.filter(n=>fs.existsSync(path.join(root,`step-${n}`,'lesson.pdf')));if(present.length!==32) throw new Error('expected 32 pdfs, got '+present.length);if(present.includes(26)) throw new Error('step-26 must be absent');console.log('ok',present.length,'pdfs')"</automated>
  </verify>
  <done>32 файла lesson.pdf на местах step-1…25 и step-27…33; step-26 без PDF.</done>
</task>

<task type="auto">
  <name>Task 2: StepDef + loadAllLevel1Steps + seed pdfUrl</name>
  <files>prisma/lib/program-import-utils.ts, prisma/lib/level1-import-utils.ts, prisma/lib/seed-program.ts</files>
  <action>
    1. В `program-import-utils.ts` расширить `StepDef` опциональными полями: `pdfUrl?: string | null` и `content?: StepContentBlocks` (per task brief).
    2. Изменить `resolveStepContent(step)`: возвращать `step.content`, если поле задано; иначе `buildContent(step)`. Так уровни 2–5 и mini/e2e без `content` сохраняют прежнее поведение.
    3. В `loadAllLevel1Steps()`: после сборки из `level1-page{1,2,3}.json` для каждого шага выставить `content: { blocks: [] }` и `pdfUrl`: если существует `public/uploads/program/level1/step-{order}/lesson.pdf` → строка `/uploads/program/level1/step-{order}/lesson.pdf`, иначе `null` (шаг 26 и любой отсутствующий файл). **Не** читать `level1-teacher-notes.json`. Не вызывать `buildContent` для level1.
    4. В `seed-program.ts` в `createLevelWithSteps` и `upsertLevelWithSteps` (create + update) добавить запись `pdfUrl: step.pdfUrl ?? null` рядом с `content: resolveStepContent(step)`. `teacherNote` оставить `{ blocks: [] }`. Mini-program (`buildMiniStepDefs`) не обязан задавать pdfUrl — через `?? null` будет null.
    5. Реэкспорт из `level1-import-utils.ts` при необходимости (тип уже реэкспортируется). Не менять уровни 2–5 loaders. Не трогать UI, schema, миграции.
  </action>
  <verify>
    <automated>pnpm exec tsx -e "const {loadAllLevel1Steps,loadAllProgramLevelSteps,resolveStepContent}=require('./prisma/lib/program-import-utils.ts'); const l1=loadAllLevel1Steps(); if(l1.length!==33) throw new Error('l1 '+l1.length); const withPdf=l1.filter(s=>s.pdfUrl); if(withPdf.length!==32) throw new Error('pdf '+withPdf.length); const s26=l1.find(s=>s.order===26); if(s26?.pdfUrl!=null) throw new Error('26 pdf'); if(!s26 || s26.content?.blocks?.length!==0) throw new Error('26 content'); for(const s of l1){ const c=resolveStepContent(s); if(c.blocks.length!==0) throw new Error('resolve '+s.order);} const all=loadAllProgramLevelSteps(); const l2=all[1][0]; const c2=resolveStepContent(l2); if(!c2.blocks.length) throw new Error('l2 empty'); console.log('ok',withPdf.length,'pdfs, step26 null, l2 has blocks')"</automated>
  </verify>
  <done>33 StepDef уровня 1 с пустым content; 32 с pdfUrl; шаг 26 null; resolveStepContent пустой для L1 и непустой для L2; seed create/upsert пишет pdfUrl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → public/uploads | Локальные PDF копируются в статику Next.js |
| seed → DB | seedProgram пишет content/pdfUrl без user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33u-01 | Tampering | public/uploads PDF | low | accept | Ассеты из доверенного локального источника оператора; read-only в runtime |
| T-33u-02 | Information disclosure | /uploads/program/level1/*.pdf | low | accept | Тот же паттерн, что и существующие картинки program uploads (публичная статика) |
| T-33u-03 | Elevation | seed-program pdfUrl write | low | accept | Только CLI seed; UI upload уже авторизован отдельно |
| T-33u-SC | Tampering | npm installs | low | accept | Новых пакетов нет |
</threat_model>

<verification>
- 32 PDF на диске; нет lesson.pdf у step-26
- loadAllLevel1Steps: 33 шага, empty content, 32 pdfUrl, order 26 → null
- resolveStepContent(L1) → пустые blocks; resolveStepContent(L2+) → buildContent
- seed-program create/update включают pdfUrl
</verification>

<success_criteria>
После выполнения плана `pnpm db:seed:program:force` записывает для уровня 1 пустой content и pdfUrl на PDF (кроме шага 26); уровни 2–5 без регрессии; UI не менялся.
</success_criteria>

<output>
Create `.planning/quick/260718-33u-pdf/260718-33u-SUMMARY.md` when done
</output>
