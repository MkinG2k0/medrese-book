---
phase: 260723-2dd-system-help-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/help/
  - src/features/help/model/types.ts
  - src/features/help/model/teacher-guide.ts
  - src/features/help/model/manager-guide.ts
  - src/features/help/ui/HelpScreenshot.tsx
  - src/features/help/ui/HelpGuide.tsx
  - src/features/help/index.ts
  - src/app/(dashboard)/help/page.tsx
  - src/shared/lib/match-role-route.ts
  - src/shared/lib/match-role-route.test.ts
  - src/widgets/app-shell/ui/AppShell.tsx
  - e2e/help.spec.ts
autonomous: true
requirements:
  - QUICK-system-help-guide
user_setup: []

must_haves:
  truths:
    - "Учитель и менеджер (и супер-админ) открывают /help и видят русскую справку по системе"
    - "Учитель видит только учительский гайд; менеджер/супер-админ — менеджерский плюс доступ к учительскому"
    - "В гайде есть обзор, каталог фич из меню роли и пошаговые сценарии со скриншот-заглушками"
    - "В сайдбаре для TEACHER/MANAGER/SUPER_ADMIN есть пункт Справка с иконкой QuestionCircleOutlined рядом с Настройками"
    - "STUDENT и ACCOUNTANT не имеют доступа к /help (middleware deny или redirect)"
    - "Файлы в public/help/ имеют стабильные имена — можно заменить на реальные PNG без смены кода"
  artifacts:
    - path: src/features/help/
      provides: "контент-данные + UI справки (FSD feature)"
    - path: src/app/(dashboard)/help/page.tsx
      provides: "thin page с requireRoles"
    - path: public/help/
      provides: "SVG/PNG-заглушки скриншотов со стабильными именами"
    - path: e2e/help.spec.ts
      provides: "smoke доступ и видимость пункта меню"
  key_links:
    - from: AppShell MENU_ORDER_BY_ROLE
      to: "/help menuItemDefs"
      via: "пункт в конце меню перед /settings"
    - from: match-role-route ROLE_ROUTES['/help']
      to: "authorized middleware"
      via: "только TEACHER, MANAGER, SUPER_ADMIN"
    - from: help/page.tsx requireRoles
      to: HelpGuide role prop
      via: "session.user.role"
    - from: HelpGuide / HelpScreenshot
      to: "public/help/*.svg|png"
      via: "стабильные пути /help/<name>.svg"
---

<objective>
Создать страницу справки `/help` для ролей TEACHER, MANAGER и SUPER_ADMIN: обзор системы, каталог фич из меню и пошаговые инструкции со скриншот-заглушками (per D-01…D-08).

Purpose: Новый пользователь (учитель/менеджер) может понять, как пользоваться дневником, не выходя из приложения.

Output: feature `src/features/help/`, страница `/help`, пункт сайдбара, защита маршрута, smoke e2e.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@src/widgets/app-shell/ui/AppShell.tsx
@src/shared/lib/session.ts
@src/shared/lib/match-role-route.ts
@src/app/(dashboard)/settings/page.tsx
@src/features/theme-settings/index.ts
@.cursor/rules/antd-app-context.mdc
@.cursor/rules/antd-no-style-overrides.mdc
@.cursor/rules/no-antd-flex.mdc
@.cursor/rules/new-module-tests.mdc
</context>

## Current State

Справки в приложении нет. Навигация ролей задана в `AppShell.tsx` (`menuItemDefs` + `MENU_ORDER_BY_ROLE`). Middleware ACL — `ROLE_ROUTES` в `match-role-route.ts` (префиксный match); `/settings` не в таблице → `none` (доступен всем авторизованным). Для `/help` нужен явный ACL. Эталон thin page: `settings/page.tsx`. Auth page-level: `requireRoles` из `session.ts`.

**Out of scope:** реальные browser screenshots; справка для STUDENT/ACCOUNTANT; отдельный CMS/редактор контента.

## Source Coverage Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL | Страница справки/инструкции для МЕНЕДЖЕР и УЧИТЕЛЬ | COVERED — Tasks 1–3 |
| REQ | QUICK-system-help-guide | COVERED — весь план |
| RESEARCH | Quick mode, research skipped | excluded |
| CONTEXT | D-01 Route `/help` | COVERED — Task 3 |
| CONTEXT | D-02 Access TEACHER/MANAGER/SUPER_ADMIN | COVERED — Task 3 |
| CONTEXT | D-03 Nav «Справка» + QuestionCircleOutlined | COVERED — Task 3 |
| CONTEXT | D-04 Структура: обзор / каталог / шаги; role sections | COVERED — Tasks 1–2 |
| CONTEXT | D-05 Placeholder screenshots, stable filenames | COVERED — Tasks 1–2 |
| CONTEXT | D-06 Контент по реальным пунктам меню | COVERED — Task 1 |
| CONTEXT | D-07 FSD feature help/, Ant Design, no Flex | COVERED — Tasks 1–2 |
| CONTEXT | D-08 requireRoles на page | COVERED — Task 3 |
| Deferred | — | none |

<tasks>

<task type="auto">
  <name>Task 1: Help content data and screenshot placeholders</name>
  <files>public/help/, src/features/help/model/types.ts, src/features/help/model/teacher-guide.ts, src/features/help/model/manager-guide.ts</files>
  <read_first>
    - src/widgets/app-shell/ui/AppShell.tsx — точные label/key меню TEACHER и MANAGER (D-06)
    - .planning/PROJECT.md — формулировка «что это / зачем» для обзора
  </read_first>
  <action>
    Создать типизированный контент справки и заглушки изображений (per D-04, D-05, D-06).

    1. `src/features/help/model/types.ts` — типы вроде `HelpGuide`, `HelpFeature`, `HelpWalkthrough`, `HelpStep` с полями: title, description, optional `screenshotSrc` (путь вида `/help/...`), `screenshotCaption`. Без React-импортов.

    2. `teacher-guide.ts` и `manager-guide.ts` — экспортируемые константы на русском:
       - `overview` — что такое электронный дневник медресе и зачем (из PROJECT.md).
       - `features[]` — по одному элементу на каждый пункт меню роли из locked list (D-06):
         Teacher: Журнал, Доп. задания, Моя группа, Календарь, История шагов, Моя зарплата, Аналитика, Новости, Сообщения, Настройки.
         Manager: Группы, Пользователи, Предметы, Доп. задания, Аналитика, Аналитика учителей, Календарь отпусков, Награды, Журнал действий, Новости, Сообщения, Настройки.
         Краткое описание реальной пользы фичи (2–4 предложения), не маркетинг.
       - `walkthroughs[]` — минимум 2 сценария на роль с numbered steps (например учитель: провести урок в журнале; менеджер: создать группу / назначить учителя). В шагах, где уместно, ссылка на screenshotSrc.

    3. `public/help/` — SVG (предпочтительно) или простые PNG-заглушки со **стабильными** именами, например `teacher-journal.svg`, `teacher-extra-assignments.svg`, `manager-groups.svg`, … Подпись на самом изображении: «Скриншот: …» (русская). Достаточно 4–8 файлов, покрывающих ключевые шаги walkthroughs; не обязательно по одному на каждую фичу. Имена не менять в будущем при замене на реальные скрины.

    Не создавать UI-компоненты и page в этой задаче. Не импортировать antd Flex.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | Select-Object -First 40</automated>
  </verify>
  <done>
    Типы и два гайда экспортируются; в public/help/ лежат заглушки со стабильными именами; контент покрывает меню TEACHER и MANAGER per D-06.
  </done>
</task>

<task type="auto">
  <name>Task 2: HelpGuide UI and feature barrel</name>
  <files>src/features/help/ui/HelpScreenshot.tsx, src/features/help/ui/HelpGuide.tsx, src/features/help/index.ts</files>
  <read_first>
    - src/features/help/model/types.ts
    - src/features/help/model/teacher-guide.ts
    - src/features/help/model/manager-guide.ts
    - src/app/(dashboard)/settings/page.tsx — layout Title/Text + Tailwind flex-col
    - .cursor/rules/no-antd-flex.mdc
    - .cursor/rules/antd-no-style-overrides.mdc
  </read_first>
  <action>
    Собрать UI справки в `src/features/help/` (per D-04, D-07).

    1. `HelpScreenshot.tsx` (client или server-safe) — рендер изображения из `src` (next/image или img) + подпись `screenshotCaption` через Typography/Text type secondary. Alt на русском. Layout: `div` + Tailwind, без antd Flex.

    2. `HelpGuide.tsx` — `'use client'`; props: `role: 'TEACHER' | 'MANAGER' | 'SUPER_ADMIN'`.
       - TEACHER: только учительский гайд (без вкладки менеджера).
       - MANAGER и SUPER_ADMIN: Tabs — «Менеджер» (default) и «Учитель» (опциональный учительский гайд per D-04). SUPER_ADMIN видит контент менеджера (D-02).
       - Внутри гайда секции: Title «Обзор» + Text; Collapse (или список) «Возможности» по features; для walkthroughs — Title + Ant Design Steps (или нумерованный список) + HelpScreenshot где указан src.
       - Использовать Ant Design: Tabs, Collapse, Steps, Typography (или shared Title/Text). Запрещено: Flex из antd; статические Modal.*/message.*; хардкод цветов на antd-компонентах. UI строки на русском.

    3. `index.ts` — публичный barrel: экспорт `HelpGuide` (и типы при необходимости).

    Discretion: Tabs vs Collapse для каталога — Collapse для features, Tabs только для переключения ролей у менеджера.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | Select-Object -First 40</automated>
  </verify>
  <done>
    HelpGuide рендерит role-aware контент; barrel экспортирует компонент; нет Flex/static message/Modal.
  </done>
</task>

<task type="auto">
  <name>Task 3: Page, ACL, nav, and e2e smoke</name>
  <files>src/app/(dashboard)/help/page.tsx, src/shared/lib/match-role-route.ts, src/shared/lib/match-role-route.test.ts, src/widgets/app-shell/ui/AppShell.tsx, e2e/help.spec.ts</files>
  <read_first>
    - src/app/(dashboard)/analytics/page.tsx — паттерн requireRoles + session
    - src/shared/lib/match-role-route.ts — ROLE_ROUTES
    - src/shared/lib/match-role-route.test.ts — стиль тестов
    - src/widgets/app-shell/ui/AppShell.tsx — menuItemDefs, MENU_ORDER_BY_ROLE, imports иконок
    - e2e/settings-theme.spec.ts — короткий smoke-паттерн
    - e2e/helpers/auth.ts — loginAs
    - e2e/helpers/codes.ts — TEST_CODES
  </read_first>
  <action>
    Подключить маршрут, защиту и навигацию (per D-01, D-02, D-03, D-08).

    1. `src/app/(dashboard)/help/page.tsx` — Server Component: `await requireRoles(['TEACHER', 'MANAGER', 'SUPER_ADMIN'])`; Title «Справка»; передать `session.user.role` в компонент HelpGuide из `@/features/help`. Thin page — без бизнес-логики.

    2. В `ROLE_ROUTES` добавить `'/help': ['TEACHER', 'MANAGER', 'SUPER_ADMIN']`. В `match-role-route.test.ts` добавить кейсы: TEACHER/MANAGER allow; STUDENT deny; без роли → login.

    3. В `AppShell.tsx`:
       - Импорт `QuestionCircleOutlined`.
       - `menuItemDefs['/help']` — key `/help`, label «Справка», icon QuestionCircleOutlined.
       - Вставить `"/help"` в `MENU_ORDER_BY_ROLE` для TEACHER, MANAGER, SUPER_ADMIN (через managerMenuOrder) **перед** `"/settings"` (D-03). Не добавлять в STUDENT/ACCOUNTANT.

    4. `e2e/help.spec.ts` (per new-module-tests): teacher login → goto `/help` → видит заголовок «Справка» и пункт меню; student login → `/help` → не остаётся на `/help` (redirect); manager → `/help` доступен. Использовать `loginAs` + TEST_CODES.

    Не трогать другие пункты меню и не расширять ACL на STUDENT.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/shared/lib/match-role-route.test.ts 2>&1 | Select-Object -Last 30</automated>
  </verify>
  <done>
    /help открывается для TEACHER/MANAGER/SUPER_ADMIN; STUDENT denied; пункт «Справка» в сайдбаре перед Настройками; unit-тесты ROLE_ROUTES зелёные; e2e smoke написан.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware ROLE_ROUTES → /help | Неавторизованный / чужая роль не должна читать справку (low sensitivity, но ACL единообразен) |
| page requireRoles → HelpGuide | Defense-in-depth поверх middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260723-01 | Elevation of Privilege | ROLE_ROUTES `/help` | low | mitigate | Явный allowlist TEACHER/MANAGER/SUPER_ADMIN; unit-тесты deny для STUDENT |
| T-260723-02 | Information Disclosure | help content | low | accept | Справка не содержит секретов/PII — только UI-инструкции |
| T-260723-03 | Tampering | public/help assets | low | accept | Статика без auth; подмена файла = косметика, не RCE |
| T-260723-SC | Tampering | npm installs | high | accept | Новых пакетов в плане нет |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` без ошибок по новым файлам
- `pnpm exec vitest run src/shared/lib/match-role-route.test.ts` — pass
- Ручной smoke (опционально executor): teacher → /help; manager → вкладки; student → redirect
</verification>

<success_criteria>
- Страница `/help` существует и защищена requireRoles + ROLE_ROUTES
- Контент на русском: обзор, каталог фич меню, пошаговые сценарии
- Placeholder-скрины в `public/help/` со стабильными именами
- Пункт «Справка» в сайдбаре для TEACHER/MANAGER/SUPER_ADMIN перед Настройками
- e2e smoke на доступ по ролям
</success_criteria>

<output>
Create `.planning/quick/260723-2dd-system-help-guide/260723-2dd-SUMMARY.md` when done
</output>
