---
status: complete
---

# Quick Task 260704-2w3 — Summary

**Task:** Уведомления при получении сообщения  
**Commit:** d802689  
**Date:** 2026-07-04

## Что сделано

1. Enum `MESSAGE_RECEIVED` + миграция `20260703230548_add_message_received_notification`.
2. Domain event `MESSAGE_RECEIVED` при POST сообщения — уведомление только получателю (не отправителю).
3. Заголовок «Новое сообщение», тело `{имя}: {превью}`, ссылка `/messages?conversation={id}`.
4. `MessagesPage` открывает диалог по `?conversation=`.
5. Unit-тесты для сборки уведомления.

## Как работает

- **In-app:** SSE `/api/notifications/stream` → обновление badge и списка в `NotificationBell`.
- **Push:** `deliverNotifications` → web push (если пользователь подписан через PWA-баннер).

## После деплоя

```bash
pnpm db:migrate:deploy
```
