---
phase: 260718-2kp-remove-teacher-description
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/program-admin/ui/StepForm.tsx
  - src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx
  - src/shared/lib/validations/step.ts
  - src/features/journal/ui/StepCard.tsx
  - src/features/journal/lib/journal-step.ts
  - src/features/journal/actions/journal-actions.ts
  - src/features/journal/actions/journal-actions.test.ts
autonomous: true
requirements:
  - QUICK-remove-teacher-description
user_setup: []

must_haves:
  truths:
    - "В форме шага программы нет поля plain-text описания для учителя; остаётся только rich-text «Заметка учителя» (teacherNote)"
    - "В карточке шага журнала нет Collapse с plain-text описанием; блок «Заметка учителя» (teacherNote) сохранён"
    - "create/update шага больше не принимают и не пишут поле description в payload"
    - "Колонка Step.description в Prisma/БД не удаляется и не мигрируется"
  artifacts:
    - path: src/features/program-admin/ui/StepForm.tsx
      provides: "StepForm без description state/UI/payload"
    - path: src/features/journal/ui/StepCard.tsx
      provides: "StepCard без отображения step.description"
    - path: src/shared/lib/validations/step.ts
      provides: "createStepSchema без description"
  key_links:
    - from: StepForm submit payload
      to: createStep / updateStep
      via: "createStepSchema.parse — без description"
    - from: journal-actions step select
      to: mapStepMeta / StepCard
      via: "типы JournalStepMeta без description"
---

<objective>
Убрать из UI и проводки plain-text поле шага `description` («описание для учителя»), сохранив rich-text `teacherNote` («Заметка учителя») и колонку БД.

Purpose: Поле дублирует заметку учителя и больше не нужно в админке/журнале; данные в БД оставляем для безопасности.

Output: Форма шага и журнал без этого поля; Zod/create-update без description; teacherNote без изменений; без Prisma-миграции.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/features/program-admin/ui/StepForm.tsx
@src/features/journal/ui/StepCard.tsx
@src/features/journal/lib/journal-step.ts
@src/features/journal/actions/journal-actions.ts
@src/shared/lib/validations/step.ts
@src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx
</context>

## Scope

Удаляем только plain-text `Step.description` из редактирования и показа. **Не трогать** `teacherNote` / «Заметка учителя». **Не** делать Prisma migration / drop column. Не трогать `description` у Subject, LeaveRequest и прочих сущностей.

## Source Coverage Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL | Убрать поле «описание для учителя» из формы и журнала | COVERED — Task 1–2 |
| REQ | QUICK-remove-teacher-description | COVERED — весь план |
| RESEARCH | (нет research phase) | N/A |
| CONTEXT | plain-text description remove; keep teacherNote; no DB drop | COVERED — Task 1–2 |
| Deferred | — | none |
| Out of scope | Drop `Step.description` column; seed backfill; subject/leave description fields; Flex refactor in StepCard | excluded |

<tasks>

<task type="auto">
  <name>Task 1: Убрать description из формы шага и Zod</name>
  <files>src/features/program-admin/ui/StepForm.tsx, src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx, src/shared/lib/validations/step.ts</files>
  <action>
    1. В `StepForm.tsx`: удалить `description` из props `initial`, state (`useState`), submit payload и весь Form.Item с TextArea для plain-text teacher hint. Блок StepEditor для `teacherNote` («Заметка учителя») оставить без изменений.
    2. На edit page шага убрать передачу `description: step.description` в `initial` у StepForm.
    3. В `src/shared/lib/validations/step.ts` удалить поле `description` из `createStepSchema` (update наследует через `.partial()`). Не менять `teacherNote` / `stepContentSchema`.
    4. Не менять `program-actions.ts`, если после удаления поля из схемы `create`/`update` просто перестают получать description (Prisma default `""` на create останется). Не трогать `prisma/schema.prisma` и миграции.
  </action>
  <verify>
    <automated>rg -n "description" src/features/program-admin/ui/StepForm.tsx; rg -n "description:" src/shared/lib/validations/step.ts; rg -n "description: step.description" "src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx"</automated>
  </verify>
  <done>StepForm и createStepSchema не содержат description; edit page не передаёт description; teacherNote в форме на месте; Prisma schema не изменён.</done>
</task>

<task type="auto">
  <name>Task 2: Убрать description из журнала и маппинга шагов</name>
  <files>src/features/journal/ui/StepCard.tsx, src/features/journal/lib/journal-step.ts, src/features/journal/actions/journal-actions.ts, src/features/journal/actions/journal-actions.test.ts</files>
  <action>
    1. В `StepCard.tsx`: удалить optional `description` из props `step` и весь Collapse-блок с key `teacher-description`, который рендерит `step.description`. Collapse/preview для `teacherNote` («Заметка учителя») не трогать. Не рефакторить прочий layout файла.
    2. В `journal-step.ts`: убрать `description` из `JournalStepMeta`, аргумента `mapStepMeta` и возвращаемого объекта.
    3. В `journal-actions.ts`: убрать `description: true` из всех Prisma `select` шагов, где оно только кормило UI описания (обычно 3 места). Не трогать select/load для `teacherNote`.
    4. В `journal-actions.test.ts`: убрать `description` из фикстур step-объектов, чтобы типы сходились.
    5. Прогнать unit-тест journal-actions; убедиться grep по UI-лейблу plain-text описания в StepForm/StepCard даёт 0 совпадений.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/features/journal/actions/journal-actions.test.ts; rg -n "teacher-description|step\.description" src/features/journal/ui/StepCard.tsx src/features/journal/lib/journal-step.ts; rg -n "description: true" src/features/journal/actions/journal-actions.ts</automated>
  </verify>
  <done>Журнал не показывает и не мапит description; selects/фикстуры очищены; vitest journal-actions зелёный; teacherNote в StepCard сохранён.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manager/admin → createStep/updateStep | Валидированный payload шага программы |
| Teacher journal → step meta display | Данные шага из server actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2kp-01 | Tampering | createStepSchema / updateStep | low | mitigate | Убрать description из Zod — клиент больше не может писать поле через форму; колонка БД остаётся read-only для приложения |
| T-2kp-02 | Information Disclosure | StepCard journal | low | accept | Поле и так было только для учителей; удаление UI снижает поверхность, не расширяет |
| T-2kp-SC | Tampering | npm installs | low | accept | Новых пакетов нет |
</threat_model>

<verification>
- `pnpm exec vitest run src/features/journal/actions/journal-actions.test.ts`
- Static: нет `description` в StepForm payload/state; нет `teacher-description` / `step.description` в StepCard; нет `description` в createStepSchema; `teacherNote` в StepForm и StepCard на месте
- Prisma: `Step.description` в schema.prisma без изменений
</verification>

<success_criteria>
- В админке шага нет plain-text поля описания для учителя; «Заметка учителя» работает как раньше
- В журнале нет Collapse с plain-text описанием; заметка учителя остаётся
- create/update шага не принимают description
- Колонка БД не дропается
</success_criteria>

<output>
Create `.planning/quick/260718-2kp-remove-teacher-description/260718-2kp-SUMMARY.md` when done
</output>
