# Quick Task 260705-t22: добавь учителей тоже, роль отображай в badge

## Tasks

### 1. Backend — учителя в контактах
- `can-message-user.ts`: добавить коллег-учителей в `getMessageableContacts` для роли TEACHER
- `canMessageUser`: разрешить TEACHER → TEACHER

### 2. UI — badge роли
- `ContactRoleBadge.tsx`: Tag с подписью роли
- `ConversationList.tsx`: optionRender + badge в списке диалогов
- `ChatPanel.tsx`: badge в заголовке чата
