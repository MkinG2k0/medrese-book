# Quick Task 260625-lbe: Замещение в хедере

## Задачи

### 1. Данные активного замещения
- Добавить `getActiveSubstitutionsForAbsentTeacher` в `substitution-access.ts`
- Создать `getSubstitutionHeaderInfo` — возвращает строки для баннера по сессии

### 2. UI в AppShell
- Загрузить info в `(dashboard)/layout.tsx`
- Показать в Header: «Вас замещает X до …» / «Замещаете X до …»
