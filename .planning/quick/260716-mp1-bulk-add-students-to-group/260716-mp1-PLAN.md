---
phase: 260716-mp1-bulk-add-students-to-group
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/validations/enrollment.ts
  - src/features/groups/actions/group-actions.ts
  - src/features/groups/actions/group-actions.test.ts
  - src/features/groups/ui/EnrollStudentModal.tsx
  - src/features/groups/ui/GroupStudentsTable.tsx
  - e2e/groups-enrollment.spec.ts
autonomous: true
requirements:
  - QUICK-bulk-enroll
user_setup: []

must_haves:
  truths:
    - "Менеджер на /groups/[groupId] может зачислить несколько существующих учеников одним действием"
    - "Все выбранные ученики получают один уровень и один стартовый шаг предмета группы"
    - "Уже зачисленные в эту группу ученики не появляются в списке выбора и не создают дубликатов"
    - "Одиночное зачисление (один ученик) продолжает работать как раньше"
  artifacts:
    - path: src/shared/lib/validations/enrollment.ts
      provides: "enrollStudentsSchema (studentIds[], levelId, localStepIndex)"
    - path: src/features/groups/actions/group-actions.ts
      provides: "enrollStudents(groupId, input) transaction bulk create"
    - path: src/features/groups/ui/EnrollStudentModal.tsx
      provides: "multi-select учеников + общий уровень/шаг"
    - path: e2e/groups-enrollment.spec.ts
      provides: "E2E сценарий массового зачисления"
  key_links:
    - from: EnrollStudentModal
      to: enrollStudents
      via: "form submit studentIds + levelId + localStepIndex"
    - from: enrollStudents
      to: GroupEnrollment + syncCompletionsForProgress
      via: "одна Prisma-транзакция на весь batch"
---

<objective>
Массовое зачисление существующих учеников в группу со страницы `/groups/[groupId]`.

Purpose: Сейчас `EnrollStudentModal` принимает одного ученика; `createUsers` уже умеет bulk-создание новых STUDENT с groupId+levelId в админке. Самый полезный gap — **(C) bulk enroll существующих** на странице группы (не дублировать admin createUsers).

Output: `enrollStudents` action + multi-select UI + unit/E2E покрытие.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-groups-enrollment/11-PATTERNS.md
@src/features/groups/actions/group-actions.ts
@src/features/groups/ui/EnrollStudentModal.tsx
@src/features/groups/ui/GroupStudentsTable.tsx
@src/shared/lib/validations/enrollment.ts
@e2e/groups-enrollment.spec.ts
@e2e/helpers/antd.ts
</context>

## Source Coverage Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL | Массовое добавление учеников в группу | COVERED — Task 1–2 |
| REQ | QUICK-bulk-enroll | COVERED — весь план |
| RESEARCH (11) | enrollStudent + EnrollStudentModal pattern | COVERED — расширяем, не заменяем |
| RESEARCH (11) | level ∈ group.subject + syncCompletions | COVERED — Task 1 |
| CONTEXT | (нет discuss / D-XX) | N/A |
| Deferred | — | none |
| Out of scope | Bulk create новых через createUsers; смена преподавателя; CSV-import | excluded |

**Gap choice:** (C) bulk enroll existing — не (A) UI create на группе, не (B) улучшение admin createUsers.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: enrollStudents schema + action + unit tests</name>
  <files>src/shared/lib/validations/enrollment.ts, src/features/groups/actions/group-actions.ts, src/features/groups/actions/group-actions.test.ts</files>
  <behavior>
    - enrollStudentsSchema: studentIds — непустой массив уникальных string; levelId обязателен; localStepIndex int ≥ 0 (default 0)
    - enrollStudents: MANAGER/SUPER_ADMIN; группа найдена; уровень принадлежит subject группы; шаг в пределах уровня
    - Если любой studentId уже в GroupEnrollment этой группы — throw на русском, create не вызывается
    - Happy path: одна $transaction создаёт enrollment для каждого studentId с одинаковыми levelId/currentStepIdx и вызывает syncCompletionsForProgress per student
    - enrollStudent (single) остаётся и ведёт себя как сейчас (можно делегировать во внутренний helper / enrollStudents с одним id — на усмотрение, без смены публичного контракта)
  </behavior>
  <action>
    1. В `enrollment.ts` добавить `enrollStudentsSchema` и тип `EnrollStudentsInput`. Не ломать `enrollStudentSchema`.
    2. В `group-actions.ts` добавить `enrollStudents(groupId, input: unknown)`: `requireRoles(['MANAGER','SUPER_ADMIN'])`, parse schema, та же проверка group/level/step что у `enrollStudent`, затем batch-проверка дубликатов (findMany where groupId + studentId in ids), затем одна транзакция: create + syncCompletionsForProgress для каждого. `revalidatePath` как у single (`/groups/${groupId}`, `/groups`, `/journal`).
    3. Вынести общую логику одного enrollment (offset, create, sync) во внутренний helper, чтобы single и bulk не расходились.
    4. Unit-тесты в `group-actions.test.ts`: happy path 2 студента; reject duplicate; reject level≠subject. Моки по образцу блока `describe('enrollStudent')`.
    5. Не трогать createUsers / user-admin.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/features/groups/actions/group-actions.test.ts</automated>
  </verify>
  <done>enrollStudents создаёт несколько GroupEnrollment в одной транзакции; дубликаты и чужой уровень отклоняются; существующие тесты enrollStudent зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-select UI + E2E bulk enroll</name>
  <files>src/features/groups/ui/EnrollStudentModal.tsx, src/features/groups/ui/GroupStudentsTable.tsx, e2e/groups-enrollment.spec.ts</files>
  <action>
    1. В `EnrollStudentModal`: Select учеников с `mode="multiple"`; форма хранит `studentIds: string[]`; уровень и текущий шаг общие на всех (как в single). Вызов `enrollStudents(groupId, values)`. Заголовок модалки и okText на русском во множественном числе («Зачислить учеников» / «Зачислить»). Успех: `message.success` с числом зачисленных. `App.useApp()` для message — без статического импорта message из antd. Layout через div+Tailwind, без Flex.
    2. Кнопка на таблице: «Добавить учеников» (или оставить «Добавить ученика» если ломает существующий E2E title — тогда обновить E2E локаторы согласованно). Предпочтительно «Добавить учеников» + обновить e2e.
    3. `searchStudentsForEnroll`: при открытии без query увеличить `take` до разумного лимита (например 200), чтобы multi-select видел больше кандидатов; поиск по имени сохранить.
    4. E2E в `groups-enrollment.spec.ts`: новый тест — открыть группу, выбрать двух незачисленных учеников (уникальные из seed / уже используемые паттерны TEST_USERS), один уровень, зачислить, оба видны в таблице. Существующий single-тест адаптировать под multi-select (выбор одного в multiple Select) через те же antd helpers (`pickAntdSelectOption` / dialog scope).
    5. Не добавлять CSV/paste импорт и не создавать новых пользователей из модалки группы.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/features/groups/actions/group-actions.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Менеджер зачисляет ≥2 существующих учеников одним OK; single-path E2E зелёный; UI на русском.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → enrollStudents server action | Недоверенный input (studentIds, levelId, localStepIndex) |
| Manager session → GroupEnrollment writes | Только MANAGER/SUPER_ADMIN |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260716-01 | Elevation | enrollStudents | high | mitigate | requireRoles MANAGER/SUPER_ADMIN; TEACHER/STUDENT не вызывают |
| T-260716-02 | Tampering | levelId vs group.subjectId | high | mitigate | level.findFirst where subjectId = group.subjectId (как enrollStudent) |
| T-260716-03 | Tampering | duplicate enrollments | medium | mitigate | pre-check findMany + @@unique; abort batch на дубликате |
| T-260716-04 | Information | searchStudentsForEnroll | low | accept | Уже ограничен ролями менеджера; список имён учеников штатный для роли |
| T-260716-SC | Tampering | npm installs | low | accept | Новых пакетов нет |
</threat_model>

<verification>
- vitest group-actions (single + bulk)
- tsc --noEmit
- e2e groups-enrollment: single + bulk (при доступном test DB: `pnpm exec playwright test e2e/groups-enrollment.spec.ts`)
</verification>

<success_criteria>
- На странице группы менеджер выбирает несколько учеников, уровень и шаг → все появляются в таблице после refresh
- createUsers / admin bulk create не менялись
- Роли и subject-guard сохранены
</success_criteria>

<output>
Create `.planning/quick/260716-mp1-bulk-add-students-to-group/260716-mp1-SUMMARY.md` when done
</output>
