# Quick Task 260625-33a: дублирование данных и ученики в «Сменить учётку»

## Проблема

`RememberedAccountsSelect` («Сменить учётку») читает localStorage. Сейчас:
- `shouldRememberAccount` пропускает STUDENT → ученики попадают в список
- дедупликация по `id` → после reseed одного кода два раза (разные id)

## Задачи

### Task 1: Исправить remembered-accounts-storage
- `shouldRememberAccount`: только `TEACHER`
- Дедупликация по `code`
- При чтении — нормализация и перезапись localStorage (убрать учеников и дубли)

**Verify:** `pnpm exec tsc --noEmit`

**Done:** список содержит только учителей, без дублей по коду
