---
phase: 260718-3bp-4-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/theme-settings/lib/constants.ts
  - src/features/theme-settings/lib/theme-settings-storage.ts
  - src/features/theme-settings/model/theme-settings-context.tsx
  - src/features/theme-settings/ui/ThemePicker.tsx
  - src/features/theme-settings/lib/antd-theme.ts
  - src/features/theme-settings/index.ts
  - src/shared/providers/theme-provider.tsx
  - src/shared/providers/antd-provider.tsx
  - src/app/globals.css
  - src/app/(dashboard)/settings/page.tsx
  - src/widgets/app-shell/ui/AppShell.tsx
  - e2e/settings-theme.spec.ts
autonomous: true
requirements:
  - QUICK-app-themes-4
user_setup: []

must_haves:
  truths:
    - "В приложении ровно 4 темы: светлая (light), тёмная (dark), мечеть/sage, сепия (sepia); по умолчанию светлая"
    - "Переключение темы на странице /settings; пункт «Настройки» в сайдбаре у всех authenticated-ролей"
    - "Тема применяется ко всему приложению: next-themes class на html + ConfigProvider algorithm/tokens + CSS-переменные"
    - "Выбор темы сохраняется в localStorage и восстанавливается после перезагрузки"
  artifacts:
    - path: src/features/theme-settings/lib/constants.ts
      provides: "AppTheme union из 4 id + русские labels + DEFAULT light"
    - path: src/features/theme-settings/lib/antd-theme.ts
      provides: "Маппинг AppTheme → algorithm + token overrides для ConfigProvider"
    - path: src/app/(dashboard)/settings/page.tsx
      provides: "Страница настроек с ThemePicker"
    - path: e2e/settings-theme.spec.ts
      provides: "E2E: nav → /settings → смена темы → class на html"
  key_links:
    - from: next-themes ThemeProvider
      to: AntdProvider
      via: "useTheme().resolvedTheme → ConfigProvider theme.algorithm/token"
    - from: ThemePicker setTheme
      to: html class + localStorage
      via: "next-themes attribute=class, storageKey app-theme"
    - from: AppShell MENU_ORDER_BY_ROLE
      to: /settings page
      via: "menuItemDefs[/settings] во всех ролях"
---

<objective>
Добавить app-wide смену из 4 тем (светлая по умолчанию) на новой странице `/settings`, с проводкой через next-themes + Ant Design ConfigProvider + CSS-токены.

Purpose: Сейчас `defaultTheme="dark"`, AntdProvider захардкожен на `darkAlgorithm`, а `theme-settings` — незавершённый Quran-shell picker. Нужна единая система тем для всего дневника.

Output: 4 темы, страница настроек, пункт меню, persistence, e2e.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/shared/providers/theme-provider.tsx
@src/shared/providers/antd-provider.tsx
@src/shared/providers/index.tsx
@src/features/theme-settings/lib/constants.ts
@src/features/theme-settings/model/theme-settings-context.tsx
@src/features/theme-settings/ui/ThemePicker.tsx
@src/widgets/app-shell/ui/AppShell.tsx
@src/app/globals.css
@src/shared/lib/auth.config.ts
@e2e/navigation.spec.ts
</context>

## Scope

**In:** 4 app themes; `/settings`; AppShell nav; wire providers; CSS tokens; e2e.

**Out:** System theme (`enableSystem` остаётся false); per-user server sync; редизайн всего AppShell (кроме theme-aware brand color); отдельные Quran-shell CSS vars `--quran-*`.

## Designer themes (locked)

| id | Label (RU) | Character |
|----|------------|-----------|
| `light` | Светлая | Белая/нейтральная (default) |
| `dark` | Тёмная | Чёрная/тёмная |
| `sage` | Мечеть | Мягкий sage/зелёный акцент (мечеть) |
| `sepia` | Сепия | Тёплый parchment/sepia |

## Source Coverage Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL | 4 темы, settings page, default light, app-wide | COVERED — Task 1–3 |
| REQ | QUICK-app-themes-4 | COVERED — весь план |
| RESEARCH | (нет) | N/A |
| CONTEXT | D: 4 themes light/dark+sage+sepia; /settings; default light; persist; nav all roles; FSD theme-settings; no Flex/static message; ConfigProvider tokens; e2e | COVERED — Task 1–3 |
| Deferred | — | none |

<tasks>

<task type="auto">
  <name>Task 1: Модель тем + провайдеры + CSS-токены</name>
  <files>src/features/theme-settings/lib/constants.ts, src/features/theme-settings/lib/antd-theme.ts, src/features/theme-settings/lib/theme-settings-storage.ts, src/features/theme-settings/model/theme-settings-context.tsx, src/features/theme-settings/index.ts, src/shared/providers/theme-provider.tsx, src/shared/providers/antd-provider.tsx, src/app/globals.css</files>
  <action>
    1. **Repurpose `theme-settings` под app-wide темы (не параллельная Quran-система):** в `constants.ts` заменить `QuranTheme` на `AppTheme = 'light' | 'dark' | 'sage' | 'sepia'`; `DEFAULT_APP_THEME = 'light'`; `APP_THEME_OPTIONS` с русскими labels: Светлая / Тёмная / Мечеть / Сепия. Экспортировать `APP_THEME_IDS` массив для валидации.

    2. Создать `src/features/theme-settings/lib/antd-theme.ts`: функция `getAntdThemeConfig(themeId: AppTheme)` возвращает `{ algorithm, token }` для ConfigProvider. Mapping: `dark` → `theme.darkAlgorithm`; `light`/`sage`/`sepia` → `theme.defaultAlgorithm`. Для `sage` задать token-акцент (например colorPrimary около `#4A7C59`, colorBgBase мягкий зелёно-серый); для `sepia` — тёплый parchment (colorPrimary терракота/коричневый акцент, colorBgBase `#F4EDE0`-подобный через token, не Tailwind-хаки на компонентах). Не хардкодить цвета antd-компонентов в UI.

    3. **ThemeProvider** (`theme-provider.tsx`): `attribute="class"`, `defaultTheme="light"`, `enableSystem={false}`, `themes={['light','dark','sage','sepia']}`, `storageKey="app-theme"`, `disableTransitionOnChange` оставить. Persistence через next-themes localStorage (отдельный quran-theme storage не нужен).

    4. **AntdProvider**: внутри ThemeProvider (порядок в `providers/index.tsx` уже правильный). Читать `resolvedTheme` / `theme` из `useTheme()` next-themes; после mount применять `getAntdThemeConfig`. До mount — безопасный fallback на light (defaultAlgorithm), чтобы не мигать dark. Не оставлять захардкоженный только `theme.darkAlgorithm`.

    5. **Убрать Quran-shell обёртку:** переписать или удалить `ThemeSettingsProvider` shell (`quran-student-shell`, `data-quran-theme`, `--quran-bg`). Не монтировать ThemeSettingsProvider в Providers — источник истины = next-themes. Удалить/упростить `theme-settings-storage.ts` и context, если они дублируют next-themes; barrel `index.ts` экспортирует ThemePicker, AppTheme, constants, при необходимости тонкий typed helper. Не оставлять мёртвый параллельный storage key `quran-theme`.

    6. **globals.css:** добавить блоки `.sage` и `.sepia` с теми же CSS-переменными что у `:root` / `.dark` (`--background`, `--foreground`, `--card`, `--primary`, `--sidebar-*` и т.д.), в духе продукта (sage — спокойный зелёный; sepia — тёплый пергамент). `:root` = светлая база. Не ломать существующий `.dark`. Не добавлять `--quran-*` theme blocks.
  </action>
  <verify>
    <automated>rg -n "defaultTheme=\"light\"|themes=\{" src/shared/providers/theme-provider.tsx; rg -n "getAntdThemeConfig|useTheme" src/shared/providers/antd-provider.tsx; rg -n "AppTheme|'sage'|'sepia'" src/features/theme-settings/lib/constants.ts; rg -n "^\.sage|^\.sepia" src/app/globals.css</automated>
  </verify>
  <done>next-themes default light + 4 themes; AntdProvider синхронизирован с resolvedTheme; CSS `.sage`/`.sepia` есть; Quran-shell/quran-theme storage убраны.</done>
</task>

<task type="auto">
  <name>Task 2: ThemePicker, страница /settings, пункт меню</name>
  <files>src/features/theme-settings/ui/ThemePicker.tsx, src/app/(dashboard)/settings/page.tsx, src/widgets/app-shell/ui/AppShell.tsx</files>
  <action>
    1. **ThemePicker:** клиентский компонент; `useTheme()` из next-themes + `APP_THEME_OPTIONS` для labels. Рендер группы из 4 кнопок (или Radio.Group antd) с `aria-pressed` / selected state; `setTheme(id)` по клику. Guard на `mounted` (паттерн next-themes), чтобы SSR не мисматичил. Layout через `div` + Tailwind flex/gap — **без** antd Flex. Без static `message`/`Modal`. Стили выбора через antd Button type/primary или semantic props, не через Tailwind color overrides на antd.

    2. **Страница** `src/app/(dashboard)/settings/page.tsx`: под `(dashboard)` layout (уже require auth). Заголовок «Настройки» (Title/Text shared или antd). Секция «Тема оформления» + ThemePicker. UI на русском. Импорт ThemePicker из `@/features/theme-settings`. Отдельный role-route в auth.config не обязателен: layout редиректит гостей; `/settings` доступен всем authenticated ролям.

    3. **AppShell:** добавить `menuItemDefs["/settings"]` с иконкой (SettingOutlined) и label «Настройки». Вставить `"/settings"` в **конец** каждого массива `MENU_ORDER_BY_ROLE` (TEACHER, MANAGER, SUPER_ADMIN, ACCOUNTANT, STUDENT) — пункт виден всем ролям. Заменить хардкод цвета бренда в сайдбаре (`text-[#E8E0D0]`) на theme-aware вариант (например class без hex / CSS var `--foreground` / antd Typography), чтобы светлая тема не ломала читаемость логотипа.
  </action>
  <verify>
    <automated>rg -n "Настройки|/settings" src/widgets/app-shell/ui/AppShell.tsx; rg -n "ThemePicker" "src/app/(dashboard)/settings/page.tsx"; rg -n "setTheme|APP_THEME_OPTIONS|useTheme" src/features/theme-settings/ui/ThemePicker.tsx</automated>
  </verify>
  <done>/settings рендерит ThemePicker; «Настройки» в меню всех ролей; ThemePicker переключает next-themes.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright e2e смены темы</name>
  <files>e2e/settings-theme.spec.ts</files>
  <action>
    1. Создать `e2e/settings-theme.spec.ts` по паттерну `e2e/navigation.spec.ts`: `test.use({ storageState: AUTH_STATE.teacher1 })`.

    2. Сценарии (минимум):
       - Открыть `/journal` (или `/settings`): menuitem «Настройки» видим; клик ведёт на `/settings`.
       - На `/settings` видны 4 опции (Светлая, Тёмная, Мечеть, Сепия) по accessible name.
       - Клик «Тёмная» → у `html` класс `dark` (или `document.documentElement.classList` содержит `dark`); localStorage key `app-theme` = `dark`.
       - Клик «Светлая» → класс `dark` снят / тема light; storage = `light`.
       - Опционально один клик «Мечеть» → class `sage`.

    3. Локаторы: `getByRole('menuitem', { name: 'Настройки' })`, кнопки/radio по имени темы; не использовать хрупкие substring getByText. Не мутировать seed-данные.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/settings-theme.spec.ts</automated>
  </verify>
  <done>E2E проходит: nav → settings → переключение dark/light отражается на html class и storage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → ThemeProvider | Недоверенная строка темы из storage |
| Client UI → setTheme | Пользователь выбирает id темы |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3bp-01 | Tampering | next-themes storage | low | mitigate | themes allowlist в ThemeProvider; неизвестные значения → default light |
| T-3bp-02 | Information Disclosure | /settings page | low | accept | Только UI-предпочтение; нет PII; страница за dashboard auth layout |
| T-3bp-SC | Tampering | npm installs | low | accept | Новых пакетов нет (next-themes и antd уже в зависимостях) |
</threat_model>

<verification>
- `rg` gates Task 1–2 зелёные
- `pnpm exec playwright test e2e/settings-theme.spec.ts` проходит
- Вручную (после execute): четыре темы визуально различимы на /settings и в журнале
</verification>

<success_criteria>
- Default = light; ровно 4 темы light/dark/sage/sepia
- /settings + пункт «Настройки» у всех ролей
- Тема меняет antd + html class + CSS tokens; persist в localStorage
- E2E settings-theme зелёный
</success_criteria>

<output>
Create `.planning/quick/260718-3bp-4-2/260718-3bp-SUMMARY.md` when done
</output>
