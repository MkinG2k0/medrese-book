---
status: complete
---

# Quick Task 260712-12i — Summary

## Что сделано

- `LevelStats` дополнен полями `levelId` и `label`
- `getLevelStats` подгружает предмет и формирует label: «N» или «N (Предмет)» при нескольких предметах
- `LevelStatsChart`: `rowKey="levelId"`, ось X и колонка таблицы по `label`

## Причина

`Level.number` уникален только в рамках `subjectId`. Без фильтра по группе возвращались уровни всех предметов с одинаковыми номерами → React warning о duplicate keys.

## Файлы

- `src/shared/lib/analytics-queries/level-stats.ts`
- `src/features/analytics/ui/LevelStats.tsx`
