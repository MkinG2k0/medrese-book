---
phase: 260718-2pl-pdf-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations
  - src/shared/lib/validations/step.ts
  - src/features/program-admin/actions/program-actions.ts
  - src/features/program-admin/ui/StepForm.tsx
  - src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx
  - src/features/program-admin/ui/StepPdfViewer.tsx
  - src/features/program-admin/ui/LessonContentView.tsx
  - src/features/journal/ui/StepCard.tsx
  - src/features/journal/actions/journal-actions.ts
  - src/features/journal/lib/journal-step.ts
  - src/features/student-portal/ui/StudentLessonsList.tsx
  - src/features/student-portal/actions/student-actions.ts
autonomous: true
requirements:
  - QUICK-step-pdf-content
user_setup: []

must_haves:
  truths:
    - "В форме шага («Редактирование: …») можно прикрепить PDF к содержанию, заменить и удалить вложение"
    - "Если PDF есть — он показывается в блоке «Содержание» (журнал и портал ученика)"
    - "Работают три кейса: только PDF, только rich-text, оба вместе"
    - "Поле teacherNote / «Заметка учителя» не удаляется и не ломается"
  artifacts:
    - path: prisma/schema.prisma
      provides: "Step.pdfUrl String? nullable"
    - path: src/features/program-admin/ui/StepForm.tsx
      provides: "Upload/remove PDF рядом с «Содержание»"
    - path: src/features/program-admin/ui/LessonContentView.tsx
      provides: "Единый рендер PDF + text для журнала и ученика"
  key_links:
    - from: StepForm pdf upload
      to: POST /api/uploads → Step.pdfUrl
      via: "createStep/updateStep payload.pdfUrl"
    - from: getJournalStepContent / student lessons
      to: LessonContentView
      via: "pdfUrl + content → блок Содержание"
---

<objective>
Добавить к шагам программы прикрепление PDF: хранение URL, загрузка в админ-форме шага, отображение как «Содержание» вместе с rich-text или вместо него.

Purpose: Менеджер может давать материал урока файлом PDF, текстом или комбинацией — учитель и ученик видят это в содержании урока.

Output: `Step.pdfUrl`, форма с Upload, общий viewer, проводка в journal + student-portal.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@prisma/schema.prisma
@src/shared/lib/validations/step.ts
@src/features/program-admin/ui/StepForm.tsx
@src/features/program-admin/ui/editor/StepEditor.tsx
@src/app/api/uploads/route.ts
@src/shared/lib/storage/upload-file.ts
@src/features/journal/ui/StepCard.tsx
@src/features/journal/actions/journal-actions.ts
@src/features/journal/lib/journal-step.ts
@src/features/student-portal/ui/StudentLessonsList.tsx
@src/features/student-portal/actions/student-actions.ts
@src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx
</context>

## Scope

- Поле на модели `Step`: nullable `pdfUrl` (отдельная колонка, не блок в JSON `content`) — так независимы кейсы PDF-only / text-only / both.
- Загрузка через существующий `POST /api/uploads` + `uploadFile` (local/S3), как картинки в StepEditor.
- UI-лейблы на русском. Не трогать `teacherNote`. Не удалять `description` колонку / не рефакторить удаление description (уже сделано в 260718-2kp).
- Не добавлять новый npm-пакет для PDF.

## Source Coverage Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL | Прикреплять PDF к урокам; показывать как Содержание; PDF / текст / оба | COVERED — Task 1–3 |
| REQ | QUICK-step-pdf-content | COVERED — весь план |
| RESEARCH | (нет research phase) | N/A |
| CONTEXT | Admin step edit; reuse uploads; keep teacherNote; three content cases | COVERED — Task 1–3 |
| Deferred | — | none |
| Out of scope | Drop description; PDF в Tiptap как блок; новый storage backend; e2e full suite | excluded |

<tasks>

<task type="auto">
  <name>Task 1: Prisma pdfUrl + Zod + program-actions</name>
  <files>prisma/schema.prisma, prisma/migrations, src/shared/lib/validations/step.ts, src/features/program-admin/actions/program-actions.ts</files>
  <action>
    1. В `prisma/schema.prisma` на модели `Step` добавить `pdfUrl String?` (nullable, без default). Создать миграцию Prisma (`pnpm prisma migrate dev` или ручная SQL в `prisma/migrations/`) с `ALTER TABLE "Step" ADD COLUMN "pdfUrl" TEXT`.
    2. В `createStepSchema` добавить optional `pdfUrl: z.string().min(1).nullable().optional()` — строка URL/path (`/uploads/...` или S3 public URL), `null` очищает поле при update. Не менять `stepContentSchema` / блоки.
    3. `createStep` / `updateStep` уже пишут parsed data в Prisma — убедиться, что `pdfUrl` проходит в `create`/`update` data. При `pdfUrl: null` на update колонка сбрасывается. Не трогать `teacherNote` и `description`.
    4. После миграции: `pnpm prisma generate` (или postinstall), чтобы клиент знал поле.
  </action>
  <verify>
    <automated>rg -n "pdfUrl" prisma/schema.prisma src/shared/lib/validations/step.ts</automated>
  </verify>
  <done>Step.pdfUrl в schema+migration; Zod принимает/очищает pdfUrl; create/update сохраняют поле; generate без ошибок типов по pdfUrl.</done>
</task>

<task type="auto">
  <name>Task 2: StepForm — прикрепить / заменить / удалить PDF</name>
  <files>src/features/program-admin/ui/StepForm.tsx, src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx</files>
  <action>
    1. Расширить `initial` StepForm: `pdfUrl?: string | null`. State `pdfUrl` / `setPdfUrl` из initial.
    2. В секции Form.Item «Содержание» (рядом с StepEditor, не вместо него): UI на русском — кнопка/Upload «Прикрепить PDF» (`accept` только PDF), показ имени/ссылки если файл уже есть, кнопки «Заменить» и «Удалить». Загрузка: `FormData` + `fetch('/api/uploads')` по паттерну StepEditor `addImage`; из ответа взять `json.data.url` и записать в state. При ошибке — `App.useApp().message.error` (не статический `message` из antd). Удаление: `setPdfUrl(null)`.
    3. В submit payload добавить `pdfUrl` (строка или `null`). Rich-text `content` остаётся обязательным по схеме как сейчас (пустые blocks допустимы для PDF-only).
    4. Edit page: передать `pdfUrl: step.pdfUrl` в `initial`. Create page (если есть) — без поля, initial undefined.
    5. Layout: div + Tailwind flex, без antd `Flex`. Не менять блок «Заметка учителя».
  </action>
  <verify>
    <automated>rg -n "pdfUrl|/api/uploads|Прикрепить PDF" src/features/program-admin/ui/StepForm.tsx; rg -n "pdfUrl" "src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx"</automated>
  </verify>
  <done>Админ может прикрепить/заменить/удалить PDF на странице редактирования шага; payload включает pdfUrl; teacherNote UI без изменений.</done>
</task>

<task type="auto">
  <name>Task 3: Отображение PDF в «Содержание» (журнал + ученик)</name>
  <files>src/features/program-admin/ui/StepPdfViewer.tsx, src/features/program-admin/ui/LessonContentView.tsx, src/features/journal/ui/StepCard.tsx, src/features/journal/actions/journal-actions.ts, src/features/journal/lib/journal-step.ts, src/features/student-portal/ui/StudentLessonsList.tsx, src/features/student-portal/actions/student-actions.ts</files>
  <action>
    1. Добавить `StepPdfViewer.tsx`: клиентский просмотр PDF по URL — `iframe` (или `object`) на всю ширину контейнера + ссылка «Открыть PDF» в новой вкладке. Высота разумная (~480–640px). Без новых зависимостей.
    2. Добавить `LessonContentView.tsx`: props `{ content: StepContent; pdfUrl?: string | null }`. Рендер: если `pdfUrl` — сначала PDF viewer; если `hasVisibleStepContent(content)` — ниже `StepContentPreview` / BlockRenderer. PDF-only / text-only / both — все три ветки. Пустое — короткий Text secondary «Нет содержания» или пустой fragment (как сейчас при пустом preview).
    3. Journal: `getJournalStepContent` select/return `pdfUrl`; StepCard хранит `pdfUrl` в state при lazy-load; блок Form.Item «Содержание» рендерит `LessonContentView` вместо голого `StepContentPreview`. Условие lazy-load: грузить, если expanded и (content пустой ИЛИ pdfUrl ещё не известен) — при первом expand всегда можно дернуть getJournalStepContent и выставить content + teacherNote + pdfUrl. Не ломать Collapse «Заметка учителя».
    4. `hasVisibleStepContent` / journal-step: при необходимости хелпер `hasLessonContent(content, pdfUrl)` = visible text OR nonempty pdfUrl; использовать в StepCard если нужно скрывать пустой блок.
    5. Student portal: в actions при сборке уроков select `pdfUrl`; в `StudentLessonItem` добавить `pdfUrl`; children Collapse — `LessonContentView` вместо только BlockRenderer.
  </action>
  <verify>
    <automated>rg -n "pdfUrl|LessonContentView|StepPdfViewer" src/features/program-admin/ui src/features/journal src/features/student-portal; pnpm exec vitest run src/features/journal/actions/journal-actions.test.ts src/features/program-admin/actions/program-actions.test.ts</automated>
  </verify>
  <done>Журнал и портал ученика показывают PDF в «Содержание» при наличии url; text-only и оба вместе работают; teacherNote не затронут; unit-тесты зелёные (обновить фикстуры под pdfUrl если типы требуют).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manager/admin → POST /api/uploads | Недоверенный файл (имя, MIME, размер) |
| Manager/admin → createStep/updateStep | pdfUrl из клиента |
| Teacher/Student → step content display | URL из БД в iframe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2pl-01 | Tampering | StepForm upload + pdfUrl | medium | mitigate | Client accept только PDF; хранить только URL от `/api/uploads` (не произвольный user HTML); Zod string; роли SUPER_ADMIN/MANAGER на upload и step mutations уже есть |
| T-2pl-02 | Information Disclosure | iframe PDF URL | low | accept | PDF доступен тем же ролям, что и содержание шага; публичный `/uploads/` уже принят для изображений |
| T-2pl-03 | Elevation | uploads route | low | accept | authorizeApiRequest уже ограничивает MANAGER/SUPER_ADMIN |
| T-2pl-SC | Tampering | npm installs | low | accept | Новых пакетов нет |
</threat_model>

<verification>
- Миграция применяет `pdfUrl` на Step
- StepForm: attach/replace/remove + save
- Journal StepCard «Содержание»: PDF-only, text-only, both
- Student lessons: то же
- teacherNote без регрессии
- vitest journal-actions / program-actions (фикстуры при необходимости)
</verification>

<success_criteria>
- Админ прикрепляет PDF на странице редактирования шага
- Содержание урока показывает PDF и/или rich-text по наличию
- Три кейса покрыты без удаления заметки учителя
</success_criteria>

<output>
Create `.planning/quick/260718-2pl-pdf-pdf/260718-2pl-SUMMARY.md` when done
</output>
