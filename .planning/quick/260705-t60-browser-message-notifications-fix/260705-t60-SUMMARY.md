---
status: complete
---

# Quick Task 260705-t60 — Summary

**Task:** Браузерные уведомления для сообщений  
**Date:** 2026-07-05

## Что сделано

1. `usePushAutoSubscribe` — при `Notification.permission === 'granted'` автоматически создаёт/синхронизирует push-подписку.
2. SSE `/api/notifications/stream` отдаёт `title`, `body`, `link`; клиент показывает `new Notification()` (кроме открытого диалога).
3. `PushSubscribePrompt` и `PwaInstallBanner` проверяют `pushManager.getSubscription()`, а не только permission.
4. Unit-тесты для `shouldShowBrowserNotification`.

## Как проверить

1. Разрешить уведомления в браузере.
2. Перезагрузить страницу (авто-подписка).
3. С другого аккаунта отправить сообщение — должно появиться системное уведомление.
