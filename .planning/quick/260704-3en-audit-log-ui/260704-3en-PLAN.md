# Quick Task 260704-3en: Журнал аудита для менеджера и супер-админа

**Goal:** Страница истории всех действий в системе с фильтрами по типу события, сущности, автору и периоду.

## Context

- `AuditEvent` уже пишется через `dispatchDomainEvent` / `writeAuditEvent`
- UI (`features/audit-log`) запланирован в ARCHITECTURE.md, но не реализован

## Tasks

### Task 1: Read path — API + entity + query
- `entities/audit-event/` — типы и React Query hook
- `features/audit-log/lib/query-audit-events.ts` — Prisma query с пагинацией и фильтрами
- `GET /api/audit-events` — только MANAGER, SUPER_ADMIN
- `shared/lib/validations/audit-event.ts` — Zod для query params

### Task 2: UI + навигация
- `features/audit-log/ui/AuditLogPage.tsx` — таблица, фильтры, модалка деталей payload
- `features/audit-log/lib/audit-labels.ts` — русские подписи action/entityType
- `/admin/audit-log` page + пункт меню в AppShell

### Task 3: E2E
- `e2e/audit-log.spec.ts` — доступ менеджеру, запрет учителю, фильтр по типу
