# Quick Task: Reorder nav menu by role

## Goal

Логично упорядочить пункты бокового меню для каждой роли. SUPER_ADMIN видит те же страницы в том же порядке, что и MANAGER.

## Tasks

### Task 1: Refactor AppShell menu ordering

**files:** `src/widgets/app-shell/ui/AppShell.tsx`

**action:**
- Ввести явный порядок пунктов меню per-role (`MENU_ORDER_BY_ROLE`)
- SUPER_ADMIN использует тот же порядок, что MANAGER
- Добавить SUPER_ADMIN в «Сообщения»
- Порядок:
  - TEACHER: Журнал → Моя группа → Календарь → История шагов → Мои часы → Аналитика → Сообщения
  - MANAGER/SUPER_ADMIN: Группы → Пользователи → Программа → Аналитика → Аналитика учителей → Календарь отпусков → Награды → Сообщения
  - STUDENT: Мой прогресс → Уроки → История занятий → Награды → Сообщения

**verify:** TypeScript compiles, menu filter uses role order

**done:** AppShell shows items in defined order per role

### Task 2: Fix SUPER_ADMIN access to messages

**files:** `src/shared/lib/auth.config.ts`

**action:** Add SUPER_ADMIN to `/messages` roleRoutes entry

**verify:** SUPER_ADMIN can access /messages route

**done:** auth.config includes SUPER_ADMIN for messages
