---
phase: 260722-x84-teacher-edit-level-step
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/user-admin/ui/UserDetailModal.tsx
  - src/features/groups/ui/GroupStudentsTable.tsx
  - src/shared/lib/validations/user.ts
autonomous: true
requirements:
  - QUICK-teacher-edit-level-step
user_setup: []

must_haves:
  truths:
    - "Учитель в модалке ученика на /my-group может менять Уровень и Текущий шаг и сохранить"
    - "После сохранения прогресс ученика в выбранной группе обновляется (levelId + currentStepIdx через updateStudentProgress)"
    - "Пустые опциональные поля (телефон, имя/телефон опекуна) в модалке без example-placeholder — пустой Input"
    - "Остальные поля в teacher-режиме (имя, телефоны) остаются read-only; статус по-прежнему редактируется"
  artifacts:
    - path: src/features/user-admin/ui/UserDetailModal.tsx
      provides: "editable level+step в teacher progress mode; submit через updateStudentProgress; без fake placeholders"
    - path: src/features/groups/ui/GroupStudentsTable.tsx
      provides: "проброс groupId в UserDetailModal"
    - path: src/shared/lib/validations/user.ts
      provides: "levelId в student form schema для UI teacher/manager progress fields"
  key_links:
    - from: UserDetailModal teacher save
      to: updateStudentProgress(studentId, { groupId, levelId, localStepIndex })
      via: "существующий requireStudentEditAccess (TEACHER своих групп)"
    - from: GroupStudentsTable groupId
      to: UserDetailModal
      via: "prop groupId для payload прогресса"
    - from: level Select onChange
      to: localStepIndex
      via: "сброс localStepIndex в 0 при смене уровня (как StudentProgressForm)"
---

<objective>
Дать учителю в модалке ученика («Моя группа») менять уровень и текущий шаг, и убрать example-placeholder у пустых опциональных полей контактов.

Purpose: Сейчас `/my-group` открывает `UserDetailModal` с `readOnly` + `canEditStatus` — уровень disabled Input, шаг disabled Select; бэкенд `updateStudentProgress` уже разрешает TEACHER, но модалка его не вызывает. Пустые телефоны/опекун выглядят как заполненные из-за example-placeholder.

Output: обновлённый `UserDetailModal` + проброс `groupId` + form schema с `levelId`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@src/features/user-admin/ui/UserDetailModal.tsx
@src/features/groups/ui/GroupStudentsTable.tsx
@src/features/groups/ui/MyGroupView.tsx
@src/features/student-admin/actions/student-admin-actions.ts
@src/features/student-admin/ui/StudentProgressForm.tsx
@src/shared/lib/validations/user.ts
@src/shared/lib/validations/student-progress.ts
@src/shared/lib/authorize-student-access.ts
</context>

## Current State

- `MyGroupView` → `GroupStudentsTable` с `readOnly` + `canEditStatus` → модалка в `statusOnlyMode`: сохраняет только `updateStudentStatus`.
- Уровень: disabled `Input` с `enrollmentGroups?.[0]?.levelTitle`.
- Текущий шаг: `Select` по `localStepIndex`, но `disabled={readOnly || !selectedLevel}`.
- `updateStudentProgress` + `requireStudentEditAccess` уже позволяют TEACHER менять `levelId`/`localStepIndex` для своей группы; отдельная страница `/students/[studentId]/edit` существует, но учитель работает через модалку.
- `/my-group/page.tsx` уже кладёт в `mapUsersToDetails` один enrollment выбранной группы — `user.student.groupId` / `levelId` / `localStepIndex` корректны для текущего контекста.
- Example-placeholder на полях телефона/опекуна в `UserDetailModal` (и staff phone) создают ложное ощущение данных.

**Out of scope:** CreateUserForm (создание), изменение ролей/прав менеджера сверх текущего full-edit, redesign модалки, multi-enrollment picker в одной модалке.

## Source Coverage Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL | Учитель меняет уровень и текущий шаг ученика | COVERED — Task 1–2 |
| GOAL | Пустые опциональные поля без fake placeholders | COVERED — Task 3 |
| REQ | QUICK-teacher-edit-level-step | COVERED — весь план |
| RESEARCH | Quick mode, research skipped; reuse updateStudentProgress | COVERED — Task 2 |
| CONTEXT | FSD features/, existing actions; no scope creep | COVERED |
| Deferred | — | none |

<tasks>

<task type="auto">
  <name>Task 1: Form schema + groupId prop wiring</name>
  <files>src/shared/lib/validations/user.ts, src/features/groups/ui/GroupStudentsTable.tsx</files>
  <read_first>
    - src/shared/lib/validations/user.ts — updateStudentUserFormSchema / UpdateStudentUserFormInput
    - src/features/groups/ui/GroupStudentsTable.tsx — UserDetailModal props (~265–275), уже есть groupId в props таблицы
    - src/features/user-admin/ui/UserDetailModal.tsx — UserDetail.student.groupId / levelId
  </read_first>
  <action>
    1. В `updateStudentUserFormSchema` добавить опциональное поле `levelId: z.string().optional()` (только form schema; server `updateStudentUserSchema` не трогать — manager full-edit по-прежнему идёт через updateUser без смены уровня).
    2. В `GroupStudentsTable` передать в `UserDetailModal` prop `groupId={groupId}` (значение уже есть у таблицы для /my-group и /groups/[id]).
    Не менять UI модалки и placeholders в этой задаче.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | Select-Object -First 40</automated>
  </verify>
  <done>
    Form schema содержит optional levelId; GroupStudentsTable пробрасывает groupId в UserDetailModal (тип prop добавлен/ожидается в Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Teacher progress edit in UserDetailModal</name>
  <files>src/features/user-admin/ui/UserDetailModal.tsx</files>
  <read_first>
    - src/features/user-admin/ui/UserDetailModal.tsx — StudentEditFields, statusOnlyMode, handleStatusOnlySubmit
    - src/features/student-admin/ui/StudentProgressForm.tsx — эталон Select уровня + сброс localStepIndex
    - src/features/student-admin/actions/student-admin-actions.ts — updateStudentProgress signature
    - src/shared/lib/validations/student-progress.ts — payload { groupId, levelId, localStepIndex }
  </read_first>
  <action>
    В `UserDetailModal.tsx` (teacher path = readOnly + canEditStatus + isStudent):

    1. Добавить prop `groupId?: string`. Режим прогресса: canEditProgress = readOnly AND canEditStatus AND isStudent.
    2. `getStudentDefaultValues`: добавить `levelId: user.student?.levelId ?? ""` и сохранить `localStepIndex` / `status` как сейчас.
    3. В `StudentEditFields`: принимать `canEditProgress` и `control`/`setValue` для `levelId`. Заменить disabled Input «Уровень» на `Select` по `levels` (лейблы как в StudentProgressForm: «Уровень N: title»), когда `canEditProgress`; при смене уровня `setValue('localStepIndex', 0)`. Источник selectedLevel — `useWatch(levelId)`, не только `enrollmentGroups[0]`.
    4. Select «Текущий шаг»: `disabled` только если нет selectedLevel или (readOnly и не canEditProgress) — учитель может менять шаг.
    5. Имя / телефоны / опекун остаются `disabled={readOnly}` (учитель их не редактирует).
    6. Submit при canEditProgress: вызвать `updateStudentProgress(student.id, { groupId: groupId ?? user.student.groupId, levelId, localStepIndex })`, затем `updateStudentStatus` если статус изменился (или всегда с текущим status — идемпотентно). Не вызывать `updateUser` в этом режиме (он только MANAGER/SUPER_ADMIN).
    7. Показывать кнопку «Сохранить» в canEditProgress (уже через statusOnlyMode / расширить условие showSaveButton).
    8. Группы по-прежнему read-only Input. Не добавлять удаление/сброс кода для учителя.

    Ошибки: message.error с текстом Error.message на русском при throw. После успеха — router.refresh + onClose.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | Select-Object -First 40</automated>
  </verify>
  <done>
    При readOnly+canEditStatus учитель видит editable Уровень и Текущий шаг; save пишет прогресс через updateStudentProgress для groupId контекста группы.
  </done>
</task>

<task type="auto">
  <name>Task 3: Empty optional fields without example placeholders</name>
  <files>src/features/user-admin/ui/UserDetailModal.tsx</files>
  <read_first>
    - src/features/user-admin/ui/UserDetailModal.tsx — Input phone / guardianName / guardianPhone (student + staff)
  </read_first>
  <action>
    В `UserDetailModal` убрать все example-placeholder у опциональных контактных Input (телефон ученика, имя опекуна, телефон опекуна, телефон staff). Поля остаются пустыми, когда value пустой строки; не подставлять демо-значения в value. CreateUserForm и другие формы не трогать.
    <!-- planner-discipline-allow: 89676123456 -->
    <!-- planner-discipline-allow: Ибрагимов Камал Ахмедович -->
  </action>
  <verify>
    <automated>pnpm exec rg -n "89676123456|Ибрагимов Камал Ахмедович" src/features/user-admin/ui/UserDetailModal.tsx; if ($LASTEXITCODE -eq 0) { throw "placeholders still present" } else { Write-Output "OK: no example placeholders in UserDetailModal" }</automated>
  </verify>
  <done>
    В UserDetailModal нет example-placeholder на контактных полях; пустые данные выглядят как пустой Input.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server action updateStudentProgress | teacher/manager отправляет groupId, levelId, localStepIndex |
| teacher→student enrollment | учитель может менять только учеников своей группы |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x84-01 | Elevation of Privilege | updateStudentProgress | high | mitigate | Уже: requireStudentEditAccess — TEACHER только если teacherId группы совпадает; level должен принадлежать subjectId группы |
| T-x84-02 | Tampering | forged groupId in modal | medium | mitigate | findEnrollmentInGroup + ownership check; UI передаёт groupId выбранной группы /my-group |
| T-x84-03 | Information Disclosure | phone/guardian fields | low | accept | Поля уже видны учителю; только убираем placeholder, не меняем ACL |
| T-x84-SC | Tampering | npm installs | low | accept | Новых пакетов нет |
</threat_model>

<verification>
- tsc без ошибок по затронутым файлам
- rg: нет example-placeholder строк в UserDetailModal
- Ручной smoke (executor note): учитель на /my-group → клик ученик → сменить уровень/шаг → Сохранить → обновить модалку/таблицу
</verification>

<success_criteria>
- Учитель меняет уровень и шаг в модалке и данные сохраняются
- Пустые контакты без фейковых подсказок-плейсхолдеров
- Scope не расширен за пределы модалки прогресса учителя + placeholders
</success_criteria>

<output>
Create `.planning/quick/260722-x84-teacher-edit-level-step/260722-x84-SUMMARY.md` when done
</output>
