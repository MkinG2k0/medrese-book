---
phase: 260718-3fl-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/program-admin/ui/StepForm.tsx
autonomous: true
requirements:
  - QUICK-step-pdf-editor-viewer
user_setup: []

must_haves:
  truths:
    - "Когда у шага есть pdfUrl, в форме редактирования («Содержание») виден inline-просмотр PDF (iframe), как в журнале/портале"
    - "После «Прикрепить PDF» / «Заменить» просмотрщик показывает новый файл без перезагрузки страницы"
    - "После «Удалить» просмотрщик исчезает; кнопки и rich-text редактор не ломаются"
  artifacts:
    - path: src/features/program-admin/ui/StepForm.tsx
      provides: "StepPdfViewer под контролами PDF при непустом pdfUrl"
    - path: src/features/program-admin/ui/StepPdfViewer.tsx
      provides: "Переиспользуемый iframe-viewer (без изменений API)"
  key_links:
    - from: StepForm pdfUrl state
      to: StepPdfViewer url prop
      via: "условный рендер при truthy pdfUrl"
    - from: uploadPdf / Удалить
      to: setPdfUrl
      via: "тот же state, что уже обновляет ссылку и payload сохранения"
---

<objective>
Показать существующий `StepPdfViewer` в редакторе шага (`StepForm`), когда прикреплён PDF — тот же inline-просмотр, что уже есть в журнале и портале ученика через `LessonContentView`.

Purpose: Админ при редактировании шага сразу видит содержимое PDF, а не только имя файла и внешнюю ссылку.

Output: `StepForm` рендерит `StepPdfViewer` при `pdfUrl`; без новых компонентов и npm-пакетов.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260718-2pl-pdf-pdf/260718-2pl-SUMMARY.md
@src/features/program-admin/ui/StepForm.tsx
@src/features/program-admin/ui/StepPdfViewer.tsx
@src/features/program-admin/ui/LessonContentView.tsx
</context>

## Scope

- Только UI в `StepForm`: встроить уже существующий `StepPdfViewer`.
- Не создавать второй viewer, не трогать `LessonContentView`, journal, student-portal, Prisma, Zod, `/api/uploads`.
- Не добавлять npm-пакеты для PDF.
- Сохранить текущие контролы: имя файла / «Заменить» / «Удалить» / «Прикрепить PDF» и блок «Заметка учителя».

## Source Coverage Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL | Просмотрщик PDF в редакторе шага | COVERED — Task 1 |
| REQ | QUICK-step-pdf-editor-viewer | COVERED — Task 1 |
| RESEARCH | (нет research phase) | N/A |
| CONTEXT | Reuse StepPdfViewer; show after upload/replace; hide after delete | COVERED — Task 1 |
| Deferred | — | none |
| Out of scope | Новый PDF-компонент; npm PDF libs; изменения журнала/портала | excluded |

<tasks>

<task type="auto">
  <name>Task 1: Встроить StepPdfViewer в StepForm</name>
  <files>src/features/program-admin/ui/StepForm.tsx</files>
  <read_first>
    - src/features/program-admin/ui/StepForm.tsx — секция Form.Item «Содержание», state `pdfUrl`, Upload/Удалить
    - src/features/program-admin/ui/StepPdfViewer.tsx — props `{ url: string }`, клиентский iframe
    - src/features/program-admin/ui/LessonContentView.tsx — эталон условного рендера StepPdfViewer при hasPdf и pdfUrl
  </read_first>
  <action>
    1. Импортировать `StepPdfViewer` из `@/features/program-admin/ui/StepPdfViewer` (тот же путь, что в `LessonContentView`). Не создавать новый файл viewer и не менять публичный API `StepPdfViewer`.
    2. В Form.Item «Содержание», внутри колонки `flex flex-col gap-3`: после ряда с именем файла / «Заменить» / «Удалить» / «Прикрепить PDF» и до `StepEditor` условно отрендерить просмотрщик, когда `pdfUrl` непустой (trim / Boolean, как в LessonContentView). Передать `url={pdfUrl}`.
    3. Чтобы после «Заменить» iframe гарантированно обновился при смене URL, задать `key={pdfUrl}` на `StepPdfViewer` (discretion: remount при новом URL).
    4. Не дублировать логику `pdfEmbedUrl` в StepForm. Не менять upload/delete handlers — они уже вызывают `setPdfUrl`; viewer должен автоматически появиться/обновиться/исчезнуть через React state.
    5. Layout: только `div` + Tailwind; без antd Flex. Не трогать «Заметка учителя», submit payload, create/edit pages.
  </action>
  <verify>
    <automated>rg -n "StepPdfViewer" src/features/program-admin/ui/StepForm.tsx; rg -n "from '@/features/program-admin/ui/StepPdfViewer'" src/features/program-admin/ui/StepForm.tsx src/features/program-admin/ui/LessonContentView.tsx</automated>
  </verify>
  <done>
    StepForm импортирует и рендерит StepPdfViewer при truthy pdfUrl; после upload/replace viewer виден с новым URL; после delete — не рендерится; StepPdfViewer.tsx и LessonContentView не изменены без необходимости.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → iframe src | URL PDF из state (upload API или сохранённый Step.pdfUrl) загружается в iframe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3fl-01 | Tampering | StepForm iframe url | low | accept | URL уже приходит только из `/api/uploads` или БД; фаза не расширяет источники |
| T-3fl-02 | Information Disclosure | PDF в админ-форме | low | accept | Страница уже за ролями manager/super-admin; тот же контент, что сохраняется в шаг |
| T-3fl-SC | Tampering | npm installs | low | accept | Новых пакетов нет — reuse StepPdfViewer |
</threat_model>

<verification>
- `rg -n "StepPdfViewer" src/features/program-admin/ui/StepForm.tsx` находит import и JSX.
- Логически: pdfUrl set → viewer; pdfUrl null → нет viewer; замена URL → новый key/src.
</verification>

<success_criteria>
- В редакторе шага с прикреплённым PDF виден inline-просмотр через существующий StepPdfViewer.
- Upload / replace / delete управляют видимостью viewer через текущий `pdfUrl` state.
- Нет новых npm-зависимостей и нет второго PDF-компонента.
</success_criteria>

<output>
Create `.planning/quick/260718-3fl-pdf/260718-3fl-SUMMARY.md` when done
</output>
