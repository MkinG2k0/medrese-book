# Quick Task 260701-x5w: Fix substitution label display logic

Метка «Учитель — Замещение» показывалась по наличию `switchOwnerId` в сессии, без проверки активного замещения в БД.

## Задачи

1. Добавить `isTeacherActivelySubstituting` и `getSubstitutionTargetUserIds` в `get-substitution-header-info.ts`
2. Передавать флаги из `(dashboard)/layout.tsx` в `AppShell` и `UserSwitcher`
3. Показывать метку только при реальном активном замещении
