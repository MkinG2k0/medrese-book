# Quick Task 260702-07p: Продовый сидер супер-админа из env

## Задачи

### 1. Скрипт `prisma/seed-prod.ts`
- Создаёт только пользователя `SUPER_ADMIN`
- Код из `SUPER_ADMIN_CODE` или `SUPER_ADMIN_PASSWORD` (6 цифр)
- Имя из `SUPER_ADMIN_NAME` (опционально)
- Идемпотентен: не перезаписывает существующего супер-админа
- Не удаляет данные

### 2. Env-шаблоны
- `.env.production.example` — шаблон для прода
- `.env.example` — добавить переменные сидера

### 3. npm-скрипт `db:seed:prod`
