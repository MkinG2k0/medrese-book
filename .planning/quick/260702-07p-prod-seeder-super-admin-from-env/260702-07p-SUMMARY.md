---
status: complete
---

# Quick Task 260702-07p: Продовый сидер супер-админа из env

## Сделано

- `prisma/seed-prod.ts` — создаёт только `SUPER_ADMIN`, код из env, без очистки БД
- `pnpm db:seed:prod` — команда запуска
- `.env.production.example` — шаблон prod-окружения
- `.env.example` — добавлены `SUPER_ADMIN_CODE`, `SUPER_ADMIN_NAME`
- `.env` — добавлены переменные для локального запуска
- `.gitignore` — разрешён коммит `.env.example` и `.env.production.example`

## Проверка

`pnpm db:seed:prod` — успешно, идемпотентный skip при существующем супер-админе.

## Использование на проде

```bash
cp .env.production.example .env
# заполнить DATABASE_URL, AUTH_SECRET, SUPER_ADMIN_CODE
pnpm db:migrate deploy
pnpm db:seed:prod
```
