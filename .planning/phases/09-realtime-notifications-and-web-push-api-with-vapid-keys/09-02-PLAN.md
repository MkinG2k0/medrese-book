---
phase: 09-realtime-notifications-and-web-push-api-with-vapid-keys
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/shared/lib/validations/notification.ts
  - src/app/api/notifications/route.ts
  - src/app/api/notifications/unread-count/route.ts
  - src/app/api/notifications/mark-read/route.ts
  - src/entities/notification/model/types.ts
  - src/entities/notification/api/use-notifications.ts
  - src/entities/notification/api/use-unread-count.ts
  - src/entities/notification/api/use-mark-notification-read.ts
  - src/entities/notification/index.ts
  - src/features/notifications/ui/NotificationBell.tsx
  - src/features/notifications/ui/NotificationList.tsx
  - src/features/notifications/index.ts
  - src/widgets/app-shell/ui/AppShell.tsx
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-04
must_haves:
  truths:
    - "Авторизованный пользователь любой роли видит колокольчик в Header с Badge непрочитанных"
    - "Dropdown показывает список уведомлений (прочитанные + непрочитанные, newest first)"
    - "Пользователь отмечает уведомление прочитанным — Badge уменьшается"
    - "API возвращает только уведомления session.user.id (IDOR невозможен)"
    - "Пустое состояние: «Нет новых уведомлений»"
  artifacts:
    - path: src/app/api/notifications/unread-count/route.ts
      provides: "GET unread count"
      exports: ["GET"]
    - path: src/features/notifications/ui/NotificationBell.tsx
      provides: "BellOutlined + Badge + Dropdown"
    - path: src/widgets/app-shell/ui/AppShell.tsx
      provides: "NotificationBell in Header for all roles"
      pattern: "NotificationBell"
  key_links:
    - from: NotificationBell.tsx
      to: /api/notifications/unread-count
      via: useUnreadCount hook
      pattern: "notifications.*unread-count|useUnreadCount"
    - from: NotificationList.tsx
      to: /api/notifications/mark-read
      via: useMarkNotificationRead mutation
      pattern: "mark-read"
---

<objective>
REST API для in-app уведомлений, React Query hooks и колокольчик в AppShell для всех ролей.

Purpose: NOTF-01, NOTF-02 — Phase 6 foundation UI; NOTF-04 — отображение системных событий отпусков.
Output: GET list, unread count, mark-read; NotificationBell + NotificationList в Header.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-01-SUMMARY.md
@src/widgets/app-shell/ui/AppShell.tsx
@src/app/api/students/route.ts
@src/entities/student/api/use-students.ts
@.planning/phases/08-leave-requests/08-UI-SPEC.md

<interfaces>
API response shape from @/shared/api:
```typescript
{ data: T | null, error: string | null }
```

Notification DTO:
```typescript
type NotificationItem = {
  id: string
  type: string
  title: string
  body: string
  link: string | null
  readAt: string | null
  createdAt: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REST API — list, unread-count, mark-read</name>
  <files>
    src/shared/lib/validations/notification.ts
    src/app/api/notifications/route.ts
    src/app/api/notifications/unread-count/route.ts
    src/app/api/notifications/mark-read/route.ts
  </files>
  <read_first>
    - src/app/api/students/route.ts — authorizeApiRequest + success/error
    - src/shared/lib/authorize-api-request.ts
    - prisma/schema.prisma — Notification model (post 09-01)
    - .planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md — Pattern 5 API routes
  </read_first>
  <acceptance_criteria>
    - GET `/api/notifications` — query `?limit=50` optional; `where: { userId: session.user.id }`; orderBy createdAt desc
    - GET `/api/notifications/unread-count` — count where `readAt: null` AND `userId: session.user.id`
    - PATCH `/api/notifications/mark-read` — body Zod `{ ids: string[] }` OR `{ all: true }`; update only rows matching userId
    - Unauthenticated → `unauthorized()`; no userId query param trusted
    - `grep "userId: session" src/app/api/notifications/` — все routes filter by session
    - Response DTO: id, type, title, body, link, readAt ISO, createdAt ISO
  </acceptance_criteria>
  <action>
    `authorizeApiRequest` allowedRoles: `['TEACHER', 'MANAGER', 'SUPER_ADMIN', 'STUDENT']` — все dashboard роли.
  mark-read: `prisma.notification.updateMany({ where: { id: { in: ids }, userId }, data: { readAt: new Date() } })`.
  mark all: `where: { userId, readAt: null }`.
  Zod schema в `src/shared/lib/validations/notification.ts` (новый файл) с русскими ошибками.
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/app/api/notifications/route.ts src/app/api/notifications/unread-count/route.ts src/app/api/notifications/mark-read/route.ts</automated>
  </verify>
  <done>API endpoints готовы с session-scoped access control</done>
</task>

<task type="auto">
  <name>Task 2: entities/notification React Query hooks</name>
  <files>
    src/entities/notification/model/types.ts
    src/entities/notification/api/use-notifications.ts
    src/entities/notification/api/use-unread-count.ts
    src/entities/notification/api/use-mark-notification-read.ts
    src/entities/notification/index.ts
  </files>
  <read_first>
    - src/entities/student/api/use-students.ts — queryKey + json.error pattern
    - src/entities/session/api/use-sessions.ts — mutation invalidateQueries if exists
  </read_first>
  <acceptance_criteria>
    - `useNotifications()` — queryKey `['notifications']`, fetch GET /api/notifications
    - `useUnreadCount()` — queryKey `['notifications', 'unread-count']`, refetchInterval optional 60s fallback
    - `useMarkNotificationRead()` — PATCH mark-read; onSuccess invalidate `['notifications']` and `['notifications', 'unread-count']`
    - `entities/notification/index.ts` exports types + hooks
    - `pnpm exec tsc --noEmit` passes
  </acceptance_criteria>
  <action>
    Client hooks only — no server imports in entities layer.
    mutationFn throws on json.error per project convention.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Клиентские hooks готовы для UI колокольчика</done>
</task>

<task type="auto">
  <name>Task 3: NotificationBell + NotificationList в AppShell Header</name>
  <files>
    src/features/notifications/ui/NotificationBell.tsx
    src/features/notifications/ui/NotificationList.tsx
    src/features/notifications/index.ts
    src/widgets/app-shell/ui/AppShell.tsx
  </files>
  <read_first>
    - src/widgets/app-shell/ui/AppShell.tsx — Header block (lines ~240-247)
    - CLAUDE.md — Ant Design + Tailwind conventions (layout-only Tailwind, no Flex from antd)
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — spacing, typography
  </read_first>
  <acceptance_criteria>
    - `grep "NotificationBell" src/widgets/app-shell/ui/AppShell.tsx` — import and render in Header BEFORE user name block
    - Bell visible for ALL roles (no RoleGuard hiding bell)
    - `BellOutlined` + antd `Badge` count from useUnreadCount; count=0 hides badge dot
    - Dropdown width ~360px; `NotificationList` inside
    - Unread items: `readAt === null` styled with `Text strong` or background highlight (layout Tailwind only)
    - Click item → mark read + navigate to `link` if present via `router.push`
    - Empty state copy: «Нет новых уведомлений»
    - Footer link/button: «Отметить все прочитанными» when unread > 0
    - `grep "Нет новых уведомлений" src/features/notifications/` — match
  </acceptance_criteria>
  <action>
    `'use client'` components in `src/features/notifications/ui/`.
    NotificationList: antd `List` with `dataSource` from useNotifications; show title + body + relative/short date.
    Do NOT add RoleGuard around bell — students get empty list in v1 (A3 in RESEARCH).
    Header layout: `justify-end gap-4` — bell left of user name div.
    features/notifications/index.ts barrel export NotificationBell only (list internal).
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/features/notifications/ui/NotificationBell.tsx src/widgets/app-shell/ui/AppShell.tsx</automated>
  </verify>
  <done>Колокольчик в шапке для всех ролей с in-app списком (NOTF-01, NOTF-02)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→GET /api/notifications | Session cookie auth; IDOR via id param |
| client→PATCH mark-read | Must not mark other users' notifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-04 | E | GET /api/notifications | mitigate | `where: { userId: session.user.id }` on every query |
| T-9-05 | E | PATCH mark-read | mitigate | updateMany scoped to session.user.id; reject foreign ids silently (count 0) |
| T-9-06 | I | Notification list API | mitigate | No admin override in v1; managers see only own notifications |
| T-9-07 | T | XSS in body | mitigate | React text escape; body server-generated only |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit`
- Manual: login manager → create leave as teacher (separate session) → manager bell shows count
</verification>

<success_criteria>
- NOTF-01 bell with unread Badge
- NOTF-02 list + mark read
- NOTF-04 leave notifications visible in dropdown
</success_criteria>

<output>
After completion, create `.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-02-SUMMARY.md`
</output>
