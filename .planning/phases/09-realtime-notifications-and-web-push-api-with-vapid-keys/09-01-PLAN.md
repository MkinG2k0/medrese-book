---
phase: 09-realtime-notifications-and-web-push-api-with-vapid-keys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/shared/lib/notifications/build-notification.ts
  - src/shared/lib/notifications/enqueue-notifications.ts
  - src/shared/lib/notifications/deliver-notifications.ts
  - src/shared/lib/domain-events/handlers/notifications.ts
  - src/shared/lib/domain-events/dispatch.ts
  - src/features/leave-requests/actions/leave-actions.ts
  - src/shared/lib/notifications/build-notification.test.ts
autonomous: true
requirements:
  - NOTF-04
must_haves:
  truths:
    - "Событие LEAVE_REQUEST_CREATED создаёт Notification для каждого MANAGER и SUPER_ADMIN"
    - "Событие LEAVE_REQUEST_APPROVED создаёт Notification для teacherUserId из payload"
    - "Событие SUBSTITUTION_ACTIVATED создаёт Notification для userId замещающего преподавателя"
    - "dispatchDomainEvent возвращает созданные Notification[] внутри транзакции"
    - "После commit leave-actions вызывает deliverNotifications (stub без сетевого I/O)"
  artifacts:
    - path: prisma/schema.prisma
      provides: "Notification + PushSubscription models"
      contains: "model Notification"
    - path: src/shared/lib/notifications/build-notification.ts
      provides: "recipient + copy mapping per DomainEventAction"
      exports: ["buildNotificationsForEvent"]
    - path: src/shared/lib/notifications/enqueue-notifications.ts
      provides: "persist notifications in transaction"
      exports: ["enqueueNotifications"]
    - path: src/shared/lib/domain-events/dispatch.ts
      provides: "returns created notifications"
      pattern: "return enqueueNotifications"
  key_links:
    - from: leave-actions.ts
      to: deliverNotifications
      via: "void after $transaction commit"
      pattern: "deliverNotifications\\("
    - from: enqueue-notifications.ts
      to: prisma.notification.createMany
      via: "transaction client"
      pattern: "notification\\.create"
---

<objective>
Создать Prisma-модели уведомлений и push-подписок, реализовать enqueue из domain events, post-commit delivery stub.

Purpose: Фундамент Phase 6 (никогда не был реализован) + NOTF-04; источник событий — Phase 8 leave-actions.
Output: Миграция применена, Notification rows при create/approve/reject/substitution, unit-тесты copy/recipients.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-CONTEXT.md
@.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md
@.planning/phases/08-leave-requests/08-UI-SPEC.md
@prisma/schema.prisma
@src/shared/lib/domain-events/types.ts
@src/shared/lib/domain-events/dispatch.ts
@src/shared/lib/domain-events/handlers/notifications.ts
@src/features/leave-requests/actions/leave-actions.ts

<interfaces>
From src/shared/lib/domain-events/types.ts:
```typescript
export type DomainEventAction =
  | 'LEAVE_REQUEST_CREATED'
  | 'LEAVE_REQUEST_APPROVED'
  | 'LEAVE_REQUEST_REJECTED'
  | 'SUBSTITUTION_ACTIVATED'
  // ... other actions
```

From leave-actions buildLeaveEventPayload:
```typescript
{
  leaveRequestId, teacherUserId, teacherId, type,
  startDate, endDate, substituteTeacherId?
}
```

Copywriting (08-UI-SPEC + RESEARCH):
- LEAVE_REQUEST_CREATED: title «Новая заявка на отсутствие», body `{teacherName}, {typeLabel}, {dates}`
- LEAVE_REQUEST_APPROVED: title «Заявка подтверждена»
- LEAVE_REQUEST_REJECTED: title «Заявка отклонена», body includes rejectionReason
- SUBSTITUTION_ACTIVATED: «Вы замещаете {ФИО} с {дата} по {дата}»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma Notification и PushSubscription</name>
  <files>
    prisma/schema.prisma
  </files>
  <read_first>
    - prisma/schema.prisma — model User, enum patterns
    - C:\Users\mk\.cursor\plugins\cache\cursor-public\prisma\4584a0a9175ba74053d7ee946c6234d3369a5a33\rules\schema-conventions.mdc
    - .planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md — Prisma models section
  </read_first>
  <acceptance_criteria>
    - `grep "enum NotificationType" prisma/schema.prisma` → LEAVE_REQUEST_CREATED, LEAVE_REQUEST_APPROVED, LEAVE_REQUEST_REJECTED, SUBSTITUTION_ACTIVATED
    - `model Notification` содержит: userId, type, title, body, link?, payload Json?, readAt?, createdAt @default(now())
    - `model PushSubscription` содержит: userId, endpoint @unique, p256dh, auth, createdAt, updatedAt @updatedAt
    - User: relations `notifications Notification[]`, `pushSubscriptions PushSubscription[]`
    - @@index([userId, readAt]), @@index([userId, createdAt]), @@index([userId]) на PushSubscription
    - onDelete: Cascade на user relations
  </acceptance_criteria>
  <action>
    Добавить enum `NotificationType` и модели per RESEARCH.md.
    `link` nullable — для навигации: `/admin/leave-calendar` (manager), `/calendar` (teacher), `/journal` (substitute).
    Не добавлять NOTF-03 enum values (PERFORMANCE_ALERT и т.п.) — defer NOTF-03.
    PushSubscription готов для Wave 09-04, но schema создаётся здесь.
  </action>
  <verify>
    <automated>pnpm prisma validate</automated>
  </verify>
  <done>schema.prisma валиден, модели Notification и PushSubscription определены</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build-notification, enqueue, dispatch return, deliver stub</name>
  <files>
    src/shared/lib/notifications/build-notification.ts
    src/shared/lib/notifications/build-notification.test.ts
    src/shared/lib/notifications/enqueue-notifications.ts
    src/shared/lib/notifications/deliver-notifications.ts
    src/shared/lib/domain-events/handlers/notifications.ts
    src/shared/lib/domain-events/dispatch.ts
    src/features/leave-requests/actions/leave-actions.ts
  </files>
  <behavior>
    - LEAVE_REQUEST_CREATED → N notifications (one per MANAGER + SUPER_ADMIN userId)
    - LEAVE_REQUEST_APPROVED → 1 notification for payload.teacherUserId, title «Заявка подтверждена»
    - LEAVE_REQUEST_REJECTED → 1 for teacherUserId, body contains rejectionReason
    - SUBSTITUTION_ACTIVATED → 1 for substitute userId (lookup Teacher by substituteTeacherId)
    - Unknown action → empty array
  </behavior>
  <read_first>
    - src/shared/lib/domain-events/handlers/notifications.ts — текущий no-op
    - src/features/leave-requests/lib/leave-labels.ts — русские метки типов
    - src/features/leave-requests/actions/leave-actions.ts — buildLeaveEventPayload, $transaction pattern
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — Copywriting Contract (substitution line)
  </read_first>
  <acceptance_criteria>
    - `grep "buildNotificationsForEvent" src/shared/lib/notifications/build-notification.ts` — exported async function
    - `handlers/notifications.ts` re-exports or delegates to `enqueue-notifications.ts` (no duplicate logic)
    - `dispatch.ts`: `return await enqueueNotifications(event, tx)` после writeAuditEvent
    - `enqueueNotifications`: createMany/create per built row inside tx; returns `Notification[]` with ids
    - `deliver-notifications.ts`: exported `deliverNotifications(notifications)` — no-op stub with comment «push + SSE in 09-03/09-04»
    - leave-actions: createLeaveRequest, approveLeaveRequest, rejectLeaveRequest собирают notifications из dispatch внутри tx; после tx `void deliverNotifications(all)`
    - approve: concat notifications from LEAVE_REQUEST_APPROVED + SUBSTITUTION_ACTIVATED
    - `pnpm test:unit -- src/shared/lib/notifications/build-notification.test.ts` passes
  </acceptance_criteria>
  <action>
    `build-notification.ts`: pure mapping + async prisma lookups only in enqueue layer.
    Manager fan-out: `prisma.user.findMany({ where: { role: { in: ['MANAGER', 'SUPER_ADMIN'] } } })`.
    SUBSTITUTION_ACTIVATED: lookup `teacher.findUnique({ where: { id: payload.substituteTeacherId }, include: { user: true } })` + absent teacher name for copy «Вы замещаете {ФИО} с {дата} по {дата}» (format DD.MM.YYYY via date-fns).
    Date format in body: `DD.MM.YYYY — DD.MM.YYYY`.
    Type labels: reuse leave-labels или inline «Отпуск»/«Отгул»/«Больничный».
    Unit test: mock prisma in build layer OR test pure recipient/copy helpers with fixture payloads (no DB).
    Не импортировать из `@/features/leave-requests` в shared — только domain event payload fields.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/notifications/build-notification.test.ts</automated>
  </verify>
  <done>Domain events создают Notification rows; deliverNotifications вызывается post-commit</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Миграция Prisma</name>
  <files>
    prisma/migrations/
  </files>
  <read_first>
    - package.json — script db:migrate
    - C:\Users\mk\.cursor\plugins\cache\cursor-public\prisma\4584a0a9175ba74053d7ee946c6234d3369a5a33\rules\migration-best-practices.mdc
  </read_first>
  <acceptance_criteria>
    - Последняя миграция содержит `Notification` и `PushSubscription`
    - `pnpm prisma migrate status` — Applied, нет pending
    - `pnpm db:generate` завершается без ошибок
    - `pnpm exec tsc --noEmit` проходит с новым Prisma client
  </acceptance_criteria>
  <action>
    После Task 1–2 выполнить: `pnpm db:migrate -- --name notifications_push_subscriptions`
    Затем `pnpm db:generate`.
    Миграция ОБЯЗАТЕЛЬНА до любой верификации enqueue против реальной БД.
    Не запускать db:seed.
  </action>
  <verify>
    <automated>pnpm prisma migrate status</automated>
  </verify>
  <done>БД синхронизирована, Prisma Client с Notification и PushSubscription</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain event → enqueue | Server-side only; recipients derived from DB, not client input |
| post-commit deliver | External I/O isolated from Prisma transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | I | enqueueNotifications recipients | mitigate | Recipients from role lookup / payload teacherUserId; never trust client userId |
| T-9-02 | T | notification body | mitigate | Server-generated copy only from audited event payload |
| T-9-03 | D | Prisma migration | mitigate | `pnpm db:migrate --name` not db push in prod path |
</threat_model>

<verification>
- `pnpm prisma validate` && `pnpm prisma migrate status`
- `pnpm test:unit -- src/shared/lib/notifications/build-notification.test.ts`
- `pnpm exec tsc --noEmit`
</verification>

<success_criteria>
- Notification rows persist on leave/substitution domain events (NOTF-04)
- dispatchDomainEvent returns created notifications for post-commit delivery
- PushSubscription schema ready for Wave 09-04
</success_criteria>

<output>
After completion, create `.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-01-SUMMARY.md`
</output>
