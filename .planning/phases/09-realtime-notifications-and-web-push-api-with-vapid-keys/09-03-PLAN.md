---
phase: 09-realtime-notifications-and-web-push-api-with-vapid-keys
plan: 03
type: execute
wave: 3
depends_on:
  - "09-02"
files_modified:
  - src/app/api/notifications/stream/route.ts
  - src/entities/notification/api/use-notification-stream.ts
  - src/entities/notification/index.ts
  - src/features/notifications/ui/NotificationBell.tsx
  - middleware.ts
  - e2e/notifications-realtime.spec.ts
autonomous: true
requirements:
  - NOTF-05
must_haves:
  truths:
    - "Открытая dashboard-страница получает новое уведомление без reload (Badge обновляется)"
    - "SSE endpoint требует сессию — без auth нет stream"
    - "Service worker path sw.js исключён из auth middleware (подготовка к 09-04)"
    - "Клиент переподключается при disconnect; heartbeat каждые 30s"
  artifacts:
    - path: src/app/api/notifications/stream/route.ts
      provides: "SSE text/event-stream"
      exports: ["GET"]
    - path: src/entities/notification/api/use-notification-stream.ts
      provides: "EventSource hook"
      exports: ["useNotificationStream"]
    - path: middleware.ts
      provides: "sw.js excluded from matcher"
      pattern: "sw\\.js"
  key_links:
    - from: useNotificationStream
      to: /api/notifications/stream
      via: EventSource onmessage
      pattern: "EventSource.*notifications/stream"
    - from: onmessage handler
      to: React Query cache
      via: invalidateQueries notifications + unread-count
      pattern: "invalidateQueries.*notifications"
---

<objective>
Realtime-доставка in-app уведомлений через SSE: серверный stream, клиентский hook, middleware fix для sw.js.

Purpose: NOTF-05 — новые уведомления без перезагрузки страницы.
Output: GET /api/notifications/stream, useNotificationStream в NotificationBell, E2E realtime badge.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-02-SUMMARY.md
@.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md
@middleware.ts
@src/features/notifications/ui/NotificationBell.tsx
@e2e/leave-requests.spec.ts

<interfaces>
SSE event payload:
```typescript
{ type: 'notification', id: string }
```

Required route exports:
```typescript
export const dynamic = 'force-dynamic'
export const runtime = 'nodejs'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSE route GET /api/notifications/stream</name>
  <files>
    src/app/api/notifications/stream/route.ts
  </files>
  <read_first>
    - .planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md — Pattern 2 SSE skeleton
    - src/shared/lib/auth.ts or auth() import pattern from other API routes
    - src/app/api/notifications/route.ts — session user id pattern
  </read_first>
  <acceptance_criteria>
    - `grep "runtime = 'nodejs'" src/app/api/notifications/stream/route.ts`
    - `grep "text/event-stream" src/app/api/notifications/stream/route.ts`
    - `grep "X-Accel-Buffering" src/app/api/notifications/stream/route.ts` → no
    - Auth: `await auth()` — 401 if no session; userId ONLY from session.user.id
    - Poll loop every 2000ms: `findMany({ where: { userId, createdAt: { gt: lastSeen } } })`
    - Heartbeat every 30000ms: `: heartbeat\n\n`
    - `request.signal` abort clears intervals and closes stream
    - No userId query param used for filtering
  </acceptance_criteria>
  <action>
    ReadableStream + TextEncoder per RESEARCH skeleton.
    Headers: Content-Type text/event-stream, Cache-Control no-cache no-transform, Connection keep-alive, X-Accel-Buffering no.
    Emit `data: ${JSON.stringify({ type: 'notification', id: row.id })}\n\n` per new row.
    Update lastSeen to max createdAt of emitted rows.
    Use prisma singleton (not tx) — read-only poll outside mutations.
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/app/api/notifications/stream/route.ts</automated>
  </verify>
  <done>SSE endpoint отдаёт новые уведомления авторизованному пользователю</done>
</task>

<task type="auto">
  <name>Task 2: useNotificationStream + wire в NotificationBell</name>
  <files>
    src/entities/notification/api/use-notification-stream.ts
    src/entities/notification/index.ts
    src/features/notifications/ui/NotificationBell.tsx
    middleware.ts
  </files>
  <read_first>
    - src/entities/notification/api/use-unread-count.ts
    - src/features/notifications/ui/NotificationBell.tsx
    - middleware.ts — current matcher
    - .planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md — Pitfall 1 middleware sw.js
  </read_first>
  <acceptance_criteria>
    - `useNotificationStream()` opens `EventSource('/api/notifications/stream')` in useEffect
    - onmessage: JSON.parse; if type==='notification' → invalidateQueries `['notifications']` and `['notifications', 'unread-count']`
    - cleanup: es.close() on unmount
    - NotificationBell calls useNotificationStream() at top level
    - middleware matcher excludes `sw.js`: pattern `(?!...|sw\.js)` in matcher string
    - `grep "sw\\\\.js" middleware.ts` — match
    - entities/notification/index.ts exports useNotificationStream
  </acceptance_criteria>
  <action>
    Hook uses useQueryClient from @tanstack/react-query.
    On error/onerror: optional console-free reconnect with 5s delay (max 3 retries) — use setTimeout + new EventSource.
    middleware.ts: extend negative lookahead to include `sw\\.js` alongside existing exclusions.
    Do not register service worker in this task — only middleware prep for 09-04.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Клиент слушает SSE и обновляет Badge без reload</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: E2E realtime badge update</name>
  <files>
    e2e/notifications-realtime.spec.ts
  </files>
  <behavior>
    - Manager page open on /admin/leave-calendar with bell visible
    - Teacher creates leave request in parallel context or sequential login
    - Manager badge count increases within 10s WITHOUT page.reload()
  </behavior>
  <read_first>
    - e2e/leave-requests.spec.ts — serial mode, TEST_CODES
    - e2e/helpers/leave-requests.ts — createLeaveViaUI
    - e2e/helpers/auth-state.ts
    - e2e/helpers/codes.ts — manager 100002, teacher1 200001
  </read_first>
  <acceptance_criteria>
    - File `e2e/notifications-realtime.spec.ts` exists with describe «Realtime уведомления»
    - Test uses `page.getByRole('banner')` scope for bell badge (antd Badge sup)
    - Assert: badge text changes from hidden/0 to >=1 after teacher creates leave
    - NO `page.reload()` between create and assertion
    - `grep "notifications-realtime" e2e/` — match
    - `pnpm test:e2e e2e/notifications-realtime.spec.ts` passes (or env-blocked noted in SUMMARY)
  </acceptance_criteria>
  <action>
    test.describe.configure({ mode: 'serial' }).
    Use two browser contexts OR login/logout sequence: prefer manager context stays open, teacher creates via API/server action helper if UI too slow — but prefer UI createLeaveViaUI for integration realism.
    Wait for badge with `expect.poll` timeout 10000ms.
    Badge locator: `header .ant-badge-count` or `getByRole('banner').locator('.ant-scroll-number')`.
    Unique description via Date.now() in leave create.
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/notifications-realtime.spec.ts</automated>
  </verify>
  <done>E2E подтверждает realtime обновление Badge (NOTF-05)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→SSE stream | Long-lived authenticated connection |
| middleware→sw.js | Public static asset must not require session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-08 | S | SSE stream route | mitigate | auth() required; userId from session only |
| T-9-09 | I | SSE poll query | mitigate | prisma filter userId = session.user.id |
| T-9-10 | D | SSE connection flood | accept | v1 single-instance; rate limit deferred |
| T-9-11 | I | sw.js middleware | mitigate | exclude sw.js from auth matcher before push phase |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit`
- `pnpm test:e2e e2e/notifications-realtime.spec.ts`
</verification>

<success_criteria>
- NOTF-05: badge updates without page reload via SSE
- middleware allows future /sw.js registration
</success_criteria>

<output>
After completion, create `.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-03-SUMMARY.md`
</output>
