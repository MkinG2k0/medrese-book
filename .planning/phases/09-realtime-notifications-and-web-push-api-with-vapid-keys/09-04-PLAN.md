---
phase: 09-realtime-notifications-and-web-push-api-with-vapid-keys
plan: 04
type: execute
wave: 4
depends_on:
  - "09-03"
files_modified:
  - package.json
  - pnpm-lock.yaml
  - public/sw.js
  - src/shared/lib/push/vapid.ts
  - src/shared/lib/push/send-push.ts
  - src/shared/lib/validations/push-subscription.ts
  - src/app/api/push/subscribe/route.ts
  - src/shared/lib/notifications/deliver-notifications.ts
  - src/features/notifications/ui/PushSubscribePrompt.tsx
  - src/features/notifications/ui/NotificationBell.tsx
  - src/features/notifications/index.ts
  - .env.test.example
autonomous: true
requirements:
  - NOTF-06
user_setup:
  - service: web-push-vapid
    why: "Web Push requires per-environment VAPID key pair"
    env_vars:
      - name: VAPID_PUBLIC_KEY
        source: "npx web-push generate-vapid-keys --json → publicKey"
      - name: VAPID_PRIVATE_KEY
        source: "generate-vapid-keys → privateKey (server only, never commit)"
      - name: VAPID_SUBJECT
        source: "mailto:admin@toykhana.ru or https://toykhana.ru"
must_haves:
  truths:
    - "Пользователь может подписаться на Web Push — запись PushSubscription в БД"
    - "deliverNotifications отправляет push после commit для каждого созданного Notification"
    - "HTTP 410 от push service удаляет stale PushSubscription"
    - "VAPID private key только в env, не в client bundle"
    - "Service worker /sw.js показывает notificationclick → openWindow(url)"
  artifacts:
    - path: public/sw.js
      provides: "push + notificationclick handlers"
    - path: src/app/api/push/subscribe/route.ts
      provides: "POST upsert subscription"
      exports: ["POST", "DELETE"]
    - path: src/shared/lib/push/send-push.ts
      provides: "webpush.sendNotification wrapper"
      exports: ["sendPushToUser"]
    - path: src/shared/lib/notifications/deliver-notifications.ts
      provides: "post-commit push delivery"
      pattern: "sendPushToUser"
  key_links:
    - from: PushSubscribePrompt.tsx
      to: /api/push/subscribe
      via: fetch POST after pushManager.subscribe
      pattern: "api/push/subscribe"
    - from: deliver-notifications.ts
      to: send-push.ts
      via: "for each notification userId"
      pattern: "sendPushToUser"
---

<objective>
Web Push с VAPID: service worker, subscribe API, server-side send в deliverNotifications, opt-in UI.

Purpose: NOTF-06 — push при ключевых событиях (заявка, решение, замещение).
Output: web-push dependency, public/sw.js, subscribe endpoint, PushSubscribePrompt.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-03-SUMMARY.md
@.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md
@src/shared/lib/notifications/deliver-notifications.ts
@middleware.ts

<interfaces>
Push subscription POST body (Web Push API shape):
```typescript
{
  endpoint: string
  keys: { p256dh: string; auth: string }
}
```

Push payload JSON (sw.js):
```typescript
{ title: string; body: string; url?: string; notificationId?: string }
```

VAPID env:
- VAPID_PUBLIC_KEY — exposed to client for subscribe only
- VAPID_PRIVATE_KEY — server only
- VAPID_SUBJECT — mailto:admin@toykhana.ru
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: web-push, VAPID config, public/sw.js</name>
  <files>
    package.json
    pnpm-lock.yaml
    public/sw.js
    src/shared/lib/push/vapid.ts
    .env.test.example
  </files>
  <read_first>
    - package.json — dependencies
    - .planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md — Pattern 3 Web Push, service worker snippet
    - middleware.ts — confirm sw.js excluded (09-03)
  </read_first>
  <acceptance_criteria>
    - `grep "web-push" package.json` — dependency web-push@^3.6.7
    - `public/sw.js` contains `addEventListener('push'` and `addEventListener('notificationclick'`
    - `src/shared/lib/push/vapid.ts` calls `webpush.setVapidDetails` with process.env VAPID_*
    - `grep "VAPID_PRIVATE_KEY" src/` — only vapid.ts and send-push (no client components)
    - `.env.test.example` has VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT placeholders
    - notificationclick opens `event.notification.data.url` or `/`
  </acceptance_criteria>
  <action>
    Run `pnpm add web-push`.
    vapid.ts: lazy init guard (set once per process); throw clear Error in dev if env missing when send attempted.
    sw.js: static file in public/ — push handler showNotification with title/body; icon `/icons/icon-192.png` or existing favicon path if icons/ exists.
    .env.test.example: dummy base64 placeholders for CI (push tests mocked in 09-05).
    VAPID_SUBJECT use mailto: format per web-push Safari note.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>web-push установлен, SW и VAPID module готовы</done>
</task>

<task type="auto">
  <name>Task 2: Subscribe API + send-push + deliverNotifications</name>
  <files>
    src/shared/lib/validations/push-subscription.ts
    src/app/api/push/subscribe/route.ts
    src/shared/lib/push/send-push.ts
    src/shared/lib/notifications/deliver-notifications.ts
  </files>
  <read_first>
    - prisma/schema.prisma — PushSubscription model
    - src/app/api/notifications/route.ts — auth pattern
    - .planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md — 410 cleanup, push payload
  </read_first>
  <acceptance_criteria>
    - POST `/api/push/subscribe` — Zod validate body; upsert by endpoint; userId = session.user.id
    - DELETE `/api/push/subscribe` — body `{ endpoint }` or delete all for user; scoped to session.user.id
    - `sendPushToUser(userId, { title, body, url, notificationId })` — loads all PushSubscription for user; sends via web-push
    - On statusCode 410: `prisma.pushSubscription.delete({ where: { endpoint } })`
    - `deliverNotifications`: for each notification call sendPushToUser (fire-and-forget Promise.allSettled); no throw to caller
    - `grep "sendPushToUser" src/shared/lib/notifications/deliver-notifications.ts` — match
    - Errors logged only in development per project logging rules
  </acceptance_criteria>
  <action>
    Import vapid init at top of send-push.ts (ensure setVapidDetails before send).
    TTL: 86400 seconds per RESEARCH.
    Push payload JSON matches sw.js expectations.
    deliver-notifications replaces stub from 09-01; keep function signature `(notifications: Notification[])`.
    Do NOT call web-push inside Prisma transaction.
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/app/api/push/subscribe/route.ts src/shared/lib/push/send-push.ts src/shared/lib/notifications/deliver-notifications.ts</automated>
  </verify>
  <done>Push delivery wired post-commit; stale subscriptions cleaned on 410</done>
</task>

<task type="auto">
  <name>Task 3: PushSubscribePrompt UI</name>
  <files>
    src/features/notifications/ui/PushSubscribePrompt.tsx
    src/features/notifications/ui/NotificationBell.tsx
    src/features/notifications/index.ts
  </files>
  <read_first>
    - src/features/notifications/ui/NotificationBell.tsx
    - .planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-RESEARCH.md — client subscribe snippet, Open Q1 opt-in UX
  </read_first>
  <acceptance_criteria>
    - `PushSubscribePrompt.tsx` — soft banner below Header or inside dropdown footer
    - Shown when `Notification.permission === 'default'` and localStorage key `push-prompt-dismissed` absent
    - CTA «Включить уведомления» → register `/sw.js` → pushManager.subscribe → POST /api/push/subscribe
    - Uses `NEXT_PUBLIC_VAPID_PUBLIC_KEY` or fetch public key from GET `/api/push/vapid-public` (if env not public — add minimal GET route returning public key only)
    - Dismiss stores localStorage; «Не сейчас» button
    - `urlBase64ToUint8Array` helper in feature lib (not shared unless reused)
    - Copy RU: «Получайте уведомления о заявках и замещениях даже при закрытой вкладке»
    - NotificationBell renders PushSubscribePrompt
  </acceptance_criteria>
  <action>
    applicationServerKey: prefer env `NEXT_PUBLIC_VAPID_PUBLIC_KEY` documented in .env.test.example — add to example file.
    If only server VAPID_PUBLIC_KEY exists, add `src/app/api/push/vapid-public/route.ts` GET returning `{ data: { publicKey } }` — include in this task if needed (add to files_modified mentally — executor may add route).
    Respect userVisibleOnly: true.
    On permission denied — hide prompt permanently for session via localStorage.
    Export PushSubscribePrompt from features/notifications/index.ts optional (internal to bell OK).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Opt-in Web Push UX готов (NOTF-06 client side)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /api/push/subscribe | Forged subscription endpoint |
| server→push service | VAPID-signed outbound |
| env→runtime | Private key exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-12 | S | push subscribe | mitigate | Bind subscription to session.user.id server-side |
| T-9-13 | I | VAPID_PRIVATE_KEY | mitigate | process.env only; grep CI for leaked key |
| T-9-14 | I | sendPushToUser | mitigate | Only send for notification.userId already persisted |
| T-9-15 | T | push payload | mitigate | Server builds title/body from Notification row |
| T-9-16 | D | 410 stale endpoints | mitigate | Delete PushSubscription on 410 response |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit`
- Manual checklist: DevTools → Application → SW active; permission granted; DB row in PushSubscription
</verification>

<success_criteria>
- NOTF-06: subscribe flow + server push on domain events
- VAPID via env (SC-3)
- deliverNotifications sends Web Push post-commit
</success_criteria>

<output>
After completion, create `.planning/phases/09-realtime-notifications-and-web-push-api-with-vapid-keys/09-04-SUMMARY.md`
</output>
