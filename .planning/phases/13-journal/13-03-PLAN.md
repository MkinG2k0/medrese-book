---
phase: 13-journal
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/features/journal/actions/journal-actions.ts
  - src/features/journal/actions/journal-actions.test.ts
  - src/app/(dashboard)/journal/[studentId]/page.tsx
  - src/features/journal/ui/lesson/LessonPageHeader.tsx
  - src/features/journal/model/use-lesson-page.ts
  - src/features/journal/lib/lesson-types.ts
autonomous: true
requirements:
  - SUBJ-13
must_haves:
  truths:
    - Страница урока требует groupId в URL; без него — notFound (D-13 pattern для lesson)
    - getStudentLesson использует переданный groupId, не findFirst (D-05, D-09)
    - Шаги программы и сессия scoped по subjectId группы через enrollment (SUBJ-13)
    - LessonPageHeader показывает «Группа · Предмет» (D-08)
    - «Назад» и «История шагов» сохраняют groupId+date (D-11)
    - nextStudent и groupEnrollments в том же groupId (bugfix line 250)
  artifacts:
    - path: src/app/(dashboard)/journal/[studentId]/page.tsx
      provides: groupId из searchParams в getStudentLesson
    - path: src/features/journal/ui/lesson/LessonPageHeader.tsx
      provides: secondary text группа · предмет
    - path: src/features/journal/actions/journal-actions.ts
      provides: getStudentLesson с обязательным groupId, assertTeacherOwnsGroup
  key_links:
    - from: getStudentLesson
      to: enrollment.group.subjectId
      via: getTotalProgramSteps(subjectId)
    - from: use-lesson-page journalBackHref
      to: /journal?groupId=&date=
      via: useJournalDate journalHref
---

<objective>
Страница урока в контексте группы: оценки по шагам предмета, посещаемость, таймер с groupId.

Purpose: SUBJ-13 и success criteria п.3–4 (subject via groupId per D-09 discretion).
Output: /journal/[studentId] с обязательным groupId, контекст в шапке урока.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-journal/13-CONTEXT.md
@.planning/phases/12-progress-sessions/12-CONTEXT.md
@src/features/journal/actions/journal-actions.ts
@src/app/(dashboard)/journal/[studentId]/page.tsx
@src/features/journal/ui/lesson/LessonPageHeader.tsx
@src/features/journal/model/use-lesson-page.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: journal-actions — groupId обязателен в lesson paths</name>
  <files>src/features/journal/actions/journal-actions.ts, src/features/journal/actions/journal-actions.test.ts</files>
  <read_first>
    src/features/journal/actions/journal-actions.ts (getStudentLesson, requireTeacherStudent, getTeacherGroupForSession)
    src/features/groups/actions/group-actions.test.ts (vitest mock pattern)
    .planning/phases/13-journal/13-CONTEXT.md (D-05, D-09)
    .planning/phases/12-progress-sessions/12-CONTEXT.md
  </read_first>
  <behavior>
    - getStudentLesson(studentId, date, groupId) с чужим groupId → null
    - getStudentLesson с валидным enrollment → return.groupId === переданный groupId
    - groupEnrollments query использует targetGroupId, не findFirst group
    - assertTeacherOwnsGroup: group.teacherId !== session.teacherId → null
  </behavior>
  <action>
    Сначала создать journal-actions.test.ts (vitest, mock prisma + requireRole по паттерну group-actions.test.ts):
    - describe getStudentLesson: чужой groupId → null; валидный groupId → return.groupId совпадает
    - describe assertTeacherOwnsGroup (через getStudentLesson или экспорт для теста): teacherId mismatch → null
    Запустить тесты — RED до реализации, GREEN после.

    Wire assertTeacherOwnsGroup из 13-01 во все teacher-scoped paths.

    getStudentLesson(studentId, calendarDate, groupId: string): groupId обязателен; убрать fallback на getTeacherGroupForSession/findFirst (D-05).

    requireTeacherStudent(studentId, groupId): assertTeacherOwnsGroup + findEnrollmentInGroup.

    resumeStudentFromPause(studentId, groupId): то же.

    Исправить баг: prisma.groupEnrollment.findMany where groupId: teacherGroup.id → targetGroupId (строка ~250).

    Вернуть в getStudentLesson payload для UI header: groupName, subjectName из enrollment.group include subject.

    Удалить getTeacherGroupForSession и getTeacherGroup если больше нет call sites (grep перед удалением).

    getStudentStepHistory(studentId, groupId) — подготовить сигнатуру для 13-04; минимально добавить groupId param здесь.
  </action>
  <acceptance_criteria>
    getStudentLesson не вызывает findFirst для выбора группы
    groupEnrollments scoped к targetGroupId
    Чужой groupId возвращает null
    Return включает groupName и subjectName
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:unit -- src/features/journal/actions/journal-actions.test.ts</automated>
  </verify>
  <done>Server actions урока привязаны к выбранной группе (SUBJ-13)</done>
</task>

<task type="auto">
  <name>Task 2: Student lesson page — groupId из URL</name>
  <files>src/app/(dashboard)/journal/[studentId]/page.tsx, src/features/journal/lib/lesson-types.ts</files>
  <read_first>
    src/app/(dashboard)/journal/[studentId]/page.tsx
    src/features/journal/lib/lesson-types.ts
    src/features/journal/lib/journal-url.ts (JOURNAL_GROUP_PARAM)
  </read_first>
  <action>
    searchParams: Promise&lt;{ date?: string; groupId?: string }&gt;.

    Если !groupId → notFound() (явный контекст группы обязателен).

    getStudentLesson(studentId, calendarDate, groupId).

    Передать в LessonPage новые props: groupName, subjectName из lesson return (добавить в lesson-types LessonPageProps).

    LessonPage по-прежнему получает groupId для таймера и мутаций сессий.
  </action>
  <acceptance_criteria>
    /journal/[id] без groupId → 404
    getStudentLesson вызывается с groupId из query
    LessonPageProps расширен groupName, subjectName
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Route урока требует groupId (SUBJ-13)</done>
</task>

<task type="auto">
  <name>Task 3: LessonPageHeader и навигация с groupId</name>
  <files>src/features/journal/ui/lesson/LessonPageHeader.tsx, src/features/journal/model/use-lesson-page.ts, src/features/journal/ui/LessonPage.tsx</files>
  <read_first>
    src/features/journal/ui/lesson/LessonPageHeader.tsx
    src/features/journal/model/use-lesson-page.ts
    src/features/journal/model/use-journal-date.ts
    .planning/phases/13-journal/13-CONTEXT.md (D-08, D-11)
  </read_first>
  <action>
    LessonPageHeader: добавить props groupName, subjectName.

    Под именем ученика (или над level line) добавить Text type="secondary": «{groupName} · {subjectName}» per D-08.

    Ссылка «История шагов»: `/journal/${studentId}/history?groupId=${groupId}` — groupId из props/hook; при необходимости также date.

    use-lesson-page: использовать useJournalDate с allowedGroupIds=[groupId] и defaultGroupId=groupId чтобы journalHref сохранял groupId+date при «Назад» и save-and-next (D-11).

    LessonPage: пробросить groupName, subjectName в LessonPageHeader.

    useTeachingSession(groupId, date) уже есть — проверить что save session POST включает groupId (Phase 12); без изменений если уже работает.
  </action>
  <acceptance_criteria>
    LessonPageHeader отображает «Группа · Предмет» (D-08)
    journalBackHref содержит groupId и date
    Ссылка истории шагов содержит groupId
  </acceptance_criteria>
  <verify>
    <automated>rg "groupName|subjectName" src/features/journal/ui/lesson/LessonPageHeader.tsx</automated>
  </verify>
  <done>UI урока показывает контекст группы/предмета и сохраняет URL (D-08, D-11, SUBJ-13)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→getStudentLesson | groupId из URL; server assertTeacherOwnsGroup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Elevation | getStudentLesson IDOR | high | mitigate | assertTeacherOwnsGroup + enrollment check |
| T-13-06 | Tampering | Session save wrong group | medium | mitigate | groupId required in API (Phase 12); lesson page passes same groupId |
</threat_model>

<verification>
pnpm test:unit -- src/features/journal/actions/journal-actions.test.ts
pnpm exec tsc --noEmit
rg "getTeacherGroupForSession|findFirst" src/features/journal/actions/journal-actions.ts → 0 matches после рефактора
rg "groupName" src/features/journal/
</verification>

<success_criteria>
Урок ведётся в контексте groupId/subjectId группы
Шапка урока показывает группу и предмет
Навигация сохраняет groupId
</success_criteria>

<artifacts_this_phase_produces>
- journal-actions.test.ts (getStudentLesson groupId ownership)
- getStudentLesson(studentId, date, groupId) без findFirst
- requireTeacherStudent(studentId, groupId)
- resumeStudentFromPause(studentId, groupId)
- getStudentStepHistory(studentId, groupId) signature
- lesson return: groupName, subjectName
- [studentId]/page.tsx с обязательным groupId query
- LessonPageProps: groupName, subjectName
- LessonPageHeader контекст «Группа · Предмет»
- history link с ?groupId=
- use-lesson-page journalBackHref с groupId
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/13-journal/13-03-SUMMARY.md` when done
</output>
