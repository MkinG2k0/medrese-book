---
phase: 13-journal
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/app/(dashboard)/journal/page.tsx
  - src/features/journal/ui/StudentList.tsx
  - src/features/journal/ui/JournalStudentsTable.tsx
autonomous: true
requirements:
  - SUBJ-11
  - SUBJ-12
must_haves:
  truths:
    - Учитель видит Ant Design Select группы в одной строке с датой (D-01, D-04)
    - Опции Select в формате «Название группы — Предмет» (D-07)
    - Предмет не выбирается отдельно — только в подписи опции (D-06)
    - Смена группы обновляет список учеников с enrollment progress (SUBJ-12)
    - При отсутствии групп — понятное пустое состояние
    - Ссылки на урок включают groupId в URL (D-11)
    - Таймер урока и сохранение сессии привязаны к выбранной группе (subject via groupId) — ROADMAP success criterion 4
  artifacts:
    - path: src/app/(dashboard)/journal/page.tsx
      provides: SSR загрузка getTeacherGroups, resolve default groupId
    - path: src/features/journal/ui/StudentList.tsx
      provides: Group Select в header row
    - path: src/features/journal/ui/JournalStudentsTable.tsx
      provides: href с groupId+date
  key_links:
    - from: StudentList Select onChange
      to: useJournalDate setGroupId
      via: URL ?groupId= + localStorage
    - from: useStudents(groupId, date)
      to: GET /api/students?groupId=
      via: enrollment currentStepIdx, levelNumber
---

<objective>
Главная страница журнала: выбор группы учителем, список учеников с прогрессом зачисления.

Purpose: Закрыть SUBJ-11 (выбор группы, предмет из группы) и SUBJ-12 (прогресс per enrollment).
Output: Рабочий /journal с Select группы и group-scoped таблицей.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-journal/13-CONTEXT.md
@.planning/phases/13-journal/13-01-SUMMARY.md
@src/app/(dashboard)/journal/page.tsx
@src/features/journal/ui/StudentList.tsx
@src/features/journal/ui/JournalStudentsTable.tsx
@src/entities/student/api/use-students.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: journal/page.tsx — группы и default groupId</name>
  <files>src/app/(dashboard)/journal/page.tsx</files>
  <read_first>
    src/app/(dashboard)/journal/page.tsx
    src/features/journal/actions/journal-actions.ts (getTeacherGroups)
    src/features/journal/lib/journal-storage.ts
    .planning/phases/13-journal/13-CONTEXT.md (D-02, D-03, D-05)
  </read_first>
  <action>
    Заменить getTeacherGroup() на getTeacherGroups() (D-05).

    Если groups.length === 0: return &lt;Text&gt;Группа не назначена&lt;/Text&gt; (или аналогичный CTA — discretion).

    Передать в StudentList:
    - groups: TeacherJournalGroup[]
    - initialGroupId: первая из URL нельзя резолвить на сервере без searchParams в RSC — передать groups[0].id как server default; клиент уточнит через useJournalDate + searchParams + readJournalGroupId(JOURNAL_GROUP_STORAGE_KEY) per D-03.

    Удалить import getTeacherGroup.

    Паттерн: Server Component грузит список → Client StudentList резолвит активную группу.
  </action>
  <acceptance_criteria>
    page.tsx не вызывает getTeacherGroup / findFirst
    Пустое состояние при groups.length === 0
    StudentList получает массив groups
  </acceptance_criteria>
  <verify>
    <automated>rg "getTeacherGroup" "src/app/(dashboard)/journal/page.tsx" && exit 1 || exit 0</automated>
  </verify>
  <done>SSR entry point отдаёт список групп учителя (SUBJ-11)</done>
</task>

<task type="auto">
  <name>Task 2: StudentList — Select группы в шапке</name>
  <files>src/features/journal/ui/StudentList.tsx</files>
  <read_first>
    src/features/journal/ui/StudentList.tsx
    src/features/groups/ui/GroupsList.tsx (Ant Design Select/filter pattern)
    .planning/phases/13-journal/13-CONTEXT.md (D-01, D-04, D-06, D-07, D-09)
    src/features/journal/model/use-journal-date.ts (после 13-01)
  </read_first>
  <action>
    Изменить props: groups: TeacherJournalGroup[] вместо только groupId.

    Вызвать useJournalDate с allowedGroupIds = groups.map(g =&gt; g.id) и defaultGroupId = readJournalGroupId(JOURNAL_GROUP_STORAGE_KEY) ?? groups[0]?.id.

    Использовать groupId из hook (не prop) для useStudents, useTeachingSession, useStudentRiskFlags, LessonTimerBar, JournalDatePicker.

    В header row (div с Title и date controls per D-01): добавить Ant Design Select СПРАВА от date picker или между Title и date — в той же flex row justify-between:
    - options: groups.map(g =&gt; ({ value: g.id, label: `${g.name} — ${g.subjectName}` })) per D-07
    - value: groupId, onChange: setGroupId
    - className min-width как в GroupsList filters
    - loading/disabled если groups.length &lt;= 1 (Select всё равно показывать с одной опцией)

    При смене группы во время активного teaching session (teachingSession?.status === 'ACTIVE'): discretion — modal.confirm «Урок идёт в другой группе. Переключить?» через App.useApp(); onOk → setGroupId.

    Не добавлять отдельный subject badge (D-06).
  </action>
  <acceptance_criteria>
    Select в одной строке с JournalDatePicker (D-01)
    Label опции «Группа — Предмет» (D-07)
    groupId из URL/storage синхронизируется при смене Select
    Таймер и таблица реагируют на новый groupId
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Селектор группы на главной странице журнала (SUBJ-11)</done>
</task>

<task type="auto">
  <name>Task 3: JournalStudentsTable — ссылки с groupId</name>
  <files>src/features/journal/ui/JournalStudentsTable.tsx</files>
  <read_first>
    src/features/journal/ui/JournalStudentsTable.tsx
    src/features/journal/lib/journal-url.ts
    .planning/phases/13-journal/13-CONTEXT.md (D-11)
  </read_first>
  <action>
    Добавить проп journalGroupId: string.

    StudentList передаёт journalGroupId={groupId} из hook.

    Заменить buildJournalHref(`/journal/${studentId}`, journalDate) на buildJournalHref с journalGroupId (три аргумента или opts object per 13-01).

    openLesson router.push — тот же href с groupId.

    Таблица продолжает показывать currentStepIdx, levelNumber из API — это enrollment progress (SUBJ-12), без изменений колонок если данные уже верны.
  </action>
  <acceptance_criteria>
    Все ссылки на /journal/[studentId] содержат ?groupId= и ?date=
    JournalStudentsTable принимает journalGroupId
  </acceptance_criteria>
  <verify>
    <automated>rg "groupId|JOURNAL_GROUP_PARAM|journalGroupId" src/features/journal/ui/JournalStudentsTable.tsx</automated>
  </verify>
  <done>Навигация на урок сохраняет контекст группы (D-11, SUBJ-12)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher→group Select | Только own groups в options (D-02); API students уже проверяет groupId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Information Disclosure | StudentList | low | mitigate | useStudents только после groupId из allowedGroupIds |
| T-13-04 | Denial of Service | Group switch during lesson | low | accept | modal.confirm перед switch (discretion) |
</threat_model>

<verification>
pnpm exec tsc --noEmit
rg "getTeacherGroup" src/app/(dashboard)/journal/page.tsx → 0 matches
rg "Select" src/features/journal/ui/StudentList.tsx
</verification>

<success_criteria>
Учитель выбирает группу в шапке журнала
Список учеников показывает прогресс enrollment выбранной группы
Ссылки на урок содержат groupId
</success_criteria>

<artifacts_this_phase_produces>
- journal/page.tsx с getTeacherGroups и пустым состоянием
- StudentList props: groups + Group Select в header
- JournalStudentsTable prop journalGroupId
- buildJournalHref вызовы с groupId в таблице
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/13-journal/13-02-SUMMARY.md` when done
</output>
