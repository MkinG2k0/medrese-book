---
phase: 13-journal
plan: 04
type: execute
wave: 4
depends_on:
  - 13-02
  - 13-03
files_modified:
  - src/app/(dashboard)/journal/history/page.tsx
  - src/features/journal/ui/JournalHistoryPage.tsx
  - src/app/(dashboard)/journal/[studentId]/history/page.tsx
  - src/features/journal/actions/journal-actions.ts
  - prisma/seed-e2e.ts
  - e2e/journal.spec.ts
  - e2e/helpers/journal.ts
autonomous: true
requirements:
  - SUBJ-11
  - SUBJ-12
  - SUBJ-13
must_haves:
  truths:
    - /journal/history имеет собственный Select группы (D-12)
    - История использует отдельный localStorage ключ (D-14)
    - /journal/[studentId]/history требует groupId в URL (D-13)
    - E2E journal проходит с groupId в URL
    - getTeacherGroup полностью удалён из journal feature
  artifacts:
    - path: src/features/journal/ui/JournalHistoryPage.tsx
      provides: Group Select независимый от главного журнала
    - path: e2e/journal.spec.ts
      provides: assertions на groupId в URL
  key_links:
    - from: JournalHistoryPage
      to: JOURNAL_HISTORY_GROUP_STORAGE_KEY
      via: отдельный default от главного журнала
    - from: Step history route
      to: getStudentStepHistory(studentId, groupId)
      via: enrollment progress
---

<objective>
Страницы истории с независимым выбором группы, E2E и финальная зачистка getTeacherGroup.

Purpose: D-12, D-13, D-14 и регрессионное покрытие SUBJ-11…13.
Output: history routes + green e2e/journal.spec.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-journal/13-CONTEXT.md
@src/app/(dashboard)/journal/history/page.tsx
@src/features/journal/ui/JournalHistoryPage.tsx
@src/app/(dashboard)/journal/[studentId]/history/page.tsx
@e2e/journal.spec.ts
@prisma/seed-e2e.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: /journal/history — свой Select группы</name>
  <files>src/app/(dashboard)/journal/history/page.tsx, src/features/journal/ui/JournalHistoryPage.tsx</files>
  <read_first>
    src/app/(dashboard)/journal/history/page.tsx
    src/features/journal/ui/JournalHistoryPage.tsx
    src/features/journal/ui/StudentList.tsx (после 13-02 — pattern Select)
    .planning/phases/13-journal/13-CONTEXT.md (D-12, D-14)
    src/features/journal/lib/journal-storage.ts
  </read_first>
  <action>
    history/page.tsx: getTeacherGroups() вместо getTeacherGroup(); передать groups в JournalHistoryPage; пустое состояние если нет групп.

    JournalHistoryPage:
    - props: groups: TeacherJournalGroup[]
    - локальный state/hook для selectedGroupId с JOURNAL_HISTORY_GROUP_STORAGE_KEY (D-14) — НЕ JOURNAL_GROUP_STORAGE_KEY
    - Ant Design Select группы в header рядом с Select ученика (D-12): label «{name} — {subjectName}»
    - useStudents(selectedGroupId) — без date на history list или с date filter как сейчас
    - URL query ?groupId= опционально для history (discretion) — минимум localStorage + state

    Не наследовать группу с /journal автоматически (D-12).
  </action>
  <acceptance_criteria>
    JournalHistoryPage рендерит Select групп
    Используется JOURNAL_HISTORY_GROUP_STORAGE_KEY
    history/page.tsx не использует getTeacherGroup
  </acceptance_criteria>
  <verify>
    <automated>rg "JOURNAL_HISTORY_GROUP_STORAGE_KEY" src/features/journal/</automated>
  </verify>
  <done>История занятий с независимым выбором группы (D-12, D-14)</done>
</task>

<task type="auto">
  <name>Task 2: /journal/[studentId]/history — обязательный groupId</name>
  <files>src/app/(dashboard)/journal/[studentId]/history/page.tsx, src/features/journal/actions/journal-actions.ts</files>
  <read_first>
    src/app/(dashboard)/journal/[studentId]/history/page.tsx
    src/features/journal/actions/journal-actions.ts (getStudentStepHistory)
    .planning/phases/13-journal/13-CONTEXT.md (D-13)
  </read_first>
  <action>
    Route searchParams: { groupId?: string }.

    Без groupId → notFound() (D-13).

    getStudentStepHistory(studentId, groupId): assertTeacherOwnsGroup + findEnrollmentInGroup; вернуть enrollment progress (currentStepIdx, level).

    backHref: buildJournalHref(`/journal/${studentId}`, date from discretion or today, groupId) — «Назад к уроку» с контекстом.

    StepHistoryPage: useStepCompletions уже scoped — проверить groupId передаётся в API если требуется Phase 12; wire groupId в query hooks при необходимости.
  </action>
  <acceptance_criteria>
    history route без groupId → 404
    getStudentStepHistory принимает groupId
    backHref включает groupId
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>История шагов ученика в контексте зачисления (D-13, SUBJ-12)</done>
</task>

<task type="auto">
  <name>Task 3: E2E и seed-e2e для groupId</name>
  <files>prisma/seed-e2e.ts, e2e/journal.spec.ts, e2e/helpers/journal.ts</files>
  <read_first>
    e2e/journal.spec.ts
    e2e/helpers/journal.ts
    prisma/seed-e2e.ts (group1 teacher1)
    e2e/helpers/codes.ts
  </read_first>
  <action>
    seed-e2e: добавить вторую группу teacher1 (другой subject если есть в seed — Tajweed или второй Quran group «Группа Аль-Ихлас») с 1–2 enrollments для E2E switch test. Не ломать существующих student Ali/Usman/Bilal в group1.

    e2e/helpers/journal.ts:
    - export const E2E_GROUP_AL_FATIHA = '...' или resolve groupId из seed constant / page URL after load
    - gotoJournal(page, { groupId? }): page.goto(`/journal?groupId=${id}&date=...`)

    journal.spec.ts:
    - beforeEach: expect URL to match groupId= after load
    - test: смена группы в Select меняет список учеников (если 2 группы у teacher1)
    - lesson flow: click student → URL has groupId; back preserves groupId
    - обновить startLessonIfNeeded — без изменений если кнопка на странице та же

    Финальный grep: удалить getTeacherGroup из journal-actions если остались dead exports; обновить feature index если есть.
  </action>
  <acceptance_criteria>
    journal.spec.ts assertions на groupId в URL
    teacher1 имеет ≥2 группы в seed-e2e OR тесты проверяют preservation без switch
    getTeacherGroup отсутствует в journal feature call sites
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:e2e -- e2e/journal.spec.ts</automated>
  </verify>
  <done>E2E покрывает group-scoped журнал (D-05 discretion, SUBJ-11…13)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E→seed | Тестовые группы не должны ломать teacher2 сценарии |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Repudiation | E2E regression | medium | mitigate | journal.spec.ts serial + groupId assertions |
| T-13-SC | Tampering | seed-e2e | low | accept | Только e2e DB |
</threat_model>

<verification>
pnpm test:e2e -- e2e/journal.spec.ts
pnpm exec tsc --noEmit
rg "getTeacherGroup" src/ → 0 matches in journal paths
</verification>

<success_criteria>
History pages с group context
E2E green
Tech debt getTeacherGroup удалён
SUBJ-11, SUBJ-12, SUBJ-13 верифицированы end-to-end
</success_criteria>

<artifacts_this_phase_produces>
- JournalHistoryPage с Group Select
- history/page.tsx с getTeacherGroups
- [studentId]/history/page.tsx с обязательным groupId
- getStudentStepHistory(studentId, groupId) wired
- seed-e2e вторая группа teacher1 (если добавлена)
- e2e/helpers/journal.ts gotoJournal с groupId
- e2e/journal.spec.ts обновлён
- удаление getTeacherGroup / getTeacherGroupForSession
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/13-journal/13-04-SUMMARY.md` when done
</output>
