---
phase: 13-journal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/journal/lib/journal-url.ts
  - src/features/journal/lib/journal-url.test.ts
  - src/features/journal/lib/journal-storage.ts
  - src/features/journal/model/use-journal-date.ts
  - src/features/journal/actions/journal-actions.ts
autonomous: true
requirements:
  - SUBJ-11
must_haves:
  truths:
    - URL журнала поддерживает ?groupId= наряду с ?date= (D-10)
    - buildJournalHref сохраняет groupId и date при навигации (D-11, базовый слой)
    - getTeacherGroups возвращает только группы текущего учителя с subject для подписей (D-02, D-05)
    - localStorage хранит последнюю группу журнала отдельно от истории (D-14, ключи)
  artifacts:
    - path: src/features/journal/lib/journal-url.ts
      provides: JOURNAL_GROUP_PARAM, buildJournalHref с groupId+date, resolveJournalGroupId
    - path: src/features/journal/lib/journal-storage.ts
      provides: JOURNAL_GROUP_STORAGE_KEY, JOURNAL_HISTORY_GROUP_STORAGE_KEY
    - path: src/features/journal/actions/journal-actions.ts
      provides: getTeacherGroups, TeacherJournalGroup type
  key_links:
    - from: use-journal-date journalHref
      to: buildJournalHref
      via: передача groupId из searchParams и localStorage
    - from: getTeacherGroups
      to: prisma.group.findMany where teacherId
      via: без canAccessGroupAsTeacher / substitution (D-02)
---

<objective>
Заложить инфраструктуру выбора группы: URL-параметры, localStorage, server action списка групп учителя.

Purpose: SUBJ-11 и D-10/D-11 требуют единого контракта groupId+date до UI-селекторов.
Output: journal-url/storage/hook extensions, getTeacherGroups().
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-journal/13-CONTEXT.md
@.planning/phases/12-progress-sessions/12-05-SUMMARY.md
@src/features/journal/lib/journal-url.ts
@src/features/journal/model/use-journal-date.ts
@src/features/groups/ui/GroupsList.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: journal-url и journal-storage под groupId</name>
  <files>src/features/journal/lib/journal-url.ts, src/features/journal/lib/journal-url.test.ts, src/features/journal/lib/journal-storage.ts</files>
  <read_first>
    src/features/journal/lib/journal-url.ts
    src/features/journal/lib/journal-url.test.ts
    .planning/phases/13-journal/13-CONTEXT.md (D-10, D-11, D-14)
  </read_first>
  <behavior>
    - buildJournalHref('/journal', { date: '2026-07-05', groupId: 'g1' }) → '/journal?date=2026-07-05&groupId=g1'
    - buildJournalHref без groupId → только date (обратная совместимость)
    - resolveJournalGroupId('g1', ['g1','g2'], 'g2') → 'g1'
    - resolveJournalGroupId(null, ['g1','g2'], 'g2') → 'g2' (fallback)
    - resolveJournalGroupId('invalid', ['g1'], 'g1') → 'g1'
  </behavior>
  <action>
    Добавить JOURNAL_GROUP_PARAM = 'groupId'.

    Расширить buildJournalHref: сигнатура buildJournalHref(pathname, date, groupId?: string) ИЛИ объект opts { date, groupId? } — выбрать объект если уже есть 3+ потребителя с опциональным groupId; обновить все существующие вызовы в journal feature на новую сигнатуру в этом плане только в journal-url.test.ts (остальные файлы — планы 13-02/03).

    Добавить resolveJournalGroupId(groupIdParam, allowedIds, fallbackGroupId): string — возвращает param если он в allowedIds, иначе fallback.

    Создать journal-storage.ts:
    - export const JOURNAL_GROUP_STORAGE_KEY = 'journal:lastGroupId'
    - export const JOURNAL_HISTORY_GROUP_STORAGE_KEY = 'journal:history:lastGroupId'
    - readJournalGroupId(key): string | null (safe localStorage, SSR guard)
    - writeJournalGroupId(key, groupId): void

    Обновить journal-url.test.ts под новые кейсы groupId.
  </action>
  <acceptance_criteria>
    JOURNAL_GROUP_PARAM экспортирован
    buildJournalHref включает groupId в query когда передан
    resolveJournalGroupId валидирует against allowedIds
    Два отдельных storage key для журнала и истории
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:unit -- src/features/journal/lib/journal-url.test.ts</automated>
  </verify>
  <done>URL и storage контракты готовы для D-10, D-11, D-14</done>
</task>

<task type="auto">
  <name>Task 2: getTeacherGroups server action</name>
  <files>src/features/journal/actions/journal-actions.ts</files>
  <read_first>
    src/features/journal/actions/journal-actions.ts (getTeacherGroup, getTeacherGroupForSession)
    prisma/schema.prisma (Group, Subject)
    .planning/phases/13-journal/13-CONTEXT.md (D-02, D-05, D-07)
  </read_first>
  <action>
    Экспортировать тип TeacherJournalGroup = { id, name, subjectId, subjectName }.

    Добавить getTeacherGroups(): Promise&lt;TeacherJournalGroup[]&gt;:
    - requireRole('TEACHER')
    - prisma.group.findMany({ where: { teacherId: session.user.teacherId! }, include: { subject: { select: { id: true, name: true } } }, orderBy: { name: 'asc' } })
    - map в TeacherJournalGroup; subjectName из subject.name
    - НЕ включать группы замещаемых учителей (D-02): только teacherId = текущий, без canAccessGroupAsTeacher

    Оставить getTeacherGroup() временно (deprecated comment) — удаление в планах 13-02/04 когда все call sites заменены.

    Добавить приватный assertTeacherOwnsGroup(teacherId, groupId): Promise&lt;Group | null&gt; — findUnique group where id=groupId AND teacherId; null если чужая группа. Использовать в плане 13-03 (можно добавить skeleton здесь без wiring).
  </action>
  <acceptance_criteria>
    getTeacherGroups возвращает массив с subjectName
    Запрос фильтрует только group.teacherId = текущий учитель
    TeacherJournalGroup экспортирован для UI
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Server action списка групп готов для Select (D-05)</done>
</task>

<task type="auto">
  <name>Task 3: use-journal-date — синхронизация groupId с URL</name>
  <files>src/features/journal/model/use-journal-date.ts</files>
  <read_first>
    src/features/journal/model/use-journal-date.ts
    src/features/journal/lib/journal-url.ts
    src/features/journal/lib/journal-storage.ts
  </read_first>
  <action>
    Расширить useJournalDate (не переименовывать файл — меньше churn):

    Принять опциональный проп allowedGroupIds: string[] и defaultGroupId: string.

    Добавить в return:
    - groupId: string (из resolveJournalGroupId(searchParams, allowed, default))
    - setGroupId(groupId): обновляет URL ?groupId=, пишет JOURNAL_GROUP_STORAGE_KEY, router.replace сохраняя date

    journalHref(targetPathname) → buildJournalHref с текущими date + groupId (D-11).

    useEffect: если groupId отсутствует в URL но есть default — replace URL с groupId (аналог date bootstrap).

    Не добавлять Select UI здесь — только hook contract для StudentList (13-02).
  </action>
  <acceptance_criteria>
    groupId читается из URL с fallback на defaultGroupId
    setGroupId обновляет URL и localStorage
    journalHref включает groupId и date
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Клиентский hook готов для селектора группы (D-03, D-10)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→URL query | groupId в URL может быть подменён; server actions должны проверять ownership (D-02) |
| client→localStorage | Storage — только UX default, не источник авторизации |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Spoofing | getTeacherGroups | medium | mitigate | findMany только teacherId = session; assertTeacherOwnsGroup в 13-03 |
| T-13-02 | Tampering | URL groupId | medium | mitigate | resolveJournalGroupId whitelist allowedGroupIds на клиенте; server verify в actions |
| T-13-SC | Tampering | npm installs | high | accept | Новые пакеты не требуются |
</threat_model>

<verification>
pnpm test:unit -- src/features/journal/lib/journal-url.test.ts
pnpm exec tsc --noEmit
rg "getTeacherGroups|JOURNAL_GROUP_PARAM|JOURNAL_GROUP_STORAGE_KEY" src/features/journal/
</verification>

<success_criteria>
journal-url и storage поддерживают groupId
getTeacherGroups возвращает группы учителя с предметом
useJournalDate синхронизирует groupId в URL
</success_criteria>

<artifacts_this_phase_produces>
- JOURNAL_GROUP_PARAM в journal-url.ts
- buildJournalHref с опциональным groupId
- resolveJournalGroupId в journal-url.ts
- JOURNAL_GROUP_STORAGE_KEY, JOURNAL_HISTORY_GROUP_STORAGE_KEY в journal-storage.ts
- readJournalGroupId, writeJournalGroupId в journal-storage.ts
- TeacherJournalGroup type в journal-actions.ts
- getTeacherGroups() в journal-actions.ts
- assertTeacherOwnsGroup (private) в journal-actions.ts
- useJournalDate: groupId, setGroupId, journalHref с groupId
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/13-journal/13-01-SUMMARY.md` when done
</output>
