---
phase: 12-progress-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/shared/lib/validations/session.ts
  - src/shared/lib/validations/session.test.ts
autonomous: true
requirements:
  - SUBJ-08
  - SUBJ-09
must_haves:
  truths:
    - GroupEnrollment хранит currentStepIdx как единственный источник прогресса зачисления (D-01, D-02)
    - Student.currentStepIdx удалён из схемы (D-03)
    - Session имеет обязательный groupId с уникальностью studentId + date + groupId (D-04)
    - Миграция переносит глобальный idx в primary enrollment; остальные получают offset уровня (D-06, D-07)
    - Legacy Session без groupId привязаны к primary enrollment группы ученика
  artifacts:
    - path: prisma/schema.prisma
      provides: GroupEnrollment.currentStepIdx, Session.groupId, Student без currentStepIdx
      contains: currentStepIdx Int
    - path: prisma/migrations/
      provides: Prod-safe SQL backfill и unique constraint
    - path: src/shared/lib/validations/session.ts
      provides: createSessionSchema с обязательным groupId
  key_links:
    - from: prisma/migrations/
      to: GroupEnrollment
      via: UPDATE currentStepIdx FROM Student для primary enrollment
    - from: Session.groupId
      to: Group
      via: FK onDelete Restrict; backfill через primary enrollment
    - from: createSessionSchema.groupId
      to: Session @@unique([studentId, date, groupId])
      via: API validation в plan 12-03
---

<objective>
Добавить GroupEnrollment.currentStepIdx и Session.groupId, удалить Student.currentStepIdx, выполнить prod-safe миграцию с backfill и подготовить Zod-контракт сессии.

Purpose: Фундамент фазы 12 — прогресс и сессии в скоупе группы (D-01…D-07, SUBJ-08, SUBJ-09).
Output: Применённая миграция, обновлённая схема, createSessionSchema с groupId.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-progress-sessions/12-CONTEXT.md
@.planning/phases/11-groups-enrollment/11-06-SUMMARY.md
@prisma/schema.prisma
@prisma/lib/subject-constants.ts
@src/shared/lib/enrollment.ts
@src/shared/lib/student-progress/offsets.ts
@.cursor/rules/prisma-migrations.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Схема GroupEnrollment.currentStepIdx и Session.groupId</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    prisma/schema.prisma (GroupEnrollment, Session, Student)
    .planning/phases/12-progress-sessions/12-CONTEXT.md (D-01..D-04)
  </read_first>
  <action>
    Per D-01 D-02: добавить на GroupEnrollment поле currentStepIdx Int @default(0).

    Per D-03: удалить Student.currentStepIdx и все ссылки на это поле в модели Student.

    Per D-04 D-05: на Session добавить groupId String, group Group @relation(fields: [groupId], references: [id], onDelete: Restrict); relations Group.sessions Session[] если отсутствует. Добавить @@unique([studentId, date, groupId]) и @@index([groupId, date]). Поле groupId сначала nullable в схеме только если миграция требует двухшагового backfill — иначе сразу NOT NULL после SQL backfill в Task 2.

    Не добавлять StudentSubjectProgress и subjectId на Session — отклонено в CONTEXT deferred.
  </action>
  <acceptance_criteria>
    prisma validate проходит без ошибок
    GroupEnrollment содержит currentStepIdx Int @default(0)
    Student не содержит currentStepIdx
    Session содержит groupId с FK на Group и @@unique([studentId, date, groupId])
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec prisma validate</automated>
  </verify>
  <done>Схема отражает group-scoped progress и sessions per D-01..D-04</done>
</task>

<task type="auto">
  <name>Task 2: Prod-safe миграция с backfill прогресса и сессий</name>
  <files>prisma/migrations/</files>
  <read_first>
    prisma/migrations/20260707180000_add_subject/migration.sql
    prisma/lib/subject-constants.ts
    src/shared/lib/enrollment.ts (primaryEnrollmentOrderBy)
    src/shared/lib/student-progress/offsets.ts
    .cursor/rules/prisma-migrations.mdc
  </read_first>
  <action>
    Создать миграцию: pnpm db:migrate -- --name enrollment_progress_and_session_group.

    SQL (prod-safe, без TRUNCATE/DELETE данных):

    1) ALTER GroupEnrollment ADD currentStepIdx INT NOT NULL DEFAULT 0.

    2) Backfill прогресса per D-06: для каждого Student с enrollments — найти primary enrollment (MIN enrolledAt per studentId, как findPrimaryEnrollment) и SET GroupEnrollment.currentStepIdx = Student.currentStepIdx WHERE enrollment.id = primary.

    3) Backfill per D-07: для остальных enrollments того же student — SET currentStepIdx = сумма шагов предыдущих уровней предмета группы. Использовать подзапрос: JOIN Group ON enrollment.groupId, JOIN Level ON enrollment.levelId, посчитать offset как SUM(steps.count) для levels WHERE subjectId = group.subjectId AND number less than enrollment.level.number. Паттерн как getLevelStepOffsets в offsets.ts.

    4) Session: ADD groupId nullable; UPDATE Session SET groupId = subquery primary enrollment groupId per studentId (enrollment с MIN enrolledAt); для adjustment-сессий без student enrollments — fallback на группу учителя через legacy TeachingSession или первую группу предмета DEFAULT_QURAN — задокументировать выбор в migration.sql комментарием.

    5) ALTER Session SET groupId NOT NULL; ADD FK и unique(studentId, date, groupId).

    6) DROP COLUMN Student.currentStepIdx.

    Проверить COUNT: все Session имеют groupId; primary enrollments получили legacy idx.

    Не использовать db:push. После правки SQL — не применять на prod из dev; только commit файлов миграции.
  </action>
  <acceptance_criteria>
    migration.sql содержит backfill primary enrollment и offset для остальных
    migration.sql backfill Session.groupId без потери строк
    Student.currentStepIdx удалён в финальном шаге SQL
    Нет TRUNCATE и массового DELETE
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec prisma validate && node -e "const fs=require('fs');const p='prisma/migrations';if(!fs.readdirSync(p).some(d=>fs.existsSync(p+'/'+d+'/migration.sql')))process.exit(1)"</automated>
  </verify>
  <done>Файл миграции готов с полным backfill per D-06 D-07 D-04</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [BLOCKING] Применить миграцию и Zod session с groupId</name>
  <files>prisma/migrations/, src/shared/lib/validations/session.ts, src/shared/lib/validations/session.test.ts</files>
  <read_first>
    src/shared/lib/validations/session.ts
    src/shared/lib/validations/enrollment.test.ts
    .cursor/rules/prisma-migrations.mdc
  </read_first>
  <action>
    [BLOCKING] Применить миграцию локально: pnpm db:migrate (non-interactive). Затем pnpm exec prisma generate. Без этого шага typecheck даст ложноположительный результат.

    Обновить createSessionSchema: добавить groupId z.string().min(1, 'groupId обязателен'). Экспорт CreateSessionInput обновится автоматически.

    Создать session.test.ts (Vitest): RED→GREEN — отклонение пустого groupId; валидный payload с groupId проходит.

    Wave 0: session.test.ts создаётся здесь для plan 12-03.
  </action>
  <acceptance_criteria>
    pnpm db:migrate завершается успешно
    prisma migrate status показывает applied migration
    createSessionSchema требует groupId
    session.test.ts зелёный
  </acceptance_criteria>
  <verify>
    <automated>pnpm db:migrate && pnpm exec prisma generate && pnpm test:unit -- src/shared/lib/validations/session.test.ts</automated>
  </verify>
  <done>БД синхронизирована со схемой; Zod-контракт сессии включает groupId</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL | Выполняется на prod DB; ошибка backfill ломает прогресс всех учеников |
| Session.groupId backfill | Неверная группа свяжет completions с чужим контекстом |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | migration.sql backfill | critical | mitigate | Двухшаговый nullable→backfill→NOT NULL; COUNT-проверки в verify |
| T-12-02 | Information Disclosure | Session unique constraint | medium | accept | Ограничение видимости — в API auth (plan 12-03) |
| T-12-SC | Tampering | prisma migrate | high | mitigate | Только pnpm db:migrate; без db:push per prisma-migrations rule |
</threat_model>

<verification>
pnpm exec prisma migrate status — Database schema is up to date
pnpm exec prisma validate
pnpm test:unit -- src/shared/lib/validations/session.test.ts
</verification>

<success_criteria>
GroupEnrollment.currentStepIdx существует; Student.currentStepIdx удалён
Session.groupId NOT NULL с unique(studentId, date, groupId)
Миграция применена локально; createSessionSchema требует groupId
</success_criteria>

<artifacts_this_phase_produces>
- GroupEnrollment.currentStepIdx (Prisma field)
- Session.groupId (Prisma field + FK)
- Session @@unique([studentId, date, groupId])
- createSessionSchema с полем groupId
- createSessionSchema тесты в session.test.ts
- prisma/migrations/*_enrollment_progress_and_session_group/
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-progress-sessions/12-01-SUMMARY.md` when done
</output>
