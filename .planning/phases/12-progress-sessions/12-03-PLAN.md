---
phase: 12-progress-sessions
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - src/app/api/sessions/route.ts
  - src/app/api/step-completions/route.ts
  - src/app/api/step-completions/[id]/route.ts
  - src/features/journal/actions/journal-actions.ts
  - src/features/journal/lib/get-student-session.ts
autonomous: true
requirements:
  - SUBJ-09
  - SUBJ-10
must_haves:
  truths:
    - POST /api/sessions требует groupId и upsert по student+date+groupId (D-04, D-05)
    - GET /api/sessions фильтрует по groupId при наличии
    - step-completions вызывают recalculate с groupId enrollment
    - getStudentLesson возвращает enrollment.currentStepIdx и session с groupId
    - Сессия дня ищется с фильтром groupId = teacherGroup.id
  artifacts:
    - path: src/app/api/sessions/route.ts
      provides: group-scoped session CRUD
    - path: src/features/journal/actions/journal-actions.ts
      provides: getStudentLesson с enrollment progress
  key_links:
    - from: POST /api/sessions
      to: recalculateStudentStepIdx(studentId, groupId)
      via: после save completions в transaction
    - from: getStudentLesson
      to: Session
      via: student.sessions where groupId = teacherGroup.id
---

<objective>
Обновить sessions API, step-completions routes и journal-actions на group-scoped сессии и пересчёт.

Purpose: SUBJ-09 — сессия уникальна по ученику + дате + группе; API-контракты для Phase 13.
Output: Рабочие endpoints и getStudentLesson с enrollment progress.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-progress-sessions/12-CONTEXT.md
@src/app/api/sessions/route.ts
@src/app/api/step-completions/route.ts
@src/app/api/step-completions/[id]/route.ts
@src/features/journal/actions/journal-actions.ts
@src/shared/lib/validations/session.ts
@src/shared/lib/student-progress/recalculate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST/GET /api/sessions с groupId</name>
  <files>src/app/api/sessions/route.ts</files>
  <read_first>
    src/app/api/sessions/route.ts
    src/shared/lib/validations/session.ts
    src/shared/lib/authorize-api-request.ts
    .planning/phases/12-progress-sessions/12-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    POST: парсить groupId из createSessionSchema. authorizeApiRequest с context { studentId, groupId }.

    existingSession lookup: where studentId + groupId + date range (не только studentId+date). Upsert/update/create включать groupId в data.

    После save: recalculateStudentStepIdx(studentId, groupId, tx).

    GET (если есть): принимать query groupId; фильтровать sessions по groupId. Сохранить authorize и формат success/error.

    Сообщения ошибок на русском: «groupId обязателен», «Ученик не зачислен в группу» при отсутствии enrollment.

    Не менять UI journal — только API contract per Phase 12 boundary.
  </action>
  <acceptance_criteria>
    POST отклоняет body без groupId
    Две сессии одного ученика в один день в разных группах возможны
    recalculate вызывается с groupId
  </acceptance_criteria>
  <verify>
    <automated>rg "groupId" src/app/api/sessions/route.ts && rg "recalculateStudentStepIdx\\(studentId, groupId" src/app/api/sessions/route.ts && rg "groupId" src/shared/lib/validations/session.ts</automated>
  </verify>
  <done>Sessions API group-scoped per D-04 SUBJ-09</done>
</task>

<task type="auto">
  <name>Task 2: step-completions routes с groupId</name>
  <files>src/app/api/step-completions/route.ts, src/app/api/step-completions/[id]/route.ts</files>
  <read_first>
    src/app/api/step-completions/route.ts
    src/app/api/step-completions/[id]/route.ts
    src/shared/lib/student-progress/recalculate.ts
  </read_first>
  <action>
    POST/PATCH/DELETE step-completions: после мутации вызывать recalculateStudentStepIdx с groupId.

    Получить groupId из: (a) query/body groupId если добавлен в schema; или (b) session.groupId completion.sessionId include — предпочтительно (b) для PATCH/DELETE по id; для POST — из body groupId или sessionId→session.groupId.

    Обновить Zod schema step-completion если есть — добавить optional groupId или обязательный sessionId с валидацией принадлежности.

    Не ломать существующие клиенты без groupId: journal POST sessions уже передаст groupId в plan 13; interim — принимать groupId в body completions route.
  </action>
  <acceptance_criteria>
    recalculateStudentStepIdx вызывается с корректным groupId после каждой мутации completion
    Нет вызовов recalculate только с studentId
  </acceptance_criteria>
  <verify>
    <automated>rg "recalculateStudentStepIdx\\(studentId, groupId" src/app/api/step-completions/ && ! rg "recalculateStudentStepIdx\\(studentId,\\s*tx\\)" src/app/api/step-completions/ src/app/api/sessions/route.ts</automated>
  </verify>
  <done>Step completions триггерят enrollment-scoped recalculate</done>
</task>

<task type="auto">
  <name>Task 3: journal-actions getStudentLesson и session lookup</name>
  <files>src/features/journal/actions/journal-actions.ts, src/features/journal/lib/get-student-session.ts</files>
  <read_first>
    src/features/journal/actions/journal-actions.ts (getStudentLesson, getStudentStepHistory)
    src/features/journal/lib/get-student-session.ts
    src/shared/lib/enrollment.ts
  </read_first>
  <action>
    getStudentLesson: заменить student.currentStepIdx на enrollment.currentStepIdx в return payload.

    student.sessions include: добавить where groupId = teacherGroup.id чтобы daySession относилась к группе (D-05).

    getStudentStepHistory: enrollment.currentStepIdx вместо student.currentStepIdx.

    findStudentSessionForDay / serializeDaySession: если используют session без groupId — добавить параметр groupId и фильтр.

    Опциональный параметр groupId в getStudentLesson signature для forward-compat Phase 13; default = teacherGroup.id (текущее поведение).

    totalProgramSteps: getTotalProgramSteps(enrollment.group.subjectId) вместо default Quran only.
  </action>
  <acceptance_criteria>
    getStudentLesson возвращает currentStepIdx из enrollment
    daySession фильтруется по groupId учителя
    getStudentStepHistory использует enrollment progress
  </acceptance_criteria>
  <verify>
    <automated>rg "enrollment\\.currentStepIdx" src/features/journal/actions/journal-actions.ts && rg "groupId.*teacherGroup|teacherGroup\\.id" src/features/journal/actions/journal-actions.ts && ! rg "student\\.currentStepIdx" src/features/journal/actions/journal-actions.ts src/features/journal/lib/get-student-session.ts</automated>
  </verify>
  <done>Journal data layer отдаёт group-scoped progress для Phase 13 UI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /api/sessions | groupId от клиента — проверить enrollment и teacher access |
| step-completions | sessionId должен принадлежать studentId из auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Spoofing | sessions POST groupId | high | mitigate | authorizeApiRequest context groupId + enrollment check |
| T-12-06 | Tampering | step-completions PATCH | medium | mitigate | Resolve groupId from session FK, not client-only |
| T-12-SC | Tampering | npm installs | high | accept | Нет новых зависимостей |
</threat_model>

<verification>
rg "recalculateStudentStepIdx\\(studentId, groupId" src/app/api/sessions/route.ts src/app/api/step-completions/
rg "enrollment\\.currentStepIdx" src/features/journal/actions/journal-actions.ts
Full tsc deferred to 12-04 Task 3 and 12-05 Task 3 (wave-3 parallel compile sweep)
</verification>

<success_criteria>
Sessions API требует и сохраняет groupId
Journal actions возвращают enrollment.currentStepIdx
step-completions вызывают recalculate с groupId
</success_criteria>

<artifacts_this_phase_produces>
- POST/GET /api/sessions с обязательным groupId
- step-completions routes с enrollment-scoped recalculate
- getStudentLesson payload с enrollment.currentStepIdx
- getStudentStepHistory с enrollment progress
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-progress-sessions/12-03-SUMMARY.md` when done
</output>
