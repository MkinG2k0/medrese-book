---
phase: 12-progress-sessions
plan: 04
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - src/app/api/students/route.ts
  - src/features/student-admin/actions/student-admin-actions.ts
  - src/features/groups/actions/group-actions.ts
  - src/features/groups/actions/group-actions.test.ts
  - src/features/user-admin/actions/user-actions.ts
  - src/shared/lib/validations/student-progress.ts
  - src/shared/lib/validations/student-progress.test.ts
  - src/shared/lib/authorize-student-access.ts
  - src/entities/student/api/use-students.ts
autonomous: true
requirements:
  - SUBJ-08
  - SUBJ-10
must_haves:
  truths:
    - GET /api/students возвращает enrollment.currentStepIdx per groupId query (SUBJ-08)
    - student-admin progress edit обновляет конкретное enrollment, не global Student (D-01)
    - enrollStudent создаёт GroupEnrollment.currentStepIdx и вызывает sync с groupId (D-02)
    - updateStudentProgressSchema принимает groupId для выбора enrollment
    - Все runtime-чтения Student.currentStepIdx заменены на enrollment field
  artifacts:
    - path: src/features/student-admin/actions/student-admin-actions.ts
      provides: getStudentProgressEdit/updateStudentProgress per enrollment
    - path: src/app/api/students/route.ts
      provides: currentStepIdx from enrollment
    - path: src/shared/lib/validations/student-progress.ts
      provides: groupId в schema
  key_links:
    - from: updateStudentProgress
      to: GroupEnrollment
      via: update currentStepIdx + levelId; syncCompletionsForProgress с groupId
    - from: enrollStudent
      to: GroupEnrollment.create
      via: currentStepIdx в data enrollment, не student.update
---

<objective>
Адаптировать students API, student-admin, group enrollment и user-admin на прогресс GroupEnrollment.

Purpose: SUBJ-08 — независимый прогресс per enrollment; admin/API contracts без journal UI (Phase 13).
Output: API и server actions с groupId/enrollment context; compile без Student.currentStepIdx.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-progress-sessions/12-CONTEXT.md
@src/app/api/students/route.ts
@src/features/student-admin/actions/student-admin-actions.ts
@src/features/groups/actions/group-actions.ts
@src/features/user-admin/actions/user-actions.ts
@src/shared/lib/authorize-student-access.ts
@src/shared/lib/validations/student-progress.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: student-progress schema и student-admin per enrollment</name>
  <files>src/shared/lib/validations/student-progress.ts, src/shared/lib/validations/student-progress.test.ts, src/features/student-admin/actions/student-admin-actions.ts, src/shared/lib/authorize-student-access.ts</files>
  <read_first>
    src/features/student-admin/actions/student-admin-actions.ts
    src/shared/lib/validations/student-progress.ts
    src/shared/lib/authorize-student-access.ts
    .planning/phases/12-progress-sessions/12-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
    updateStudentProgressSchema: добавить groupId z.string().min(1, 'Выберите группу').

    requireStudentEditAccess: принимать optional groupId; если передан — findEnrollmentInGroup; иначе findPrimaryEnrollment (backward compat admin UI).

    getStudentProgressEdit(studentId, groupId?): возвращать enrollment.currentStepIdx, localStepIndex от enrollment (не student). Включить groupId, enrollmentId в response.

    updateStudentProgress: использовать enrollment по groupId из input; syncCompletionsForProgress(tx, studentId, groupId, ...); обновлять GroupEnrollment levelId + currentStepIdx; recalculateStudentStepIdx(studentId, groupId, tx). Удалить tx.student.update currentStepIdx.

    student-progress.test.ts: schema rejects missing groupId; accepts valid payload.

    Ошибки на русском сохранить.
  </action>
  <acceptance_criteria>
    updateStudentProgress пишет только в GroupEnrollment
    getStudentProgressEdit читает enrollment.currentStepIdx
    Schema требует groupId
    student-progress.test.ts зелёный
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/validations/student-progress.test.ts</automated>
  </verify>
  <done>student-admin target specific enrollment per D-01</done>
</task>

<task type="auto">
  <name>Task 2: students API и group enrollStudent</name>
  <files>src/app/api/students/route.ts, src/features/groups/actions/group-actions.ts, src/features/groups/actions/group-actions.test.ts, src/entities/student/api/use-students.ts</files>
  <read_first>
    src/app/api/students/route.ts
    src/features/groups/actions/group-actions.ts (enrollStudent)
    src/features/groups/actions/group-actions.test.ts
    src/entities/student/api/use-students.ts
  </read_first>
  <action>
    GET /api/students: currentStepIdx: enrollment.currentStepIdx (не student). todaySession lookup: фильтр sessions по groupId из query (уже есть groupId param). Session include where groupId = groupId.

    enrollStudent: при create GroupEnrollment включить currentStepIdx в data; удалить student.update currentStepIdx; syncCompletionsForProgress(tx, studentId, groupId, level.steps, localStepIndex).

    Обновить group-actions.test.ts: expect groupEnrollment.create с currentStepIdx; expect syncCompletionsForProgress вызван с groupId; убрать student.update currentStepIdx assertions.

    use-students.ts тип StudentListItem: currentStepIdx остаётся number (значение из enrollment для запрошенной группы).
  </action>
  <acceptance_criteria>
    students API возвращает enrollment-scoped currentStepIdx
    enrollStudent устанавливает progress на enrollment
    group-actions.test.ts обновлён и зелёный
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:unit -- src/features/groups/actions/group-actions.test.ts</automated>
  </verify>
  <done>Students list и enrollment flow group-scoped per SUBJ-08</done>
</task>

<task type="auto">
  <name>Task 3: user-admin и compile sweep Student.currentStepIdx</name>
  <files>src/features/user-admin/actions/user-actions.ts, src/features/user-admin/lib/map-users-to-details.ts</files>
  <read_first>
    src/features/user-admin/actions/user-actions.ts
    src/features/user-admin/lib/map-users-to-details.ts
    Grep результат Student.currentStepIdx по src/
  </read_first>
  <action>
    user-actions createUsers/updateUser progress paths: использовать primary enrollment currentStepIdx для display; при создании ученика — set currentStepIdx на GroupEnrollment при create enrollment (не Student).

    map-users-to-details: читать progress из primary enrollment include currentStepIdx.

    Compile sweep: grep src/ на currentStepIdx и student.currentStepIdx — заменить оставшиеся runtime reads:
    - student-portal, awards, student-metrics, journal UI components — использовать enrollment.currentStepIdx из загруженных данных или primary enrollment в server loaders
    - Не менять Phase 13 journal UX (picker) — только чтобы tsc проходил

    Файлы вне files_modified разрешены для минимальных one-line fixes если блокируют compile; перечислить в SUMMARY.

    seed.ts / seed-e2e.ts: обновить создание Student без currentStepIdx; set на GroupEnrollment.
  </action>
  <acceptance_criteria>
    pnpm exec tsc --noEmit проходит
    grep -r "student\.currentStepIdx" src/ | wc -l равно 0
    user-admin отображает progress из enrollment
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Кодовая база компилируется без Student.currentStepIdx per D-03</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student-admin | groupId выбирает какое enrollment менять |
| teacher access | requireStudentEditAccess проверяет teacherId группы |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Elevation | updateStudentProgress wrong group | high | mitigate | findEnrollmentInGroup + teacherId check |
| T-12-08 | Tampering | enrollStudent progress | medium | mitigate | level validated against group.subjectId |
| T-12-SC | Tampering | npm installs | high | accept | Нет новых пакетов |
</threat_model>

<verification>
pnpm exec tsc --noEmit
pnpm test:unit -- src/features/groups/actions/group-actions.test.ts src/shared/lib/validations/student-progress.test.ts
</verification>

<success_criteria>
API students и admin actions используют enrollment.currentStepIdx
enrollStudent создаёт progress на enrollment
Нет обращений к student.currentStepIdx в src/
</success_criteria>

<artifacts_this_phase_produces>
- updateStudentProgressSchema с groupId
- getStudentProgressEdit/updateStudentProgress per enrollment
- GET /api/students с enrollment.currentStepIdx
- enrollStudent с GroupEnrollment.currentStepIdx
- Обновлённые group-actions.test.ts, student-progress.test.ts
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-progress-sessions/12-04-SUMMARY.md` when done
</output>
