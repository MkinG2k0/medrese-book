---
phase: 12-progress-sessions
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/shared/lib/student-progress/recalculate.ts
  - src/shared/lib/student-progress/recalculate.test.ts
  - src/shared/lib/student-progress/sync-for-progress.ts
  - src/shared/lib/student-progress/sync-for-progress.test.ts
  - src/shared/lib/student-progress/index.ts
  - src/shared/lib/recalculate-step-progress.ts
  - src/shared/lib/sync-completions-for-progress.ts
autonomous: true
requirements:
  - SUBJ-08
  - SUBJ-10
must_haves:
  truths:
    - recalculateStudentStepIdx обновляет GroupEnrollment.currentStepIdx, не Student (D-01, D-03)
    - Пересчёт принимает контекст зачисления (studentId + groupId), не только primary (D-08)
    - Auto-promote меняет levelId и currentStepIdx только текущего enrollment (D-08)
    - syncCompletionsForProgress создаёт adjustment Session с groupId (SUBJ-10)
    - Offsets считаются по group.subjectId, не глобально
  artifacts:
    - path: src/shared/lib/student-progress/recalculate.ts
      provides: recalculateStudentStepIdx(studentId, groupId, tx?)
    - path: src/shared/lib/student-progress/sync-for-progress.ts
      provides: syncCompletionsForProgress(tx, studentId, groupId, levelSteps, localStepIndex)
    - path: src/shared/lib/student-progress/recalculate.test.ts
      provides: Unit-тесты enrollment-scoped recalculate и auto-promote
  key_links:
    - from: recalculateStudentStepIdx
      to: GroupEnrollment
      via: update where id = enrollment.id
    - from: syncCompletionsForProgress
      to: Session
      via: findFirst/create с studentId + groupId + isAdjustment + date
    - from: getStepOffsetForLevel
      to: enrollment.group.subjectId
      via: второй аргумент subjectId
---

<objective>
Переписать recalculateStudentStepIdx и syncCompletionsForProgress на скоуп GroupEnrollment (groupId), с unit-тестами.

Purpose: SUBJ-08 и SUBJ-10 — пересчёт и prior credit per enrollment, не глобально (D-08).
Output: Новые сигнатуры, тесты, barrel re-exports без изменения путей импорта.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-progress-sessions/12-CONTEXT.md
@src/shared/lib/student-progress/recalculate.ts
@src/shared/lib/student-progress/sync-for-progress.ts
@src/shared/lib/student-progress/offsets.ts
@src/shared/lib/enrollment.ts
@src/features/groups/actions/group-actions.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recalculateStudentStepIdx per enrollment</name>
  <files>src/shared/lib/student-progress/recalculate.ts, src/shared/lib/student-progress/recalculate.test.ts</files>
  <read_first>
    src/shared/lib/student-progress/recalculate.ts
    src/shared/lib/step-completion.ts
    src/shared/lib/enrollment.ts
    .planning/phases/12-progress-sessions/12-CONTEXT.md (D-01, D-03, D-08)
  </read_first>
  <action>
    Изменить сигнатуру: recalculateStudentStepIdx(studentId: string, groupId: string, tx?: Tx).

    Загрузка enrollment через findEnrollmentInGroup(studentId, groupId, db) — не findPrimaryEnrollment (D-06 только для миграции).

    Фильтр completions: stepId IN level.steps текущего enrollment.level; subject scope через level.subjectId enrollment.

    getStepOffsetForLevel(level.number, enrollment.group.subjectId) — передавать subjectId явно.

    newIdx = stepOffset + countConsecutivePassedSteps. Обновлять GroupEnrollment.currentStepIdx (и при allPassed — levelId следующего уровня того же subjectId). Не вызывать student.update для currentStepIdx — поле удалено (D-03).

    Per D-08: при auto-promote обновлять только enrollment.id из запроса; не трогать другие enrollments ученика.

    Создать recalculate.test.ts (Vitest, mock prisma): тесты — (1) обновляет enrollment.currentStepIdx при новых completions; (2) auto-promote меняет enrollment.levelId без side-effect на второй enrollment; (3) no-op когда enrollment не найден.
  </action>
  <acceptance_criteria>
    Функция принимает groupId и не читает Student.currentStepIdx
    Запись прогресса идёт в GroupEnrollment.currentStepIdx
    Auto-promote обновляет только целевое enrollment
    recalculate.test.ts проходит
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/student-progress/recalculate.test.ts</automated>
  </verify>
  <done>recalculate enrollment-scoped с тестами per D-01 D-08</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: syncCompletionsForProgress с groupId</name>
  <files>src/shared/lib/student-progress/sync-for-progress.ts, src/shared/lib/student-progress/sync-for-progress.test.ts</files>
  <read_first>
    src/shared/lib/student-progress/sync-for-progress.ts
    src/shared/lib/calendar-date.ts
    .planning/phases/12-progress-sessions/12-CONTEXT.md (D-04, D-05, SUBJ-10)
  </read_first>
  <action>
    Изменить сигнатуру: syncCompletionsForProgress(tx, studentId, groupId, levelSteps, localStepIndex).

    Adjustment session findFirst/create: фильтр studentId + groupId + isAdjustment:true + date range (отдельная корректировка per group per day per discretion).

    При create adjustment Session включать groupId в data.

    Удалить completions future steps и создать/обновить prior credit как сейчас — без изменения бизнес-логики grade/PASSING_GRADE.

    sync-for-progress.test.ts: (1) создаёт adjustment session с groupId; (2) находит существующую adjustment той же группы в тот же день; (3) не смешивает adjustment разных groupId.
  </action>
  <acceptance_criteria>
    syncCompletionsForProgress требует groupId
    Adjustment Session всегда имеет groupId
    Тесты покрывают изоляцию по groupId
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/student-progress/sync-for-progress.test.ts</automated>
  </verify>
  <done>Prior credit scoped per enrollment/group per SUBJ-10</done>
</task>

<task type="auto">
  <name>Task 3: Barrel exports и временные overload-заглушки</name>
  <files>src/shared/lib/student-progress/index.ts, src/shared/lib/recalculate-step-progress.ts, src/shared/lib/sync-completions-for-progress.ts</files>
  <read_first>
    src/shared/lib/student-progress/index.ts
    src/shared/lib/recalculate-step-progress.ts
    src/shared/lib/sync-completions-for-progress.ts
  </read_first>
  <action>
    Обновить index.ts — экспорт новых сигнатур без переименования функций.

    Re-export файлы recalculate-step-progress.ts и sync-completions-for-progress.ts оставить; они проксируют из student-progress/.

    Не менять call sites в этом task — plan 12-03/12-04 обновят вызовы. Здесь только убедиться что TypeScript ошибки ожидаемы до wave 3 (compile gate в 12-05).

    Добавить JSDoc на recalculateStudentStepIdx: требует groupId с Phase 12.
  </action>
  <acceptance_criteria>
    Barrel exports указывают на обновлённые модули
    Нет дублирования логики в legacy re-export файлах
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/student-progress/</automated>
  </verify>
  <done>Модуль student-progress готов для wiring в API (wave 3)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recalculate input | groupId от API — должен совпадать с enrollment ученика |
| prior credit | Неверный groupId создаст completions в чужой adjustment session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering | recalculateStudentStepIdx | high | mitigate | findEnrollmentInGroup; отказ если enrollment null |
| T-12-04 | Elevation | auto-promote cross-enrollment | medium | mitigate | update только by enrollment.id per D-08 |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | Нет новых пакетов в этой фазе |
</threat_model>

<verification>
pnpm test:unit -- src/shared/lib/student-progress/
</verification>

<success_criteria>
recalculateStudentStepIdx(studentId, groupId) пишет в GroupEnrollment
syncCompletionsForProgress использует groupId для adjustment sessions
Unit-тесты recalculate и sync зелёные
</success_criteria>

<artifacts_this_phase_produces>
- recalculateStudentStepIdx(studentId, groupId, tx?)
- syncCompletionsForProgress(tx, studentId, groupId, levelSteps, localStepIndex)
- recalculate.test.ts
- sync-for-progress.test.ts
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-progress-sessions/12-02-SUMMARY.md` when done
</output>
