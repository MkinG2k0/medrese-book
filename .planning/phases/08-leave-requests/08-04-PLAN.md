---
phase: 08-leave-requests
plan: 04
type: execute
wave: 4
depends_on:
  - "08-02"
  - "08-03"
files_modified:
  - src/app/(dashboard)/admin/leave-calendar/page.tsx
  - src/features/leave-requests/ui/ManagerLeaveCalendarPage.tsx
  - src/features/leave-requests/ui/LeaveRequestsTable.tsx
  - src/features/leave-requests/ui/ApproveLeaveModal.tsx
  - src/features/leave-requests/ui/RejectLeaveModal.tsx
  - src/features/leave-requests/actions/leave-actions.ts
  - src/shared/lib/auth.config.ts
  - src/widgets/app-shell/ui/AppShell.tsx
autonomous: false
requirements:
  - LEAV-01
  - LEAV-03
  - LEAV-04
  - TCHR-03
must_haves:
  truths:
    - "Менеджер видит календарь всех заявок: CREATED серый, APPROVED зелёный, REJECTED скрыт"
    - "Грид под календарём с фильтрами и действиями Подтвердить/Отклонить для CREATED"
    - "Подтверждение требует выбор замещающего; отклонение — обязательную причину"
    - "После подтверждения — message «Заявка подтверждена, замещение активировано»"
    - "Пункт «Календарь отпусков» в меню менеджера"
  artifacts:
    - path: src/app/(dashboard)/admin/leave-calendar/page.tsx
      provides: "Manager leave calendar route"
    - path: src/features/leave-requests/ui/LeaveRequestsTable.tsx
      provides: "Filtered grid with approve/reject actions"
    - path: src/features/leave-requests/ui/ApproveLeaveModal.tsx
      provides: "Substitute selection on approve"
  key_links:
    - from: ApproveLeaveModal
      to: approveLeaveRequest
      via: "substituteTeacherId required"
      pattern: "approveLeaveRequest"
    - from: LeaveRequestsTable
      to: RejectLeaveModal
      via: "reject with reason"
      pattern: "rejectLeaveRequest"
    - from: ManagerLeaveCalendarPage
      to: useLeaveRequests
      via: "all teachers scope"
      pattern: "useLeaveRequests"
---

<objective>
Календарь и грид менеджера: просмотр всех заявок, согласование с назначением замещающего или отклонение с причиной.

Purpose: LEAV-01, LEAV-03, LEAV-04, TCHR-03 — manager workflow per UI-SPEC.
Output: Route /admin/leave-calendar, table + modals, nav entry.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-leave-requests/08-UI-SPEC.md
@.planning/phases/08-leave-requests/08-03-SUMMARY.md
@src/features/leave-requests/ui/LeaveCalendar.tsx
@src/features/analytics/ui/TeacherLessonsPicker.tsx
@src/features/user-admin/ui/UsersTable.tsx

<interfaces>
From leave-actions:
- approveLeaveRequest({ leaveRequestId, substituteTeacherId })
- rejectLeaveRequest({ leaveRequestId, rejectionReason })
- listTeachers for substitute Select — использовать getAnalyticsTeachers или prisma teachers query из analytics-actions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route /admin/leave-calendar и навигация</name>
  <files>
    src/app/(dashboard)/admin/leave-calendar/page.tsx
    src/shared/lib/auth.config.ts
    src/widgets/app-shell/ui/AppShell.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/admin/users/page.tsx — requireRoles pattern
    - src/shared/lib/auth.config.ts — /admin prefix already MANAGER
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — Routes table
  </read_first>
  <acceptance_criteria>
    - page.tsx: requireRoles(['MANAGER', 'SUPER_ADMIN'])
    - AppShell menu: `/admin/leave-calendar`, label «Календарь отпусков», CalendarOutlined, after «Аналитика учителей»
    - TEACHER cannot access route (redirect)
    - Title level={3}: «Календарь отпусков»
  </acceptance_criteria>
  <action>
    /admin/* уже покрыт roleRoutes '/admin': MANAGER/SUPER_ADMIN — дополнительный ключ не обязателен, но можно добавить явный `/admin/leave-calendar` для ясности.
    Delegate to ManagerLeaveCalendarPage.
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/app/(dashboard)/admin/leave-calendar/page.tsx src/widgets/app-shell/ui/AppShell.tsx</automated>
  </verify>
  <done>Менеджерский маршрут и пункт меню на месте</done>
</task>

<task type="auto">
  <name>Task 2: Manager calendar + LeaveRequestsTable</name>
  <files>
    src/features/leave-requests/ui/ManagerLeaveCalendarPage.tsx
    src/features/leave-requests/ui/LeaveRequestsTable.tsx
  </files>
  <read_first>
    - src/features/leave-requests/ui/LeaveCalendar.tsx — reuse with manager cellRender
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — Manager Grid, filters
    - src/features/analytics/ui/TeacherLessonsPicker.tsx — teacher Select pattern
  </read_first>
  <acceptance_criteria>
    - Manager calendar: cell shows teacher surname abbreviated + status color; REJECTED hidden
    - LeaveRequestsTable columns: teacherName, type Tag, period, status Tag, description ellipsis+Tooltip, substituteName, actions
    - Filters: status Select default «Все», teacher Select searchable, type Select, optional RangePicker
    - Actions column only for CREATED: «Подтвердить» link primary, «Отклонить» link danger
    - Rejected rows visible when filter «Отклонена»; rejection column in detail modal
    - pagination pageSize 20, default sort createdAt desc
    - Empty grid copy from UI-SPEC
    - layout gap-6 between calendar and table
  </acceptance_criteria>
  <action>
    Reuse LeaveCalendar with variant prop `mode: 'teacher' | 'manager'` или cellRender override prop.
    ManagerLeaveCalendarPage loads all requests via useLeaveRequests({}).
    Table row click opens LeaveDetailModal with approve/reject buttons when CREATED.
    Load teacher list for filters from server action getSubstituteTeacherCandidates (new helper in leave-actions: all TEACHER users id+name).
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/features/leave-requests/ui/ManagerLeaveCalendarPage.tsx src/features/leave-requests/ui/LeaveRequestsTable.tsx</automated>
  </verify>
  <done>Менеджер видит календарь и фильтруемый грид</done>
</task>

<task type="auto">
  <name>Task 3: ApproveLeaveModal и RejectLeaveModal</name>
  <files>
    src/features/leave-requests/ui/ApproveLeaveModal.tsx
    src/features/leave-requests/ui/RejectLeaveModal.tsx
    src/features/leave-requests/actions/leave-actions.ts
  </files>
  <read_first>
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — Approve/Reject modals
    - src/features/leave-requests/actions/leave-actions.ts — approve/reject
    - src/shared/lib/validations/leave-request.ts
  </read_first>
  <acceptance_criteria>
    - ApproveLeaveModal: Descriptions summary (ФИО, тип, период, описание); Select замещающий required; exclude applicant; showSearch; width={560}
    - Footer: «Отмена» + «Подтвердить заявку» primary; success message per UI-SPEC
    - RejectLeaveModal: TextArea причина min 5; «Отклонить» danger; width={480}
    - Нельзя approve без substituteTeacherId (client + server)
    - invalidateQueries on success; revalidate paths
    - dispatchDomainEvent already in approve (plan 02) — UI only wires
  </acceptance_criteria>
  <action>
    getSubstituteTeacherCandidates in leave-actions: TEACHER role users except leaveRequest.teacherId.
    zodResolver for approve/reject schemas.
    Reject: no Modal.confirm without form — full modal per UI-SPEC.
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/features/leave-requests/ui/ApproveLeaveModal.tsx src/features/leave-requests/ui/RejectLeaveModal.tsx</automated>
  </verify>
  <done>Согласование и отклонение работают end-to-end в UI</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Проверка manager flow в браузере</name>
  <files>src/app/(dashboard)/admin/leave-calendar/page.tsx</files>
  <read_first>
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — Copywriting Contract
    - e2e/helpers/codes.ts — TEST_CODES manager/teacher
  </read_first>
  <acceptance_criteria>
    - Manager подтверждает заявку с substitute — зелёный badge на календаре
    - Отклонённая заявка не отображается на календаре менеджера
    - Причина отклонения видна в гриде при фильтре «Отклонена»
  </acceptance_criteria>
  <action>
    Checkpoint после Task 1–3. Автоматизация уже выполнена; пользователь проверяет визуальный flow в браузере по шагам ниже.
  </action>
  <what-built>Календарь отпусков менеджера: грид, подтверждение с замещающим, отклонение с причиной</what-built>
  <how-to-verify>
    1. `pnpm dev` — войти как manager (код 100002)
    2. Открыть «Календарь отпусков» в sidebar
    3. Убедиться: пустой или существующие заявки; CREATED серые, APPROVED зелёные на календаре
    4. Войти как teacher1 (200001), создать отгул на /calendar
    5. Как manager: в гриде «Подтвердить» → выбрать teacher2 → подтвердить
    6. Проверить substituteName в гриде и зелёный badge на календаре
    7. Создать вторую заявку teacher1 → «Отклонить» с причиной ≥5 символов → статус Отклонена в гриде, не на календаре
  </how-to-verify>
  <verify>
    <automated>echo "Human checkpoint — run steps in how-to-verify"</automated>
  </verify>
  <done>Пользователь подтвердил manager flow или описал дефекты</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager→approveLeaveRequest | Только MANAGER/SUPER_ADMIN |
| ApproveLeaveModal | substituteTeacherId client-side; server re-validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | E | approveLeaveRequest | mitigate | requireRoles + status CREATED check |
| T-08-13 | I | LeaveRequestsTable | mitigate | API returns all only for manager roles |
| T-08-14 | T | RejectLeaveModal reason | mitigate | min 5 chars Zod client+server |
| T-08-15 | E | substitute Select | mitigate | exclude applicant; TEACHER role only |
</threat_model>

<verification>
- Human checkpoint after automated lint
- Manager can approve with substitute and reject with reason
</verification>

<success_criteria>
- LEAV-01 manager calendar
- LEAV-03 approve/reject with validation
- LEAV-04 substitution activated on approve visible in grid
</success_criteria>

<output>
After completion, create `.planning/phases/08-leave-requests/08-04-SUMMARY.md`
</output>
