---
phase: 08-leave-requests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/shared/lib/validations/leave-request.ts
  - src/shared/lib/domain-events/types.ts
  - src/features/leave-requests/lib/leave-labels.ts
  - src/features/leave-requests/lib/substitution-active.ts
  - src/features/leave-requests/model/types.ts
  - src/features/leave-requests/index.ts
autonomous: true
requirements:
  - LEAV-05
  - TCHR-05
must_haves:
  truths:
    - "Менеджер может сохранить заявку типа «Больничный» (SICK_LEAVE) через валидацию createLeaveRequestSchema"
    - "При одобрении заявки создаётся запись Substitution, связанная с LeaveRequest"
    - "События LEAVE_REQUEST_CREATED и SUBSTITUTION_ACTIVATED попадают в dispatchDomainEvent"
  artifacts:
    - path: prisma/schema.prisma
      provides: "LeaveRequest + Substitution models"
      contains: "model LeaveRequest"
    - path: src/shared/lib/validations/leave-request.ts
      provides: "createLeaveRequestSchema, approveLeaveRequestSchema, rejectLeaveRequestSchema"
    - path: src/features/leave-requests/lib/substitution-active.ts
      provides: "isSubstitutionCurrentlyActive"
      exports: ["isSubstitutionCurrentlyActive"]
  key_links:
    - from: prisma/schema.prisma
      to: Teacher model
      via: "leaveRequests + substitutions relations"
      pattern: "leaveRequests|substitutionsAsAbsent"
    - from: src/shared/lib/domain-events/types.ts
      to: dispatchDomainEvent consumers
      via: "LEAVE_REQUEST_CREATED action"
      pattern: "LEAVE_REQUEST_CREATED|SUBSTITUTION_ACTIVATED"
---

<objective>
Создать схему данных заявок на отсутствие и замещений, Zod-валидации и типы доменных событий.

Purpose: Фундамент для backend и UI фазы 8; LEAV-05 (SICK_LEAVE) и TCHR-05 (история замещений).
Output: Prisma-модели применены к БД, validations + lib helpers, расширенные DomainEventAction.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-leave-requests/08-CONTEXT.md
@.planning/phases/08-leave-requests/08-UI-SPEC.md
@prisma/schema.prisma
@src/shared/lib/domain-events/types.ts
@src/shared/lib/validations/student-status.ts

<interfaces>
From prisma/schema.prisma (Teacher — добавить relations):
```prisma
model Teacher {
  id               String            @id @default(cuid())
  userId           String            @unique
  // ... existing fields
}
```

From src/shared/lib/domain-events/types.ts:
```typescript
export type DomainEventAction =
  | 'STUDENT_PROGRESS_CHANGED'
  // ... existing
  | 'LESSON_ENDED'
```

Решения UI-SPEC (locked):
- SICK_LEAVE — отдельный enum-тип (D-UI-01)
- Один замещающий на весь диапазон дат (D-UI-02)
- Авто-деактивация замещения после endDate конца дня (D-UI-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma-модели LeaveRequest и Substitution</name>
  <files>
    prisma/schema.prisma
  </files>
  <read_first>
    - prisma/schema.prisma — полный файл, паттерны enum/model/index
    - C:\Users\mk\.cursor\plugins\cache\cursor-public\prisma\4584a0a9175ba74053d7ee946c6234d3369a5a33\rules\schema-conventions.mdc
  </read_first>
  <acceptance_criteria>
    - `grep "enum LeaveRequestType" prisma/schema.prisma` → VACATION, DAY_OFF, SICK_LEAVE
    - `grep "enum LeaveRequestStatus" prisma/schema.prisma` → CREATED, APPROVED, REJECTED
    - `model LeaveRequest` содержит: teacherId, type, status @default(CREATED), startDate, endDate, description, rejectionReason?, substituteTeacherId?, substitutionId? @unique, createdAt, updatedAt
    - `model Substitution` содержит: absentTeacherId, substituteTeacherId, leaveRequestId? @unique, startDate, endDate, isActive @default(true), createdAt, updatedAt
    - Teacher: relations leaveRequests, substitutionsAsAbsent, substitutionsAsSubstitute (обе стороны @relation)
    - @@index на [teacherId, status], [startDate, endDate], [substituteTeacherId, isActive]
    - Нет breaking changes существующих моделей
  </acceptance_criteria>
  <action>
    Добавить enums `LeaveRequestType` и `LeaveRequestStatus` per UI-SPEC.
    `LeaveRequest.teacherId` → `Teacher` (заявитель). `substituteTeacherId` nullable — заполняется при approve.
    `Substitution`: absentTeacherId = заявитель, substituteTeacherId = замещающий; опциональная 1:1 связь с LeaveRequest через leaveRequestId.
    Даты хранить как DateTime (начало дня UTC для startDate, конец дня для endDate — нормализация в lib, не в schema).
    Не добавлять Notification model (Phase 6).
  </action>
  <verify>
    <automated>pnpm prisma validate</automated>
  </verify>
  <done>schema.prisma валиден, модели и индексы на месте</done>
</task>

<task type="auto">
  <name>Task 2: Zod-схемы, типы фичи и domain events</name>
  <files>
    src/shared/lib/validations/leave-request.ts
    src/shared/lib/domain-events/types.ts
    src/features/leave-requests/model/types.ts
    src/features/leave-requests/lib/leave-labels.ts
    src/features/leave-requests/lib/substitution-active.ts
    src/features/leave-requests/index.ts
  </files>
  <read_first>
    - src/shared/lib/validations/student-status.ts — стиль сообщений на русском
    - src/shared/lib/domain-events/types.ts
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — Copywriting Contract, Modal Forms validation
  </read_first>
  <acceptance_criteria>
    - `createLeaveRequestSchema`: type enum VACATION|DAY_OFF|SICK_LEAVE, startDate/endDate ISO strings, endDate >= startDate, description min 2 max 500
    - `approveLeaveRequestSchema`: leaveRequestId string, substituteTeacherId string (cuid)
    - `rejectLeaveRequestSchema`: leaveRequestId string, rejectionReason min 5 max 500
    - `grep "LEAVE_REQUEST_CREATED" src/shared/lib/domain-events/types.ts` — также LEAVE_REQUEST_APPROVED, LEAVE_REQUEST_REJECTED, SUBSTITUTION_ACTIVATED
    - `leave-labels.ts` экспортирует русские метки: «Создана», «Подтверждена», «Отклонена», «Отпуск», «Отгул», «Больничный»
    - `isSubstitutionCurrentlyActive(sub, now?)` возвращает false если now > endDate конца дня или isActive=false
    - `src/features/leave-requests/index.ts` реэкспортирует types и leave-labels
  </acceptance_criteria>
  <action>
    Создать validations per UI-SPEC (поля «Период отсутствия», «Описание», «Причина отклонения»).
    Расширить DomainEventAction четырьмя leave/substitution actions (payload: teacherId, substituteTeacherId, dates, type).
    `substitution-active.ts`: helper с date-fns `endOfDay` для проверки активности без cron.
    `model/types.ts`: экспорт типов LeaveRequestDto, SubstitutionDto (mapped from Prisma, без any).
    Barrel index.ts — только публичные exports.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Валидации, лейблы и domain event types готовы для backend</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Применить миграцию Prisma</name>
  <files>
    prisma/migrations/
  </files>
  <read_first>
    - package.json — scripts db:migrate
    - C:\Users\mk\.cursor\plugins\cache\cursor-public\prisma\4584a0a9175ba74053d7ee946c6234d3369a5a33\rules\migration-best-practices.mdc
  </read_first>
  <acceptance_criteria>
    - Директория `prisma/migrations/*leave*` или последняя миграция содержит LeaveRequest и Substitution
    - `pnpm prisma migrate status` — нет pending migrations (или Applied)
    - `pnpm db:generate` завершается без ошибок
  </acceptance_criteria>
  <action>
    После Task 1–2 выполнить: `pnpm db:migrate -- --name leave_requests_substitutions`
    Если migrate dev требует интерактив — использовать неинтерактивный флаг --name.
    Затем `pnpm db:generate`.
    Не запускать db:seed в этой задаче.
  </action>
  <verify>
    <automated>pnpm prisma migrate status</automated>
  </verify>
  <done>БД синхронизирована со schema, Prisma Client сгенерирован</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema | Новые таблицы доступны только через Prisma с auth-слоем |
| Validation input | Невалидные даты/типы отсекаются Zod до записи |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | leave-request.ts dates | mitigate | Zod refine endDate >= startDate; нормализация start/end of day в actions |
| T-08-02 | I | Substitution.isActive | mitigate | isSubstitutionCurrentlyActive проверяет даты + флаг |
| T-08-03 | D | Prisma migrations | mitigate | migrate dev с именем; не db push в production path |
</threat_model>

<verification>
- `pnpm prisma validate` && `pnpm prisma migrate status`
- Grep подтверждает все enum values и domain event actions
</verification>

<success_criteria>
- LeaveRequest и Substitution в БД
- SICK_LEAVE как отдельный тип (LEAV-05)
- Substitution model для истории (TCHR-05 foundation)
</success_criteria>

<output>
After completion, create `.planning/phases/08-leave-requests/08-01-SUMMARY.md`
</output>
