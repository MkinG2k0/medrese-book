---
phase: 08-leave-requests
plan: 03
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - src/app/(dashboard)/calendar/page.tsx
  - src/features/leave-requests/ui/TeacherLeaveCalendarPage.tsx
  - src/features/leave-requests/ui/LeaveCalendar.tsx
  - src/features/leave-requests/ui/CreateLeaveModal.tsx
  - src/features/leave-requests/ui/LeaveDetailModal.tsx
  - src/widgets/app-shell/ui/AppShell.tsx
  - src/shared/lib/auth.config.ts
autonomous: true
requirements:
  - LEAV-02
  - LEAV-05
must_haves:
  truths:
    - "Преподаватель открывает /calendar и видит только свои заявки"
    - "Три кнопки: Создать отпуск / отгул / больничный открывают модалку с RangePicker и описанием"
    - "После отправки — message.success «Заявка отправлена на согласование», статус Создана на календаре"
    - "Клик по ячейке открывает read-only детали; при REJECTED показана причина"
    - "Пункт меню «Календарь» виден только TEACHER"
  artifacts:
    - path: src/app/(dashboard)/calendar/page.tsx
      provides: "Teacher calendar route"
    - path: src/features/leave-requests/ui/CreateLeaveModal.tsx
      provides: "Create leave form modal"
    - path: src/features/leave-requests/ui/LeaveCalendar.tsx
      provides: "Ant Design Calendar with cellRender badges"
  key_links:
    - from: CreateLeaveModal
      to: createLeaveRequest
      via: "server action on submit"
      pattern: "createLeaveRequest"
    - from: LeaveCalendar
      to: useLeaveRequests
      via: "fetch own requests"
      pattern: "useLeaveRequests|/api/leave-requests"
    - from: AppShell.tsx
      to: /calendar
      via: "menu item CalendarOutlined"
      pattern: "/calendar"
---

<objective>
Страница календаря преподавателя: создание отпуска, отгула и больничного; отображение своих заявок.

Purpose: LEAV-02, LEAV-05 (SICK_LEAVE + teacher calendar per UI-SPEC).
Output: Route /calendar, UI components, nav entry, middleware guard.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-leave-requests/08-UI-SPEC.md
@.planning/phases/08-leave-requests/08-02-SUMMARY.md
@src/features/analytics/ui/TeacherLessonsDateFilter.tsx
@src/features/auth/ui/LoginForm.tsx
@src/widgets/app-shell/ui/AppShell.tsx

<interfaces>
From 08-UI-SPEC — CreateLeaveModal:
- leaveType: 'VACATION' | 'DAY_OFF' | 'SICK_LEAVE'
- RangePicker format="DD.MM.YYYY", description TextArea required min 2
- Titles: «Новый отпуск» / «Новый отгул» / «Новый больничный»

From leave-actions (plan 02):
- createLeaveRequest(input: unknown)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route, guard и навигация</name>
  <files>
    src/app/(dashboard)/calendar/page.tsx
    src/shared/lib/auth.config.ts
    src/widgets/app-shell/ui/AppShell.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/journal/page.tsx — requireRole pattern
    - src/shared/lib/auth.config.ts — roleRoutes
    - src/widgets/app-shell/ui/AppShell.tsx — allMenuItems
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — Pages & Routes
  </read_first>
  <acceptance_criteria>
    - `src/app/(dashboard)/calendar/page.tsx` вызывает requireRole('TEACHER')
    - auth.config roleRoutes: `'/calendar': ['TEACHER']`
    - AppShell: menu item key `/calendar`, label «Календарь», icon CalendarOutlined, roles ['TEACHER'], после «Моя группа»
    - MANAGER visiting /calendar → redirect per auth.config
    - `grep "CalendarOutlined" src/widgets/app-shell/ui/AppShell.tsx`
  </acceptance_criteria>
  <action>
    Thin page.tsx делегирует в TeacherLeaveCalendarPage client component.
    Импорт CalendarOutlined из @ant-design/icons.
    Не использовать Flex из antd; layout через div + Tailwind.
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/app/(dashboard)/calendar/page.tsx src/widgets/app-shell/ui/AppShell.tsx src/shared/lib/auth.config.ts</automated>
  </verify>
  <done>Маршрут /calendar защищён и доступен из меню учителя</done>
</task>

<task type="auto">
  <name>Task 2: LeaveCalendar с badges и легендой</name>
  <files>
    src/features/leave-requests/ui/LeaveCalendar.tsx
    src/features/leave-requests/ui/TeacherLeaveCalendarPage.tsx
  </files>
  <read_first>
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — Calendar Component, Color badges
    - src/entities/leave-request/api/use-leave-requests.ts
    - src/features/leave-requests/lib/leave-labels.ts
  </read_first>
  <acceptance_criteria>
    - Ant Design Calendar: fullscreen={false}, month mode
    - cellRender: badge на каждый день диапазона заявки; max 3 + «+N»
    - CREATED badge: Tailwind `bg-neutral-600/40 text-neutral-300` на div wrapper
    - APPROVED badge: `bg-green-700/50 text-green-200`
    - REJECTED не рендерится на календаре
    - Teacher cell text: «Отпуск · Создана» (тип + статус кратко)
    - Legend под календарём: «Создана» серый swatch, «Подтверждена» зелёный
    - Empty state: «Заявок пока нет» + body copy из UI-SPEC
    - Spin при первой загрузке
  </acceptance_criteria>
  <action>
    TeacherLeaveCalendarPage: layout `div.flex.flex-col.gap-6`, Title level={3} «Календарь», toolbar с 3 primary кнопками.
    LeaveCalendar принимает requests[], onDateClick(request), loading.
    Развернуть заявки по дням в useMemo (startDate…endDate inclusive).
    Без antd style overrides на Tag в cellRender — только div/span Tailwind per UI-SPEC.
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/features/leave-requests/ui/LeaveCalendar.tsx src/features/leave-requests/ui/TeacherLeaveCalendarPage.tsx</automated>
  </verify>
  <done>Календарь отображает заявки учителя с цветами по статусу</done>
</task>

<task type="auto">
  <name>Task 3: CreateLeaveModal и LeaveDetailModal</name>
  <files>
    src/features/leave-requests/ui/CreateLeaveModal.tsx
    src/features/leave-requests/ui/LeaveDetailModal.tsx
  </files>
  <read_first>
    - src/features/auth/ui/LoginForm.tsx — react-hook-form + zodResolver + Controller
    - src/features/analytics/ui/TeacherLessonsDateFilter.tsx — RangePicker props
    - src/features/leave-requests/actions/leave-actions.ts — createLeaveRequest
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — Modal Forms, Copywriting
  </read_first>
  <acceptance_criteria>
    - CreateLeaveModal: props open, leaveType, onClose; destroyOnClose; width={480}
    - Form: RangePicker + TextArea; zodResolver(createLeaveRequestSchema subset)
    - Submit вызывает createLeaveRequest; success message «Заявка отправлена на согласование»; invalidateQueries ['leave-requests']
    - Три trigger-кнопки в toolbar открывают modal с type VACATION / DAY_OFF / SICK_LEAVE и правильным title
    - LeaveDetailModal read-only: статус Tag, тип Tag, период DD.MM.YYYY, описание; REJECTED → rejectionReason
    - Dialog accessible name = modal title (для E2E)
    - Error submit: «Не удалось сохранить заявку…» из UI-SPEC
  </acceptance_criteria>
  <action>
    Map form dates to ISO strings for server action.
    App.useApp().message для feedback.
    Teacher: detail modal без approve/reject кнопок.
    Экспортировать компоненты из feature ui/ (не обязательно barrel если не принято в проекте).
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/features/leave-requests/ui/CreateLeaveModal.tsx src/features/leave-requests/ui/LeaveDetailModal.tsx</automated>
  </verify>
  <done>Учитель создаёт заявки всех трёх типов и просматривает детали</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→createLeaveRequest | Client form; server re-validates role TEACHER |
| /calendar route | Middleware + requireRole double guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | I | TeacherLeaveCalendarPage | mitigate | useLeaveRequests scoped server-side to own teacherId |
| T-08-10 | S | auth.config /calendar | mitigate | TEACHER-only roleRoutes |
| T-08-11 | T | CreateLeaveModal dates | mitigate | Zod on client + server action parse |
</threat_model>

<verification>
- Lint clean on new UI files
- Teacher can open /calendar from sidebar
</verification>

<success_criteria>
- LEAV-02 teacher UX complete
- LEAV-05 SICK_LEAVE button and type supported
</success_criteria>

<output>
After completion, create `.planning/phases/08-leave-requests/08-03-SUMMARY.md`
</output>
