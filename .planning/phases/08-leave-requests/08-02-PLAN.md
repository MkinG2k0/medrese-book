---
phase: 08-leave-requests
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/features/leave-requests/actions/leave-actions.ts
  - src/app/api/leave-requests/route.ts
  - src/entities/leave-request/api/use-leave-requests.ts
  - src/entities/leave-request/index.ts
  - src/features/auth/lib/resolve-switch-access.ts
  - src/features/auth/actions/switch-user-actions.ts
  - src/shared/lib/auth.ts
  - src/shared/lib/authorize-student.ts
  - src/shared/lib/group-access.ts
  - src/shared/lib/substitution-access.ts
  - src/features/leave-requests/lib/query-leave-requests.ts
autonomous: true
requirements:
  - LEAV-01
  - LEAV-02
  - LEAV-03
  - LEAV-04
  - TCHR-01
  - TCHR-03
  - TCHR-04
must_haves:
  truths:
    - "Преподаватель создаёт заявку со статусом CREATED через server action"
    - "Менеджер одобряет заявку с выбором замещающего — создаётся Substitution"
    - "Отклонение требует rejectionReason min 5 символов"
    - "Учитель видит только свои заявки; менеджер — все"
    - "Замещающий преподаватель может переключиться только на замещаемого при активном Substitution"
    - "Замещающий имеет доступ к ученикам замещаемого в журнале"
  artifacts:
    - path: src/features/leave-requests/actions/leave-actions.ts
      provides: "createLeaveRequest, approveLeaveRequest, rejectLeaveRequest, listLeaveRequests"
    - path: src/app/api/leave-requests/route.ts
      provides: "GET leave requests with role scoping"
      exports: ["GET"]
    - path: src/features/auth/lib/resolve-switch-access.ts
      provides: "substitution-aware switch access for TEACHER"
  key_links:
    - from: approveLeaveRequest
      to: prisma.substitution.create
      via: "$transaction"
      pattern: "substitution\\.create|dispatchDomainEvent"
    - from: authorizeTeacherStudent
      to: isSubstitutionCurrentlyActive
      via: "substitute teacherId check"
      pattern: "isSubstitutionCurrentlyActive|substitutionsAsSubstitute"
    - from: switch-user-actions.ts getSwitchableUsers
      to: active Substitution
      via: "filter absent teacher users"
      pattern: "substitutionsAsSubstitute"
---

<objective>
Реализовать backend заявок: CRUD-операции, согласование с замещением, ограничение switch-user и доступ substitute к ученикам.

Purpose: LEAV-01…04, TCHR-01,03,04 — ядро бизнес-логики до UI.
Output: Server actions, GET API, auth extensions, domain events на create/approve/reject.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-leave-requests/08-01-SUMMARY.md
@src/features/student-admin/actions/student-admin-actions.ts
@src/shared/lib/domain-events/dispatch.ts
@src/features/auth/lib/resolve-switch-access.ts
@src/features/auth/actions/switch-user-actions.ts
@src/shared/lib/auth.ts
@src/shared/lib/authorize-student.ts
@src/app/api/students/route.ts

<interfaces>
From src/shared/lib/domain-events/dispatch.ts:
```typescript
export async function dispatchDomainEvent(event: DomainEvent, tx?: Prisma.TransactionClient)
```

From src/features/auth/lib/resolve-switch-access.ts:
```typescript
export type SwitchAccess = { allowed: boolean; switchOwnerId: string | null }
export async function resolveSwitchAccess(session: Session): Promise<SwitchAccess>
```

From src/shared/lib/authorize-api-request.ts — паттерн allowedRoles + context.
Session user fields: id, role, teacherId, switchOwnerId (auth.config jwt).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server actions — create, approve, reject, list</name>
  <files>
    src/features/leave-requests/actions/leave-actions.ts
  </files>
  <read_first>
    - src/features/leave-requests/lib/substitution-active.ts
    - src/shared/lib/validations/leave-request.ts
    - src/features/student-admin/actions/student-admin-actions.ts — паттерн dispatchDomainEvent в $transaction
    - src/shared/lib/domain-events/dispatch.ts
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — approve/reject rules
  </read_first>
  <acceptance_criteria>
    - `createLeaveRequest(input)`: requireRole('TEACHER'); teacherId = session.user.teacherId; status CREATED; dispatch LEAVE_REQUEST_CREATED
    - `approveLeaveRequest(input)`: requireRoles MANAGER/SUPER_ADMIN; только status CREATED; substituteTeacherId !== applicant teacherId; substitute role TEACHER; в транзакции: status APPROVED, Substitution create (dates from request, isActive true), link leaveRequest.substitutionId; dispatch LEAVE_REQUEST_APPROVED + SUBSTITUTION_ACTIVATED
    - `rejectLeaveRequest(input)`: requireRoles; rejectionReason required; status REJECTED; dispatch LEAVE_REQUEST_REJECTED
    - `listLeaveRequests(filters?)`: TEACHER → where teacherId = own; MANAGER/SUPER_ADMIN → all; optional filters status, type, teacherId, dateRange
    - Один substitute на весь диапазон (D-UI-02) — одна Substitution запись
    - IDOR: teacher cannot approve/reject; cannot fetch others' requests as TEACHER
  </acceptance_criteria>
  <action>
    `'use server'` файл leave-actions.ts.
    Нормализовать даты: startDate = startOfDay, endDate = endOfDay (date-fns).
    При approve: exclude заявителя и non-TEACHER из валидации substituteTeacherId.
    Payload domain events: { leaveRequestId, teacherUserId, type, startDate, endDate, substituteTeacherId? }.
    revalidatePath('/calendar') и revalidatePath('/admin/leave-calendar') после мутаций.
    Бросать Error с русскими сообщениями при бизнес-ошибках (как user-actions).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Мутации заявок и замещений работают с domain events</done>
</task>

<task type="auto">
  <name>Task 2: GET API и React Query hook</name>
  <files>
    src/app/api/leave-requests/route.ts
    src/entities/leave-request/api/use-leave-requests.ts
    src/entities/leave-request/index.ts
  </files>
  <read_first>
    - src/app/api/students/route.ts — authorizeApiRequest + success/error
    - src/entities/student/api/use-students.ts — queryKey pattern
    - src/features/leave-requests/actions/leave-actions.ts — listLeaveRequests (reuse query logic or call shared lib)
  </read_first>
  <acceptance_criteria>
    - GET `/api/leave-requests` возвращает `{ data, error }` shape
    - Query params: status?, type?, teacherId?, from?, to?
    - TEACHER: только свои заявки (403/empty если teacherId param чужой)
    - MANAGER/SUPER_ADMIN: все заявки
    - Response DTO включает: id, type, status, startDate, endDate, description, rejectionReason?, teacherName, substituteName?, createdAt
    - `useLeaveRequests(filters)` — queryKey ['leave-requests', filters]
  </acceptance_criteria>
  <action>
    Вынести общую Prisma-query в `src/features/leave-requests/lib/query-leave-requests.ts` если нужно DRY между action и API.
    authorizeApiRequest allowedRoles: ['TEACHER', 'MANAGER', 'SUPER_ADMIN'].
    Для TEACHER игнорировать/запретить teacherId filter !== self.
    entities/leave-request — только client hook + types re-export.
  </action>
  <verify>
    <automated>pnpm lint -- --max-warnings=0 src/app/api/leave-requests/route.ts src/entities/leave-request/api/use-leave-requests.ts</automated>
  </verify>
  <done>Клиент может загружать заявки через API + React Query</done>
</task>

<task type="auto">
  <name>Task 3: Substitution auth — switch-user и доступ к ученикам</name>
  <files>
    src/features/auth/lib/resolve-switch-access.ts
    src/features/auth/actions/switch-user-actions.ts
    src/shared/lib/auth.ts
    src/shared/lib/authorize-student.ts
    src/shared/lib/group-access.ts
  </files>
  <read_first>
    - src/features/auth/lib/resolve-switch-access.ts
    - src/features/auth/actions/switch-user-actions.ts
    - src/shared/lib/auth.ts — authorize credentials switchOwnerId block
    - src/features/leave-requests/lib/substitution-active.ts
    - src/shared/lib/authorize-student.ts
    - src/shared/lib/group-access.ts
  </read_first>
  <acceptance_criteria>
    - TCHR-01: TEACHER без MANAGER/SUPER_ADMIN и без активного Substitution → resolveSwitchAccess.allowed = false
    - TEACHER с активным Substitution (substituteTeacherId = session.teacherId) → allowed = true, switchOwnerId = session.user.id
    - getSwitchableUsers для TEACHER: только User записи absent teachers из активных Substitutions + self (текущий user)
    - getSwitchableUsers для MANAGER/SUPER_ADMIN: без изменений (все non-STUDENT)
    - switchUser: TEACHER может switch только на userId из списка разрешённых substitute targets
    - auth.ts authorize: разрешить switchOwnerId если owner — TEACHER с активной Substitution к target user (расширить проверку помимо MANAGER)
    - TCHR-04: authorizeTeacherStudent пропускает если session.teacherId === student.group.teacherId OR exists active Substitution where absentTeacherId = group.teacherId AND substituteTeacherId = session.teacherId
    - canEditGroup аналогично для substitute
  </acceptance_criteria>
  <action>
    Создать `src/features/leave-requests/lib/active-substitutions.ts` с `getActiveSubstitutionsForSubstitute(teacherId)` и `canSubstituteAccessGroup(substituteTeacherId, groupTeacherId)` — использовать в auth файлах (импорт из features в shared допустим только если FSD нарушается; предпочтительно положить helpers в `src/shared/lib/substitution-access.ts` и вызывать из features + shared).
    **Предпочтение:** `src/shared/lib/substitution-access.ts` — prisma queries + isSubstitutionCurrentlyActive; импорт в authorize-student, group-access, resolve-switch-access.
    Не ломать существующий manager switch flow.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Замещающий переключается и работает с учениками только при активном замещении</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server actions | Недоверенный input; role из session |
| client→GET /api/leave-requests | IDOR риск по teacherId param |
| switch-user | Privilege escalation без Substitution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | E | approveLeaveRequest | mitigate | requireRoles MANAGER/SUPER_ADMIN; transaction |
| T-08-05 | I | GET /api/leave-requests | mitigate | TEACHER scope teacherId = session.teacherId only |
| T-08-06 | E | switchUser | mitigate | whitelist target users from active Substitutions |
| T-08-07 | I | authorizeTeacherStudent | mitigate | verify active Substitution date range |
| T-08-08 | T | createLeaveRequest dates | mitigate | Zod + startOfDay/endOfDay normalization |
</threat_model>

<verification>
- Typecheck проходит для новых auth/leave files
- Manual: teacher create → manager list shows request
</verification>

<success_criteria>
- LEAV-02 create, LEAV-03 approve/reject, LEAV-04 auto substitution
- LEAV-01 data layer ready for calendars
- TCHR-01,03,04 enforced in auth layer
</success_criteria>

<output>
After completion, create `.planning/phases/08-leave-requests/08-02-SUMMARY.md`
</output>
