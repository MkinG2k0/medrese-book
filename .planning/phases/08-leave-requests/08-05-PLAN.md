---
phase: 08-leave-requests
plan: 05
type: execute
wave: 5
depends_on:
  - "08-03"
  - "08-04"
files_modified:
  - e2e/leave-requests.spec.ts
  - e2e/helpers/leave-requests.ts
  - e2e/navigation.spec.ts
  - e2e/domain-events.spec.ts
  - src/shared/lib/domain-events/handlers/notifications.ts
autonomous: true
requirements:
  - LEAV-01
  - LEAV-02
  - LEAV-03
  - LEAV-04
  - LEAV-05
  - TCHR-01
  - TCHR-03
  - TCHR-04
  - TCHR-05
must_haves:
  truths:
    - "E2E: teacher создаёт отпуск через модалку — заявка видна на календаре"
    - "E2E: manager подтверждает с substitute — success message"
    - "E2E: manager отклоняет с причиной — заявка не на календаре менеджера"
    - "E2E: teacher2 не видит /calendar menu; teacher1 не видит /admin/leave-calendar"
    - "E2E: teacher2 без замещения не переключается на teacher1"
    - "E2E: после approve teacher2 переключается на teacher1 и открывает журнал ученика замещаемого (TCHR-04 positive)"
    - "Domain events LEAVE_* emitted (audit trail exists)"
  artifacts:
    - path: e2e/leave-requests.spec.ts
      provides: "Playwright scenarios for leave module"
      min_lines: 80
    - path: e2e/helpers/leave-requests.ts
      provides: "createLeaveViaUI, approveLeaveViaUI helpers"
  key_links:
    - from: e2e/leave-requests.spec.ts
      to: /calendar and /admin/leave-calendar
      via: "page.goto + dialog scopes"
      pattern: "getByRole\\('dialog'"
    - from: domain-events.spec.ts
      to: AuditEvent
      via: "LEAVE_REQUEST_CREATED action"
      pattern: "LEAVE_REQUEST_CREATED"
---

<objective>
Playwright E2E для модуля отпусков, проверка role access и интеграция domain events для Phase 6 notifications hook.

Purpose: new-module-tests.mdc compliance; end-to-end verification всех requirement IDs фазы.
Output: e2e/leave-requests.spec.ts GREEN, helpers, domain event smoke extension.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.cursor/rules/new-module-tests.mdc
@e2e/student-status.spec.ts
@e2e/helpers/codes.ts
@e2e/domain-events.spec.ts
@.planning/phases/08-leave-requests/08-UI-SPEC.md — E2E section

<interfaces>
TEST_CODES:
- teacher1: 200001 (Учитель Ахмад)
- teacher2: 200002
- manager: 100002

Copywriting for dialogs:
- «Новый отпуск», «Подтвердить заявку», «Отклонить заявку»
- «Заявка отправлена на согласование»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E helpers для leave requests</name>
  <files>
    e2e/helpers/leave-requests.ts
  </files>
  <read_first>
    - e2e/helpers/journal.ts — helper style
    - e2e/helpers/antd.ts — clickRadioButton etc
    - .planning/phases/08-leave-requests/08-UI-SPEC.md — dialog names, Select patterns
  </read_first>
  <acceptance_criteria>
    - `createLeaveViaUI(page, { type: 'vacation'|'dayoff'|'sick', startLabel, endLabel, description })` — opens correct modal, fills RangePicker, submits
    - `approveLeaveViaUI(page, teacherName, substituteName)` — grid actions + Approve modal
    - `rejectLeaveViaUI(page, teacherName, reason)` — Reject modal with reason
    - Helpers use `getByRole('dialog', { name: '...' })` scoping
    - Select substitute: `.ant-select` + `getByTitle` pattern from new-module-tests.mdc
  </acceptance_criteria>
  <action>
    Export helpers from e2e/helpers/leave-requests.ts.
    Use unique description strings with Date.now() for isolation.
    Date picking: prefer filling RangePicker via antd panel clicks or typeable inputs per existing e2e patterns.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Reusable E2E helpers готовы для spec</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: e2e/leave-requests.spec.ts — основные сценарии</name>
  <files>
    e2e/leave-requests.spec.ts
    e2e/navigation.spec.ts
  </files>
  <behavior>
    - Teacher creates vacation → sees success toast → calendar shows «Создана» badge
    - Manager approves with teacher2 as substitute → success toast with «замещение активировано»
    - Manager rejects another request → «Отклонена» in grid filter, not on calendar
    - teacher2 menu has no «Календарь» when logged as manager role check inverse
    - teacher1 cannot navigate to /admin/leave-calendar (redirect)
    - teacher2 without substitution: UserSwitcher absent or cannot switch to teacher1 (TCHR-01 negative)
  </behavior>
  <read_first>
    - e2e/student-status.spec.ts — serial mode, beforeAll db helpers
    - e2e/helpers/auth-state.ts
    - e2e/helpers/codes.ts
    - e2e/navigation.spec.ts — extend with calendar nav items
  </read_first>
  <acceptance_criteria>
    - File `e2e/leave-requests.spec.ts` exists with describe «Заявки на отсутствие»
    - Minimum 6 tests covering happy path create, approve, reject, role visibility, switch negative, substitute journal access (TCHR-04 positive)
    - Uses TEST_CODES.teacher1, teacher2, manager
    - navigation.spec.ts includes assertion: teacher sees «Календарь», manager sees «Календарь отпусков»
    - `pnpm test:e2e e2e/leave-requests.spec.ts` passes (or MISSING noted if env blocked)
  </acceptance_criteria>
  <action>
    test.describe.configure({ mode: 'serial' }) for mutating flows.
    Use AUTH_STATE or login per existing auth helper.
    Sick leave test: button «Создать больничный» + dialog «Новый больничный» (LEAV-05).
    Calendar color smoke: check badge class or text «Создана»/«Подтверждена» in calendar cell.
    TCHR-04 positive: after manager approves leave with teacher2 as substitute, login teacher2 → switch to teacher1 via UserSwitcher → navigate to /journal → verify student list loads (not forbidden).
    Do not hardcode prisma ids.
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/leave-requests.spec.ts</automated>
  </verify>
  <done>E2E spec проходит локально в CI profile</done>
</task>

<task type="auto">
  <name>Task 3: Domain events smoke + notifications hook stub</name>
  <files>
    e2e/domain-events.spec.ts
    src/shared/lib/domain-events/handlers/notifications.ts
  </files>
  <read_first>
    - e2e/domain-events.spec.ts — existing audit event patterns
    - src/shared/lib/domain-events/handlers/notifications.ts
    - src/shared/lib/domain-events/types.ts — LEAVE_* actions
  </read_first>
  <acceptance_criteria>
    - notifications.ts: comment documents LEAVE_REQUEST_CREATED, LEAVE_REQUEST_APPROVED, LEAVE_REQUEST_REJECTED, SUBSTITUTION_ACTIVATED as Phase 6 consumers (still no-op body)
    - domain-events.spec.ts OR leave-requests.spec.ts: after create leave, AuditEvent with action LEAVE_REQUEST_CREATED exists (via e2e/helpers/db if pattern exists, or API query)
    - `grep "LEAVE_REQUEST_CREATED" e2e/` returns match
  </acceptance_criteria>
  <action>
    Extend domain-events e2e or leave-requests spec to verify audit trail for leave create (FND-04 integration).
    If db helper missing, add `getLatestAuditEvent(action)` to e2e/helpers/db.ts minimal.
    enqueueNotifications remains no-op but add JSDoc listing event actions for Phase 6.
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/domain-events.spec.ts</automated>
  </verify>
  <done>Domain events для отпусков проверяются автоматически</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E auth states | Pre-seeded codes; tests verify access control |
| Test data | Unique descriptions prevent cross-test pollution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | E | E2E switch negative test | mitigate | Assert teacher2 cannot impersonate teacher1 |
| T-08-17 | I | navigation role tests | mitigate | Menu items filtered by role in AppShell |
| T-08-18 | R | AuditEvent leave actions | mitigate | e2e verifies audit write on create |
</threat_model>

<verification>
- `pnpm test:e2e e2e/leave-requests.spec.ts`
- All requirement IDs covered by e2e or prior plan acceptance
</verification>

<success_criteria>
- Full phase E2E coverage per new-module-tests.mdc
- TCHR-01,05 verified via substitution/switch tests
- LEAV-01…05 scenarios green
</success_criteria>

<output>
After completion, create `.planning/phases/08-leave-requests/08-05-SUMMARY.md`
</output>
