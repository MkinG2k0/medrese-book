---
phase: 01-student-analytics-history
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/shared/lib/student-metrics/load-student-metrics.ts
  - src/shared/lib/analytics-queries/at-risk-students.ts
  - src/shared/lib/analytics-queries/index.ts
  - src/shared/lib/analytics.ts
  - src/app/api/student-metrics/route.ts
  - src/app/api/at-risk-students/route.ts
  - src/app/api/students/risk-flags/route.ts
  - src/app/api/step-completions/route.ts
  - src/shared/lib/teaching-session-duration-map.ts
  - src/entities/student-metrics/model/types.ts
  - src/entities/student-metrics/api/use-student-metrics.ts
  - src/entities/student-metrics/api/use-at-risk-students.ts
  - src/entities/student-metrics/api/use-student-risk-flags.ts
  - src/entities/student-metrics/index.ts
autonomous: true
requirements:
  - ANLY-03
  - ANLY-04
  - ANLY-05
  - ANLY-07
  - ANLY-08
  - ANLY-09
  - ANLY-10
must_haves:
  truths:
    - "GET /api/student-metrics возвращает метрики периода для авторизованного ученика или ученика с доступом учителя/менеджера"
    - "GET /api/at-risk-students недоступен роли STUDENT (D-07)"
    - "GET /api/students/risk-flags возвращает riskFlags[] для batch studentIds журнала"
    - "GET /api/step-completions включает sessionDurationMinutes для ANLY-09"
  artifacts:
    - path: src/app/api/at-risk-students/route.ts
      provides: "Список отстающих за месяц с фильтром teacher"
      exports: ["GET"]
    - path: src/shared/lib/analytics-queries/at-risk-students.ts
      provides: "getAtRiskStudents(month, teacherId?)"
      exports: ["getAtRiskStudents"]
    - path: src/shared/lib/teaching-session-duration-map.ts
      provides: "Маппинг дата→durationMinutes по groupId"
      exports: ["buildTeachingSessionDurationByDate"]
  key_links:
    - from: src/app/api/at-risk-students/route.ts
      to: src/shared/lib/authorize-api-request.ts
      via: "allowedRoles TEACHER, MANAGER, SUPER_ADMIN"
      pattern: "authorizeApiRequest"
    - from: src/app/api/step-completions/route.ts
      to: src/shared/lib/teaching-session-duration-map.ts
      via: "session.date + student.groupId"
      pattern: "sessionDurationMinutes"
---

<objective>
Подключить student-metrics к Prisma-загрузчикам и REST API; обогатить step-completions длительностью занятия.

Purpose: Серверный контракт для UI аналитики, журнала и истории; закрывает data layer для ANLY-08/09 и at-risk.
Output: Три API route, entity hooks React Query, getAtRiskStudents для SSR /analytics.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-student-analytics-history/01-CONTEXT.md
@.planning/phases/01-student-analytics-history/01-UI-SPEC.md
@src/shared/lib/authorize-api-request.ts
@src/app/api/students/route.ts
@src/app/api/step-completions/route.ts
@src/shared/lib/student-metrics/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma-загрузчики и getAtRiskStudents</name>
  <files>
    src/shared/lib/student-metrics/load-student-metrics.ts
    src/shared/lib/analytics-queries/at-risk-students.ts
    src/shared/lib/analytics-queries/index.ts
    src/shared/lib/analytics.ts
    src/shared/lib/teaching-session-duration-map.ts
  </files>
  <read_first>
    - src/shared/lib/student-metrics/index.ts — публичные функции из 01-01
    - src/shared/lib/analytics-queries/top-students.ts — фильтр teacherId, месяц
    - src/features/journal/lib/teaching-session-queries.ts — findTeachingSessionForDay
  </read_first>
  <acceptance_criteria>
    - loadStudentMetricsContext(studentId, dateRange) загружает student+group+sessions+completions+steps+teachingSessions одним или минимальным числом запросов
    - getAtRiskStudents(month, teacherId?) возвращает только учеников с непустым riskFlags; поля соответствуют AtRiskStudentRow
    - buildTeachingSessionDurationByDate(groupId, dateRange) → Map<YYYY-MM-DD, durationMinutes|null>
    - analytics.ts реэкспортирует getAtRiskStudents для SSR page
    - pnpm test:unit -- src/shared/lib/student-metrics проходит
  </acceptance_criteria>
  <action>
    load-student-metrics.ts: оркестрация Prisma + вызов computePeriodMetrics, evaluateTimeNormForLevel, buildStudentRiskFlags из 01-01.
    at-risk-students.ts: findMany students (фильтр group.teacherId как top-students), для каждого build metrics; отфильтровать riskFlags.length === 0.
    teaching-session-duration-map.ts: запрос TeachingSession по groupId и date range; ключ — getLocalDateString(session.date).
    Не отдавать at-risk данные в функциях, вызываемых из student-portal actions для STUDENT.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/student-metrics && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Server-side агрегации готовы для API и SSR</done>
</task>

<task type="auto">
  <name>Task 2: REST API student-metrics, at-risk, risk-flags</name>
  <files>
    src/app/api/student-metrics/route.ts
    src/app/api/at-risk-students/route.ts
    src/app/api/students/risk-flags/route.ts
    src/entities/student-metrics/model/types.ts
    src/entities/student-metrics/api/use-student-metrics.ts
    src/entities/student-metrics/api/use-at-risk-students.ts
    src/entities/student-metrics/api/use-student-risk-flags.ts
    src/entities/student-metrics/index.ts
  </files>
  <read_first>
    - src/app/api/students/route.ts — authorizeApiRequest + query params
    - src/shared/api/index.ts — success, error, unauthorized, forbidden
    - src/entities/student/api/use-students.ts — паттерн queryFn и json.error
  </read_first>
  <acceptance_criteria>
    - GET /api/student-metrics?studentId=&month=YYYY-MM — roles TEACHER|MANAGER|SUPER_ADMIN|STUDENT; STUDENT только свой studentId; ответ { lessonsCount, stepsCount, totalMinutes, levelProgress }
    - GET /api/at-risk-students?month=&teacher= — только TEACHER|MANAGER|SUPER_ADMIN; STUDENT → forbidden
    - GET /api/students/risk-flags?groupId=&date=YYYY-MM-DD — batch riskFlags по studentIds группы; TEACHER|MANAGER|SUPER_ADMIN
    - Zod safeParse query params; ошибки на русском
    - Entity hooks: queryKey ['student-metrics', studentId, month], ['at-risk-students', month, teacherId], ['student-risk-flags', groupId, date]
  </acceptance_criteria>
  <action>
    Создать routes по паттерну authorizeApiRequest → load → success().
    student-metrics: parse month как parseAnalyticsMonth; teacher filter не нужен (per-student).
    at-risk-students: resolveAnalyticsTeacherFilter аналогично analytics page (импорт из features/analytics/lib допустим только в route если уже есть — иначе дублировать минимальный resolve в shared).
    risk-flags: для журнала — месяц из date param (первый день месяца date) + consecutive по всей истории countable sessions.
    entities/student-metrics: типы ответов API, хуки с throw on json.error.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>API и React Query hooks доступны клиенту</done>
</task>

<task type="auto">
  <name>Task 3: Длительность занятия в step-completions</name>
  <files>
    src/app/api/step-completions/route.ts
    src/entities/step-completion/api/use-step-completions.ts
  </files>
  <read_first>
    - src/app/api/step-completions/route.ts — текущий GET mapper
    - src/shared/lib/teaching-session-duration-map.ts
    - src/features/analytics/ui/StudentStudyHistoryModal.tsx — ожидаемые поля
  </read_first>
  <acceptance_criteria>
    - Каждый completion в GET response содержит session.sessionDurationMinutes: number | null
    - Значение берётся из TeachingSession группы ученика на календарный день session.date
    - StepCompletionRow тип обновлён; prior credit completions по-прежнему возвращаются (фильтр UI), duration заполнен
    - Отсутствие TeachingSession → null, не 0
  </acceptance_criteria>
  <action>
    После authorize: загрузить student с groupId; buildTeachingSessionDurationByDate для min/max дат completions.
    В mapper добавить sessionDurationMinutes по getLocalDateString(session.date).
    Обновить use-step-completions тип StepCompletionRow.session.
    Не ломать DELETE/PATCH handlers в том же файле.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>История через API несёт длительность занятия (ANLY-09 data layer)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/at-risk-students | Недоверенный запрос; default-deny по роли |
| Client → /api/student-metrics | studentId должен проходить authorize student access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | at-risk-students | high | mitigate | authorizeApiRequest без STUDENT; teacher scope через group.teacherId |
| T-01-05 | Information Disclosure | student-metrics | high | mitigate | authorizeApiRequest context studentId; STUDENT только self |
| T-01-06 | Spoofing | risk-flags batch | medium | mitigate | groupId context в authorizeApiRequest как students route |
| T-01-SC | Tampering | npm installs | high | mitigate | Без новых зависимостей в этом плане |
</threat_model>

<verification>
- pnpm exec tsc --noEmit
- Ручная проверка: curl GET /api/at-risk-students с cookie STUDENT → 403 JSON
</verification>

<success_criteria>
- API контракты соответствуют UI-SPEC и D-07
- getAtRiskStudents готов для SSR в 01-04
- step-completions обогащён duration для модалки истории
</success_criteria>

## Artifacts this phase produces

- `src/shared/lib/analytics-queries/at-risk-students.ts`, `load-student-metrics.ts`, `teaching-session-duration-map.ts`
- `src/app/api/student-metrics/`, `at-risk-students/`, `students/risk-flags/`
- `src/entities/student-metrics/` — типы и React Query hooks

<output>
Create `.planning/phases/01-student-analytics-history/01-02-SUMMARY.md` when done
</output>
