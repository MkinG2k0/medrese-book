---
phase: 01-student-analytics-history
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/features/journal/ui/JournalRiskBadge.tsx
  - src/features/journal/ui/NormWarningAlert.tsx
  - src/features/journal/ui/JournalStudentsTable.tsx
  - src/features/journal/ui/StudentList.tsx
  - src/features/journal/ui/LessonPage.tsx
  - src/features/journal/ui/lesson/LessonPageHeader.tsx
  - src/features/journal/actions/journal-actions.ts
  - src/features/analytics/ui/StudentMetricsCards.tsx
  - src/app/api/teaching-sessions/route.ts
  - src/features/journal/lib/teaching-session.test.ts
  - src/shared/lib/student-metrics/at-risk-config.ts
  - src/shared/lib/student-metrics/period-metrics.test.ts
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03
  - ANLY-04
  - ANLY-05
  - ANLY-06
  - ANLY-07
must_haves:
  truths:
    - "Учитель видит elapsed таймер при активном уроке и сохранённую длительность после завершения (ANLY-02)"
    - "Badge риска отображается у ученика в журнале для TEACHER/MANAGER при непустых riskFlags (D-08)"
    - "На странице урока Alert при TIME_NORM; ученик не видит at-risk (D-07)"
    - "startedAt/endedAt сохраняются через POST/PATCH teaching-sessions (ANLY-01)"
    - "После верификации таймера actualTimeSource = teaching_session; totalMinutes из durationMinutes (D-04)"
  artifacts:
    - path: src/features/journal/ui/JournalRiskBadge.tsx
      provides: "Tag+Tooltip по UI-SPEC"
      exports: ["JournalRiskBadge"]
    - path: src/features/journal/ui/NormWarningAlert.tsx
      provides: "Alert type=warning на уроке"
      exports: ["NormWarningAlert"]
  key_links:
    - from: src/features/journal/ui/StudentList.tsx
      to: src/entities/student-metrics/api/use-student-risk-flags.ts
      via: "useStudentRiskFlags(groupId, dateFilter)"
      pattern: "useStudentRiskFlags"
    - from: src/features/journal/ui/LessonPage.tsx
      to: src/features/journal/actions/journal-actions.ts
      via: "getStudentLesson returns riskFlags and periodMetrics"
      pattern: "getStudentLesson"
---

<objective>
Завершить журнальный UX: верификация таймера, badge отставания и предупреждение о нормативе на странице урока.

Purpose: Оперативное вмешательство учителя в журнале (D-08); ANLY-01/02 уже частично в коде — закрыть пробелы.
Output: JournalRiskBadge, NormWarningAlert, интеграция risk flags в таблицу журнала.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-student-analytics-history/01-UI-SPEC.md
@src/features/journal/ui/LessonTimerBar.tsx
@src/features/journal/ui/StudentList.tsx
@src/features/journal/ui/JournalStudentsTable.tsx
@src/entities/student-metrics/api/use-student-risk-flags.ts
@src/app/api/teaching-sessions/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Верификация ANLY-01/02 teaching session</name>
  <files>
    src/app/api/teaching-sessions/route.ts
    src/app/api/teaching-sessions/[id]/route.ts
    src/features/journal/lib/teaching-session.test.ts
    src/features/journal/ui/LessonTimerBar.tsx
    src/shared/lib/student-metrics/at-risk-config.ts
    src/shared/lib/student-metrics/period-metrics.test.ts
  </files>
  <read_first>
    - src/app/api/teaching-sessions/route.ts — POST start, GET
    - src/app/api/teaching-sessions/[id]/route.ts — PATCH end
    - src/features/journal/lib/teaching-session.ts — serializeTeachingSession
    - .planning/phases/01-student-analytics-history/01-UI-SPEC.md — LessonTimerBar contract
  </read_first>
  <acceptance_criteria>
    - POST создаёт TeachingSession с startedAt; PATCH/end выставляет endedAt
    - serializeTeachingSession возвращает durationMinutes для завершённой сессии
    - LessonTimerBar показывает тик elapsed 1s при isActive и copy из UI-SPEC
    - teaching-session.test.ts покрывает durationMinutes и isActive
    - Блокировка журнала до старта урока (studentsBlocked) сохранена в StudentList
    - После подтверждения ANLY-01: AT_RISK_CONFIG.actualTimeSource по умолчанию 'teaching_session' (D-04)
    - period-metrics.test.ts: при actualTimeSource teaching_session totalMinutes = сумма durationMinutes, не sessionsCount × avgStepHours × 60
    - period-metrics.test.ts: evaluateTimeNormForLevel использует totalMinutes из teaching_session path (тот же durationMinutes), не proxy
  </acceptance_criteria>
  <action>
    Аудит существующего flow; исправить только реальные пробелы (например отсутствие message.error copy из UI-SPEC при ошибке мутации).
    Дополнить unit-тесты если duration или resume/idempotency не покрыты.
    Не переписывать LessonTimerBar без расхождения с UI-SPEC.
    Убедиться dispatchDomainEvent LESSON_STARTED/ENDED уже вызывается — при отсутствии добавить по паттерну route.ts POST.
    После успешной верификации таймера per D-04: в at-risk-config.ts переключить default actualTimeSource с 'proxy' на 'teaching_session'; пороги и enabledSignals не менять.
    Добавить/обновить unit-тесты в period-metrics.test.ts: fixture с двумя TeachingSession (45 и 30 min) и countable sessions — totalMinutes === 75, не proxy-формула; отдельный кейс time-norm с тем же input — actualMinutes === 75.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/features/journal/lib/teaching-session.test.ts src/features/journal/lib/teaching-session-start.test.ts src/shared/lib/student-metrics/period-metrics.test.ts</automated>
  </verify>
  <done>ANLY-01/02 подтверждены; D-04 миграция на teaching_session с unit-тестами</done>
</task>

<task type="auto">
  <name>Task 2: JournalRiskBadge и batch risk flags в журнале</name>
  <files>
    src/features/journal/ui/JournalRiskBadge.tsx
    src/features/journal/ui/JournalStudentsTable.tsx
    src/features/journal/ui/StudentList.tsx
  </files>
  <read_first>
    - .planning/phases/01-student-analytics-history/01-UI-SPEC.md — JournalRiskBadge, Role Visibility
    - src/entities/student-metrics/api/use-student-risk-flags.ts
    - .cursor/rules/antd-no-style-overrides.mdc
  </read_first>
  <acceptance_criteria>
    - JournalRiskBadge: riskFlags пустой → null; TIME_NORM → Tag color=warning + Tooltip «Превышен норматив времени»; ATTENDANCE → Tag color=error + Tooltip «Много пропусков»; оба — два Tag gap-1
    - JournalStudentsTable: колонка «Ученик» — flex items-center gap-2: имя + badge
    - StudentList вызывает useStudentRiskFlags(groupId, dateFilter) и мержит riskFlags в rows
    - showRiskBadge true только для TEACHER/MANAGER (useSession role check)
    - aria-label на Tag/tooltip по UI-SPEC
  </acceptance_criteria>
  <action>
    Создать JournalRiskBadge per UI-SPEC — только semantic antd Tag/Tooltip, без кастомных hex.
    Расширить JournalStudentRow типом riskFlags?: RiskFlag[].
    Не блокировать клик по строке таблицы.
    При loading risk flags — показывать строку без badge (не Spin на каждую ячейку).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Badge в журнале отражает at-risk сигналы (D-08)</done>
</task>

<task type="auto">
  <name>Task 3: NormWarningAlert и метрики в шапке урока</name>
  <files>
    src/features/journal/ui/NormWarningAlert.tsx
    src/features/journal/ui/LessonPage.tsx
    src/features/journal/ui/lesson/LessonPageHeader.tsx
    src/features/journal/actions/journal-actions.ts
    src/features/analytics/ui/StudentMetricsCards.tsx
  </files>
  <read_first>
    - .planning/phases/01-student-analytics-history/01-UI-SPEC.md — NormWarningAlert copy
    - src/features/journal/ui/LessonPage.tsx — placement над LessonStepsSection
    - src/shared/lib/student-metrics/load-student-metrics.ts
  </read_first>
  <acceptance_criteria>
    - NormWarningAlert visible только если riskFlags включает TIME_NORM
    - Alert type=warning, showIcon, message и description из UI-SPEC Copywriting Contract
    - Размещение: над LessonStepsSection, className mb-4 (layout-only)
    - getStudentLesson возвращает riskFlags и periodMetrics для studentId
    - LessonPageHeader: compact StudentMetricsCards (3 Statistic) под именем ученика для teacher
    - StudentMetricsCards.tsx создан с prop variant compact | portal
    - Нет at-risk UI на /student/* (D-07)
  </acceptance_criteria>
  <action>
    NormWarningAlert.tsx — тонкая обёртка Alert.
    StudentMetricsCards.tsx: общий компонент с variant portal (3 Card grid) и compact (3 Statistic в LessonPageHeader).
    В journal-actions getStudentLesson: loadStudentMetricsContext + buildStudentRiskFlags + period metrics для текущего месяца.
    LessonPage: NormWarningAlert между header и LessonStepsSection; передать metrics в LessonPageHeader.
    Портал /student/me подключит тот же компонент в 01-04.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Учитель видит предупреждение о нормативе на уроке (ANLY-07)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Journal UI → risk-flags API | Только авторизованный teacher/manager группы |
| Lesson page norm alert | Данные через server action после requireRole TEACHER |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Information Disclosure | JournalRiskBadge | medium | mitigate | showRiskBadge=false для STUDENT; API 403 для student |
| T-01-08 | Tampering | teaching session POST | medium | mitigate | date === today check; authorizeApiRequest groupId |
| T-01-SC | Tampering | npm installs | high | mitigate | Без новых пакетов |
</threat_model>

<verification>
- pnpm test:unit -- src/features/journal/lib/teaching-session
- pnpm exec tsc --noEmit
</verification>

<success_criteria>
- Журнал показывает таймер и badge; урок — norm alert при нарушении
- ANLY-01, ANLY-02, ANLY-07 закрыты в journal flow
</success_criteria>

## Artifacts this phase produces

- `src/features/journal/ui/JournalRiskBadge.tsx`, `NormWarningAlert.tsx`
- Интеграция risk flags в `StudentList` / `JournalStudentsTable` / `LessonPage`

<output>
Create `.planning/phases/01-student-analytics-history/01-03-SUMMARY.md` when done
</output>
