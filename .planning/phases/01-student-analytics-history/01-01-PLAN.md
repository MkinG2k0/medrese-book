---
phase: 01-student-analytics-history
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/student-metrics/at-risk-config.ts
  - src/shared/lib/student-metrics/types.ts
  - src/shared/lib/student-metrics/period-metrics.ts
  - src/shared/lib/student-metrics/time-norm.ts
  - src/shared/lib/student-metrics/attendance-risk.ts
  - src/shared/lib/student-metrics/risk-flags.ts
  - src/shared/lib/student-metrics/index.ts
  - src/shared/lib/student-metrics/period-metrics.test.ts
  - src/shared/lib/student-metrics/time-norm.test.ts
  - src/shared/lib/student-metrics/attendance-risk.test.ts
autonomous: true
requirements:
  - ANLY-03
  - ANLY-04
  - ANLY-05
  - ANLY-06
  - ANLY-07
  - ANLY-10
must_haves:
  truths:
    - "Метрики периода (уроки, шаги, время) исключают adjustment-сессии и prior credit"
    - "Норматив времени сравнивает факт с суммой Step.hours пройденных шагов текущего уровня (D-01…D-03)"
    - "Флаг посещаемости срабатывает при 3+ ABSENT за месяц или 3 подряд (D-09)"
    - "riskFlags[] содержит только TIME_NORM и/или ATTENDANCE при срабатывании правил (D-06)"
  artifacts:
    - path: src/shared/lib/student-metrics/at-risk-config.ts
      provides: "Единый конфиг порогов и режимов (D-05)"
      exports: ["AT_RISK_CONFIG"]
    - path: src/shared/lib/student-metrics/time-norm.ts
      provides: "Расчёт нарушения норматива по уровню"
      exports: ["evaluateTimeNormForLevel"]
    - path: src/shared/lib/student-metrics/risk-flags.ts
      provides: "Сборка riskFlags для ученика"
      exports: ["buildStudentRiskFlags"]
  key_links:
    - from: src/shared/lib/student-metrics/period-metrics.ts
      to: src/shared/lib/analytics-queries/filters.ts
      via: "countableSessionWhere / countableCompletionWhere"
      pattern: "countableSessionWhere|countableCompletionWhere"
    - from: src/shared/lib/student-metrics/time-norm.ts
      to: src/shared/lib/student-metrics/at-risk-config.ts
      via: "actualTimeSource proxy | teaching_session"
      pattern: "AT_RISK_CONFIG"
---

<objective>
Реализовать модуль `student-metrics` с unit-тестами: метрики периода, прогресс по уровню, норматив времени и флаги отставания.

Purpose: Единый источник расчётов для аналитики, журнала и портала; закрывает ANLY-03…07 и ANLY-10 без дублирования логики.
Output: Типизированный DTO, конфиг D-05, проходящие vitest-тесты на граничных случаях.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-student-analytics-history/01-CONTEXT.md
@.planning/phases/01-student-analytics-history/01-UI-SPEC.md
@.planning/REQUIREMENTS.md
@src/shared/lib/analytics-queries/filters.ts
@src/shared/lib/analytics-queries/top-students.ts
@prisma/schema.prisma
@src/features/journal/lib/teaching-session.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Конфиг at-risk и типы DTO</name>
  <files>
    src/shared/lib/student-metrics/at-risk-config.ts
    src/shared/lib/student-metrics/types.ts
    src/shared/lib/student-metrics/time-norm.test.ts
    src/shared/lib/student-metrics/index.ts
  </files>
  <read_first>
    - .planning/phases/01-student-analytics-history/01-CONTEXT.md — решения D-01…D-09
    - src/shared/lib/analytics-queries/filters.ts — countableSessionWhere, countableCompletionWhere
    - prisma/schema.prisma — Step.hours, Session, StepCompletion.isPriorCredit
  </read_first>
  <acceptance_criteria>
    - Экспорт AT_RISK_CONFIG с полями: enabledSignals (TIME_NORM, ATTENDANCE), attendanceMonthThreshold (3), attendanceConsecutiveThreshold (3), actualTimeSource ('proxy' | 'teaching_session'), accumulationMode 'per_level'
    - Тип RiskFlag = 'TIME_NORM' | 'ATTENDANCE'
    - Тип StudentPeriodMetrics содержит lessonsCount, stepsCount, totalMinutes, monthLabel
    - Тип TimeNormResult содержит isViolated, actualMinutes, budgetMinutes, levelId
    - time-norm.test.ts существует и падает до реализации evaluateTimeNormForLevel
  </acceptance_criteria>
  <behavior>
    - Конфиг: attendanceMonthThreshold === 3 и attendanceConsecutiveThreshold === 3
    - RiskFlag union содержит ровно TIME_NORM и ATTENDANCE
  </behavior>
  <action>
    Создать src/shared/lib/student-metrics/at-risk-config.ts per D-05: один типизированный объект AT_RISK_CONFIG, без разбросанных magic numbers в фичах.
    types.ts: RiskFlag, StudentPeriodMetrics, TimeNormResult, AtRiskStudentRow (поля для UI-SPEC: student, teacherName, levelTitle, riskFlags, absencesInMonth, actualTimeLabel, normBudgetLabel — labels формируются в UI, в DTO хранить числа).
    index.ts реэкспортирует публичный API модуля.
    RED: написать time-norm.test.ts с кейсом «факт 300 мин, бюджет 240 мин на уровне → isViolated true» до реализации функции.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/student-metrics/time-norm.test.ts</automated>
  </verify>
  <done>Конфиг и типы на месте; failing test зафиксирован</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Метрики периода и прогресс по уровню</name>
  <files>
    src/shared/lib/student-metrics/period-metrics.ts
    src/shared/lib/student-metrics/period-metrics.test.ts
  </files>
  <read_first>
    - src/shared/lib/analytics-queries/top-students.ts — паттерн месячной агрегации
    - src/shared/lib/student-progress/index.ts — getLocalStepIdx, offsets
    - src/features/journal/lib/teaching-session.ts — durationMinutes из startedAt/endedAt
  </read_first>
  <acceptance_criteria>
    - lessonsCount считает только Session с isAdjustment false в диапазоне дат (ANLY-03, ANLY-10)
    - stepsCount считает StepCompletion с isPriorCredit false в диапазоне (ANLY-04, ANLY-10)
    - totalMinutes: при actualTimeSource teaching_session — сумма durationMinutes TeachingSession группы ученика за дни с countable Session; при proxy — sessionsCount × средний Step.hours пройденных шагов уровня × 60 (D-04)
    - computeLevelProgress возвращает currentLevelNumber, completedStepsOnLevel, totalStepsOnLevel, localStepIdx (ANLY-06)
    - period-metrics.test.ts: prior credit и adjustment не входят в счётчики
  </acceptance_criteria>
  <behavior>
    - 2 countable sessions + 1 adjustment → lessonsCount 2
    - 3 completions, 1 isPriorCredit → stepsCount 2
    - proxy: 4 sessions, avg step hours 2 → totalMinutes 480
  </behavior>
  <action>
    period-metrics.ts: чистые функции computePeriodMetrics(input) и computeLevelProgress(input) принимают уже загруженные Prisma-данные (не импортировать features).
    Использовать countableSessionWhere / countableCompletionWhere из analytics-queries/filters.
    Для teaching_session: маппинг date (календарный день группы) → durationMinutes из serializeTeachingSession.
    Не добавлять UI-форматирование — только числа в минутах/штуках.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/student-metrics/period-metrics.test.ts</automated>
  </verify>
  <done>Метрики периода и прогресс покрыты тестами и проходят</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Норматив времени, посещаемость и riskFlags</name>
  <files>
    src/shared/lib/student-metrics/time-norm.ts
    src/shared/lib/student-metrics/attendance-risk.ts
    src/shared/lib/student-metrics/attendance-risk.test.ts
    src/shared/lib/student-metrics/risk-flags.ts
    src/shared/lib/student-metrics/time-norm.test.ts
  </files>
  <read_first>
    - .planning/phases/01-student-analytics-history/01-CONTEXT.md — D-01…D-04, D-06, D-09
    - src/shared/lib/student-metrics/at-risk-config.ts
    - src/shared/lib/student-metrics/period-metrics.ts
  </read_first>
  <acceptance_criteria>
    - evaluateTimeNormForLevel: budgetMinutes = сумма Step.hours пройденных шагов уровня (без prior credit); isViolated когда actualMinutes > budgetMinutes (D-01, D-02, D-03)
    - evaluateAttendanceRisk: true при absencesInMonth >= 3 ИЛИ maxConsecutiveAbsent >= 3 (D-09)
    - buildStudentRiskFlags: включает TIME_NORM только если enabledSignals содержит TIME_NORM и norm violated; ATTENDANCE аналогично (D-06)
    - Все тесты в student-metrics/*.test.ts проходят
  </acceptance_criteria>
  <behavior>
    - Бюджет 120 мин, факт 150 → riskFlags содержит TIME_NORM
    - 3 ABSENT в месяце → ATTENDANCE в riskFlags
    - 2 ABSENT с перерывом, затем 3 подряд → ATTENDANCE
    - Норма в норме и 0 пропусков → riskFlags пустой массив
  </behavior>
  <action>
    time-norm.ts: evaluateTimeNormForLevel использует actualMinutes из period-metrics (не дублировать расчёт факта).
    attendance-risk.ts: сортировка сессий по date asc для consecutive; месячный счётчик только ABSENT и только countable sessions.
    risk-flags.ts: buildStudentRiskFlags(studentId, context) — тонкая композиция без Prisma (вход — структуры из queries в плане 01-02).
    GREEN: довести time-norm.test.ts и attendance-risk.test.ts до прохождения.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/student-metrics</automated>
  </verify>
  <done>Норматив, посещаемость и riskFlags реализованы с полным unit-покрытием</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared/lib → consumers | Чистые функции; не принимают сырые HTTP-параметры |
| Prisma data → metrics | Доверенные данные после authorizeApiRequest на уровне API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | period-metrics input | medium | mitigate | Только countable filters; unit-тесты на prior credit/adjustment exclusion |
| T-01-02 | Information Disclosure | riskFlags в DTO | low | accept | Флаги без PII; доступ ограничивается API auth в 01-02 |
| T-01-03 | Denial of Service | тяжёлые агрегации | medium | mitigate | Batch-запросы в 01-02; индексы Session.date, TeachingSession groupId+date уже в schema |
| T-01-SC | Tampering | npm installs | high | mitigate | Новые пакеты не требуются; slopcheck при любом добавлении |
</threat_model>

<verification>
- pnpm test:unit -- src/shared/lib/student-metrics — все тесты зелёные
- grep -r "isPriorCredit: false" src/shared/lib/student-metrics — используется фильтр из filters.ts, не дублированные литералы в нескольких местах без импорта
</verification>

<success_criteria>
- Модуль student-metrics экспортирует расчёт метрик, норматива и riskFlags
- Все решения D-01…D-06, D-09 отражены в коде и тестах
- ANLY-10 обеспечен через countable filters
</success_criteria>

## Artifacts this phase produces

- `src/shared/lib/student-metrics/` — конфиг, типы, period-metrics, time-norm, attendance-risk, risk-flags, unit-тесты

<output>
Create `.planning/phases/01-student-analytics-history/01-01-SUMMARY.md` when done
</output>
