---
phase: 15-student-portal-extra-assignments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/student-portal/lib/student-portal-query.ts
  - src/features/student-portal/lib/student-portal-query.test.ts
  - src/features/student-portal/lib/student-portal-storage.ts
  - src/features/student-portal/actions/student-actions.ts
  - src/features/student-portal/ui/StudentEnrollmentCard.tsx
  - src/app/(dashboard)/student/me/page.tsx
autonomous: true
requirements:
  - SUBJ-16
must_haves:
  truths:
    - На /student/me ученик видит карточку на каждое зачисление (GroupEnrollment), не один агрегат
    - На карточке отображаются предмет, группа, уровень, прогресс-бар и метрики месяца per enrollment
    - Две группы одного предмета дают две отдельные карточки
    - Прогресс считается в скоупе предмета группы (getTotalProgramSteps(subjectId))
  artifacts:
    - src/features/student-portal/lib/student-portal-query.ts
    - src/features/student-portal/lib/student-portal-storage.ts
    - src/features/student-portal/actions/student-actions.ts#getStudentEnrollmentDashboard
    - src/features/student-portal/ui/StudentEnrollmentCard.tsx
    - src/app/(dashboard)/student/me/page.tsx
  key_links:
    - from: StudentEnrollmentCard
      to: getStudentEnrollmentDashboard
      via: server action данные enrollment + periodMetrics
    - from: getStudentEnrollmentDashboard
      to: loadStudentMetricsForMonth
      via: subjectId + groupId per enrollment
    - from: getStudentEnrollmentDashboard
      to: getTotalProgramSteps
      via: enrollment.group.subjectId
---

<objective>
Дашборд `/student/me` с карточками по каждому зачислению и subject-scoped прогрессом/метриками.

Purpose: SUBJ-16, success criteria п.1; locked D-01, D-02, D-03, D-04.
Output: query/storage helpers, getStudentEnrollmentDashboard, StudentEnrollmentCard, обновлённая страница me.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-student-portal-extra-assignments/15-CONTEXT.md
@.planning/phases/12-progress-sessions/12-CONTEXT.md
@.planning/phases/14-analytics/14-CONTEXT.md
@src/features/student-portal/actions/student-actions.ts
@src/features/journal/lib/journal-url.ts
@src/features/journal/lib/journal-storage.ts
@src/shared/lib/enrollment.ts
@src/shared/lib/student-metrics/load-student-metrics.ts
@src/app/(dashboard)/student/me/page.tsx
@src/features/analytics/ui/StudentMetricsCards.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: student-portal query и storage helpers</name>
  <files>src/features/student-portal/lib/student-portal-query.ts, src/features/student-portal/lib/student-portal-query.test.ts, src/features/student-portal/lib/student-portal-storage.ts</files>
  <read_first>
    src/features/journal/lib/journal-url.ts
    src/features/journal/lib/journal-storage.ts
    src/features/analytics/lib/analytics-query.ts
    src/features/analytics/lib/analytics-storage.ts
  </read_first>
  <behavior>
    - STUDENT_PORTAL_GROUP_PARAM = 'groupId'
    - buildStudentPortalHref(pathname, groupId?) включает groupId в query string
    - resolveStudentGroupId(groupIdParam, allowedIds, fallbackGroupId) возвращает param если в allowedIds, иначе fallback
    - readStudentPortalGroupId / writeStudentPortalGroupId через STUDENT_PORTAL_GROUP_STORAGE_KEY
  </behavior>
  <action>
    Per D-07: создать student-portal-query.ts с константой STUDENT_PORTAL_GROUP_PARAM, buildStudentPortalHref (паттерн buildJournalHref без date), resolveStudentGroupId (копия логики resolveJournalGroupId). Создать student-portal-storage.ts с ключом STUDENT_PORTAL_GROUP_STORAGE_KEY = 'student-portal:lastGroupId' и read/write helpers по образцу journal-storage. Vitest в student-portal-query.test.ts: resolveStudentGroupId (валидный param, невалидный → fallback, пустой allowed → fallback), buildStudentPortalHref с/без groupId.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/features/student-portal/lib/student-portal-query.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest для student-portal-query.test.ts проходит
    - buildStudentPortalHref('/student/lessons', 'g1') содержит groupId=g1
    - resolveStudentGroupId с невалидным param возвращает fallbackGroupId
    - STUDENT_PORTAL_GROUP_STORAGE_KEY экспортирован из student-portal-storage.ts
  </acceptance_criteria>
  <done>URL/query слой портала ученика готов для groupId per D-07.</done>
</task>

<task type="auto">
  <name>Task 2: getStudentEnrollmentDashboard server action</name>
  <files>src/features/student-portal/actions/student-actions.ts</files>
  <read_first>
    src/features/student-portal/actions/student-actions.ts
    src/shared/lib/enrollment.ts
    src/shared/lib/student-progress/offsets.ts
    src/shared/lib/student-metrics/load-student-metrics.ts
    src/shared/lib/analytics.ts
  </read_first>
  <action>
    Per D-02, D-03, D-04: добавить getStudentEnrollmentDashboard() — requireRole('STUDENT'), загрузить student.user.name, prisma.groupEnrollment.findMany where studentId orderBy enrolledAt asc include group.subject, group (name), level. Для каждого enrollment параллельно (Promise.all): getTotalProgramSteps(enrollment.group.subjectId), loadStudentMetricsForMonth(studentId, startOfMonth(new Date()), formatAnalyticsMonth(...), { subjectId, groupId }). Вернуть { studentName, enrollments: [{ groupId, subjectName, groupName, levelTitle, currentStepIdx, totalSteps, periodMetrics }] }. Не удалять getStudentProfile/getStudentPeriodMetrics пока — их заменит dashboard в plan 02; оставить для совместимости lessons/history до 15-02. Исправить getStudentProfile/getStudentPeriodMetrics/getStudentLessons: везде где вызывается getTotalProgramSteps() или getStepOffsetForLevel(number) без subjectId — передать enrollment.group.subjectId (минимальный fix в этом же файле для корректности данных).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getStudentEnrollmentDashboard экспортирован и типизирован
    - enrollments отсортированы по enrolledAt asc (primary первый)
    - totalSteps использует getTotalProgramSteps(enrollment.group.subjectId)
    - periodMetrics загружается с subjectId + groupId per enrollment
    - tsc --noEmit без ошибок в student-actions.ts
  </acceptance_criteria>
  <done>Server action возвращает данные для карточек всех зачислений per D-02/D-03/D-04.</done>
</task>

<task type="auto">
  <name>Task 3: StudentEnrollmentCard и дашборд /student/me</name>
  <files>src/features/student-portal/ui/StudentEnrollmentCard.tsx, src/app/(dashboard)/student/me/page.tsx</files>
  <read_first>
    src/app/(dashboard)/student/me/page.tsx
    src/features/analytics/ui/StudentMetricsCards.tsx
    src/shared/ui/ProgressBar.tsx
    src/shared/ui/Text.tsx
    src/shared/ui/Title.tsx
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-04: создать StudentEnrollmentCard.tsx (server-safe presentational): props enrollment card data; Ant Design Card или bordered div; заголовок «{subjectName} — {groupName}»; Text type secondary с levelTitle; ProgressBar current={currentStepIdx} total={totalSteps}; подпись «Прогресс: шаг {currentStepIdx+1} из {totalSteps}»; StudentMetricsCards metrics={periodMetrics} variant="portal". Пока без ссылок на lessons/history — добавятся в 15-02. Обновить student/me/page.tsx: requireRole('STUDENT'); dashboard = await getStudentEnrollmentDashboard(); notFound если null или enrollments пуст; Title level={3} studentName; grid/flex flex-col gap-4 с картой enrollments.map → StudentEnrollmentCard. Удалить одиночный ProgressBar и getStudentProfile/getStudentPeriodMetrics с этой страницы.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - /student/me рендерит N карточек для N enrollments
    - Каждая карточка показывает subjectName, groupName, levelTitle, ProgressBar, StudentMetricsCards
    - Нет единого subject-select и нет табов на странице
    - tsc --noEmit проходит
  </acceptance_criteria>
  <done>Дашборд с карточками per enrollment live на /student/me per D-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→Prisma | getStudentEnrollmentDashboard читает только enrollments текущего studentId из сессии |
| client→URL | groupId helpers готовятся для plan 02; в этом plan URL не мутируется с клиента |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | getStudentEnrollmentDashboard | medium | mitigate | requireRole('STUDENT'); studentId только из session.user |
| T-15-02 | Spoofing | resolveStudentGroupId | low | mitigate | Валидация groupId против allowedIds enrollment (plan 02) |
| T-15-SC | Tampering | npm installs | high | mitigate | Новые пакеты не устанавливаются |
</threat_model>

<verification>
pnpm exec vitest run src/features/student-portal/lib/student-portal-query.test.ts
pnpm exec tsc --noEmit
Ручная проверка: /student/me показывает карточки per enrollment с метриками месяца.
</verification>

<success_criteria>
SUBJ-16 п.1: ученик видит прогресс по каждому зачислению на дашборде; D-01–D-04 выполнены.
</success_criteria>

<artifacts>
| Артефакт | Путь / символ |
|----------|----------------|
| URL param | STUDENT_PORTAL_GROUP_PARAM в student-portal-query.ts |
| Storage key | STUDENT_PORTAL_GROUP_STORAGE_KEY в student-portal-storage.ts |
| Resolver | resolveStudentGroupId в student-portal-query.ts |
| Href builder | buildStudentPortalHref в student-portal-query.ts |
| Server action | getStudentEnrollmentDashboard в student-actions.ts |
| UI card | StudentEnrollmentCard.tsx |
| Dashboard page | src/app/(dashboard)/student/me/page.tsx |
</artifacts>

<output>
Create `.planning/phases/15-student-portal-extra-assignments/15-01-SUMMARY.md` when done
</output>
