---
phase: 15-student-portal-extra-assignments
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/features/student-portal/actions/student-actions.ts
  - src/features/student-portal/ui/StudentEnrollmentCard.tsx
  - src/features/student-portal/ui/StudentPortalGroupLink.tsx
  - src/app/(dashboard)/student/lessons/page.tsx
  - src/app/(dashboard)/student/history/page.tsx
  - src/widgets/app-shell/ui/AppShell.tsx
autonomous: true
requirements:
  - SUBJ-16
must_haves:
  truths:
    - Клик с карточки ведёт на /student/lessons или /student/history с ?groupId= в URL
    - Прямой вход в «Уроки»/«История» из меню без groupId использует primary enrollment
    - groupId из URL переживает перезагрузку; localStorage запоминает последний выбор с карточки
    - Уроки и история показывают данные только выбранного зачисления (sessions filtered by groupId)
    - Пункты меню «Уроки» и «История занятий» остаются в AppShell
  artifacts:
    - src/features/student-portal/ui/StudentPortalGroupLink.tsx
    - src/app/(dashboard)/student/lessons/page.tsx
    - src/app/(dashboard)/student/history/page.tsx
    - src/features/student-portal/actions/student-actions.ts#getStudentLessons
    - src/features/student-portal/actions/student-actions.ts#getStudentSessionHistory
  key_links:
    - from: StudentPortalGroupLink
      to: writeStudentPortalGroupId
      via: onClick перед router.push
    - from: lessons/history page.tsx
      to: resolveStudentGroupId
      via: searchParams.groupId + allowed enrollment groupIds
    - from: getStudentSessionHistory
      to: Session.groupId
      via: prisma where groupId = enrollment.groupId
---

<objective>
Навигация ученика с контекстом groupId: deep links с карточек, дефолт primary enrollment, scoped lessons/history.

Purpose: SUBJ-16; locked D-05, D-06, D-07, D-08.
Output: groupId в URL/storage, scoped server actions, обновлённые страницы lessons/history, ссылки на карточках.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-student-portal-extra-assignments/15-CONTEXT.md
@.planning/phases/13-journal/13-CONTEXT.md
@.planning/phases/15-student-portal-extra-assignments/15-01-PLAN.md
@src/features/student-portal/lib/student-portal-query.ts
@src/features/student-portal/lib/student-portal-storage.ts
@src/features/student-portal/actions/student-actions.ts
@src/app/(dashboard)/student/lessons/page.tsx
@src/app/(dashboard)/student/history/page.tsx
@src/widgets/app-shell/ui/AppShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server actions с groupId scope</name>
  <files>src/features/student-portal/actions/student-actions.ts</files>
  <read_first>
    src/features/student-portal/actions/student-actions.ts
    src/shared/lib/enrollment.ts
    src/shared/lib/student-progress/offsets.ts
    src/shared/lib/step-completion.ts
    src/shared/lib/teaching-session-duration-map.ts
  </read_first>
  <action>
    Per D-08: добавить internal helper resolveStudentEnrollment(studentId, groupId?: string) — если groupId передан, findEnrollmentInGroup; иначе findPrimaryEnrollment; return null если нет. Рефакторить getStudentLessons(groupId?: string) и getStudentSessionHistory(groupId?: string) использовать resolveStudentEnrollment. getStudentLessons: stepOffset/getStepOffsetForLevel(enrollment.level.number, enrollment.group.subjectId); completions filter по stepIds уровня enrollment; вернуть groupName, subjectName, levelTitle для заголовка. getStudentSessionHistory: prisma.session.findMany where studentId AND groupId = enrollment.groupId (не все sessions student); include completions; duration map по enrollment.groupId. Добавить getStudentEnrollmentGroupIds() → string[] для allowedIds на страницах. Удалить или deprecate getStudentProfile/getStudentPeriodMetrics если больше не используются (grep перед удалением).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getStudentLessons принимает optional groupId
    - getStudentSessionHistory фильтрует sessions по enrollment.groupId
    - resolveStudentEnrollment без groupId возвращает primary enrollment
    - getStepOffsetForLevel и getTotalProgramSteps вызываются с subjectId группы
    - tsc --noEmit проходит
  </acceptance_criteria>
  <done>Данные уроков и истории scoped by enrollment/groupId per Phase 12.</done>
</task>

<task type="auto">
  <name>Task 2: Страницы lessons и history с groupId в URL</name>
  <files>src/app/(dashboard)/student/lessons/page.tsx, src/app/(dashboard)/student/history/page.tsx</files>
  <read_first>
    src/app/(dashboard)/student/lessons/page.tsx
    src/app/(dashboard)/student/history/page.tsx
    src/app/(dashboard)/journal/page.tsx
    src/features/student-portal/lib/student-portal-query.ts
  </read_first>
  <action>
    Per D-07, D-08: обе страницы — async Server Components с searchParams Promise (Next.js 16 pattern как analytics/journal). Загрузить allowedGroupIds = getStudentEnrollmentGroupIds(); primaryGroupId = allowedGroupIds[0] (primary); resolvedGroupId = resolveStudentGroupId(searchParams.groupId, allowedGroupIds, primaryGroupId). Передать resolvedGroupId в getStudentLessons(resolvedGroupId) / getStudentSessionHistory(resolvedGroupId). Заголовок: Title «Уроки»/«История занятий»; Text type secondary «{subjectName} — {groupName}» из данных action. notFound если enrollment null. Не добавлять global group Select на страницу (deferred); контекст только через URL/deep link.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - /student/lessons?groupId=... загружает уроки выбранного enrollment
    - /student/lessons без groupId использует primary enrollment
    - /student/history?groupId=... показывает только sessions этой группы
    - Подзаголовок содержит subjectName и groupName
    - tsc --noEmit проходит
  </acceptance_criteria>
  <done>Lessons/history читают groupId из URL с fallback на primary per D-08.</done>
</task>

<task type="auto">
  <name>Task 3: Deep links с карточек и localStorage</name>
  <files>src/features/student-portal/ui/StudentEnrollmentCard.tsx, src/features/student-portal/ui/StudentPortalGroupLink.tsx, src/widgets/app-shell/ui/AppShell.tsx</files>
  <read_first>
    src/features/student-portal/ui/StudentEnrollmentCard.tsx
    src/features/student-portal/lib/student-portal-query.ts
    src/features/student-portal/lib/student-portal-storage.ts
    src/widgets/app-shell/ui/AppShell.tsx
  </read_first>
  <action>
    Per D-05, D-06, D-07: создать client component StudentPortalGroupLink.tsx ('use client'): props { href, groupId, children }; onClick вызывает writeStudentPortalGroupId(groupId), затем router.push(href) или использовать Link с onClick. Обновить StudentEnrollmentCard: две кнопки/link «Уроки» и «История» через StudentPortalGroupLink с buildStudentPortalHref('/student/lessons', groupId) и buildStudentPortalHref('/student/history', groupId). Per D-06: проверить AppShell — STUDENT nav содержит /student/lessons и /student/history без изменений (не удалять). Опционально: client wrapper StudentPortalMenuSync на layout student — при mount readStudentPortalGroupId и если на lessons/history без groupId в URL, redirect с groupId из storage (discretion: только если не конфликтует с D-08 primary default; приоритет: URL param > storage > primary — реализовать на server через searchParams, storage только при клике с карточки).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && rg "/student/lessons" "src/widgets/app-shell/ui/AppShell.tsx" && rg "/student/history" "src/widgets/app-shell/ui/AppShell.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Карточка содержит ссылки «Уроки» и «История» с groupId в href
    - StudentPortalGroupLink пишет STUDENT_PORTAL_GROUP_STORAGE_KEY при клике
    - AppShell STUDENT nav всё ещё включает «Уроки» и «История занятий»
    - tsc --noEmit проходит
  </acceptance_criteria>
  <done>Deep link навигация с карточек и сохранение меню per D-05/D-06/D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→URL | groupId в query может быть подменён |
| server→Prisma | resolveStudentEnrollment проверяет membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03 | Spoofing | resolveStudentGroupId | medium | mitigate | allowedGroupIds только из enrollments текущего ученика |
| T-15-04 | Information Disclosure | getStudentSessionHistory | medium | mitigate | sessions filter groupId; requireRole STUDENT |
| T-15-SC | Tampering | npm installs | high | mitigate | Без новых пакетов |
</threat_model>

<verification>
pnpm exec tsc --noEmit
Ручная проверка: клик «Уроки» с карточки → URL с groupId; F5 сохраняет контекст; меню «Уроки» без param → primary enrollment.
</verification>

<success_criteria>
Навигация ученика с groupId в URL, deep links с карточек, меню сохранено; D-05–D-08 выполнены.
</success_criteria>

<artifacts>
| Артефакт | Путь / символ |
|----------|----------------|
| Enrollment resolver | resolveStudentEnrollment в student-actions.ts |
| Group ids helper | getStudentEnrollmentGroupIds в student-actions.ts |
| Client link | StudentPortalGroupLink.tsx |
| Lessons page | src/app/(dashboard)/student/lessons/page.tsx |
| History page | src/app/(dashboard)/student/history/page.tsx |
</artifacts>

<output>
Create `.planning/phases/15-student-portal-extra-assignments/15-02-SUMMARY.md` when done
</output>
