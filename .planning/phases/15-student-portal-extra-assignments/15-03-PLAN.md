---
phase: 15-student-portal-extra-assignments
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/extra-assignments/actions/extra-assignment-actions.ts
  - src/shared/lib/validations/extra-assignment.ts
  - src/app/api/extra-assignments/route.ts
  - src/features/extra-assignments/ui/ExtraAssignmentCatalogPage.tsx
  - src/features/extra-assignments/ui/AssignExtraAssignmentModal.tsx
  - src/app/(dashboard)/extra-assignments/page.tsx
  - src/features/journal/ui/lesson/LessonStepsSection.tsx
  - src/features/journal/actions/journal-actions.ts
autonomous: true
requirements:
  - SUBJ-17
must_haves:
  truths:
    - Справочник допзаданий фильтрует шаги и шаблоны по выбранному предмету
    - getProgramStepsForExtraAssignments возвращает только уровни/шаги subjectId
    - На уроке модалка назначения показывает только шаблоны шагов предмета группы
    - Cross-step внутри предмета сохранён (шаблон другого шага того же предмета)
    - Subject scope через join Step.level.subjectId — без новой колонки в schema
  artifacts:
    - src/features/extra-assignments/actions/extra-assignment-actions.ts#getProgramStepsForExtraAssignments
    - src/app/api/extra-assignments/route.ts
    - src/shared/lib/validations/extra-assignment.ts#listExtraAssignmentsQuerySchema
    - src/features/extra-assignments/ui/ExtraAssignmentCatalogPage.tsx
    - src/features/extra-assignments/ui/AssignExtraAssignmentModal.tsx
  key_links:
    - from: ExtraAssignmentCatalogPage
      to: GET /api/extra-assignments
      via: query param subjectId
    - from: AssignExtraAssignmentModal
      to: useExtraAssignments
      via: filters subjectId from lesson group
    - from: getProgramStepsForExtraAssignments
      to: Level.subjectId
      via: prisma where subjectId
---

<objective>
Subject-scoped допзадания: справочник и назначение на уроке фильтруют шаблоны по предмету группы.

Purpose: SUBJ-17, success criteria п.2; discretion — subject через Step.level.subjectId.
Output: subjectId в API/query, subject picker в каталоге, subject filter в assign modal.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-student-portal-extra-assignments/15-CONTEXT.md
@.planning/quick/260705-q01-extra-assignments/260705-q01-CONTEXT.md
@src/features/extra-assignments/actions/extra-assignment-actions.ts
@src/app/api/extra-assignments/route.ts
@src/features/extra-assignments/ui/ExtraAssignmentCatalogPage.tsx
@src/features/extra-assignments/ui/AssignExtraAssignmentModal.tsx
@src/features/journal/ui/lesson/LessonStepsSection.tsx
@src/features/journal/actions/journal-actions.ts
@src/shared/lib/subject-constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: getProgramStepsForExtraAssignments и API subjectId filter</name>
  <files>src/features/extra-assignments/actions/extra-assignment-actions.ts, src/shared/lib/validations/extra-assignment.ts, src/app/api/extra-assignments/route.ts</files>
  <read_first>
    src/features/extra-assignments/actions/extra-assignment-actions.ts
    src/app/api/extra-assignments/route.ts
    src/shared/lib/validations/extra-assignment.ts
    src/features/analytics/actions/analytics-actions.ts
  </read_first>
  <action>
    SUBJ-17 subject scope via joins: расширить listExtraAssignmentsQuerySchema optional subjectId. GET /api/extra-assignments: при subjectId добавить where step: { level: { subjectId } } (шаблоны без stepId не показывать при активном subject filter). Изменить getProgramStepsForExtraAssignments(subjectId: string): prisma.level.findMany where subjectId, include steps orderBy order asc — вернуть ProgramLevelWithSteps[]. Добавить getExtraAssignmentSubjects() для каталога: requireRoles TEACHER/MANAGER/SUPER_ADMIN; teacher — distinct subjectId из своих групп; manager/admin — все subjects orderBy name; вернуть { id, name }[]. Без prisma schema migration.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getProgramStepsForExtraAssignments требует subjectId и фильтрует levels
    - listExtraAssignmentsQuerySchema включает optional subjectId
    - GET /api/extra-assignments с subjectId возвращает только шаблоны шагов этого предмета
    - getExtraAssignmentSubjects экспортирован
    - tsc --noEmit проходит
  </acceptance_criteria>
  <done>Data layer допзаданий subject-scoped без миграции schema.</done>
</task>

<task type="auto">
  <name>Task 2: Subject picker в справочнике допзаданий</name>
  <files>src/features/extra-assignments/ui/ExtraAssignmentCatalogPage.tsx, src/app/(dashboard)/extra-assignments/page.tsx</files>
  <read_first>
    src/features/extra-assignments/ui/ExtraAssignmentCatalogPage.tsx
    src/app/(dashboard)/extra-assignments/page.tsx
    src/features/analytics/ui/AnalyticsSubjectPicker.tsx
  </read_first>
  <action>
    Обновить extra-assignments/page.tsx: загрузить subjects = getExtraAssignmentSubjects(); defaultSubjectId = subjects[0]?.id ?? DEFAULT_QURAN_SUBJECT_ID если в списке; programLevels = getProgramStepsForExtraAssignments(defaultSubjectId); передать subjects, initialSubjectId, programLevels в CatalogPage. ExtraAssignmentCatalogPage: props subjects, initialSubjectId, initialProgramLevels; state selectedSubjectId; Ant Design Select aria-label «Предмет»; при смене subject — refetch programLevels через client fetch к новому server action getProgramStepsForExtraAssignments(subjectId) (добавить lightweight server action или reuse с 'use server' export) и сброс level/step filters; передать subjectId в useExtraAssignments filters. ExtraAssignmentFormModal programLevels только выбранного предмета.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - /extra-assignments показывает Select предмета
    - Смена предмета обновляет level/step options в фильтрах и форме
    - useExtraAssignments получает subjectId в query params
    - tsc --noEmit проходит
  </acceptance_criteria>
  <done>Справочник допзаданий работает в subject scope.</done>
</task>

<task type="auto">
  <name>Task 3: Assign modal subject filter на уроке</name>
  <files>src/features/extra-assignments/ui/AssignExtraAssignmentModal.tsx, src/features/journal/ui/lesson/LessonStepsSection.tsx, src/features/journal/actions/journal-actions.ts</files>
  <read_first>
    src/features/extra-assignments/ui/AssignExtraAssignmentModal.tsx
    src/features/journal/ui/lesson/LessonStepsSection.tsx
    src/features/journal/actions/journal-actions.ts
  </read_first>
  <action>
    Per SUBJ-17 assign context: AssignExtraAssignmentModal — добавить required prop subjectId: string; включить subjectId в useExtraAssignments filters (API join filter). Step filter options только шаги templates текущего subject. Сохранить cross-step: шаблоны с stepId другого шага того же subject остаются видимы. LessonStepsSection: передать subjectId из lesson page props (journal getStudentLesson уже возвращает subjectId — проверить и пробросить через parent). Если subjectId не в props LessonStepsSection — добавить в journal lesson page data flow из getStudentLesson result. Не фильтровать displayStepId — только список шаблонов.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AssignExtraAssignmentModal принимает subjectId
    - useExtraAssignments в модалке передаёт subjectId
    - LessonStepsSection передаёт subjectId из контекста урока (group.subjectId)
    - tsc --noEmit проходит
  </acceptance_criteria>
  <done>Назначение на уроке ограничено шаблонами предмета группы; cross-step внутри предмета сохранён.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | subjectId в query справочника |
| server→Prisma | getExtraAssignmentSubjects role-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05 | Information Disclosure | getExtraAssignmentSubjects | medium | mitigate | Teacher только subjectId своих групп |
| T-15-06 | Tampering | subjectId query | low | mitigate | API фильтрует через join; не доверять client-only |
| T-15-SC | Tampering | npm installs | high | mitigate | Без новых пакетов |
</threat_model>

<verification>
pnpm exec tsc --noEmit
Ручная проверка: каталог — смена предмета меняет шаги; на уроке модалка не показывает шаблоны другого предмета.
</verification>

<success_criteria>
SUBJ-17: допзадания фильтруются и назначаются в контексте шагов предмета группы.
</success_criteria>

<artifacts>
| Артефакт | Путь / символ |
|----------|----------------|
| Program steps loader | getProgramStepsForExtraAssignments(subjectId) |
| Subjects list | getExtraAssignmentSubjects() |
| Query schema | subjectId в listExtraAssignmentsQuerySchema |
| Catalog UI | ExtraAssignmentCatalogPage subject Select |
| Assign modal | AssignExtraAssignmentModal subjectId prop |
</artifacts>

<output>
Create `.planning/phases/15-student-portal-extra-assignments/15-03-SUMMARY.md` when done
</output>
