---
phase: 14-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/analytics/lib/analytics-query.ts
  - src/features/analytics/lib/analytics-query.test.ts
  - src/features/analytics/lib/analytics-storage.ts
  - src/features/analytics/actions/analytics-actions.ts
  - src/features/analytics/ui/AnalyticsSubjectPicker.tsx
  - src/features/analytics/ui/AnalyticsTeacherPicker.tsx
  - src/features/analytics/ui/AnalyticsGroupPicker.tsx
  - src/features/analytics/ui/AnalyticsMonthPicker.tsx
  - src/app/(dashboard)/analytics/page.tsx
autonomous: true
requirements:
  - SUBJ-14
must_haves:
  truths:
    - На /analytics в шапке рядом с учителем/группой/месяцем есть селект предмета
    - subjectId сохраняется в URL и переживает перезагрузку
    - Учитель видит только предметы своих групп; менеджер/супер-админ — все предметы
    - При смене предмета группы в picker совместимы с выбранным предметом
    - Без валидного subjectId страница не рендерит метрики со смешанными предметами
  artifacts:
    - src/features/analytics/ui/AnalyticsSubjectPicker.tsx
    - src/features/analytics/lib/analytics-storage.ts
    - src/features/analytics/actions/analytics-actions.ts#getAnalyticsSubjects
    - src/features/analytics/lib/analytics-query.ts#resolveAnalyticsSubjectFilter
  key_links:
    - from: AnalyticsSubjectPicker
      to: buildAnalyticsSearchParams
      via: router.push с subjectId
    - from: analytics/page.tsx
      to: getAnalyticsSubjects
      via: server-side загрузка списка + resolveAnalyticsSubjectFilter
    - from: analytics/page.tsx
      to: AnalyticsGroupPicker
      via: groups отфильтрованы по filterSubjectId
---

<objective>
Добавить селект предмета и слой URL/query на странице аналитики: обязательный subject scope до загрузки метрик.

Purpose: SUBJ-14 — пользователь явно выбирает предмет; D-01, D-02, D-03, D-05, D-09.
Output: AnalyticsSubjectPicker, getAnalyticsSubjects, resolveAnalyticsSubjectFilter, обновлённые pickers и page.tsx.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics/14-CONTEXT.md
@.planning/phases/13-journal/13-CONTEXT.md
@src/features/analytics/lib/analytics-query.ts
@src/app/(dashboard)/analytics/page.tsx
@src/features/journal/lib/journal-storage.ts
@src/shared/lib/subject-constants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: URL/query helpers и resolveAnalyticsSubjectFilter</name>
  <files>src/features/analytics/lib/analytics-query.ts, src/features/analytics/lib/analytics-query.test.ts, src/features/analytics/lib/analytics-storage.ts</files>
  <read_first>
    src/features/analytics/lib/analytics-query.ts
    src/features/analytics/lib/analytics-query.test.ts
    src/features/journal/lib/journal-storage.ts
    src/shared/lib/subject-constants.ts
  </read_first>
  <behavior>
    - buildAnalyticsSearchParams включает subjectId когда передан
    - resolveAnalyticsSubjectFilter: валидный URL param → filterSubjectId; невалидный/отсутствующий → DEFAULT_QURAN_SUBJECT_ID если в списке, иначе первый id; никогда null при непустом списке
    - readAnalyticsSubjectId / writeAnalyticsSubjectId через ANALYTICS_SUBJECT_STORAGE_KEY (паттерн journal-storage)
  </behavior>
  <action>
    Расширить analytics-query.ts per D-02: добавить константу ANALYTICS_SUBJECT_PARAM = 'subjectId'; включить subjectId в buildAnalyticsSearchParams. Добавить resolveAnalyticsSubjectFilter(subjectParam, validSubjectIds) → { filterSubjectId, selectedSubjectId } per D-05 — без валидного id не возвращать null при непустом списке (дефолт: DEFAULT_QURAN_SUBJECT_ID из subject-constants если в validSubjectIds, иначе первый отсортированный id). Создать analytics-storage.ts с ANALYTICS_SUBJECT_STORAGE_KEY, read/write по образцу journal-storage (только client-safe helpers). Расширить resolveAnalyticsGroupFilter: добавить опциональный validGroupIds уже отфильтрованный по subject — логику не менять, совместимость groupId проверяется на page. Vitest: тесты resolveAnalyticsSubjectFilter (валидный param, невалидный → дефолт, пустой список → null), buildAnalyticsSearchParams с subjectId.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/features/analytics/lib/analytics-query.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - pnpm exec vitest run src/features/analytics/lib/analytics-query.test.ts проходит
    - buildAnalyticsSearchParams({ month, teacher, groupId, subjectId }) содержит subjectId в query string
    - resolveAnalyticsSubjectFilter с невалидным param возвращает DEFAULT_QURAN_SUBJECT_ID когда он в validSubjectIds
    - analytics-storage.ts экспортирует ANALYTICS_SUBJECT_STORAGE_KEY
  </acceptance_criteria>
  <done>Query-слой аналитики поддерживает subjectId в URL и детерминированный дефолт без смешанного скоупа.</done>
</task>

<task type="auto">
  <name>Task 2: getAnalyticsSubjects и AnalyticsSubjectPicker</name>
  <files>src/features/analytics/actions/analytics-actions.ts, src/features/analytics/ui/AnalyticsSubjectPicker.tsx</files>
  <read_first>
    src/features/analytics/actions/analytics-actions.ts
    src/features/analytics/ui/AnalyticsTeacherPicker.tsx
    src/features/analytics/ui/AnalyticsGroupPicker.tsx
    src/features/subject-admin/actions/subject-actions.ts
  </read_first>
  <action>
    Per D-03: добавить getAnalyticsSubjects() в analytics-actions.ts (не переиспользовать getSubjects — там requireRoles только MANAGER/SUPER_ADMIN). Логика: requireRoles(['TEACHER','MANAGER','SUPER_ADMIN']); для MANAGER/SUPER_ADMIN — prisma.subject.findMany orderBy name; для TEACHER — distinct subjectId из prisma.group.where teacherId = session.user.teacherId, include subject name. Вернуть AnalyticsSubjectOption[] { id, name }. Создать AnalyticsSubjectPicker.tsx по паттерну AnalyticsTeacherPicker: Ant Design Select, aria-label «Предмет», onChange → router.push с buildAnalyticsSearchParams сохраняя month/teacher/groupId; при смене subjectId сбрасывать groupId на ALL_GROUPS per D-09 (не переносить несовместимую группу). При смене subject писать writeAnalyticsSubjectId в localStorage (discretion: fallback для первого визита на клиенте).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getAnalyticsSubjects экспортирован из analytics-actions.ts
    - AnalyticsSubjectPicker рендерит Select с опциями предметов
    - onChange subject сбрасывает groupId до ALL_GROUPS в URL
    - tsc --noEmit без ошибок в изменённых файлах
  </acceptance_criteria>
  <done>Server action и UI-селект предмета готовы с role-scoped списком.</done>
</task>

<task type="auto">
  <name>Task 3: Интеграция page.tsx и синхронизация pickers</name>
  <files>src/app/(dashboard)/analytics/page.tsx, src/features/analytics/ui/AnalyticsTeacherPicker.tsx, src/features/analytics/ui/AnalyticsGroupPicker.tsx, src/features/analytics/ui/AnalyticsMonthPicker.tsx</files>
  <read_first>
    src/app/(dashboard)/analytics/page.tsx
    src/features/analytics/ui/AnalyticsTeacherPicker.tsx
    src/features/analytics/ui/AnalyticsGroupPicker.tsx
    src/features/analytics/ui/AnalyticsMonthPicker.tsx
  </read_first>
  <action>
    Per D-01: в шапке /analytics добавить AnalyticsSubjectPicker первым или рядом с teacher/group/month. page.tsx: расширить searchParams тип subjectId; загрузить subjects = getAnalyticsSubjects(); resolveAnalyticsSubjectFilter; если filterSubjectId null (нет предметов) — early return с Text «Нет доступных предметов» без вызова getTopStudents/getLevelStats/getAtRiskStudents per D-05. Фильтровать teacherGroups по group.subjectId === filterSubjectId перед resolveAnalyticsGroupFilter per D-09. Передать selectedSubjectId и subjects в picker; передать subjectId prop в TopStudents (для плана 14-03). Обновить AnalyticsTeacherPicker, AnalyticsGroupPicker, AnalyticsMonthPicker: при onChange всегда прокидывать subjectId из searchParams (или selectedSubjectId prop). TeacherPicker при смене учителя сбрасывает groupId (как сейчас) но сохраняет subjectId. GroupPicker disabled когда ALL_TEACHERS как сейчас. Метрики пока вызывать со старыми сигнатурами — subject wiring в 14-02; page готовит filterSubjectId.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - /analytics searchParams включает subjectId
    - AnalyticsSubjectPicker виден в шапке рядом с другими фильтрами
    - teacherGroups отфильтрованы по выбранному subjectId
    - TopStudents получает prop subjectId
    - При пустом списке предметов метрики не загружаются
  </acceptance_criteria>
  <done>Страница аналитики требует и отображает subject scope; pickers сохраняют subjectId в URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→URL | Пользователь подставляет произвольный subjectId в query |
| server→Prisma | getAnalyticsSubjects и page фильтруют по роли |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Spoofing | resolveAnalyticsSubjectFilter | medium | mitigate | Валидировать subjectId только против role-scoped validSubjectIds |
| T-14-02 | Information Disclosure | getAnalyticsSubjects | medium | mitigate | Учитель получает только subjectId из своих групп |
| T-14-03 | Tampering | URL query params | low | mitigate | Невалидный subjectId → дефолт из allowed set, не произвольные данные |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | Новые пакеты не устанавливаются в этой фазе |
</threat_model>

<verification>
pnpm exec vitest run src/features/analytics/lib/analytics-query.test.ts
pnpm exec tsc --noEmit
Ручная проверка: /analytics?subjectId=... сохраняет предмет после F5; смена предмета сбрасывает группу.
</verification>

<success_criteria>
Селект предмета на /analytics, subjectId в URL, role-scoped список, группы совместимы с предметом, без валидного subject нет смешанных метрик.
</success_criteria>

<artifacts>
| Артефакт | Путь / символ |
|----------|----------------|
| Константа URL param | ANALYTICS_SUBJECT_PARAM в analytics-query.ts |
| Storage key | ANALYTICS_SUBJECT_STORAGE_KEY в analytics-storage.ts |
| Resolver | resolveAnalyticsSubjectFilter в analytics-query.ts |
| Server action | getAnalyticsSubjects в analytics-actions.ts |
| UI | AnalyticsSubjectPicker.tsx |
| Тип | AnalyticsSubjectOption в analytics-actions.ts |
</artifacts>

<output>
Create `.planning/phases/14-analytics/14-01-SUMMARY.md` when done
</output>
