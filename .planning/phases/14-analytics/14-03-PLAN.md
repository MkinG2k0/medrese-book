---
phase: 14-analytics
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - src/entities/step-completion/api/use-step-completions.ts
  - src/app/api/step-completions/route.ts
  - src/features/analytics/ui/StudentStudyHistoryModal.tsx
  - src/features/analytics/ui/TopStudents.tsx
  - e2e/analytics-student-history.spec.ts
  - e2e/student-analytics.spec.ts
autonomous: true
requirements:
  - SUBJ-14
  - SUBJ-15
must_haves:
  truths:
    - История учёбы в модалке показывает только completions/sessions выбранного предмета
    - UI модалки не изменён — только data scope per D-08
    - E2E подтверждают subject picker и subject-scoped history
  artifacts:
    - src/app/api/step-completions/route.ts
    - src/entities/step-completion/api/use-step-completions.ts
    - src/features/analytics/ui/StudentStudyHistoryModal.tsx
  key_links:
    - from: TopStudents
      to: StudentStudyHistoryModal
      via: prop subjectId с page
    - from: useStepCompletions
      to: GET /api/step-completions
      via: query param subjectId
    - from: step-completions route
      to: session.group.subjectId
      via: Prisma where filter
---

<objective>
Ограничить историю учёбы в аналитике выбранным предметом и закрепить поведение E2E-тестами.

Purpose: success criteria п.3, D-07, D-08; SUBJ-14/15 end-to-end.
Output: subject filter в API/hook/modal, обновлённые e2e specs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics/14-CONTEXT.md
@src/features/analytics/ui/StudentStudyHistoryModal.tsx
@src/features/analytics/ui/TopStudents.tsx
@src/entities/step-completion/api/use-step-completions.ts
@src/app/api/step-completions/route.ts
@e2e/analytics-student-history.spec.ts
@e2e/student-analytics.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: subjectId в API step-completions и useStepCompletions</name>
  <files>src/app/api/step-completions/route.ts, src/entities/step-completion/api/use-step-completions.ts, src/app/api/step-completions/route.test.ts</files>
  <read_first>
    src/app/api/step-completions/route.ts
    src/entities/step-completion/api/use-step-completions.ts
    src/shared/lib/analytics-queries/filters.ts
  </read_first>
  <behavior>
    - GET /api/step-completions?studentId&subjectId возвращает только completions где session.group.subjectId = subjectId
    - isPriorCredit и adjustment sessions исключаются когда subjectId задан (FND-01 через существующие фильтры или session.isAdjustment)
    - Без subjectId — сохранить текущее поведение для journal/history call sites
  </behavior>
  <action>
    Per D-07: в GET route добавить опциональный query param subjectId. Когда subjectId передан: where stepCompletion includes session: { group: { subjectId } }; убрать зависимость от primary enrollment для duration map — для каждого completion использовать session.groupId в buildTeachingSessionDurationByDate (groupId из completion.session). Когда subjectId не передан — оставить legacy primary enrollment path для обратной совместимости journal. Расширить useStepCompletions(studentId, date, subjectId?): queryKey ["step-completions", studentId, date ?? "all", subjectId ?? "all"]; добавить subjectId в URLSearchParams когда задан. Добавить route.test.ts (или расширить существующий) с мок prisma: при subjectId where содержит group.subjectId.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/app/api/step-completions/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - route.test.ts проходит
    - useStepCompletions принимает optional subjectId
    - GET с subjectId фильтрует по session.group.subjectId
    - Call sites без subjectId не сломаны
  </acceptance_criteria>
  <done>API и React Query hook поддерживают subject-scoped completions.</done>
</task>

<task type="auto">
  <name>Task 2: StudentStudyHistoryModal data scope</name>
  <files>src/features/analytics/ui/StudentStudyHistoryModal.tsx, src/features/analytics/ui/TopStudents.tsx</files>
  <read_first>
    src/features/analytics/ui/StudentStudyHistoryModal.tsx
    src/features/analytics/ui/TopStudents.tsx
    src/app/(dashboard)/analytics/page.tsx
  </read_first>
  <action>
    Per D-07, D-08: добавить prop subjectId: string в StudentStudyHistoryModal; передать в useStepCompletions(studentId, null, subjectId). TopStudents: принять subjectId prop с page (из 14-01) и прокинуть в модалку. Не менять структуру Modal/Tabs/Table/columns — только data source. Вкладка «Доп. задания» без subject filter в этой фазе (out of scope AASN). Убедиться destroyOnHidden сбрасывает cache при смене subjectId через queryKey.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - StudentStudyHistoryModal принимает subjectId
    - useStepCompletions вызывается с subjectId из модалки
    - JSX структура модалки (Modal, Tabs, columns) не рефакторена
    - TopStudents передаёт subjectId в модалку
  </acceptance_criteria>
  <done>Клик по ученику в топе открывает историю только по выбранному предмету.</done>
</task>

<task type="auto">
  <name>Task 3: E2E — subject picker и subject-scoped analytics</name>
  <files>e2e/analytics-student-history.spec.ts, e2e/student-analytics.spec.ts</files>
  <read_first>
    e2e/analytics-student-history.spec.ts
    e2e/student-analytics.spec.ts
    e2e/helpers/codes.ts
    prisma/seed-e2e.ts
  </read_first>
  <action>
    Обновить E2E per discretion: analytics-student-history.spec.ts — перед кликом по ученику убедиться что URL содержит subjectId (или выбрать предмет в Select aria-label «Предмет»); проверить что после выбора другого предмета URL обновляется. student-analytics.spec.ts — в openHistoryModalFromAnalytics добавить ожидание subject Select; тест «страница загружается» — expect combobox «Предмет» visible. Добавить тест: смена предмета сбрасывает groupId в URL (D-09). Использовать seed-e2e предметы (Коран/Таджвид) если доступны двум учителям. Не добавлять E2E для /analytics/teachers.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/analytics-student-history.spec.ts e2e/student-analytics.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - playwright test для двух spec файлов проходит
    - E2E проверяет наличие селекта предмета на /analytics
    - E2E history modal открывается с subjectId в URL
    - Тест смены предмета присутствует
  </acceptance_criteria>
  <done>E2E подтверждают SUBJ-14/15 и историю в subject scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | subjectId в query step-completions |
| API→authorize | authorizeApiRequest с studentId до фильтра |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Information Disclosure | GET step-completions | high | mitigate | authorizeApiRequest до выдачи; subjectId не расширяет доступ вне student authorize |
| T-14-08 | Tampering | subjectId query | medium | mitigate | Фильтр только сужает выдачу внутри authorized student |
| T-14-SC | Tampering | npm installs | high | mitigate | Без новых зависимостей |
</threat_model>

<verification>
pnpm exec vitest run src/app/api/step-completions/route.test.ts
pnpm exec tsc --noEmit
pnpm exec playwright test e2e/analytics-student-history.spec.ts e2e/student-analytics.spec.ts
</verification>

<success_criteria>
История учёбы в аналитике subject-scoped; E2E зелёные; UI модалки не переписан.
</success_criteria>

<artifacts>
| Артефакт | Изменение |
|----------|-----------|
| useStepCompletions | +optional subjectId param |
| GET /api/step-completions | +subjectId filter |
| StudentStudyHistoryModal | +subjectId prop |
| TopStudents | передаёт subjectId |
| route.test.ts | subject filter tests |
| e2e/*.spec.ts | subject picker assertions |
</artifacts>

<output>
Create `.planning/phases/14-analytics/14-03-SUMMARY.md` when done
</output>
