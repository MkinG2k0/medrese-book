---
phase: 14-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/shared/lib/analytics-queries/top-students.ts
  - src/shared/lib/analytics-queries/level-stats.ts
  - src/shared/lib/analytics-queries/at-risk-students.ts
  - src/shared/lib/analytics-queries/top-students.test.ts
  - src/shared/lib/analytics-queries/level-stats.test.ts
  - src/shared/lib/analytics-queries/at-risk-students.test.ts
  - src/shared/lib/student-metrics/load-student-metrics.ts
  - src/shared/lib/student-metrics/load-student-metrics.test.ts
  - src/app/(dashboard)/analytics/page.tsx
  - src/app/api/at-risk-students/route.ts
  - src/app/api/at-risk-students/route.test.ts
  - src/entities/student-metrics/model/types.ts
  - src/entities/student-metrics/api/use-student-metrics.ts
  - src/features/analytics/ui/TopStudents.tsx
autonomous: true
requirements:
  - SUBJ-15
must_haves:
  truths:
    - getTopStudents считает метрики только по сессиям/completions групп выбранного предмета
    - getLevelStats возвращает уровни одного предмета с rowKey=levelId без суффикса предмета в label
    - getAtRiskStudents оценивает риск в скоупе предмета, не смешивая enrollments других предметов
    - Ученик в двух группах одного предмета — одна строка в топе (агрегация по studentId)
  artifacts:
    - src/shared/lib/analytics-queries/top-students.ts
    - src/shared/lib/analytics-queries/level-stats.ts
    - src/shared/lib/analytics-queries/at-risk-students.ts
    - src/shared/lib/student-metrics/load-student-metrics.ts
  key_links:
    - from: analytics/page.tsx
      to: getTopStudents/getLevelStats/getAtRiskStudents
      via: filterSubjectId как обязательный аргумент
    - from: top-students.ts
      to: Session.group.subjectId
      via: Prisma where session.group.subjectId = subjectId
    - from: level-stats.ts
      to: Level.subjectId
      via: where subjectId без hasMultipleSubjects workaround
---

<objective>
Прокинуть subjectId во все query-функции аналитики и server page: топ, уровни, at-risk в едином предметном скоупе.

Purpose: SUBJ-15, D-04, D-06; агрегация по studentId (discretion).
Output: обновлённые top-students, level-stats, at-risk-students, load-student-metrics, unit-тесты, page.tsx wiring.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics/14-CONTEXT.md
@.planning/phases/12-progress-sessions/12-CONTEXT.md
@src/shared/lib/analytics-queries/top-students.ts
@src/shared/lib/analytics-queries/level-stats.ts
@src/shared/lib/analytics-queries/at-risk-students.ts
@src/shared/lib/analytics-queries/filters.ts
@src/shared/lib/student-metrics/load-student-metrics.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getTopStudents и getLevelStats с subjectId</name>
  <files>src/shared/lib/analytics-queries/top-students.ts, src/shared/lib/analytics-queries/level-stats.ts, src/shared/lib/analytics-queries/top-students.test.ts, src/shared/lib/analytics-queries/level-stats.test.ts</files>
  <read_first>
    src/shared/lib/analytics-queries/top-students.ts
    src/shared/lib/analytics-queries/level-stats.ts
    src/shared/lib/analytics-queries/filters.ts
    .planning/quick/260712-12i-fix-duplicate-react-keys-in-levelstats-t/260712-12i-PLAN.md
  </read_first>
  <behavior>
    - getTopStudents(month, teacherId, groupId, subjectId): subjectId обязателен; sessions/completions фильтруются session.group.subjectId = subjectId (и groupId если задан)
    - Ученик с двумя enrollments в группах одного предмета — одна TopEntry, метрики суммируются/агрегируются по studentId
    - getLevelStats: levels where subjectId; label = String(level.number) без суффикса предмета; hasMultipleSubjects удалён per D-06
  </behavior>
  <action>
    Per D-04: изменить сигнатуры getTopStudents и getLevelStats — добавить subjectId: string как обязательный 4-й/4-й параметр после groupId. Общий фильтр subject: enrollments/sessions/completions через session: { group: { subjectId } } и groupId когда задан. top-students: where enrollments some { group: { subjectId, ...(teacherId && { teacherId }), ...(groupId && { id: groupId }) } }; completions/sessions с тем же subject scope; агрегировать по studentId (discretion: одна строка на ученика в предмете). level-stats: убрать groupSubjectId lookup и hasMultipleSubjects; levels.findMany where subjectId; enrollments filter по teacher/group как сейчас но всегда в subject scope; label всегда String(level.number) per D-06. Vitest с vi.mock prisma: top-students.test.ts проверяет что where включает group.subjectId; level-stats.test.ts — label без скобок при single subject, levels query filtered by subjectId.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/shared/lib/analytics-queries/top-students.test.ts src/shared/lib/analytics-queries/level-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest для top-students и level-stats проходит
    - getTopStudents принимает subjectId обязательным параметром
    - getLevelStats не содержит hasMultipleSubjects и не формирует label с именем предмета в скобках
    - Prisma where в тестах содержит subjectId scope
  </acceptance_criteria>
  <done>Топ учеников и статистика уровней считаются в скоупе одного предмета.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getAtRiskStudents и loadStudentMetricsForMonth с subject scope</name>
  <files>src/shared/lib/analytics-queries/at-risk-students.ts, src/shared/lib/analytics-queries/at-risk-students.test.ts, src/shared/lib/student-metrics/load-student-metrics.ts, src/shared/lib/student-metrics/load-student-metrics.test.ts</files>
  <read_first>
    src/shared/lib/analytics-queries/at-risk-students.ts
    src/shared/lib/student-metrics/load-student-metrics.ts
    src/shared/lib/enrollment.ts
    src/shared/lib/analytics-queries/top-students.test.ts
  </read_first>
  <behavior>
    - loadStudentMetricsForMonth(studentId, month, monthLabel, { subjectId, groupId? }) — subjectId обязателен; выбирает enrollment в скоупе предмета
    - При нескольких enrollments одного предмета: worst-case — enrollment с наибольшим числом riskFlags (discretion)
    - getAtRiskStudents(month, teacherId, groupId, subjectId) — subjectId обязателен; фильтрует студентов по enrollments в subject scope
    - at-risk-students.test.ts: Prisma where включает group.subjectId; enrollment вне subject не учитывается
    - load-student-metrics.test.ts: при двух enrollments одного предмета выбирается worst-case; sessions/completions фильтруются по groupIds в subject scope
  </behavior>
  <action>
    Per D-04: getAtRiskStudents(month, teacherId, groupId, subjectId) — subjectId обязателен (не optional). Расширить loadStudentMetricsContext/loadStudentMetricsForMonth обязательным scope: { subjectId: string; groupId?: string | null }. Вместо primary enrollment globally: find enrollments where group.subjectId = subjectId, optionally groupId; если groupId — один enrollment; иначе при нескольких — вычислить metrics для каждого, взять worst-case (max riskFlags.length, tie-break по absencesInMonth). Sessions/completions в loadStudentRecord фильтровать по groupIds в subject scope для period metrics и absences. getAtRiskStudents: student.findMany where enrollments some { group: { subjectId, teacherId?, id? } }; для каждого вызвать loadStudentMetricsForMonth с subjectId/groupId. Сохранить сортировку по имени. Создать at-risk-students.test.ts и load-student-metrics.test.ts с vi.mock prisma (паттерн top-students.test.ts): unit-тесты subject scope и worst-case enrollment — не ограничиваться route.test.ts.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/shared/lib/analytics-queries/at-risk-students.test.ts src/shared/lib/student-metrics/load-student-metrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - loadStudentMetricsForMonth требует scope.subjectId (обязательный)
    - getAtRiskStudents принимает subjectId обязательным 4-м параметром
    - at-risk-students.test.ts и load-student-metrics.test.ts проходят
    - tsc --noEmit без ошибок в student-metrics и at-risk-students
  </acceptance_criteria>
  <done>At-risk метрики считаются per subject с корректным выбором enrollment; unit-тесты покрывают subject scope.</done>
</task>

<task type="auto">
  <name>Task 3: Подключить filterSubjectId на page.tsx и API/hook</name>
  <files>src/app/(dashboard)/analytics/page.tsx, src/app/api/at-risk-students/route.ts, src/app/api/at-risk-students/route.test.ts, src/entities/student-metrics/model/types.ts, src/entities/student-metrics/api/use-student-metrics.ts, src/features/analytics/ui/TopStudents.tsx</files>
  <read_first>
    src/app/(dashboard)/analytics/page.tsx
    src/app/api/at-risk-students/route.ts
    src/entities/student-metrics/api/use-student-metrics.ts
    src/shared/lib/analytics.ts
  </read_first>
  <action>
    Per D-04, D-05: заменить placeholder gate из 14-01 — вернуть Promise.all с getAtRiskStudents(month, filterTeacherId, filterGroupId, filterSubjectId), getTopStudents(..., filterSubjectId), getLevelStats(..., filterSubjectId); смонтировать TopStudents, LevelStatsChart, AtRiskStudentsTable с реальными данными. TopStudents: принять prop subjectId={filterSubjectId} (подготовка для 14-03). at-risk API route: расширить atRiskStudentsQuerySchema — subjectId обязателен (z.string().min(1)); route парсит subjectId, валидирует против role-scoped subjects (getAnalyticsSubjects или prisma filter), передаёт в getAtRiskStudents(..., subjectId). useAtRiskStudents(month, teacherId, subjectId): subjectId обязателен в queryKey и URLSearchParams. Grep все call sites getTopStudents/getLevelStats/getAtRiskStudents/useAtRiskStudents — subjectId обязателен везде; compile errors исправить. route.test.ts: обновить моки/ожидания под обязательный subjectId. Не трогать /analytics/teachers (out of scope).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/app/api/at-risk-students/route.test.ts && pnpm exec tsc --noEmit && rg "getTopStudents\(|getLevelStats\(|getAtRiskStudents\(" src --glob "!**/*.test.ts" -n | rg -v "subjectId" ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx передаёт filterSubjectId во все три query functions и рендерит блоки метрик
    - TopStudents получает prop subjectId
    - atRiskStudentsQuerySchema и useAtRiskStudents требуют subjectId
    - route.test.ts проходит с обязательным subjectId
    - tsc --noEmit проходит; нет вызовов query functions без subjectId
  </acceptance_criteria>
  <done>Страница /analytics и at-risk API/hook загружают метрики только в скоупе выбранного предмета.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→DB | subjectId из validated set попадает в Prisma where |
| metrics→UI | Агрегированные данные не раскрывают чужие предметы |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Information Disclosure | getTopStudents | high | mitigate | Жёсткий where group.subjectId = validated subjectId |
| T-14-05 | Information Disclosure | loadStudentMetrics | medium | mitigate | Enrollment selection только в groups выбранного subjectId |
| T-14-06 | Tampering | subjectId param | medium | mitigate | page.tsx передаёт только filterSubjectId из resolveAnalyticsSubjectFilter |
| T-14-SC | Tampering | npm installs | high | mitigate | Без новых зависимостей |
</threat_model>

<verification>
pnpm exec vitest run src/shared/lib/analytics-queries/top-students.test.ts src/shared/lib/analytics-queries/level-stats.test.ts src/shared/lib/analytics-queries/at-risk-students.test.ts src/shared/lib/student-metrics/load-student-metrics.test.ts src/app/api/at-risk-students/route.test.ts
pnpm exec tsc --noEmit
</verification>

<success_criteria>
Топ, уровни и at-risk на /analytics отражают только выбранный предмет; LevelStats без дублирующих label; агрегация ученика в двух группах одного предмета — одна строка.
</success_criteria>

<artifacts>
| Артефакт | Изменение |
|----------|-----------|
| getTopStudents | +subjectId, subject session filter, aggregate by studentId |
| getLevelStats | +subjectId, убран hasMultipleSubjects |
| getAtRiskStudents | +subjectId, worst-case enrollment |
| loadStudentMetricsForMonth | +scope { subjectId, groupId? } |
| top-students.test.ts | новый |
| level-stats.test.ts | новый |
| at-risk-students.test.ts | новый — subject scope unit |
| load-student-metrics.test.ts | новый — worst-case enrollment |
| at-risk route + useAtRiskStudents | subjectId обязателен |
</artifacts>

<output>
Create `.planning/phases/14-analytics/14-02-SUMMARY.md` when done
</output>
