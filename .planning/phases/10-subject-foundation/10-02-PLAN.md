---
phase: 10-subject-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/features/subject-admin/actions/subject-actions.ts
  - src/features/subject-admin/ui/SubjectsList.tsx
  - src/features/subject-admin/ui/CreateSubjectForm.tsx
  - src/features/subject-admin/ui/EditSubjectForm.tsx
  - src/features/subject-admin/index.ts
  - src/app/(dashboard)/admin/subjects/page.tsx
  - src/widgets/app-shell/ui/AppShell.tsx
  - src/features/subject-admin/actions/subject-actions.test.ts
autonomous: true
requirements:
  - SUBJ-01
must_haves:
  truths:
    - Manager or super-admin can list subjects at /admin/subjects per D-04 D-07
    - Create and edit subject via modal with name and description per D-01
    - Delete subject blocked when levels exist per D-02
    - AppShell shows Предметы linking to /admin/subjects; /admin/program removed per D-06
  artifacts:
    - path: src/features/subject-admin/actions/subject-actions.ts
      provides: getSubjects createSubject updateSubject deleteSubject getSubject
    - path: src/features/subject-admin/ui/SubjectsList.tsx
      provides: Subject list table with modals and delete confirm
    - path: src/app/(dashboard)/admin/subjects/page.tsx
      provides: Server page with requireRoles
    - path: src/widgets/app-shell/ui/AppShell.tsx
      provides: Menu key /admin/subjects label Предметы
  key_links:
    - from: SubjectsList
      to: subject-actions
      via: createSubject updateSubject deleteSubject server calls
    - from: admin/subjects/page.tsx
      to: SubjectsList
      via: mapped SubjectRow props with levelCount and stepCount
    - from: AppShell
      to: /admin/subjects
      via: managerMenuOrder entry replacing /admin/program
---

<objective>
Deliver subject CRUD admin UI and navigation for managers and super-admins.

Purpose: SUBJ-01 — managers manage subjects before configuring per-subject programs.
Output: subject-admin feature, /admin/subjects page, updated AppShell menu.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subject-foundation/10-CONTEXT.md
@.planning/phases/10-subject-foundation/10-UI-SPEC.md
@.planning/phases/10-subject-foundation/10-PATTERNS.md
@src/features/groups/actions/group-actions.ts
@src/features/groups/ui/GroupsList.tsx
@src/features/groups/ui/CreateGroupForm.tsx
@src/app/(dashboard)/groups/page.tsx
@src/widgets/app-shell/ui/AppShell.tsx
@src/shared/lib/validations/subject.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Subject server actions and unit tests</name>
  <files>src/features/subject-admin/actions/subject-actions.ts, src/features/subject-admin/actions/subject-actions.test.ts</files>
  <behavior>
    - deleteSubject throws when subject has one or more levels (D-02 guard message in Russian per UI-SPEC)
    - createSubject rejects name shorter than 2 characters via Zod
    - getSubjects returns subjects ordered by name with level counts
  </behavior>
  <action>
    Per D-13 D-07: create subject-admin/actions/subject-actions.ts following groups pattern. Implement getSubjects (include _count levels; aggregate step count across levels or via nested _count), getSubject(subjectId), createSubject(input unknown), updateSubject(subjectId, input), deleteSubject(subjectId).

    deleteSubject per D-02: count levels for subjectId; if greater than 0 throw Error with UI-SPEC blocked-delete copy. Use createSubjectSchema and updateSubjectSchema from validations/subject.ts. revalidatePath /admin/subjects on mutations; revalidatePath /admin/subjects/${subjectId}/program on update.

    Write vitest tests mocking prisma and requireRoles per existing test patterns in src/. RED then GREEN.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/features/subject-admin/actions/subject-actions.test.ts</automated>
  </verify>
  <done>All subject action tests pass; delete guard and validation behaviors verified</done>
</task>

<task type="auto">
  <name>Task 2: SubjectsList and subject forms</name>
  <files>src/features/subject-admin/ui/SubjectsList.tsx, src/features/subject-admin/ui/CreateSubjectForm.tsx, src/features/subject-admin/ui/EditSubjectForm.tsx, src/features/subject-admin/index.ts</files>
  <action>
    Per D-01 D-04 and 10-UI-SPEC: implement SubjectsList client component mirroring GroupsList structure — Title Предметы, primary CTA Создать предмет, Table columns Название (Link to /admin/subjects/{id}/program using default antd link styling not hex override), Описание secondary dash if empty, Tag Уровней, Tag Шагов, actions Редактировать and Удалить danger.

    CreateSubjectForm and EditSubjectForm: antd Form vertical, name required min 2, description TextArea rows 3 optional, useTransition pending, message.success on success per UI-SPEC copy.

    Delete: App.useApp modal.confirm when levelCount is 0; if levelCount greater than 0 show message.error with blocked-delete copy without opening confirm.

    Export public API via index.ts barrel.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "subject-admin" || echo subject-admin-ok</automated>
  </verify>
  <done>UI components compile; copy matches UI-SPEC contract</done>
</task>

<task type="auto">
  <name>Task 3: Subjects page and AppShell navigation</name>
  <files>src/app/(dashboard)/admin/subjects/page.tsx, src/widgets/app-shell/ui/AppShell.tsx</files>
  <action>
    Per D-04 D-06 D-07: create admin/subjects/page.tsx — requireRoles SUPER_ADMIN and MANAGER, fetch getSubjects, map rows with id name description levelCount stepCount, render SubjectsList.

    AppShell: replace /admin/program menu entry with /admin/subjects key, BookOutlined icon, label Предметы. Update managerMenuOrder to use /admin/subjects. Remove /admin/program key entirely per D-06. Do not add redirect from old route — deletion happens in plan 10-04.

    auth.config already covers /admin/* — no change needed.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "admin/subjects AppShell" || echo routes-ok</automated>
  </verify>
  <done>/admin/subjects page exists; menu shows Предметы; old program menu entry removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to server actions | Untrusted admin input for subject CRUD |
| subjectId in URLs | Must not leak cross-tenant data (single-tenant app; IDOR on delete/update) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Spoofing | subject-actions | high | mitigate | requireRoles MANAGER SUPER_ADMIN on every export |
| T-10-05 | Elevation | deleteSubject | medium | mitigate | Block delete when levels exist; Prisma onDelete Restrict on Level |
| T-10-06 | Tampering | createSubject | medium | mitigate | Zod whitelist name description only |
| T-10-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
Unit tests green; tsc clean for subject-admin; manual smoke /admin/subjects loads for manager role.
</verification>

<success_criteria>
Manager can create edit and delete empty subjects; delete blocked with program; navigation points to Предметы.
</success_criteria>

<output>
Create .planning/phases/10-subject-foundation/10-02-SUMMARY.md when done
</output>
