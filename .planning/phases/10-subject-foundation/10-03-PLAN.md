---
phase: 10-subject-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/features/program-admin/actions/program-actions.ts
  - src/shared/lib/student-progress/offsets.ts
  - src/features/program-admin/lib/program-paths.ts
  - src/features/program-admin/actions/program-actions.test.ts
  - src/shared/lib/student-progress/offsets.test.ts
autonomous: true
requirements:
  - SUBJ-02
  - SUBJ-03
must_haves:
  truths:
    - getLevels and mutations require subjectId and filter Level rows by subject per D-09 D-14
    - getLevelSteps rejects level not belonging to subject (IDOR guard)
    - deleteLevel removes level with zero students and cascades steps enabling D-02 subject delete flow
    - Step offsets computed per subjectId for program editor global step numbers
    - revalidatePath uses subject-scoped program URLs not /admin/program
  artifacts:
    - path: src/features/program-admin/actions/program-actions.ts
      provides: subject-scoped level and step CRUD plus deleteLevel
    - path: src/shared/lib/student-progress/offsets.ts
      provides: getLevelStepOffsets(subjectId) and getStepOffsetForLevel(levelNumber, subjectId)
    - path: src/features/program-admin/lib/program-paths.ts
      provides: programListPath programLevelPath programStepEditPath helpers
  key_links:
    - from: createLevel
      to: Level.subjectId
      via: createLevelSchema parse includes subjectId
    - from: getLevelSteps
      to: subjectId param
      via: findFirst where id levelId AND subjectId
    - from: deleteLevel
      to: deleteSubject guard
      via: removing levels clears path to empty-subject delete
---

<objective>
Refactor program-admin server layer for subject-scoped queries, deleteLevel, offsets, and path helpers.

Purpose: SUBJ-02 SUBJ-03 — each subject owns an independent program template; D-02 requires deleteLevel.
Output: Updated program-actions, subject-scoped offsets, program-paths helper, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subject-foundation/10-CONTEXT.md
@.planning/phases/10-subject-foundation/10-RESEARCH.md
@.planning/phases/10-subject-foundation/10-PATTERNS.md
@src/features/program-admin/actions/program-actions.ts
@src/shared/lib/student-progress/offsets.ts
@src/shared/lib/validations/level.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Subject-scoped program actions and deleteLevel</name>
  <files>src/features/program-admin/actions/program-actions.ts, src/features/program-admin/actions/program-actions.test.ts</files>
  <behavior>
    - getLevels(subjectId) returns only levels for that subjectId
    - getLevelSteps(subjectId, levelId) returns null when level belongs to different subject
    - deleteLevel rejects when student count greater than 0; succeeds and deletes steps when zero students
  </behavior>
  <action>
    Per D-14 D-09: refactor program-actions.ts. Change getLevels to getLevels(subjectId: string) with where subjectId. Change getLevelSteps to getLevelSteps(subjectId, levelId) using findFirst where id and subjectId. createLevel parses createLevelSchema including subjectId; verify subject exists before create.

    Add deleteLevel(subjectId, levelId) per open question resolution: requireRoles; verify level belongs to subject; count students on level; if greater than 0 throw Russian error; else delete level (steps cascade per schema). revalidatePath subject program list and subjects list for tag counts.

    Update createStep updateStep deleteStep: resolve subjectId from level lookup; replace all revalidatePath /admin/program with program-paths helpers from Task 2.

    Keep reserveStepOrderSlot and moveStepOrder transaction logic unchanged per D-15.

    Write vitest tests with mocked prisma.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/features/program-admin/actions/program-actions.test.ts</automated>
  </verify>
  <done>Program action tests pass; deleteLevel and subject scoping behaviors verified</done>
</task>

<task type="auto">
  <name>Task 2: Program paths helper and subject-scoped offsets</name>
  <files>src/features/program-admin/lib/program-paths.ts, src/shared/lib/student-progress/offsets.ts</files>
  <action>
    Create program-paths.ts with pure functions: programListPath(subjectId), programLevelPath(subjectId, levelId), programStepNewPath(subjectId, levelId), programStepEditPath(subjectId, levelId, stepId). Use these in program-actions revalidatePath calls.

    Per D-15 and UI-SPEC global step number in subject scope: refactor offsets.ts. Add getLevelStepOffsets(subjectId: string) filtering prisma.level where subjectId. Change getStepOffsetForLevel to accept (levelNumber, subjectId). Use per-subject cache Map keyed by subjectId instead of single global cache. Extend invalidateStepOffsetCache to clear all or by subjectId.

    Do not change journal or student-progress call sites without subjectId in Phase 10 — keep backward-compatible overload or document that non-admin callers still use global behavior until Phase 12. Admin program pages in plan 10-04 will pass subjectId.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "program-paths offsets" || echo paths-offsets-ok</automated>
  </verify>
  <done>Path helpers exist; offsets accept subjectId parameter</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offsets unit tests</name>
  <files>src/shared/lib/student-progress/offsets.test.ts</files>
  <behavior>
    - getLevelStepOffsets(subjectA) and getLevelStepOffsets(subjectB) return different cumulative maps when step counts differ
    - getStepOffsetForLevel returns correct offset for level number within subject
  </behavior>
  <action>
    Write offsets.test.ts mocking prisma.level.findMany to return different level step counts per subjectId. Verify cumulative offset math matches toGlobalStepNumber usage in program editor.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/student-progress/offsets.test.ts</automated>
  </verify>
  <done>Offsets tests pass; subject isolation proven</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| levelId without subjectId | Attacker could access another subject program by guessing levelId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Elevation | getLevelSteps | high | mitigate | findFirst with both levelId and subjectId per RESEARCH IDOR pattern |
| T-10-08 | Tampering | deleteLevel | medium | mitigate | Guard student count; requireRoles on action |
| T-10-09 | Tampering | createLevel | medium | mitigate | Zod subjectId plus verify subject exists |
| T-10-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
pnpm test:unit for program-actions and offsets; grep confirms no remaining revalidatePath with /admin/program in program-actions.ts.
</verification>

<success_criteria>
All program mutations scoped by subjectId; deleteLevel enables clearing program before subject delete; offsets isolated per subject.
</success_criteria>

<output>
Create .planning/phases/10-subject-foundation/10-03-SUMMARY.md when done
</output>
