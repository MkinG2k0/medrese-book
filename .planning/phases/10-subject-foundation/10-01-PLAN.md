---
phase: 10-subject-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - prisma/lib/subject-constants.ts
  - src/shared/lib/validations/subject.ts
  - src/shared/lib/validations/level.ts
autonomous: true
requirements:
  - SUBJ-02
  - SUBJ-03
  - SUBJ-18
must_haves:
  truths:
    - Subject model exists in Prisma with name and description fields per D-01
    - Level has required subjectId with composite unique on subjectId+number per D-09
    - Existing production levels backfill to default Quran subject without data loss per D-08
    - Zod schemas validate subject and level create input including subjectId
  artifacts:
    - path: prisma/schema.prisma
      provides: Subject model and Level.subjectId relation
      contains: model Subject
    - path: prisma/migrations/
      provides: Prod-safe additive migration with Quran backfill
    - path: prisma/lib/subject-constants.ts
      provides: DEFAULT_QURAN_SUBJECT_ID shared constant
    - path: src/shared/lib/validations/subject.ts
      provides: createSubjectSchema and updateSubjectSchema
    - path: src/shared/lib/validations/level.ts
      provides: createLevelSchema with subjectId
  key_links:
    - from: prisma/migrations/
      to: prisma/lib/subject-constants.ts
      via: same DEFAULT_QURAN_SUBJECT_ID cuid in INSERT and export
    - from: Level.subjectId
      to: Subject.id
      via: Prisma relation onDelete Restrict
---

<objective>
Add Subject data model, prod-safe migration with Quran backfill, and Zod validation foundations.

Purpose: All Phase 10 features depend on subject-scoped Level rows (D-08, D-09, D-10).
Output: Migrated schema, validation modules, fixed cuid constant for default Quran subject.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-subject-foundation/10-CONTEXT.md
@.planning/phases/10-subject-foundation/10-RESEARCH.md
@.planning/phases/10-subject-foundation/10-PATTERNS.md
@prisma/schema.prisma
@src/shared/lib/validations/level.ts
@.cursor/rules/prisma-migrations.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subject model and Level.subjectId in schema</name>
  <files>prisma/schema.prisma</files>
  <action>
    Per D-08, D-09, D-10: add model Subject with id @id @default(cuid()), name String, description String @default(""), createdAt @default(now()), updatedAt @updatedAt, and levels Level[] relation.

    Modify Level: add subjectId String, subject Subject @relation(fields: [subjectId], references: [id], onDelete: Restrict), remove number @unique, add @@unique([subjectId, number]) and @@index([subjectId]). Keep Step model and @@unique([levelId, order]) unchanged per D-10.

    Do not add group.subjectId or student subject fields — deferred to Phase 11.
  </action>
  <verify>
    <automated>pnpm exec prisma validate</automated>
  </verify>
  <done>schema.prisma validates; Subject and scoped Level composite unique are defined</done>
</task>

<task type="auto">
  <name>Task 2: Prod-safe migration with fixed Quran subject cuid</name>
  <files>prisma/migrations/, prisma/lib/subject-constants.ts</files>
  <action>
  Create prisma/lib/subject-constants.ts exporting DEFAULT_QURAN_SUBJECT_ID as a fixed 25-char cuid string (e.g. clq10defaultquransubject00). Document that migration SQL must use the identical id.

  Run pnpm db:migrate -- --name add_subject. Edit generated migration.sql to be prod-safe per 10-RESEARCH Pattern 4 and prisma-migrations rule: CREATE TABLE Subject; INSERT default row with the fixed cuid and name Коран; ADD COLUMN subjectId nullable; UPDATE all Level rows SET subjectId to that cuid; SET NOT NULL; ADD FK with ON DELETE RESTRICT; DROP INDEX Level_number_key; CREATE UNIQUE INDEX Level_subjectId_number_key. No TRUNCATE, DELETE, or DROP TABLE on existing data.

  After migrate, run pnpm db:generate.
  </action>
  <verify>
    <automated>pnpm exec prisma validate && pnpm exec prisma migrate status</automated>
  </verify>
  <done>Migration applies cleanly; existing levels reference default Quran subject; composite unique index exists</done>
</task>

<task type="auto">
  <name>Task 3: Zod validation for subject and level</name>
  <files>src/shared/lib/validations/subject.ts, src/shared/lib/validations/level.ts</files>
  <action>
    Per D-01: create subject.ts with createSubjectSchema (name min 2 chars Russian message, description optional string defaulting to empty) and updateSubjectSchema as partial. Export CreateSubjectInput and UpdateSubjectInput types.

    Extend level.ts createLevelSchema with subjectId: z.string().min(1). Keep updateLevelSchema partial without requiring subjectId change after create.

    Follow naming and message style from existing validations/level.ts.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "validations/subject validations/level" || echo validations-ok</automated>
  </verify>
  <done>Both validation modules compile; subject name min-2 rule matches UI-SPEC</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL | Runs on production DB; must not destroy existing Level/Step/Completion rows |
| validation input | Server actions will parse untrusted admin input in later plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | migration.sql | critical | mitigate | Only CREATE/ADD/UPDATE/INDEX; no TRUNCATE per prisma-migrations rule |
| T-10-02 | Information Disclosure | subject-constants.ts | low | accept | Public cuid id is not secret; stable id required for backfill |
| T-10-03 | Tampering | createSubjectSchema | medium | mitigate | Whitelist name and description only per RESEARCH mass-assignment pattern |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan |
</threat_model>

<verification>
pnpm exec prisma validate passes; migrate status shows applied migration; generated Prisma client includes Subject type.
</verification>

<success_criteria>
Subject table exists; Level.subjectId is NOT NULL with composite unique; DEFAULT_QURAN_SUBJECT_ID constant matches migration INSERT; validations ready for server actions.
</success_criteria>

<output>
Create .planning/phases/10-subject-foundation/10-01-SUMMARY.md when done
</output>
