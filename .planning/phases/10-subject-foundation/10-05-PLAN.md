---
phase: 10-subject-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - prisma/lib/seed-program.ts
  - prisma/seed.ts
  - prisma/seed-program.ts
autonomous: true
requirements:
  - SUBJ-18
must_haves:
  truths:
    - Seed creates at least 3 subjects with distinct programs per D-11 D-12
    - Quran subject uses full imported program adapted to subjectId
    - Tajweed and Arabic subjects have visibly smaller distinct programs
    - Demo students and groups still reference Quran subject levels after seed
    - subject.deleteMany runs in correct FK order during seed wipe
  artifacts:
    - path: prisma/lib/seed-program.ts
      provides: seedProgram(prisma, { subjectId }) parameterized level creation
    - path: prisma/seed.ts
      provides: Three-subject demo seed with Quran Tajweed Arabic
  key_links:
    - from: prisma/seed.ts
      to: DEFAULT_QURAN_SUBJECT_ID
      via: first subject create uses same id as migration constant for consistency
    - from: seed student levelId
      to: Quran subject levels
      via: findMany levels where subjectId is quran id
---

<objective>
Refactor seed scripts for multi-subject demo data with Quran Tajweed and Arabic programs.

Purpose: SUBJ-18 fresh start demo — three subjects prove multi-subject data model per D-11 D-12.
Output: Parameterized seed-program.ts and updated seed.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subject-foundation/10-CONTEXT.md
@.planning/phases/10-subject-foundation/10-RESEARCH.md
@.planning/phases/10-subject-foundation/10-PATTERNS.md
@prisma/seed.ts
@prisma/lib/seed-program.ts
@prisma/lib/subject-constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize seed-program for subjectId</name>
  <files>prisma/lib/seed-program.ts</files>
  <action>
    Per D-11 D-14 discretion: refactor createLevelWithSteps and upsertLevelWithSteps to accept subjectId as first parameter after prisma. Update seedProgram signature to seedProgram(prisma, { subjectId, skipIfExists?, force? }).

    Replace findUnique where number with findFirst where subjectId and number composite key. All level.create data includes subjectId.

  Add seedMiniProgram(prisma, { subjectId, levels, stepsPerLevel, titles? }) helper for smaller programs — inline StepDef arrays with simple text blocks for Tajweed and Arabic.

    Keep loadAllProgramLevelSteps JSON import path for full Quran program only.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "seed-program" || echo seed-program-ok</automated>
  </verify>
  <done>seed-program.ts compiles with required subjectId on all level creates</done>
</task>

<task type="auto">
  <name>Task 2: Three-subject demo seed in seed.ts</name>
  <files>prisma/seed.ts</files>
  <action>
    Per D-11 D-12 D-08: add prisma.subject.deleteMany before prisma.level.deleteMany in wipe section.

    Create subjects in order:
    1. Коран — use DEFAULT_QURAN_SUBJECT_ID from subject-constants for create id so migration backfill and seed align; description Полная программа изучения Корана; call seedProgram with subjectId.
    2. Таджвид — description Правила чтения; seedMiniProgram with 2 levels and 3 steps each (or similar smaller counts).
    3. Арабский язык — description Базовый курс; seedMiniProgram with 3 levels and 5 steps each.

    Update student seed: levelId references must query levels where subjectId equals Quran subject id only — demo students stay on Quran program per RESEARCH A4. Update levelStepCounts and offsets to use Quran levels only.

    Log seed summary listing all three subjects and their level step counts.
  </action>
  <verify>
    <automated>pnpm db:seed</automated>
  </verify>
  <done>db:seed completes; database has 3 subjects with distinct level counts</done>
</task>

<task type="auto">
  <name>Task 3: Verify seed-program standalone script</name>
  <files>prisma/seed-program.ts</files>
  <action>
    Update prisma/seed-program.ts CLI entry if it exists to require subjectId or default to creating a subject first. Ensure pnpm db:seed:program still works for Quran-only import path with subjectId parameter.

    If standalone script cannot run without broader seed context, document in SUMMARY that db:seed is canonical verification for Phase 10.
  </action>
  <verify>
    <automated>pnpm exec tsx -e "import { PrismaClient } from './src/shared/lib/db'; console.log('seed-entry-ok')"</automated>
  </verify>
  <done>Seed entry points consistent with multi-subject model; no level.create without subjectId</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| destructive seed | seed.ts wipes demo data — guarded by assertDestructiveSeedAllowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-12 | Tampering | seed.ts wipe order | medium | mitigate | Delete subjects after steps students per FK order |
| T-10-13 | Denial of Service | db:seed on prod | high | accept | seed-guard already blocks prod destructive seed |
| T-10-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
pnpm db:seed succeeds; prisma studio or query shows 3 subjects; Quran has 5 levels; Tajweed and Arabic have fewer steps.
</verification>

<success_criteria>
Fresh local seed demonstrates multi-subject programs; students remain on Quran levels; SUBJ-18 satisfied.
</success_criteria>

<output>
Create .planning/phases/10-subject-foundation/10-05-SUMMARY.md when done
</output>
