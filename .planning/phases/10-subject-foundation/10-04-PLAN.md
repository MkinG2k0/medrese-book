---
phase: 10-subject-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - "10-02"
  - "10-03"
files_modified:
  - src/features/program-admin/ui/LevelsTable.tsx
  - src/features/program-admin/ui/LevelStepsTable.tsx
  - src/features/program-admin/ui/StepForm.tsx
  - src/features/program-admin/ui/CreateLevelForm.tsx
  - src/app/(dashboard)/admin/subjects/[subjectId]/program/page.tsx
  - src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/page.tsx
  - src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/new/page.tsx
  - src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx
  - src/app/(dashboard)/admin/program/
autonomous: true
requirements:
  - SUBJ-04
must_haves:
  truths:
    - Program editor accessible at /admin/subjects/[subjectId]/program per D-05 D-07
    - Level create uses modal on program page per UI-SPEC preference
    - LevelsTable and step routes use subject-scoped URLs; StepEditor reused unchanged per D-15
    - Old /admin/program route tree fully removed per D-06
    - Global step number displayed per subject using subject-scoped offsets
  artifacts:
    - path: src/app/(dashboard)/admin/subjects/[subjectId]/program/page.tsx
      provides: Subject program levels list with create level modal
    - path: src/features/program-admin/ui/CreateLevelForm.tsx
      provides: Modal form for new level with subjectId
    - path: src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/page.tsx
      provides: Level steps table with Tiptap entry points
  key_links:
    - from: program/page.tsx
      to: getLevels(subjectId)
      via: server component data fetch
    - from: StepForm
      to: program-paths
      via: redirect after save to programLevelPath
    - from: LevelsTable
      to: programLevelPath
      via: subjectId prop on row action links
---

<objective>
Ship subject-scoped program editor UI and routes; remove legacy /admin/program.

Purpose: SUBJ-04 — managers edit levels and Tiptap steps in subject context.
Output: New route tree under admin/subjects, updated program-admin UI components, deleted legacy routes.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subject-foundation/10-CONTEXT.md
@.planning/phases/10-subject-foundation/10-UI-SPEC.md
@.planning/phases/10-subject-foundation/10-PATTERNS.md
@src/app/(dashboard)/admin/program/page.tsx
@src/app/(dashboard)/admin/program/[levelId]/page.tsx
@src/features/program-admin/ui/LevelsTable.tsx
@src/features/program-admin/ui/LevelStepsTable.tsx
@src/features/program-admin/ui/StepForm.tsx
@src/features/program-admin/lib/program-paths.ts
@src/features/subject-admin/actions/subject-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor program UI components for subjectId</name>
  <files>src/features/program-admin/ui/LevelsTable.tsx, src/features/program-admin/ui/LevelStepsTable.tsx, src/features/program-admin/ui/StepForm.tsx, src/features/program-admin/ui/CreateLevelForm.tsx</files>
  <action>
    Per D-15 D-05: add subjectId prop to LevelsTable and LevelStepsTable; update all Link hrefs to program-paths helpers. Add delete level action button on LevelsTable or level page calling deleteLevel from program-actions with confirm modal — enables D-02 workflow.

    CreateLevelForm client component per UI-SPEC modal preference: antd Form with number and title fields, hidden or passed subjectId, calls createLevel, onSuccess closes modal and router.refresh. Mirror CreateSubjectForm useTransition pattern.

    StepForm: add subjectId prop; replace window.location.href and cancel links with programLevelPath and programStepEditPath. Do not modify StepEditor BlockRenderer or Tiptap extensions.

    Optional Breadcrumb Предметы → subject name → level title per UI-SPEC recommendation on level steps page.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "program-admin/ui" || echo program-ui-ok</automated>
  </verify>
  <done>All program UI components accept subjectId; no hardcoded /admin/program links remain in these files</done>
</task>

<task type="auto">
  <name>Task 2: Subject-scoped program route pages</name>
  <files>src/app/(dashboard)/admin/subjects/[subjectId]/program/page.tsx, src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/page.tsx, src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/new/page.tsx, src/app/(dashboard)/admin/subjects/[subjectId]/program/[levelId]/steps/[stepId]/edit/page.tsx</files>
  <action>
    Per D-05 D-07: create route pages copying structure from admin/program/* with Next.js 16 params Promise pattern.

    Program list page: requireRoles; await params subjectId; parallel fetch getSubject and getLevels(subjectId); notFound or Text if subject missing per UI-SPEC; Title shows subject name; empty state copy Программа пока пуста; LevelsTable with subjectId; Новый уровень opens CreateLevelForm modal not broken /new link.

    Level steps page: params subjectId and levelId; getLevelSteps(subjectId, levelId); verify level belongs to subject; getStepOffsetForLevel(level.number, subjectId) for global step column; LevelStepsTable with subjectId; links to step new and edit routes.

    Step new and edit pages: pass subjectId and levelId to StepForm; getStep for edit with notFound when missing.

    All pages requireRoles SUPER_ADMIN MANAGER.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "admin/subjects" || echo subject-routes-ok</automated>
  </verify>
  <done>Full program editor route tree exists under /admin/subjects/[subjectId]/program</done>
</task>

<task type="auto">
  <name>Task 3: Remove legacy /admin/program routes</name>
  <files>src/app/(dashboard)/admin/program/</files>
  <action>
    Per D-06: delete entire src/app/(dashboard)/admin/program directory. No redirect page.

    Grep codebase for /admin/program string literals; update any remaining references in src/ to program-paths or /admin/subjects. Known files from PATTERNS: program-actions already handled in 10-03; check README.md for doc-only update if present.

    Do not change journal analytics user-admin level pickers — Phase 11-14 scope per RESEARCH Pitfall 6.
  </action>
  <verify>
    <automated>rg "/admin/program" src -c 2>nul | findstr /V ":0$" && exit 1 || echo no-admin-program-refs</automated>
  </verify>
  <done>admin/program directory gone; zero /admin/program references under src/</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL subjectId vs levelId | User may swap subjectId in URL while keeping foreign levelId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10 | Elevation | program route pages | high | mitigate | getLevelSteps enforces subjectId match; show not found |
| T-10-11 | Tampering | CreateLevelForm | medium | mitigate | subjectId from server page props not user-editable hidden field |
| T-10-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
tsc clean; rg finds no /admin/program in src; manual path subjects → program → level → step editor works.
</verification>

<success_criteria>
Manager navigates subject program editor end-to-end; Tiptap step save redirects correctly; legacy routes removed.
</success_criteria>

<output>
Create .planning/phases/10-subject-foundation/10-04-SUMMARY.md when done
</output>
