---
phase: 00-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - "00-02"
  - "00-03"
files_modified:
  - src/shared/lib/student-progress/index.ts
  - src/shared/lib/student-progress/recalculate.ts
  - src/shared/lib/student-progress/sync-for-progress.ts
  - src/shared/lib/student-progress/offsets.ts
  - src/shared/lib/student-progress/filters.ts
  - src/shared/lib/student-progress/types.ts
  - src/shared/lib/recalculate-step-progress.ts
  - src/shared/lib/sync-completions-for-progress.ts
  - src/shared/lib/step-offset.ts
  - src/app/api/sessions/route.ts
  - src/app/api/step-completions/route.ts
  - src/app/api/step-completions/[id]/route.ts
  - src/features/journal/actions/journal-actions.ts
  - src/features/student-admin/actions/student-admin-actions.ts
  - src/features/user-admin/actions/user-actions.ts
  - src/features/student-portal/actions/student-actions.ts
  - e2e/student-progress.spec.ts
autonomous: true
requirements:
  - FND-03
must_haves:
  truths:
    - "Все call sites импортируют прогресс из @/shared/lib/student-progress"
    - "updateStudentProgress: sync → update student → recalculate в одной транзакции"
    - "Журнал и student portal показывают одинаковый currentStepIdx после admin edit"
  artifacts:
    - path: src/shared/lib/student-progress/index.ts
      provides: "публичный API модуля"
      exports: ["recalculateStudentStepIdx", "syncCompletionsForProgress", "getStepOffsetForLevel", "getLevelStepOffsets", "getLocalStepIdx", "toGlobalStepNumber", "isCountableCompletion", "isRealLessonSession"]
  key_links:
    - from: src/features/student-admin/actions/student-admin-actions.ts
      to: student-progress/sync-for-progress.ts
      via: "$transaction syncCompletionsForProgress + recalculateStudentStepIdx"
      pattern: "syncCompletionsForProgress|recalculateStudentStepIdx"
    - from: src/app/api/sessions/route.ts
      to: student-progress/recalculate.ts
      via: "recalculateStudentStepIdx after save"
      pattern: "recalculateStudentStepIdx"
---

<objective>
Реализовать FND-03: консолидировать логику прогресса в `src/shared/lib/student-progress/`, обновить 8+ call sites, атомарные мутации.

Purpose: Согласованный currentStepIdx в журнале, портале и аналитике после смены уровня/шага.
Output: Модуль student-progress, re-export stubs на старых путях, e2e student-progress GREEN.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-RESEARCH.md
@src/shared/lib/recalculate-step-progress.ts
@src/shared/lib/sync-completions-for-progress.ts
@src/shared/lib/step-offset.ts
@src/features/student-admin/actions/student-admin-actions.ts

<interfaces>
Call sites [VERIFIED grep]:
- src/app/api/sessions/route.ts
- src/app/api/step-completions/route.ts, [id]/route.ts
- src/features/journal/actions/journal-actions.ts
- src/features/student-admin/actions/student-admin-actions.ts
- src/features/user-admin/actions/user-actions.ts
- src/features/student-portal/actions/student-actions.ts

Defensive read-path (defer Phase 1 per planner decision):
- `getStudentLesson()` → `recalculateStudentStepIdx` — **оставить** в Phase 0; удаление в Phase 1 (задокументировать в SUMMARY).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать модуль student-progress</name>
  <files>
    src/shared/lib/student-progress/index.ts
    src/shared/lib/student-progress/recalculate.ts
    src/shared/lib/student-progress/sync-for-progress.ts
    src/shared/lib/student-progress/offsets.ts
    src/shared/lib/student-progress/filters.ts
    src/shared/lib/student-progress/types.ts
    src/shared/lib/recalculate-step-progress.ts
    src/shared/lib/sync-completions-for-progress.ts
    src/shared/lib/step-offset.ts
  </files>
  <read_first>
    - src/shared/lib/recalculate-step-progress.ts — полный файл
    - src/shared/lib/sync-completions-for-progress.ts — с isAdjustment/isPriorCredit из plan 02
    - src/shared/lib/step-offset.ts
    - src/shared/lib/analytics-queries/filters.ts — не дублировать; student-progress/filters.ts — domain helpers isRealLessonSession, isCountableCompletion
  </read_first>
  <acceptance_criteria>
    - Директория `src/shared/lib/student-progress/` с 6 файлами per RESEARCH structure
    - `sync-for-progress.ts` сохраняет `isAdjustment: true`, `isPriorCredit: true` из plan 02
    - `recalculate.ts` — логика level auto-advance без изменения поведения
    - `offsets.ts` — getStepOffsetForLevel, getLevelStepOffsets, getLocalStepIdx, toGlobalStepNumber
    - Старые файлы (`recalculate-step-progress.ts`, `sync-completions-for-progress.ts`, `step-offset.ts`) — re-export from student-progress (deprecated comment)
    - `grep -r "from '@/shared/lib/recalculate-step-progress'" src/` — только re-export файлы или 0 после task 2
  </acceptance_criteria>
  <action>
    Переместить код в student-progress/ без изменения бизнес-логики (copy + re-export pattern).
    `student-progress/filters.ts`:
    ```typescript
    export const isRealLessonSession = (s: { isAdjustment: boolean }) => !s.isAdjustment
    export const isCountableCompletion = (c: { isPriorCredit: boolean }) => !c.isPriorCredit
    ```
    Публичный `index.ts` per RESEARCH Pattern 3 exports list.
    Не менять call sites в этой задаче.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | findstr /C:"student-progress" /C:"error TS" || echo ok</automated>
  </verify>
  <done>Модуль создан; старые пути re-export для обратной совместимости</done>
</task>

<task type="auto">
  <name>Task 2: Обновить call sites + атомарный updateStudentProgress</name>
  <files>
    src/app/api/sessions/route.ts
    src/app/api/step-completions/route.ts
    src/app/api/step-completions/[id]/route.ts
    src/features/journal/actions/journal-actions.ts
    src/features/student-admin/actions/student-admin-actions.ts
    src/features/user-admin/actions/user-actions.ts
    src/features/student-portal/actions/student-actions.ts
  </files>
  <read_first>
    - src/features/student-admin/actions/student-admin-actions.ts — updateStudentProgress flow
    - src/features/user-admin/actions/user-actions.ts — createUsers student branch
    - .planning/phases/00-foundation/00-RESEARCH.md — Pitfall 3 (atomic transaction)
  </read_first>
  <acceptance_criteria>
    - Все импорты `recalculate-step-progress`, `sync-completions-for-progress`, `step-offset` заменены на `@/shared/lib/student-progress`
    - `updateStudentProgress` (student-admin): один `prisma.$transaction` — syncCompletionsForProgress → update student level/step → recalculateStudentStepIdx
    - `user-actions.ts` createUsers/updateUser student paths используют student-progress imports
    - API routes sessions/step-completions вызывают recalculateStudentStepIdx после мутации
    - Нет прямых импортов старых файлов из features/ и app/api/
  </acceptance_criteria>
  <action>
    Grep и заменить импорты на `@/shared/lib/student-progress`.
    Рефактор `updateStudentProgress` в атомарную транзакцию per Pitfall 3.
    **Не** вызывать `dispatchDomainEvent` здесь — plan 05.
  </action>
  <verify>
    <automated>pnpm run build 2>&1 | findstr /C:"error" /C:"Failed" || echo build ok</automated>
  </verify>
  <done>FND-03 call graph централизован; admin progress edit атомарен</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN e2e student-progress.spec.ts</name>
  <files>e2e/student-progress.spec.ts</files>
  <read_first>
    - e2e/student-progress.spec.ts — stub из plan 01
    - e2e/helpers/codes.ts — manager, teacher1, studentAli
    - e2e/journal.spec.ts — navigation patterns
  </read_first>
  <behavior>
    - Manager меняет currentStepIdx ученика через admin UI
    - Journal page и student portal показывают одинаковый шаг
    - Analytics API/query не считает adjustment sessions в метриках ученика
  </behavior>
  <acceptance_criteria>
    - `pnpm exec playwright test e2e/student-progress.spec.ts` — pass
    - Тест проверяет numeric step index или visible step title consistently
    - Тест вызывает analytics path (server action или API) и assert stepsCompleted исключает prior-credit/adjustment
  </acceptance_criteria>
  <action>
    Реализовать e2e: login manager → student admin → изменить шаг → login teacher → journal student → assert step → login student → portal assert same step → assert analytics metric via API/helper matches filtered count.
    Использовать `TEST_CODES`, antd helpers из `e2e/helpers/antd.ts`.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/student-progress.spec.ts</automated>
  </verify>
  <done>FND-03 верифицирован e2e: прогресс согласован между поверхностями</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → student progress | Только MANAGER/SUPER_ADMIN server actions |
| teacher → session save | Ownership через authorizeApiRequest (plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0-06 | Tampering | Client sets currentStepIdx | mitigate | Server-only recalculate; no client write to idx |
| T-0-07 | Integrity | Partial transaction on progress edit | mitigate | $transaction sync+update+recalculate |
</threat_model>

<verification>
- `pnpm exec playwright test e2e/student-progress.spec.ts` green
- `pnpm run build` success
</verification>

<success_criteria>
- FND-03: смена уровня/шага даёт согласованный прогресс в журнале, портале и аналитике
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-foundation-04-SUMMARY.md`
</output>
