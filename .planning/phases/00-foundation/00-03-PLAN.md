---
phase: 00-foundation
plan: 03
type: execute
wave: 1
depends_on:
  - "00-01"
files_modified:
  - src/shared/lib/authorize-api-request.ts
  - middleware.ts
  - src/app/api/students/route.ts
  - src/app/api/sessions/route.ts
  - src/app/api/step-completions/route.ts
  - src/app/api/step-completions/[id]/route.ts
  - src/app/api/uploads/route.ts
  - e2e/api-auth.spec.ts
autonomous: true
requirements:
  - FND-02
must_haves:
  truths:
    - "Неаутентифицированный запрос к /api/* (кроме auth) получает 401"
    - "STUDENT не может читать roster чужой группы"
    - "STUDENT не может читать сессии чужого ученика (включая GET без date)"
    - "TEACHER не может писать в чужую группу"
  artifacts:
    - path: src/shared/lib/authorize-api-request.ts
      provides: "default-deny API gate"
      exports: ["authorizeApiRequest"]
    - path: middleware.ts
      provides: "session required for /api/*"
  key_links:
    - from: src/app/api/students/route.ts
      to: authorize-api-request.ts
      via: "первая строка handler после parse params"
      pattern: "authorizeApiRequest"
    - from: src/app/api/sessions/route.ts
      to: authorize-api-request.ts
      via: "GET и POST с context studentId"
      pattern: "authorizeApiRequest.*studentId"
---

<objective>
Реализовать FND-02: `authorizeApiRequest()` с матрицей default-deny, middleware auth gate для `/api/*`, рефактор 5 REST handlers.

Purpose: Закрыть подтверждённые уязвимости CONCERNS.md (cross-group students, sessions without date).
Output: Все API routes через единый gate; e2e api-auth.spec.ts GREEN.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/codebase/CONCERNS.md
@src/shared/lib/authorize-student.ts
@src/shared/lib/group-access.ts
@src/app/api/students/route.ts
@src/app/api/sessions/route.ts
@middleware.ts

<interfaces>
From `src/shared/api/index.ts`:
```typescript
export function unauthorized(): NextResponse  // 401
export function forbidden(): NextResponse     // 403
```

From session user (NextAuth JWT):
```typescript
{ role: Role, teacherId?: string, studentId?: string }
```

Матрица доступа — RESEARCH Pattern 2 table (MANAGER/SUPER_ADMIN / TEACHER / STUDENT per route).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: authorizeApiRequest + middleware session gate</name>
  <files>src/shared/lib/authorize-api-request.ts, middleware.ts</files>
  <read_first>
    - src/shared/lib/authorize-student.ts — ownership patterns
    - src/shared/lib/group-access.ts — group checks
    - middleware.ts — текущий matcher
    - .planning/phases/00-foundation/00-RESEARCH.md — Pattern 2 (двухслойная защита)
  </read_first>
  <acceptance_criteria>
    - `authorize-api-request.ts` экспортирует `authorizeApiRequest(options?: { allowedRoles?, context? })`
    - Return type: `{ session } | { error: NextResponse }`
    - STUDENT + ctx.studentId !== actorStudentId → forbidden
    - STUDENT + ctx.groupId → prisma check own.groupId === ctx.groupId
    - TEACHER + ctx.studentId → student.group.teacherId === teacherId
    - TEACHER + ctx.groupId → group.teacherId === teacherId
    - `middleware.ts` matcher включает `/api/*` КРОМЕ `/api/auth` (изменить negative lookahead: убрать полное исключение api)
    - Unauthenticated API → redirect или 401 (согласовать с next-auth middleware behavior для API — JSON 401 предпочтительно для fetch clients)
  </acceptance_criteria>
  <action>
    Создать `authorizeApiRequest` per RESEARCH Pattern 2 skeleton.
    Типы: `ApiAuthContext = { groupId?, studentId?, completionId? }`.
    Default-deny: если `allowedRoles` указаны и роль не в списке → forbidden.
    Для completionId (PATCH route): загрузить completion → studentId → teacher ownership.
    Обновить `middleware.ts`:
    - Matcher: `'/((?!_next/static|_next/image|favicon.ico|robots.txt|icons|uploads|api/auth).*)'` — API попадает под auth
    - Убедиться что API routes возвращают 401 JSON, не HTML redirect (при необходимости — проверка pathname в authorized callback `auth.config.ts`)
    Решение Open Question 1: **включить middleware gate в Phase 0** (authentication-only); resource checks — только в authorizeApiRequest.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | findstr /C:"authorize-api-request" /C:"error TS" || echo ok</automated>
  </verify>
  <done>Helper и middleware компилируются; двухслойная защита настроена</done>
</task>

<task type="auto">
  <name>Task 2: Рефактор 5 API route handlers</name>
  <files>
    src/app/api/students/route.ts
    src/app/api/sessions/route.ts
    src/app/api/step-completions/route.ts
    src/app/api/step-completions/[id]/route.ts
    src/app/api/uploads/route.ts
  </files>
  <read_first>
    - Все 5 route.ts — текущие auth checks
    - .planning/phases/00-foundation/00-RESEARCH.md — матрица доступа
    - src/shared/lib/validations/session.ts — createSessionSchema
  </read_first>
  <acceptance_criteria>
    - Каждый handler вызывает `authorizeApiRequest` до бизнес-логики (не raw `auth()` + manual if)
    - GET /api/students: allowedRoles TEACHER,MANAGER,SUPER_ADMIN,STUDENT + context { groupId }
    - GET /api/sessions: authorize с { studentId } **до** ветки `if (dateStr)` — fix CONCERNS bug
    - POST /api/sessions: allowedRoles [TEACHER] + studentId ownership
    - step-completions GET/DELETE/PATCH: TEACHER only + student ownership via completionId/studentId
    - POST /api/uploads: MANAGER, SUPER_ADMIN only
    - Удалены дублирующие manual forbidden checks заменённые helper'ом
  </acceptance_criteria>
  <action>
    Заменить ad hoc auth на `authorizeApiRequest` per матрица RESEARCH.
    **Критично** `sessions/route.ts` GET: `authorizeApiRequest({ allowedRoles: [...], context: { studentId } })` сразу после parse studentId — STUDENT только self (`studentId === session.user.studentId`).
    MANAGER/SUPER_ADMIN — без teacher ownership check.
    Сохранить Zod validation и response helpers (`success`, `error`, etc.).
    MANAGER на step-completions — **оставить TEACHER-only** (Open Question 3, default-deny).
  </action>
  <verify>
    <automated>pnpm run lint -- --max-warnings=0 src/app/api/students/route.ts src/app/api/sessions/route.ts src/app/api/step-completions/route.ts src/app/api/step-completions/[id]/route.ts src/app/api/uploads/route.ts 2>&1 || pnpm run lint</automated>
  </verify>
  <done>Все 5 handlers используют единый gate; известные auth bugs закрыты в коде</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN e2e api-auth.spec.ts</name>
  <files>e2e/api-auth.spec.ts</files>
  <read_first>
    - e2e/helpers/api.ts — apiGetAs, expectForbidden, expectUnauthorized
    - e2e/helpers/codes.ts — studentAli, teacher1, teacher2
    - prisma/seed.ts — group assignments для cross-group test
  </read_first>
  <behavior>
    - Unauthenticated GET /api/students → 401
    - studentAli + foreign groupId → 403
    - studentAli + foreign studentId sessions (no date) → 403
    - teacher1 + own group students → 200
  </behavior>
  <acceptance_criteria>
    - `pnpm exec playwright test e2e/api-auth.spec.ts -g "401"` — pass
    - `pnpm exec playwright test e2e/api-auth.spec.ts -g "cross-group"` — pass
    - `pnpm exec playwright test e2e/api-auth.spec.ts -g "sessions"` — pass
  </acceptance_criteria>
  <action>
    Доработать stubs из plan 01: получить реальные groupId/studentId из seed (через API as manager или hardcode lookup после seed — предпочтительно dynamic: manager GET groups).
    Исправить тесты до GREEN.
    Добавить positive case: teacher1 читает свою группу → 200.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/api-auth.spec.ts</automated>
  </verify>
  <done>FND-02 верифицирован e2e; матрица default-deny работает</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/* | Недоверенные query/body; default-deny |
| middleware → handler | Auth-only на edge; resource на server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0-01 | Information disclosure | GET /api/students cross-group | mitigate | authorizeApiRequest groupId ownership для STUDENT |
| T-0-02 | Information disclosure | GET /api/sessions no date | mitigate | studentId scope до ветвления date |
| T-0-03 | Elevation | Forgotten auth() on new route | mitigate | middleware session gate + code review |
| T-0-04 | Spoofing | JWT forgery | accept | NextAuth signed JWT; AUTH_SECRET required |
</threat_model>

<verification>
- `pnpm exec playwright test e2e/api-auth.spec.ts` — all green
- `pnpm run build` — no type errors
</verification>

<success_criteria>
- FND-02: неавторизованный или непривилегированный API-запрос не возвращает чужие данные
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-foundation-03-SUMMARY.md`
</output>
