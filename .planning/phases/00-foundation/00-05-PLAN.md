---
phase: 00-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - "00-04"
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/shared/lib/domain-events/dispatch.ts
  - src/shared/lib/domain-events/types.ts
  - src/shared/lib/domain-events/index.ts
  - src/shared/lib/domain-events/handlers/audit.ts
  - src/shared/lib/domain-events/handlers/notifications.ts
  - src/shared/lib/audit/write-audit-event.ts
  - src/features/student-admin/actions/student-admin-actions.ts
  - src/features/user-admin/actions/user-actions.ts
  - src/app/api/sessions/route.ts
  - src/app/api/step-completions/route.ts
  - src/app/api/step-completions/[id]/route.ts
  - e2e/domain-events.spec.ts
  - e2e/helpers/db.ts
autonomous: true
requirements:
  - FND-04
must_haves:
  truths:
    - "updateStudentProgress создаёт AuditEvent STUDENT_PROGRESS_CHANGED"
    - "POST /api/sessions создаёт AuditEvent SESSION_SAVED"
    - "dispatchDomainEvent вызывается внутри той же $transaction что и мутация"
    - "notifications handler — no-op stub до Phase 6"
  artifacts:
    - path: src/shared/lib/domain-events/dispatch.ts
      provides: "fan-out audit + notifications"
      exports: ["dispatchDomainEvent"]
    - path: prisma/schema.prisma
      provides: "AuditEvent model"
      contains: "model AuditEvent"
  key_links:
    - from: src/features/student-admin/actions/student-admin-actions.ts
      to: domain-events/dispatch.ts
      via: "dispatchDomainEvent after progress change in tx"
      pattern: "dispatchDomainEvent"
    - from: domain-events/handlers/audit.ts
      to: prisma AuditEvent
      via: "writeAuditEvent create"
      pattern: "auditEvent\\.create|writeAuditEvent"
---

<objective>
Реализовать FND-04: `dispatchDomainEvent`, модель `AuditEvent`, audit write path, подключение критических мутаций, no-op notifications.

Purpose: Единый механизм событий без cross-feature imports; основа для Phase 6 notifications и Phase 7 audit UI.
Output: AuditEvent в БД после мутаций; e2e domain-events GREEN.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-RESEARCH.md
@src/features/student-admin/actions/student-admin-actions.ts
@src/features/user-admin/actions/user-actions.ts

<interfaces>
Domain events Phase 0 (RESEARCH):
- STUDENT_PROGRESS_CHANGED — updateStudentProgress
- STUDENT_CREATED / STUDENT_UPDATED — createUsers / updateUser
- SESSION_SAVED — POST /api/sessions
- COMPLETION_CHANGED — DELETE/PATCH step-completions

```prisma
model AuditEvent {
  id         String   @id @default(cuid())
  actorId    String
  action     String
  entityType String
  entityId   String
  payload    Json
  createdAt  DateTime @default(now())
  @@index([createdAt])
  @@index([entityType, entityId])
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditEvent schema + domain-events module</name>
  <files>
    prisma/schema.prisma
    prisma/migrations/
    src/shared/lib/domain-events/dispatch.ts
    src/shared/lib/domain-events/types.ts
    src/shared/lib/domain-events/index.ts
    src/shared/lib/domain-events/handlers/audit.ts
    src/shared/lib/domain-events/handlers/notifications.ts
    src/shared/lib/audit/write-audit-event.ts
  </files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md — Pattern 4
    - prisma/schema.prisma — существующие модели
    - src/shared/lib/auth.ts — session user id для actorId
  </read_first>
  <acceptance_criteria>
    - `model AuditEvent` в schema per RESEARCH
    - `domain-events/types.ts` — union `DomainEvent` с полями action, entityType, entityId, payload, actorId
    - `dispatchDomainEvent(event, tx?)` вызывает writeAuditEvent + enqueueNotifications
    - `handlers/notifications.ts` — `enqueueNotifications` пустая async function (no-op)
    - `write-audit-event.ts` — `prisma.auditEvent.create` с переданным tx или default prisma
    - FSD: нет imports из `@/features/*` в domain-events
  </acceptance_criteria>
  <action>
    Добавить AuditEvent в schema; миграция `foundation_audit_event`.
    Реализовать dispatch per RESEARCH Pattern 4.
    Payload — server-side snapshot (old/new values), не client input.
    **Pitfall 4:** принимать optional `Prisma.TransactionClient` и использовать для create.
  </action>
  <verify>
    <automated>pnpm exec prisma validate</automated>
  </verify>
  <done>Модуль domain-events и schema AuditEvent готовы</done>
</task>

<task type="auto" blocking="true">
  <name>[BLOCKING] Task 2: Применить миграцию AuditEvent</name>
  <files>prisma/migrations/</files>
  <read_first>
    - package.json — db:migrate
    - plan 02 — паттерн [BLOCKING] migrate
  </read_first>
  <acceptance_criteria>
    - `pnpm db:migrate` или `prisma migrate deploy` — AuditEvent table exists
    - `pnpm exec prisma migrate status` — no pending
  </acceptance_criteria>
  <action>
    Запустить `pnpm db:migrate --name foundation_audit_event`.
    При недоступной БД — checkpoint:human-action для ручного migrate.
    `pnpm db:generate` после успеха.
  </action>
  <verify>
    <automated>pnpm exec prisma migrate status</automated>
  </verify>
  <done>Таблица AuditEvent существует в БД</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire mutations + GREEN e2e domain-events</name>
  <files>
    src/features/student-admin/actions/student-admin-actions.ts
    src/features/user-admin/actions/user-actions.ts
    src/app/api/sessions/route.ts
    src/app/api/step-completions/route.ts
    src/app/api/step-completions/[id]/route.ts
    e2e/domain-events.spec.ts
    e2e/helpers/db.ts
  </files>
  <read_first>
    - plan 04 — atomic transaction в updateStudentProgress
    - e2e/domain-events.spec.ts — stub
    - e2e/global-setup.ts — test DB connection
  </read_first>
  <behavior>
    - After updateStudentProgress → AuditEvent with action STUDENT_PROGRESS_CHANGED
    - actorId = session user id
    - Event inside same transaction as mutation
  </behavior>
  <acceptance_criteria>
    - `grep -r "dispatchDomainEvent" src/features/student-admin` — в updateStudentProgress
    - `grep "dispatchDomainEvent" src/app/api/sessions/route.ts` — после save
    - `grep "dispatchDomainEvent" src/app/api/step-completions` — DELETE и PATCH
    - `grep "dispatchDomainEvent" src/features/user-admin` — createUsers/updateUser student
    - `e2e/helpers/db.ts` — read-only countAuditEvents(action, entityId?)
    - `pnpm exec playwright test e2e/domain-events.spec.ts` — pass
    - Features НЕ импортируют audit-log feature
  </acceptance_criteria>
  <action>
    В каждой критической мутации: после успешного write, внутри `$transaction`, вызвать `dispatchDomainEvent({...}, tx)`.
    actorId из `session.user.id` (server actions) или `authResult.session.user.id` (API).
    Создать `e2e/helpers/db.ts` с Prisma client из dotenv `.env.test` для assert count.
    e2e: manager changes progress → assert AuditEvent row exists with correct action.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/domain-events.spec.ts</automated>
  </verify>
  <done>FND-04: критические мутации порождают AuditEvent через единый dispatch</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → mutation | Payload audit только server-derived |
| features → audit | Только через dispatchDomainEvent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0-08 | Tampering | Client injects audit payload | mitigate | Server-only writeAuditEvent; no API for audit create |
| T-0-09 | Repudiation | Mutation without audit | mitigate | dispatchDomainEvent in same tx as mutation |
| T-0-10 | Information disclosure | AuditEvent payload PII | accept | Phase 7 UI will filter; payload minimal for Phase 0 |
</threat_model>

<verification>
- `pnpm exec playwright test e2e/domain-events.spec.ts` green
- `pnpm test:e2e` full suite (phase gate)
</verification>

<success_criteria>
- FND-04: критические доменные мутации автоматически порождают записи аудита через dispatchDomainEvent
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-foundation-05-SUMMARY.md`
</output>
