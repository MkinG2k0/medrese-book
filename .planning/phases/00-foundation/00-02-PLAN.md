---
phase: 00-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - "00-01"
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/shared/lib/analytics-queries/filters.ts
  - src/shared/lib/analytics-queries/index.ts
  - src/shared/lib/analytics-queries/top-students.ts
  - src/shared/lib/analytics-queries/level-stats.ts
  - src/shared/lib/analytics.ts
  - src/shared/lib/sync-completions-for-progress.ts
  - src/features/analytics/actions/analytics-actions.ts
autonomous: true
requirements:
  - FND-01
must_haves:
  truths:
    - "Метрики getTopStudents и getLevelStats не включают сессии isAdjustment=true"
    - "Метрики не включают completions isPriorCredit=true"
    - "Существующие adjustment-сессии backfill-нуты в миграции"
    - "Новые adjustment-сессии создаются с isAdjustment=true"
  artifacts:
    - path: prisma/schema.prisma
      provides: "Session.isAdjustment, StepCompletion.isPriorCredit"
      contains: "isAdjustment"
    - path: src/shared/lib/analytics-queries/filters.ts
      provides: "единственный источник Prisma where-фрагментов"
      exports: ["countableSessionWhere", "countableCompletionWhere", "analyticsSessionFilter", "analyticsCompletionFilter"]
  key_links:
    - from: src/shared/lib/analytics-queries/top-students.ts
      to: analytics-queries/filters.ts
      via: "where в include completions/sessions"
      pattern: "analyticsCompletionFilter|analyticsSessionFilter"
    - from: src/shared/lib/sync-completions-for-progress.ts
      to: Session.isAdjustment
      via: "session.create data.isAdjustment: true"
      pattern: "isAdjustment:\\s*true"
---

<objective>
Реализовать FND-01: Prisma-флаги, data backfill, единый слой analytics-queries, рефакторинг analytics.ts.

Purpose: Исключить корректировки прогресса и «зачтено ранее» из всех отчётов — основа достоверной аналитики Phase 1.
Output: Миграция применена, filters.ts GREEN, analytics consumers переведены на analytics-queries.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/codebase/CONCERNS.md
@src/shared/lib/analytics.ts
@src/shared/lib/sync-completions-for-progress.ts
@src/features/analytics/actions/analytics-actions.ts

<interfaces>
From `src/shared/lib/analytics.ts`:
```typescript
export async function getTopStudents(teacherId: string | null, month: Date): Promise<TopStudent[]>
export async function getLevelStats(teacherId: string | null, month: Date): Promise<LevelStat[]>
```

From `sync-completions-for-progress.ts` — adjustment session note:
```typescript
note: 'Корректировка прогресса'  // → isAdjustment: true после миграции
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema — isAdjustment, isPriorCredit + data migration SQL</name>
  <files>prisma/schema.prisma, prisma/migrations/</files>
  <read_first>
    - prisma/schema.prisma — модели Session, StepCompletion
    - .planning/phases/00-foundation/00-RESEARCH.md — Pitfall 1 (backfill SQL)
    - C:/Users/mk/.cursor/plugins/cache/cursor-public/prisma/rules/migration-best-practices.mdc
  </read_first>
  <acceptance_criteria>
    - `grep "isAdjustment" prisma/schema.prisma` — `Boolean @default(false)` на Session
    - `grep "isPriorCredit" prisma/schema.prisma` — `Boolean @default(false)` на StepCompletion
    - В папке `prisma/migrations/` есть новая миграция с SQL:
      `UPDATE "Session" SET "isAdjustment" = true WHERE note = 'Корректировка прогресса'`
    - Та же миграция: `UPDATE "StepCompletion" ... JOIN Session ... isPriorCredit = true` для completions adjustment-сессий
  </acceptance_criteria>
  <action>
    Добавить в `Session`: `isAdjustment Boolean @default(false)`.
    Добавить в `StepCompletion`: `isPriorCredit Boolean @default(false)`.
    Создать миграцию `foundation_analytics_flags` с data migration SQL из RESEARCH (Pitfall 1).
    НЕ добавлять AuditEvent в этой задаче (plan 05).
    Seed sessions с note «Seed data» — оставить `isAdjustment: false` (Open Question 4).
  </action>
  <verify>
    <automated>pnpm exec prisma validate</automated>
  </verify>
  <done>Schema валидна; migration SQL файл содержит backfill для существующих adjustment-данных</done>
</task>

<task type="auto" blocking="true">
  <name>[BLOCKING] Task 2: Применить миграцию БД</name>
  <files>prisma/migrations/</files>
  <read_first>
    - package.json — script `db:migrate`
    - prisma.config.ts — migrations path
  </read_first>
  <acceptance_criteria>
    - `pnpm db:migrate` завершился exit code 0 ИЛИ создан checkpoint с ручным шагом
    - `pnpm exec prisma migrate status` — нет pending migrations
    - `pnpm db:generate` / postinstall — client с полями isAdjustment, isPriorCredit
  </acceptance_criteria>
  <action>
    Запустить `pnpm db:migrate --name foundation_analytics_flags`.
    Требует `DATABASE_URL` в `.env`.
    **Non-TTY / CI:** использовать `pnpm exec prisma migrate deploy`.
    Если migrate не может выполниться автономно (нет БД, interactive prompt) — остановиться с `checkpoint:human-action`: пользователь запускает `pnpm db:migrate` локально и отвечает «approved».
    После успеха: `pnpm db:generate`.
  </action>
  <verify>
    <automated>pnpm exec prisma migrate status 2>&1 | findstr /V /C:"pending" || pnpm exec prisma migrate status</automated>
  </verify>
  <done>Миграция применена; Prisma Client сгенерирован с новыми полями</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: analytics-queries + write-path flags + GREEN unit tests</name>
  <files>
    src/shared/lib/analytics-queries/filters.ts
    src/shared/lib/analytics-queries/index.ts
    src/shared/lib/analytics-queries/top-students.ts
    src/shared/lib/analytics-queries/level-stats.ts
    src/shared/lib/analytics.ts
    src/shared/lib/sync-completions-for-progress.ts
    src/features/analytics/actions/analytics-actions.ts
  </files>
  <read_first>
    - src/shared/lib/analytics.ts — полная логика getTopStudents/getLevelStats
    - src/shared/lib/analytics-queries/filters.test.ts — ожидания из plan 01
    - src/shared/lib/sync-completions-for-progress.ts
  </read_first>
  <behavior>
    - countableSessionWhere = { isAdjustment: false }
    - countableCompletionWhere = { isPriorCredit: false }
    - analyticsSessionFilter merges date + isAdjustment:false
    - syncCompletionsForProgress creates session with isAdjustment:true
    - syncCompletionsForProgress creates completions with isPriorCredit:true
    - getTopStudents step count excludes prior credit completions
  </behavior>
  <acceptance_criteria>
    - `src/shared/lib/analytics-queries/filters.ts` экспортирует 4 функции/константы из RESEARCH Pattern 1
    - `analytics.ts` re-export или thin wrapper вызывает `analytics-queries/top-students` и `level-stats`
    - `grep "isAdjustment: true" src/shared/lib/sync-completions-for-progress.ts` — при create adjustment session
    - `grep "isPriorCredit: true" src/shared/lib/sync-completions-for-progress.ts` — при create backfill completion
    - `pnpm test:unit -- filters.test.ts` — GREEN
    - Нет прямых prisma session/completion queries для метрик вне `analytics-queries/` (кроме re-export analytics.ts)
  </acceptance_criteria>
  <action>
    Реализовать `filters.ts` per RESEARCH.
    Вынести `getTopStudents` → `top-students.ts`, `getLevelStats` → `level-stats.ts` с обязательными `where` из filters.
    Обновить `analytics.ts` — re-export публичного API (parseAnalyticsMonth, formatAnalyticsMonth, getTopStudents, getLevelStats).
    Обновить `analytics-actions.ts` импорты если нужно.
    В `sync-completions-for-progress.ts`:
    - `session.create`: `isAdjustment: true`
    - `stepCompletion.create` в backfill: `isPriorCredit: true`
    - При update existing completion на adjustment session — set `isPriorCredit: true` если создаётся через sync
    Запретить фильтр по `note contains 'Корректировка'` — только boolean flags.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/analytics-queries/filters.test.ts</automated>
  </verify>
  <done>FND-01 выполнен: фильтры едины, backfill применён, unit-тесты GREEN, write-path проставляет флаги</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → analytics | Только server-side Prisma; клиент не влияет на isAdjustment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0-03 | Tampering | Client sets isAdjustment | accept | Поля не в API input schemas; только server write paths |
| T-0-04 | Information disclosure | Analytics over-counting | mitigate | filters.ts mandatory; unit tests assert exclusion |
| T-0-05 | Repudiation | Adjustment without flag | mitigate | Data migration backfill + boolean on create |
</threat_model>

<verification>
- `pnpm test:unit` GREEN для filters
- `pnpm exec prisma migrate status` — database up to date
- Ручная (Phase gate): manager `/analytics` — counts без adjustment (см. 00-VALIDATION.md Manual)
</verification>

<success_criteria>
- FND-01: единый фильтр исключает adjustment sessions и prior credit completions во всех analytics queries
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-foundation-02-SUMMARY.md`
</output>
