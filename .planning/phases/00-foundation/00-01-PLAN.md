---
phase: 00-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - e2e/helpers/api.ts
  - e2e/api-auth.spec.ts
  - e2e/student-progress.spec.ts
  - e2e/domain-events.spec.ts
  - src/shared/lib/analytics-queries/filters.test.ts
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
must_haves:
  truths:
    - "Автоматизированные тесты для FND-01–FND-04 существуют и падают (RED) до реализации"
    - "E2E-хелперы позволяют вызывать API от имени роли по TEST_CODES"
  artifacts:
    - path: e2e/helpers/api.ts
      provides: "apiGetAs, expectForbidden, login cookie для request fixture"
    - path: e2e/api-auth.spec.ts
      provides: "FND-02 матрица default-deny"
    - path: src/shared/lib/analytics-queries/filters.test.ts
      provides: "unit-тесты countableSessionWhere / countableCompletionWhere"
  key_links:
    - from: e2e/api-auth.spec.ts
      to: e2e/helpers/api.ts
      via: "import apiGetAs"
      pattern: "from.*helpers/api"
---

<objective>
Подготовить тестовую инфраструктуру Wave 0: vitest для pure-функций фильтров и Playwright API specs для FND-02/03/04.

Purpose: Nyquist — каждая последующая задача имеет `<automated>` verify; тесты создаются в RED до реализации.
Output: vitest config, e2e/helpers/api.ts, четыре spec-файла (stubs с failing assertions).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-VALIDATION.md
@e2e/helpers/codes.ts
@e2e/helpers/auth.ts
@e2e/global-setup.ts
@playwright.config.ts

<interfaces>
From `e2e/helpers/codes.ts`:
```typescript
export const TEST_CODES = {
  superAdmin: "100001",
  manager: "100002",
  teacher1: "200001",
  teacher2: "200002",
  studentAli: "300001",
  studentUsman: "300002",
  // ...
} as const;
```

From `src/shared/api/index.ts` (ожидаемый формат ответа):
```typescript
// { data: T | null, error: string | null }
export function unauthorized(): NextResponse
export function forbidden(): NextResponse
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest и npm-скрипт для unit-тестов фильтров</name>
  <files>package.json, vitest.config.ts, src/shared/lib/analytics-queries/filters.test.ts</files>
  <read_first>
    - package.json — scripts, devDependencies
    - .planning/phases/00-foundation/00-RESEARCH.md — Pattern 1 (countableSessionWhere)
  </read_first>
  <acceptance_criteria>
    - `grep -q "vitest" package.json` — devDependency vitest присутствует
    - `grep -q '"test:unit"' package.json` — скрипт `test:unit` вызывает vitest run
    - Файл `vitest.config.ts` существует с alias `@` → `./src`
    - `filters.test.ts` импортирует из `./filters` (файл filters.ts будет создан в plan 02 — тесты должны падать с module not found или assertion fail)
  </acceptance_criteria>
  <action>
    Установить `pnpm add -D vitest`. Добавить скрипт `"test:unit": "vitest run"`.
    Создать `vitest.config.ts` с `resolve.alias: { '@': path.resolve(__dirname, 'src') }`, `test.environment: 'node'`.
    Создать `src/shared/lib/analytics-queries/filters.test.ts` с тестами (RED):
    1) `countableSessionWhere` содержит `{ isAdjustment: false }`
    2) `countableCompletionWhere` содержит `{ isPriorCredit: false }`
    3) `analyticsSessionFilter({ gte, lte })` объединяет date range + isAdjustment:false
    4) `analyticsCompletionFilter` объединяет createdAt range + isPriorCredit:false
    Не создавать `filters.ts` — только тесты.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/analytics-queries/filters.test.ts 2>&1 | findstr /C:"FAIL" /C:"Error" /C:"not found" || exit 0</automated>
  </verify>
  <done>Vitest настроен; unit-тесты фильтров существуют и падают (нет filters.ts или assertions fail)</done>
</task>

<task type="auto">
  <name>Task 2: E2E API-хелперы</name>
  <files>e2e/helpers/api.ts</files>
  <read_first>
    - e2e/helpers/auth.ts — паттерн loginAs через UI
    - e2e/global-setup.ts — DATABASE_URL, baseURL
    - playwright.config.ts — use.baseURL
  </read_first>
  <acceptance_criteria>
    - `e2e/helpers/api.ts` экспортирует `apiGetAs(code: string, path: string): Promise&lt;APIResponse&gt;`
    - Экспортирует `expectForbidden(response)` — status 403
    - Экспортирует `expectUnauthorized(response)` — status 401
    - Логин через credentials: POST `/api/auth/callback/credentials` или UI-flow с сохранением cookies в `APIRequestContext` (следовать next-auth 5 beta pattern проекта)
  </acceptance_criteria>
  <action>
    Создать `e2e/helpers/api.ts`:
    - `createAuthenticatedContext(code)` — получает session cookie для `APIRequestContext` (через `request.newContext` + login endpoint next-auth; если сложно — reuse `page` context storage state после `loginAs`).
    - `apiGetAs(code, path)` — GET с cookie сессии.
    - `apiPostAs(code, path, body?)` — POST JSON.
    - `expectForbidden` / `expectUnauthorized` — assert status + optional `json.error`.
    Использовать `TEST_CODES` из `e2e/helpers/codes.ts`. Сообщения assert на английском (Playwright convention); бизнес-error из API — на русском.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /C:"helpers/api" || echo "no ts errors in api helper"</automated>
  </verify>
  <done>Хелперы компилируются; готовы для import в spec-файлах</done>
</task>

<task type="auto">
  <name>Task 3: Stub E2E specs (RED) для FND-02/03/04</name>
  <files>e2e/api-auth.spec.ts, e2e/student-progress.spec.ts, e2e/domain-events.spec.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-VALIDATION.md — Per-Task Verification Map
    - .planning/codebase/CONCERNS.md — Known Bugs (students cross-group, sessions without date)
    - e2e/journal.spec.ts — структура spec-файла
  </read_first>
  <acceptance_criteria>
    - `e2e/api-auth.spec.ts` содержит test с title matching `/401/` — unauthenticated GET `/api/students?groupId=...` → 401
    - Содержит test `/cross-group/` — STUDENT `studentAli` + чужой groupId → 403
    - Содержит test `/sessions/` — STUDENT + чужой studentId без date → 403
    - `e2e/student-progress.spec.ts` — test: manager меняет progress → journal и student portal показывают один currentStepIdx
    - `e2e/domain-events.spec.ts` — test: после updateStudentProgress в БД есть AuditEvent
    - Все тесты используют `apiGetAs` / UI helpers из `e2e/helpers/`
  </acceptance_criteria>
  <action>
    Создать три spec-файла с `test.describe` и failing tests (ожидают поведение после plan 02–05):
    **api-auth.spec.ts**: группы из seed — teacher1/teacher2 разные группы; studentAli в group1.
    **student-progress.spec.ts**: login manager → admin student edit → assert journal step label (можно skip UI деталей, assert через API sessions/completions + student record).
    **domain-events.spec.ts**: после мутации — prisma query AuditEvent count ≥ 1 (через test-only helper или API если появится — пока direct prisma в global-setup pattern или fetch internal).
    Для domain-events: если нет test DB helper, использовать `test.skip` с TODO только если prisma из e2e недоступен — предпочтительно добавить `e2e/helpers/db.ts` с read-only prisma client из `.env.test`.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/api-auth.spec.ts --list 2>&1 | findstr /C:"401" /C:"cross-group" /C:"sessions"</automated>
  </verify>
  <done>Три spec-файла зарегистрированы в Playwright; тесты падают до реализации FND-02/03/04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → API | E2E тесты имитируют роли; не должны хардкодить production secrets |
| test → DB | Read-only assertions на AuditEvent — только `.env.test` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0-01 | Information disclosure | TEST_CODES в repo | accept | Seed-only dev credentials; документировано в README |
| T-0-02 | Tampering | e2e/helpers/db.ts | mitigate | Read-only Prisma client; без write из тестов кроме через API |
</threat_model>

<verification>
- `pnpm test:unit` — RED (filters.test.ts)
- `pnpm exec playwright test e2e/api-auth.spec.ts --list` — 3+ теста найдены
</verification>

<success_criteria>
- Vitest и Playwright API scaffolding готовы
- Все MISSING из 00-VALIDATION.md Wave 0 отмечены файлами (кроме migration — plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-foundation-01-SUMMARY.md`
</output>
