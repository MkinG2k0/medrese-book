---
phase: 11-groups-enrollment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - prisma/lib/subject-constants.ts
  - src/shared/lib/validations/group.ts
  - src/shared/lib/validations/enrollment.ts
  - src/shared/lib/validations/enrollment.test.ts
autonomous: true
requirements:
  - SUBJ-05
  - SUBJ-06
must_haves:
  truths:
    - Group has required subjectId FK to Subject per D-06 and SUBJ-05
    - GroupEnrollment junction is the only enrollment source per D-01 D-05
    - Student.groupId and Student.levelId columns are removed after backfill per D-01
    - Existing students and groups survive migration with Quran subject backfill
    - Zod schemas exist for group create (with subjectId) and enrollment payloads
  artifacts:
    - path: prisma/schema.prisma
      provides: Group.subjectId, GroupEnrollment model, Student without groupId/levelId
      contains: model GroupEnrollment
    - path: prisma/migrations/
      provides: Prod-safe migration with backfill and column drops
    - path: src/shared/lib/validations/group.ts
      provides: createGroupSchema with subjectId, updateGroupSchema without subjectId
    - path: src/shared/lib/validations/enrollment.ts
      provides: enrollStudentSchema and unenrollStudentSchema
    - path: src/shared/lib/validations/enrollment.test.ts
      provides: Unit tests for enrollment schema and level guard helper
  key_links:
    - from: prisma/migrations/
      to: prisma/lib/subject-constants.ts
      via: DEFAULT_QURAN_SUBJECT_ID in Group.subjectId UPDATE
    - from: GroupEnrollment
      to: Student and Group
      via: @@unique([studentId, groupId]) without unique on subjectId per D-10
    - from: GroupEnrollment.levelId
      to: Level
      via: onDelete Restrict; level validated against group.subjectId in later plans
---

<objective>
Добавить схему Group.subjectId, junction GroupEnrollment, prod-safe миграцию с backfill и Zod-валидации.

Purpose: SUBJ-05 и SUBJ-06 требуют предмет у группы и many-to-many зачисление (D-01..D-06).
Output: Мигрированная схема, константа backfill, модули validations/group.ts и validations/enrollment.ts, Wave 0 unit-тесты.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-groups-enrollment/11-CONTEXT.md
@.planning/phases/11-groups-enrollment/11-RESEARCH.md
@.planning/phases/11-groups-enrollment/11-PATTERNS.md
@prisma/schema.prisma
@prisma/migrations/20260707180000_add_subject/migration.sql
@prisma/lib/subject-constants.ts
@.cursor/rules/prisma-migrations.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group.subjectId и модель GroupEnrollment в schema</name>
  <files>prisma/schema.prisma</files>
  <action>
    Per D-01 D-05 D-06 SUBJ-05: модифицировать Group — добавить subjectId String, subject Subject @relation(onDelete: Restrict), enrollments GroupEnrollment[], @@index([subjectId]); удалить students Student[].

    Добавить model GroupEnrollment: id @id @default(cuid()), studentId, groupId, levelId, enrolledAt DateTime @default(now()); связи Student/Group/Level; onDelete Cascade на student и group, Restrict на level; @@unique([studentId, groupId]); @@index([groupId]); @@index([studentId]). Без @@unique([studentId, subjectId]) — D-10.

    Student: удалить groupId, levelId и связи group/level; добавить enrollments GroupEnrollment[]; сохранить currentStepIdx (Phase 12).

    Subject: добавить groups Group[]. Level: заменить students Student[] на enrollments GroupEnrollment[].

    Имя модели GroupEnrollment per RESEARCH discretion (не StudentGroup).
  </action>
  <verify>
    <automated>pnpm exec prisma validate</automated>
  </verify>
  <done>schema.prisma валидируется; GroupEnrollment и Group.subjectId определены; Student без groupId/levelId в схеме</done>
</task>

<task type="auto">
  <name>Task 2: Prod-safe миграция с backfill</name>
  <files>prisma/migrations/, prisma/lib/subject-constants.ts</files>
  <action>
    Per D-01 и prisma-migrations rule: pnpm db:migrate -- --name group_enrollment. Отредактировать migration.sql по образцу 20260707180000_add_subject:

    1) ALTER Group ADD subjectId nullable; UPDATE SET subjectId = DEFAULT_QURAN_SUBJECT_ID из prisma/lib/subject-constants.ts; SET NOT NULL; FK + index.
    2) CREATE TABLE GroupEnrollment с PK, FKs, unique(studentId, groupId), indexes.
    3) INSERT INTO GroupEnrollment (id, studentId, groupId, levelId, enrolledAt) SELECT gen_random_uuid()::text, id, groupId, levelId, NOW() FROM Student WHERE groupId IS NOT NULL.
    4) DROP FK и колонки Student.groupId, Student.levelId.

    Без TRUNCATE/DELETE существующих Student/Group. После migrate: pnpm exec prisma generate.

    Проверить COUNT Student с legacy groupId = COUNT GroupEnrollment до DROP (staging/local).
  </action>
  <verify>
    <automated>pnpm exec prisma validate && pnpm exec prisma migrate status</automated>
  </verify>
  <done>Миграция применена; все бывшие Student.groupId перенесены в GroupEnrollment; Group.subjectId NOT NULL</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Zod group/enrollment и unit-тесты (Wave 0)</name>
  <files>src/shared/lib/validations/group.ts, src/shared/lib/validations/enrollment.ts, src/shared/lib/validations/enrollment.test.ts</files>
  <behavior>
    - createGroupSchema отклоняет пустой subjectId с сообщением на русском (D-06)
    - updateGroupSchema не содержит поля subjectId (D-07)
    - enrollStudentSchema отклоняет пустой levelId
    - assertLevelBelongsToGroupSubject возвращает ошибку когда level.subjectId !== group.subjectId (T-11-01)
  </behavior>
  <action>
    Создать validations/group.ts: createGroupSchema (name min 1, teacherId, subjectId min 1 «Выберите предмет»); updateGroupSchema только name и teacherId — без subjectId per D-07. Экспорт типов CreateGroupInput, UpdateGroupInput.

    Создать validations/enrollment.ts: enrollStudentSchema (studentId, levelId обязательны); unenrollStudentSchema (studentId). Экспорт assertLevelBelongsToGroupSubject(groupSubjectId, levelSubjectId) — синхронный guard для unit-тестов; async DB-проверка будет в group-actions (plan 11-03).

    Создать enrollment.test.ts (Vitest): RED→GREEN для схем и guard. Паттерн mock из subject-actions.test.ts.

    Wave 0: файл enrollment.test.ts создаётся здесь — последующие планы ссылаются на него.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/validations/enrollment.test.ts</automated>
  </verify>
  <done>Unit-тесты enrollment зелёные; group/enrollment схемы компилируются</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL | Выполняется на prod DB; не должен терять привязки учеников к группам |
| validation input | Схемы будут принимать admin input в server actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | migration.sql | critical | mitigate | INSERT enrollment до DROP Student.groupId; только ADD/UPDATE/CREATE per prisma-migrations |
| T-11-02 | Tampering | enrollment guard | medium | mitigate | assertLevelBelongsToGroupSubject + server findFirst в plan 11-03 |
| T-11-03 | Information Disclosure | subject-constants.ts | low | accept | Фиксированный cuid для backfill — не секрет |
| T-11-SC | Tampering | npm installs | high | mitigate | Новых пакетов нет в фазе 11 |

</threat_model>

<verification>
pnpm exec prisma validate; migrate status applied; pnpm test:unit enrollment.test.ts green; generated client содержит GroupEnrollment.
</verification>

<success_criteria>
Group.subjectId NOT NULL; GroupEnrollment backfill завершён; Student без groupId/levelId; validations и Wave 0 unit-тесты готовы.
</success_criteria>

<output>
Create .planning/phases/11-groups-enrollment/11-01-SUMMARY.md when done
</output>
