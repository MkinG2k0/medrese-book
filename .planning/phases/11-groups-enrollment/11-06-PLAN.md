---
phase: 11-groups-enrollment
plan: 06
type: execute
wave: 5
depends_on:
  - "11-03"
files_modified:
  - src/features/user-admin/actions/user-actions.ts
  - src/shared/lib/validations/user.ts
  - src/features/user-admin/lib/map-users-to-details.ts
  - src/features/user-admin/ui/CreateUserForm.tsx
  - src/features/user-admin/ui/UserDetailModal.tsx
  - src/features/student-admin/actions/student-admin-actions.ts
  - src/app/(dashboard)/students/[studentId]/edit/page.tsx
  - src/app/(dashboard)/admin/users/page.tsx
  - e2e/admin.spec.ts
autonomous: true
requirements:
  - SUBJ-06
must_haves:
  truths:
    - createUsers создаёт Student без groupId/levelId и GroupEnrollment при переданных groupId+levelId
    - updateStudentUser не меняет group/level — только профиль per D-02
    - UserDetailModal не позволяет редактировать groupId/levelId у STUDENT per D-02
    - CreateUserForm каскадирует уровни по group.subjectId
    - getStudentProgressEdit/updateStudentProgress используют enrollment, не student.groupId/levelId
    - Admin E2E не падает после удаления Student.groupId
  artifacts:
    - path: src/features/user-admin/actions/user-actions.ts
      provides: createUsers с enrollment branch
    - path: src/features/user-admin/ui/UserDetailModal.tsx
      provides: Read-only или удалённые поля group/level для STUDENT
    - path: src/features/user-admin/ui/CreateUserForm.tsx
      provides: Cascade levels по group.subjectId
    - path: src/features/student-admin/actions/student-admin-actions.ts
      provides: getStudentProgressEdit/updateStudentProgress через enrollment
    - path: src/app/(dashboard)/admin/users/page.tsx
      provides: Groups с subjectId; без глобального getLevelsForCreateUser
  key_links:
    - from: createUsers
      to: groupEnrollment.create
      via: когда переданы groupId и levelId при STUDENT create
    - from: CreateUserForm
      to: getLevels(subjectId)
      via: cascade после выбора группы
    - from: UserDetailModal
      to: D-02
      via: зачисление только на /groups/[groupId]
---

<objective>
Адаптеры user-admin и student-admin для модели GroupEnrollment: создание ученика с enrollment, read-only group/level в модалке, progress edit через junction.

Purpose: SUBJ-06 — admin flows работают после удаления Student.groupId; D-02 соблюдён в UI.
Output: Обновлённые user-actions, validations, CreateUserForm, UserDetailModal, student-admin-actions, edit page, admin E2E.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-groups-enrollment/11-CONTEXT.md
@.planning/phases/11-groups-enrollment/11-RESEARCH.md
@.planning/phases/11-groups-enrollment/11-PATTERNS.md
@src/features/user-admin/actions/user-actions.ts
@src/shared/lib/validations/user.ts
@src/features/user-admin/ui/CreateUserForm.tsx
@src/features/user-admin/ui/UserDetailModal.tsx
@src/features/student-admin/actions/student-admin-actions.ts
@src/app/(dashboard)/students/[studentId]/edit/page.tsx
@e2e/admin.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: user-admin createUsers, validations и map-users</name>
  <files>src/features/user-admin/actions/user-actions.ts, src/shared/lib/validations/user.ts, src/features/user-admin/lib/map-users-to-details.ts</files>
  <action>
    Per D-02 и RESEARCH Pattern 5:

    createUsers STUDENT branch: student.create без groupId/levelId; после create если data.groupId и level — verify level.subjectId === group.subjectId; tx.groupEnrollment.create; revalidatePath /groups/${groupId}.

    Сохранить обязательные groupId+levelId в createUserFormSchema для bulk UX (A4).

    updateStudentUserSchema / updateStudentUserFormSchema: убрать groupId и levelId — профиль только per D-02 (RESOLVED Open Question 1).

    map-users-to-details: levelId/groupName с enrollment; UsersTable groupName — «N групп» или список enrollment groups (discretion).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json --pretty false 2>&1 | tee .tsc-11-06-t1.out; if rg -q "error TS.*(user-admin|validations/user|map-users)" .tsc-11-06-t1.out; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>createUsers создаёт enrollment; update не трогает group/level; map-users отражает enrollments</done>
</task>

<task type="auto">
  <name>Task 2: CreateUserForm, UserDetailModal, admin/users page и student-admin progress</name>
  <files>src/features/user-admin/ui/CreateUserForm.tsx, src/features/user-admin/ui/UserDetailModal.tsx, src/features/student-admin/actions/student-admin-actions.ts, src/app/(dashboard)/students/[studentId]/edit/page.tsx, src/app/(dashboard)/admin/users/page.tsx</files>
  <action>
    admin/users/page.tsx: убрать getLevelsForCreateUser() и статический prop levels; передать groups с subjectId (include subject в prisma.group.findMany); CreateUserForm/CreateUserModal каскадирует уровни по group.subjectId на клиенте.

    CreateUserForm per D-04: cascade levels после выбора группы — onGroupChange загружает getLevels(group.subjectId) или helper; level Select disabled до выбора группы; не использовать getLevelsForCreateUser() без subjectId.

    UserDetailModal per D-02: для роли STUDENT убрать или сделать read-only поля groupId и levelId; смена зачисления только на /groups/[groupId]; отображать список текущих групп read-only (опционально).

    student-admin-actions.ts: getStudentProgressEdit — levelId/groupId из enrollment (первая или контекстная); updateStudentProgress — обновлять enrollment.levelId где применимо, не student.levelId; revalidatePath для всех enrollment.groupId вместо student.groupId.

    students/[studentId]/edit/page.tsx: убрать group/level form fields или read-only; прогресс edit через enrollment model.

    Смена levelId существующего зачисления в UI — только при enroll (RESOLVED Open Question 2); глобальный progress edit остаётся tech debt до Phase 12.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json --pretty false 2>&1 | tee .tsc-11-06-t2.out; if rg -q "error TS.*(user-admin|student-admin|admin/users|students/\[studentId\]/edit)" .tsc-11-06-t2.out; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>Admin UI без TS-ошибок в scoped paths; UserDetailModal не редактирует group/level; student-admin использует enrollment</done>
</task>

<task type="auto">
  <name>Task 3: Regression E2E admin</name>
  <files>e2e/admin.spec.ts</files>
  <action>
    Обновить e2e/admin.spec.ts: создание ученика с группой+уровнем через enrollment flow; ожидать ученика в составе группы на /groups/[id].

    Зависит от seed-e2e из plan 11-05 (transitive через depends_on 11-03 → 11-05).
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/admin.spec.ts</automated>
  </verify>
  <done>Admin E2E зелёный после user-admin adapter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → createUsers | Недоверенный groupId/levelId при bulk create |
| UserDetailModal | Попытка сменить зачисление вне страницы группы |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09 | Tampering | createUsers enrollment | high | mitigate | level.subjectId === group.subjectId в transaction |
| T-11-13 | Tampering | UserDetailModal | medium | mitigate | Remove/disable groupId levelId edit per D-02 |
| T-11-14 | Tampering | updateStudentProgress | medium | mitigate | Update enrollment.levelId not student.levelId |
| T-11-SC | Tampering | npm installs | high | mitigate | Без новых пакетов |

</threat_model>

<verification>
Scoped tsc tasks 1–2 green; full repo tsc — plan 11-04 Task 4; pnpm test:e2e admin green; manual create student → visible in group enrollment table.
</verification>

<success_criteria>
Admin flows работают с GroupEnrollment; D-02 соблюдён в UserDetailModal; CreateUserForm каскадирует уровни по предмету группы.
</success_criteria>

<output>
Create .planning/phases/11-groups-enrollment/11-06-SUMMARY.md when done
</output>
