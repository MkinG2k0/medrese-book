---
phase: 11-groups-enrollment
plan: 04
type: execute
wave: 5
depends_on:
  - "11-03"
files_modified:
  - src/app/api/students/route.ts
  - src/app/api/students/risk-flags/route.ts
  - src/app/api/step-completions/route.ts
  - src/shared/lib/authorize-api-request.ts
  - src/shared/lib/authorize-student.ts
  - src/shared/lib/authorize-student-access.ts
  - src/shared/lib/student-progress/recalculate.ts
  - src/features/program-admin/actions/program-actions.ts
  - src/shared/lib/messaging/can-message-user.ts
  - src/features/accounting/lib/query-student-payments.ts
  - src/features/journal/actions/journal-actions.ts
  - src/features/student-portal/actions/student-actions.ts
  - src/shared/lib/student-metrics/load-student-metrics.ts
  - src/shared/lib/analytics-queries/level-stats.ts
  - e2e/journal.spec.ts
  - e2e/api-auth.spec.ts
autonomous: true
requirements:
  - SUBJ-06
must_haves:
  truths:
    - GET /api/students?groupId= возвращает учеников через GroupEnrollment join
    - authorize-api-request проверяет membership через GroupEnrollment per T-11-03
    - risk-flags, step-completions, student-portal, metrics и level-stats не читают student.groupId
    - recalculateStudentStepIdx использует enrollment.level вместо student.level (interim: первая enrollment)
    - deleteLevel считает enrollments, не student.count({ levelId })
    - authorize-student, authorize-student-access, messaging, accounting не читают student.group
    - Полный repo tsc зелёный; rg student.groupId|group.students|student.levelId → 0 в src/
    - Journal E2E не падает после удаления Student.groupId
  artifacts:
    - path: src/app/api/students/route.ts
      provides: Enrollment-based student list для журнала
    - path: src/shared/lib/authorize-api-request.ts
      provides: GroupEnrollment check для STUDENT role
    - path: src/app/api/students/risk-flags/route.ts
      provides: Risk flags через enrollment membership
    - path: src/app/api/step-completions/route.ts
      provides: Step completions без student.groupId
    - path: src/shared/lib/student-progress/recalculate.ts
      provides: Progress recalc через enrollment.level (interim Phase 12 tech debt)
    - path: src/features/program-admin/actions/program-actions.ts
      provides: deleteLevel enrollment count guard
  key_links:
    - from: /api/students
      to: group.enrollments
      via: map levelNumber/levelTitle из enrollment.level
    - from: authorize-api-request
      to: GroupEnrollment
      via: studentId_groupId unique lookup
    - from: load-student-metrics
      to: GroupEnrollment
      via: enrollment-based group resolution
---

<objective>
API, journal и read-path адаптеры для модели GroupEnrollment без полного subject-scoped журнала (Phase 13).

Purpose: SUBJ-06 — journal/API/analytics paths работают после удаления Student.groupId.
Output: Обновлённые API routes, auth, journal-actions, metrics/analytics queries, regression E2E journal/api-auth.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-groups-enrollment/11-CONTEXT.md
@.planning/phases/11-groups-enrollment/11-RESEARCH.md
@.planning/phases/11-groups-enrollment/11-PATTERNS.md
@src/app/api/students/route.ts
@src/app/api/students/risk-flags/route.ts
@src/app/api/step-completions/route.ts
@src/shared/lib/authorize-api-request.ts
@src/shared/lib/authorize-student.ts
@src/shared/lib/authorize-student-access.ts
@src/shared/lib/student-progress/recalculate.ts
@src/features/program-admin/actions/program-actions.ts
@src/shared/lib/messaging/can-message-user.ts
@src/features/accounting/lib/query-student-payments.ts
@src/features/journal/actions/journal-actions.ts
@src/features/student-portal/actions/student-actions.ts
@src/shared/lib/student-metrics/load-student-metrics.ts
@src/shared/lib/analytics-queries/level-stats.ts
@e2e/journal.spec.ts
@e2e/api-auth.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Journal API и authorize-api-request через enrollments</name>
  <files>src/app/api/students/route.ts, src/shared/lib/authorize-api-request.ts, src/features/journal/actions/journal-actions.ts</files>
  <action>
    Per RESEARCH Pattern 4 и T-11-03 — минимальный adapter (не full subject journal):

    api/students/route.ts: заменить group.students на group.enrollments include student.user, enrollment.level, sessions; map students с levelNumber/levelTitle из enrollment.level; filter isJournalVisibleStatus.

    authorize-api-request.ts: STUDENT + ctx.groupId — groupEnrollment.findUnique({ studentId_groupId }); forbidden если нет записи. Teacher paths: где читался student.groupId — enrollment-based lookup; не ломать findFirst getTeacherGroup (tech debt Phase 13).

    journal-actions.ts: минимальные правки где student.groupId / student.group.students — использовать enrollment или первую enrollment группы ученика; сохранить currentStepIdx глобальный (Phase 12). Цель — tsc + journal E2E green, не полный subject refactor.

    Не добавлять subjectId в session API — Phase 12.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json --pretty false 2>&1 | tee .tsc-11-04-t1.out; if rg -q "error TS.*(api/students|authorize-api|journal-actions)" .tsc-11-04-t1.out; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>Journal API paths без TS-ошибок; API и journal-actions используют enrollments вместо student.groupId</done>
</task>

<task type="auto">
  <name>Task 2: recalculate.ts и program-actions deleteLevel</name>
  <files>src/shared/lib/student-progress/recalculate.ts, src/features/program-admin/actions/program-actions.ts</files>
  <action>
    recalculate.ts (критично для journal E2E save-lesson path через step-completions/sessions):

    Заменить include student.level на enrollment-based level load: findFirst GroupEnrollment для studentId (orderBy createdAt asc — interim «primary» enrollment); include level.steps; если нет enrollment — early return.

    При auto-promote на следующий уровень: обновлять enrollment.levelId (не student.levelId — поле удалено в 11-01); currentStepIdx на student остаётся глобальным (Phase 12 subject-scoped progress).

    deleteLevel в program-actions.ts: заменить `prisma.student.count({ where: { levelId } })` на `prisma.groupEnrollment.count({ where: { levelId } })`; сообщение об ошибке на русском если count &gt; 0.

    Вызывается из step-completions/route.ts, sessions/route.ts, student-admin-actions (11-06), user-actions (11-06) — после этой задачи compile gap закрыт для save-lesson.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/shared/lib/student-progress/recalculate.test.ts 2>/dev/null || pnpm exec tsc --noEmit -p tsconfig.json --pretty false 2>&1 | tee .tsc-11-04-t2.out; if rg -q "error TS.*(recalculate|program-actions)" .tsc-11-04-t2.out; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>recalculate и deleteLevel компилируются; levelId пишется в enrollment, не в student</done>
</task>

<task type="auto">
  <name>Task 3: Auth helpers, messaging, accounting и read-path адаптеры</name>
  <files>src/shared/lib/authorize-student.ts, src/shared/lib/authorize-student-access.ts, src/shared/lib/messaging/can-message-user.ts, src/features/accounting/lib/query-student-payments.ts, src/app/api/students/risk-flags/route.ts, src/app/api/step-completions/route.ts, src/features/student-portal/actions/student-actions.ts, src/shared/lib/student-metrics/load-student-metrics.ts, src/shared/lib/analytics-queries/level-stats.ts</files>
  <action>
    Полный sweep legacy groupId/group.students/level.students в read paths (явный список):

    authorize-student.ts: teacherId check через enrollment.group.teacherId (или первую enrollment группы ученика).

    authorize-student-access.ts: session.user.teacherId !== student.group.teacherId → enrollment-based group lookup.

    can-message-user.ts: target.student.group.teacherId → enrollment join.

    query-student-payments.ts: student.group.name → enrollment group name (первая enrollment или join).

    risk-flags/route.ts: заменить group.students на enrollment join.

    step-completions/route.ts: убрать student.groupId; buildTeachingSessionDurationByDate через enrollment.groupId.

    student-portal/actions: group resolution и level steps через enrollment.level.

    load-student-metrics.ts: метрики через enrollment.level и enrollment.groupId.

    level-stats.ts: заменить level.students на groupEnrollment count/join.

    После правок: `rg "student\.groupId|student\.group|group\.students|student\.levelId|student\.level|level\.students" src/ --glob "*.{ts,tsx}"` → 0 (кроме комментариев/Phase 12 TODO если явно помечены).
  </action>
  <verify>
    <automated>rg "student\.groupId|student\.group|group\.students|student\.levelId|level\.students" src/ --glob "*.{ts,tsx}" -l | rg -v "recalculate|Phase 12" && exit 1 || exit 0</automated>
  </verify>
  <done>Все перечисленные файлы адаптированы; legacy relation strings не остались в src/</done>
</task>

<task type="auto">
  <name>Task 4: Полный compile gate (repo-wide tsc)</name>
  <files>src/</files>
  <action>
    Финальный compile gate волны 5: `pnpm exec tsc --noEmit -p tsconfig.json` без фильтра — весь проект зелёный после 11-04 tasks 1–3 и параллельного 11-06.

    Если ошибки остались в user-admin (11-06) — координировать: 11-04 и 11-06 одна волна, executor завершает оба плана перед этим task или запускает tsc после merge обоих.

    `pnpm build` smoke опционально если tsc green.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Полный repo tsc зелёный; Phase 11 compile gate пройден</done>
</task>

<task type="auto">
  <name>Task 5: Regression E2E journal и api-auth</name>
  <files>e2e/journal.spec.ts, e2e/api-auth.spec.ts</files>
  <action>
    e2e/journal.spec.ts: учитель видит список учеников группы через обновлённый API — smoke select group + student list.

    e2e/api-auth.spec.ts: STUDENT access к своей группе через enrollment check.

    Зависит от seed-e2e из plan 11-05 (transitive через depends_on 11-03 → 11-05). user-admin E2E — plan 11-06.
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/journal.spec.ts e2e/api-auth.spec.ts</automated>
  </verify>
  <done>Journal и api-auth E2E зелёные после adapter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/students | Недоверенный groupId query param |
| STUDENT role API | Доступ только к своим enrollments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Information Disclosure | /api/students | high | mitigate | authorizeApiRequest + enrollment membership check |
| T-11-10 | Elevation | authorize-api-request | high | mitigate | GroupEnrollment composite lookup вместо student.groupId |
| T-11-12 | Information Disclosure | risk-flags/step-completions | high | mitigate | enrollment-based auth в каждом route |
| T-11-SC | Tampering | npm installs | high | mitigate | Без новых пакетов |

</threat_model>

<verification>
Task 4: pnpm exec tsc --noEmit full repo green; Task 3: rg legacy strings → 0; Task 5: pnpm test:e2e journal api-auth green; manual GET /api/students?groupId= returns enrolled students.
</verification>

<success_criteria>
Journal/API/analytics read paths работают с GroupEnrollment; Student.groupId нигде не читается в API layer.
</success_criteria>

<output>
Create .planning/phases/11-groups-enrollment/11-04-SUMMARY.md when done
</output>
