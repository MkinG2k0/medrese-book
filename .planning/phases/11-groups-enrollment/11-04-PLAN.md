---
phase: 11-groups-enrollment
plan: 04
type: execute
wave: 4
depends_on:
  - "11-03"
  - "11-05"
files_modified:
  - src/app/api/students/route.ts
  - src/shared/lib/authorize-api-request.ts
  - src/features/user-admin/actions/user-actions.ts
  - src/shared/lib/validations/user.ts
  - src/features/user-admin/lib/map-users-to-details.ts
  - src/features/journal/actions/journal-actions.ts
  - src/features/student-admin/actions/student-admin-actions.ts
  - e2e/admin.spec.ts
  - e2e/journal.spec.ts
  - e2e/api-auth.spec.ts
autonomous: true
requirements:
  - SUBJ-06
must_haves:
  truths:
    - GET /api/students?groupId= возвращает учеников через GroupEnrollment join
    - authorize-api-request проверяет membership через GroupEnrollment per T-11-03
    - createUsers создаёт Student без groupId/levelId и опционально GroupEnrollment
    - updateStudentUser не меняет group/level — только профиль per D-02
    - Journal и E2E не падают после удаления Student.groupId
  artifacts:
    - path: src/app/api/students/route.ts
      provides: Enrollment-based student list для журнала
    - path: src/shared/lib/authorize-api-request.ts
      provides: GroupEnrollment check для STUDENT role
    - path: src/features/user-admin/actions/user-actions.ts
      provides: createUsers с enrollment branch
  key_links:
    - from: /api/students
      to: group.enrollments
      via: map levelNumber/levelTitle из enrollment.level
    - from: createUsers
      to: groupEnrollment.create
      via: когда переданы groupId и levelId при STUDENT create
    - from: authorize-api-request
      to: GroupEnrollment
      via: studentId_groupId unique lookup
---

<objective>
Минимальные адаптеры journal/API и user-admin для модели GroupEnrollment без полного subject-scoped журнала (Phase 13).

Purpose: SUBJ-06 — система работает после удаления Student.groupId; журнал читает состав группы через junction.
Output: Обновлённые API/auth/user-admin/journal paths, зелёные regression E2E.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-groups-enrollment/11-CONTEXT.md
@.planning/phases/11-groups-enrollment/11-RESEARCH.md
@.planning/phases/11-groups-enrollment/11-PATTERNS.md
@src/app/api/students/route.ts
@src/shared/lib/authorize-api-request.ts
@src/features/user-admin/actions/user-actions.ts
@src/shared/lib/validations/user.ts
@src/features/journal/actions/journal-actions.ts
@e2e/admin.spec.ts
@e2e/journal.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Journal API и authorize-api-request через enrollments</name>
  <files>src/app/api/students/route.ts, src/shared/lib/authorize-api-request.ts, src/features/journal/actions/journal-actions.ts</files>
  <action>
    Per RESEARCH Pattern 4 и T-11-03 — минимальный adapter (не full subject journal):

    api/students/route.ts: заменить group.students на group.enrollments include student.user, enrollment.level, sessions; map students с levelNumber/levelTitle из enrollment.level; filter isJournalVisibleStatus.

    authorize-api-request.ts: STUDENT + ctx.groupId — groupEnrollment.findUnique({ studentId_groupId }); forbidden если нет записи. Teacher paths: где читался student.groupId — enrollment-based lookup; не ломать findFirst getTeacherGroup (tech debt Phase 13).

    journal-actions.ts: минимальные правки где student.groupId / student.group.students — использовать enrollment или первую enrollment группы ученика; сохранить currentStepIdx глобальный (Phase 12). Цель — tsc + journal E2E green, не полный subject refactor.

    Не добавлять subjectId в session API — Phase 12.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "api/students authorize-api-request journal-actions" || echo journal-adapter-ok</automated>
  </verify>
  <done>tsc чистый для adapter файлов; API использует enrollments</done>
</task>

<task type="auto">
  <name>Task 2: user-admin createUsers и validations</name>
  <files>src/features/user-admin/actions/user-actions.ts, src/shared/lib/validations/user.ts, src/features/user-admin/lib/map-users-to-details.ts, src/features/student-admin/actions/student-admin-actions.ts</files>
  <action>
    Per D-02 и RESEARCH Pattern 5:

    createUsers STUDENT branch: student.create без groupId/levelId; после create если data.groupId и level — verify level.subjectId === group.subjectId; tx.groupEnrollment.create; revalidatePath /groups/${groupId}.

    Сохранить обязательные groupId+levelId в createUserFormSchema для bulk UX (A4).

    updateStudentUserSchema / updateStudentUserFormSchema: убрать groupId и levelId — профиль только per D-02.

    map-users-to-details: levelId с enrollment если caller передаёт на student object; UsersTable groupName — минимально «N групп» или первая enrollment (discretion).

    student-admin-actions: revalidatePath для всех enrollment.groupId вместо student.groupId; не обновлять student.levelId напрямую.

    CreateUserForm: cascade levels после выбора группы — load levels по group.subjectId (getLevels или helper).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "user-admin student-admin validations/user" || echo user-admin-ok</automated>
  </verify>
  <done>createUsers создаёт enrollment; update не трогает group/level</done>
</task>

<task type="auto">
  <name>Task 3: Regression E2E admin, journal, api-auth</name>
  <files>e2e/admin.spec.ts, e2e/journal.spec.ts, e2e/api-auth.spec.ts</files>
  <action>
    Обновить e2e/admin.spec.ts: создание ученика с группой+уровнем через enrollment flow; ожидать ученика в составе группы на /groups/[id].

    e2e/journal.spec.ts: учитель видит список учеников группы через обновлённый API — smoke select group + student list.

    e2e/api-auth.spec.ts: STUDENT access к своей группе через enrollment check.

    Зависит от seed-e2e из plan 11-05 (GroupEnrollment data).
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/admin.spec.ts e2e/journal.spec.ts e2e/api-auth.spec.ts</automated>
  </verify>
  <done>Regression E2E зелёные после adapter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/students | Недоверенный groupId query param |
| STUDENT role API | Доступ только к своим enrollments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Information Disclosure | /api/students | high | mitigate | authorizeApiRequest + enrollment membership check |
| T-11-09 | Tampering | createUsers enrollment | high | mitigate | level.subjectId === group.subjectId в transaction |
| T-11-10 | Elevation | authorize-api-request | high | mitigate | GroupEnrollment composite lookup вместо student.groupId |
| T-11-SC | Tampering | npm installs | high | mitigate | Без новых пакетов |

</threat_model>

<verification>
tsc clean; pnpm test:e2e admin journal api-auth green; manual GET /api/students?groupId= returns enrolled students.
</verification>

<success_criteria>
Журнал и admin flows работают с GroupEnrollment; Student.groupId нигде не читается в критических paths.
</success_criteria>

<output>
Create .planning/phases/11-groups-enrollment/11-04-SUMMARY.md when done
</output>
