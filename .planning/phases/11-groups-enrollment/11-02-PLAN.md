---
phase: 11-groups-enrollment
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/features/groups/actions/group-actions.ts
  - src/features/groups/actions/group-actions.test.ts
  - src/features/groups/ui/GroupsList.tsx
  - src/features/groups/ui/CreateGroupForm.tsx
  - src/features/groups/ui/EditGroupForm.tsx
  - src/app/(dashboard)/groups/page.tsx
  - e2e/groups-enrollment.spec.ts
autonomous: true
requirements:
  - SUBJ-05
  - SUBJ-07
must_haves:
  truths:
    - Менеджер создаёт группу только с выбранным предметом per D-06 SUBJ-07
    - При редактировании группы предмет read-only per D-07
    - Список групп показывает колонку «Предмет» и фильтр по предмету per D-08
    - createGroup отклоняет отсутствие subjectId на сервере per T-11-04
  artifacts:
    - path: src/features/groups/actions/group-actions.ts
      provides: getGroups/createGroup/updateGroup с subjectId и enrollments count
    - path: src/features/groups/ui/GroupsList.tsx
      provides: Колонка Предмет и фильтр
    - path: src/features/groups/ui/CreateGroupForm.tsx
      provides: Select предмета обязательный
    - path: src/features/groups/ui/EditGroupForm.tsx
      provides: Read-only поле предмета
    - path: e2e/groups-enrollment.spec.ts
      provides: Wave 0 E2E scaffold для SUBJ-05 и SUBJ-07
  key_links:
    - from: CreateGroupForm
      to: createGroupSchema
      via: subjectId в payload
    - from: groups/page.tsx
      to: getSubjects
      via: subjects prop для фильтра GroupsList
    - from: updateGroup
      to: updateGroupSchema
      via: subjectId исключён из parse (D-07)
---

<objective>
Привязать группы к предметам в CRUD и UI списка: создание с обязательным предметом, read-only при edit, колонка и фильтр.

Purpose: SUBJ-05, SUBJ-07 — менеджер назначает предмет при создании; одна группа = один предмет (D-06..D-09).
Output: Обновлённые group-actions, формы, GroupsList, Wave 0 E2E scaffold.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-groups-enrollment/11-CONTEXT.md
@.planning/phases/11-groups-enrollment/11-PATTERNS.md
@src/features/groups/actions/group-actions.ts
@src/features/groups/ui/GroupsList.tsx
@src/features/groups/ui/CreateGroupForm.tsx
@src/features/groups/ui/EditGroupForm.tsx
@src/app/(dashboard)/groups/page.tsx
@src/features/subject-admin/actions/subject-actions.ts
@src/shared/lib/validations/group.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: group-actions с subjectId и unit-тесты (Wave 0)</name>
  <files>src/features/groups/actions/group-actions.ts, src/features/groups/actions/group-actions.test.ts</files>
  <behavior>
    - createGroup без subjectId падает на Zod (T-11-04)
    - createGroup сохраняет subjectId в prisma.group.create
    - updateGroup игнорирует subjectId даже если передан в input (D-07)
    - getGroups включает subject и _count.enrollments
  </behavior>
  <action>
    Рефакторить group-actions.ts: импорт createGroupSchema/updateGroupSchema из validations/group.ts; удалить inline Zod.

    getGroups: include subject, _count enrollments; orderBy name.
    getGroup/getMyGroup: заменить students на enrollments с student.user и level (подготовка к plan 11-03).
    createGroup: data включает subjectId per D-06.
    updateGroup: только name, teacherId per D-07 — subjectId не в schema.

    Создать group-actions.test.ts (Vitest, mock prisma + requireRoles): RED→GREEN для create/update/get. Wave 0 scaffold.

    Не добавлять enrollStudent/unenrollStudent здесь — plan 11-03.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/features/groups/actions/group-actions.test.ts</automated>
  </verify>
  <done>group-actions тесты зелёные; createGroup требует subjectId; updateGroup не меняет предмет</done>
</task>

<task type="auto">
  <name>Task 2: Формы и список групп с предметом</name>
  <files>src/features/groups/ui/GroupsList.tsx, src/features/groups/ui/CreateGroupForm.tsx, src/features/groups/ui/EditGroupForm.tsx, src/app/(dashboard)/groups/page.tsx</files>
  <action>
    Per D-06 D-07 D-08 D-09:

    CreateGroupForm: prop subjects; Form.Item subjectId первым полем с Select; name, teacherId без level picker (D-09).

    EditGroupForm: prop subjectName; read-only Input disabled для предмета — не в submit; только name и teacherId.

    GroupsList: extend GroupRow subjectId, subjectName; колонка «Предмет»; фильтр по subjectId (паттерн UsersTable groupFilters); studentCount из enrollments.

    groups/page.tsx: Promise.all getGroups, getTeachers, getSubjects; map subjectName, studentCount; передать subjects в GroupsList и CreateGroupForm через модалку.

    App.useApp() для message/modal — не статический antd import.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | findstr /I "groups/ui groups/page" || echo groups-ui-ok</automated>
  </verify>
  <done>UI компилируется; формы отражают D-06..D-09</done>
</task>

<task type="auto">
  <name>Task 3: E2E scaffold — предмет при создании и фильтр (Wave 0)</name>
  <files>e2e/groups-enrollment.spec.ts</files>
  <action>
    Создать e2e/groups-enrollment.spec.ts (Wave 0): auth manager из e2e/helpers/codes.ts.

    Сценарии SUBJ-05/SUBJ-07:
    - Менеджер создаёт группу с выбором предмета в модалке
    - Редактирование: поле предмета disabled/read-only
    - Список /groups: колонка «Предмет» видна; фильтр по предмету сужает строки

    Role guard: TEACHER не видит /groups (если middleware так настроен — expect redirect).

    Уникальные имена через Date.now(). Тесты могут падать до полного enrollment flow — plan 11-03 расширит файл.
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/groups-enrollment.spec.ts --grep "предмет|фильтр|создаёт группу"</automated>
  </verify>
  <done>E2E scaffold существует; сценарии subject create/edit/filter проходят или задокументированы как pending enrollment</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server actions | Недоверенный admin input для create/update group |
| mass assignment | Попытка сменить subjectId при update |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | createGroup | high | mitigate | subjectId обязателен в createGroupSchema; Zod parse до prisma |
| T-11-05 | Tampering | updateGroup | high | mitigate | updateGroupSchema без subjectId; strip unknown fields |
| T-11-06 | Spoofing | group-actions | high | mitigate | requireRoles MANAGER SUPER_ADMIN на каждый export |
| T-11-SC | Tampering | npm installs | high | mitigate | Без новых пакетов |

</threat_model>

<verification>
pnpm test:unit group-actions.test.ts green; tsc clean для groups UI; E2E subject scenarios run.
</verification>

<success_criteria>
Группа создаётся с предметом; edit не меняет предмет; список показывает и фильтрует по предмету.
</success_criteria>

<output>
Create .planning/phases/11-groups-enrollment/11-02-SUMMARY.md when done
</output>
