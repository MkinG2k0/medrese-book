---
phase: 11-groups-enrollment
plan: 03
type: execute
wave: 4
depends_on:
  - "11-02"
  - "11-05"
files_modified:
  - src/features/groups/actions/group-actions.ts
  - src/features/groups/ui/GroupStudentsTable.tsx
  - src/features/groups/ui/EnrollStudentModal.tsx
  - src/app/(dashboard)/groups/[groupId]/page.tsx
  - e2e/groups-enrollment.spec.ts
autonomous: true
requirements:
  - SUBJ-06
must_haves:
  truths:
    - Зачисление и снятие ученика только на /groups/[groupId] per D-02
    - Модалка выбирает ученика из всех студентов системы per D-03
    - При зачислении обязателен уровень предмета группы per D-04 D-05
    - Ученик может быть в двух группах одного предмета per D-10 D-11
    - Уровень на enrollment независим для каждой пары ученик+группа
    - enrollStudent отклоняет levelId другого предмета per T-11-02
  artifacts:
    - path: src/features/groups/actions/group-actions.ts
      provides: enrollStudent, unenrollStudent, searchStudentsForEnroll, getGroupLevels
    - path: src/features/groups/ui/EnrollStudentModal.tsx
      provides: Модалка зачисления с picker ученика и уровня
    - path: src/features/groups/ui/GroupStudentsTable.tsx
      provides: Кнопка «Добавить ученика», unenroll, level из enrollment
    - path: src/app/(dashboard)/groups/[groupId]/page.tsx
      provides: Wiring EnrollStudentModal и canManageEnrollment (данные enrollments — plan 11-02)
  key_links:
    - from: EnrollStudentModal
      to: getLevels(subjectId)
      via: subject-scoped level options
    - from: enrollStudent
      to: prisma.level.findFirst
      via: id + subjectId === group.subjectId guard
    - from: GroupStudentsTable
      to: unenrollStudent
      via: modal.confirm + server action
---

<objective>
Реализовать зачисление и снятие учеников: EnrollStudentModal, enrollment server actions, wiring на странице группы.

Purpose: SUBJ-06 — many-to-many зачисление с levelId per enrollment (D-02..D-05, D-10, D-11). getGroup/pages — plan 11-02.
Output: enrollStudent/unenrollStudent actions, EnrollStudentModal, GroupStudentsTable с кнопками, E2E multi-enrollment (после seed-e2e 11-05).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-groups-enrollment/11-CONTEXT.md
@.planning/phases/11-groups-enrollment/11-PATTERNS.md
@src/features/groups/actions/group-actions.ts
@src/features/groups/ui/GroupStudentsTable.tsx
@src/app/(dashboard)/groups/[groupId]/page.tsx
@src/features/program-admin/actions/program-actions.ts
@src/shared/lib/validations/enrollment.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrollment server actions и unit-тесты</name>
  <files>src/features/groups/actions/group-actions.ts, src/features/groups/actions/group-actions.test.ts</files>
  <behavior>
    - enrollStudent создаёт GroupEnrollment при валидном level ∈ group.subject
    - enrollStudent бросает ошибку когда level.subjectId ≠ group.subjectId (T-11-02)
    - enrollStudent отклоняет дубликат studentId+groupId (unique constraint)
    - unenrollStudent удаляет запись enrollment
    - searchStudentsForEnroll исключает уже зачисленных в эту группу
  </behavior>
  <action>
    Добавить в group-actions.ts per D-02..D-05 (getGroup/getMyGroup уже на enrollments в plan 11-02 — не дублировать):

    enrollStudent(groupId, input): parse enrollStudentSchema; load group.subjectId; prisma.level.findFirst({ id: levelId, subjectId }); если нет — throw «Уровень не принадлежит предмету группы»; groupEnrollment.create; revalidatePath /groups/${groupId}, /groups, /journal.

    unenrollStudent(groupId, studentId): parse unenrollStudentSchema; delete where studentId_groupId composite.

    searchStudentsForEnroll(groupId, query?): findMany students с user.name search; exclude IDs уже в group enrollments; return { id, name }[] — все ученики системы per D-03.

    getGroupLevels(groupId): resolve group.subjectId → getLevels pattern из program-actions.

    Расширить group-actions.test.ts: mock enroll/unenroll/guard cases. RED→GREEN.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/features/groups/actions/group-actions.test.ts</automated>
  </verify>
  <done>Enrollment action тесты зелёные; level guard и unique enrollment проверены</done>
</task>

<task type="auto">
  <name>Task 2: EnrollStudentModal, GroupStudentsTable и wiring на странице</name>
  <files>src/features/groups/ui/EnrollStudentModal.tsx, src/features/groups/ui/GroupStudentsTable.tsx, src/app/(dashboard)/groups/[groupId]/page.tsx</files>
  <action>
    Per D-02 D-03 D-04:

    EnrollStudentModal (новый): props groupId, subjectId, open, onClose, onSuccess. Form: Select ученика showSearch из searchStudentsForEnroll; Select уровня из getLevels(subjectId); submit enrollStudent via useTransition; App.useApp message.success «Ученик зачислен».

    GroupStudentsTable: props groupId, subjectId, canManageEnrollment; кнопка «Добавить ученика»; колонка Уровень из enrollment levelTitle (не student.levelId); действие «Снять с группы» с modal.confirm (паттерн SubjectsList); state для EnrollStudentModal.

    groups/[groupId]/page.tsx: передать canManageEnrollment, subjectId, groupId в GroupStudentsTable; render EnrollStudentModal (enrollment data mapping уже в plan 11-02).

    UserDetailModal: правки group/level — plan 11-06 (read-only/remove edit per D-02).

    Сохранить колонку «Текущий шаг» из currentStepIdx (known Phase 12 limitation).

    Scoped compile gate: полный repo tsc отложен до 11-04 Task 4 — здесь только groups enrollment UI paths.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json --pretty false 2>&1 | tee .tsc-11-03.out; if rg -q "error TS.*(features/groups|groups/)" .tsc-11-03.out; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>Enrollment UI без TS-ошибок в scoped paths; level из enrollment отображается</done>
</task>

<task type="auto">
  <name>Task 3: E2E multi-enrollment (SUBJ-06)</name>
  <files>e2e/groups-enrollment.spec.ts</files>
  <action>
    Расширить e2e/groups-enrollment.spec.ts SUBJ-06 (зависит от seed-e2e plan 11-05 via depends_on):

    - Зачислить ученика с выбором уровня на странице группы через EnrollStudentModal
    - Зачислить того же ученика во вторую группу (тот же предмет допустим per D-10)
    - Проверить независимые уровни в двух группах (D-11) — разные level в UI

    Использовать TEST_USERS; router refresh после enroll. Не дублировать миграцию страниц — данные enrollments уже из plan 11-02.
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/groups-enrollment.spec.ts --grep "зачисл|групп"</automated>
  </verify>
  <done>E2E зачисления проходят; EnrollStudentModal и unenroll работают на странице группы</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → enrollStudent | Недоверенный studentId и levelId |
| IDOR | enroll/unenroll в чужую группу |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02 | Tampering | enrollStudent | high | mitigate | prisma.level.findFirst с subjectId группы; ошибка на русском |
| T-11-07 | Elevation | enrollStudent | high | mitigate | requireRoles + groupId из параметра action |
| T-11-08 | Repudiation | unenrollStudent | medium | mitigate | requireRoles; confirm modal в UI |
| T-11-SC | Tampering | npm installs | high | mitigate | Без новых пакетов |

</threat_model>

<verification>
pnpm test:unit group-actions enrollment tests green; E2E enroll scenarios green; manual smoke /groups/[id] enroll+unenroll.
</verification>

<success_criteria>
Менеджер зачисляет/снимает учеников на странице группы с уровнем; multi-group same subject работает.
</success_criteria>

<output>
Create .planning/phases/11-groups-enrollment/11-03-SUMMARY.md when done
</output>
