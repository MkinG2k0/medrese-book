---
phase: 11-groups-enrollment
plan: 05
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - prisma/seed.ts
  - prisma/seed-e2e.ts
autonomous: true
requirements:
  - SUBJ-05
  - SUBJ-06
must_haves:
  truths:
    - seed.ts создаёт группы с subjectId и GroupEnrollment вместо Student.groupId
    - seed-e2e.ts использует GroupEnrollment для тестовых учеников
    - Wipe order включает groupEnrollment.deleteMany перед student.deleteMany
    - E2E и local dev получают консистентные данные зачисления
  artifacts:
    - path: prisma/seed.ts
      provides: Demo groups с subjectId и enrollments
    - path: prisma/seed-e2e.ts
      provides: E2E fixtures через GroupEnrollment
  key_links:
    - from: seed.ts Group.create
      to: DEFAULT_QURAN_SUBJECT_ID
      via: subjectId на каждой группе
    - from: seed Student create
      to: GroupEnrollment.create
      via: перенос бывших groupId+levelId
---

<objective>
Обновить seed-скрипты для модели Group.subjectId и GroupEnrollment.

Purpose: SUBJ-05/SUBJ-06 — локальная и E2E среда отражают новую модель зачисления после миграции plan 11-01.
Output: Обновлённые prisma/seed.ts и prisma/seed-e2e.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-groups-enrollment/11-CONTEXT.md
@.planning/phases/11-groups-enrollment/11-PATTERNS.md
@prisma/seed.ts
@prisma/seed-e2e.ts
@prisma/lib/subject-constants.ts
@.planning/phases/10-subject-foundation/10-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить prisma/seed.ts</name>
  <files>prisma/seed.ts</files>
  <action>
    Per D-06 и 11-PATTERNS seed section:

    Group.create: добавить subjectId — DEFAULT_QURAN_SUBJECT_ID или соответствующий subject из multi-subject seed (Коран/Таджвид/Арабский).

    Student.create: убрать groupId и levelId.

    После создания student: groupEnrollment.create для каждой привязки с levelId предмета группы.

    Демонстрация D-10 (опционально): один ученик в двух группах одного предмета с разными levelId.

    Wipe: groupEnrollment.deleteMany() перед student.deleteMany() в правильном порядке FK.

    Импорт DEFAULT_QURAN_SUBJECT_ID из prisma/lib/subject-constants.ts.
  </action>
  <verify>
    <automated>pnpm db:seed</automated>
  </verify>
  <done>pnpm db:seed завершается без ошибок; группы имеют subjectId; enrollments созданы</done>
</task>

<task type="auto">
  <name>Task 2: Обновить prisma/seed-e2e.ts</name>
  <files>prisma/seed-e2e.ts</files>
  <action>
    Заменить все Student create с groupId на:
    - student без groupId/levelId
    - groupEnrollment.create связывающий student, group, level

    Группы E2E: subjectId на create.

    Сохранить TEST_USERS / TEST_CODES совместимость — те же логины и группы для journal/admin E2E.

    Wipe order: groupEnrollment.deleteMany в cleanup chain.

    Проверить что e2e global-setup seed проходит.
  </action>
  <verify>
    <automated>pnpm db:seed:e2e</automated>
  </verify>
  <done>seed-e2e проходит; E2E fixtures используют GroupEnrollment</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed scripts | Только dev/test; не на prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-11 | Tampering | seed.ts | low | accept | Dev-only; не deploy seed на prod |
| T-11-SC | Tampering | npm installs | high | mitigate | Без новых пакетов |

</threat_model>

<verification>
pnpm db:seed и pnpm db:seed:e2e exit 0; SQL spot-check COUNT enrollments matches expected students per group.
</verification>

<success_criteria>
Seed-скрипты создают группы с предметом и зачисления через junction; E2E setup не ломается.
</success_criteria>

<output>
Create .planning/phases/11-groups-enrollment/11-05-SUMMARY.md when done
</output>
